<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="https://bfmhno3.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://bfmhno3.github.io/" rel="alternate" type="text/html" /><updated>2026-06-18T04:54:20+00:00</updated><id>https://bfmhno3.github.io/feed.xml</id><title type="html">Better Mistakes</title><subtitle>Ever tried. Ever failed. No matter. Try Again. Fail again. Fail better.</subtitle><author><name>bfmhno3</name></author><entry><title type="html">LaTeX 项目结构与 latexmk 配置</title><link href="https://bfmhno3.github.io/note/latex-project-structure/" rel="alternate" type="text/html" title="LaTeX 项目结构与 latexmk 配置" /><published>2026-06-18T03:20:00+00:00</published><updated>2026-06-18T03:20:00+00:00</updated><id>https://bfmhno3.github.io/note/latex-project-structure</id><content type="html" xml:base="https://bfmhno3.github.io/note/latex-project-structure/"><![CDATA[<h2 id="目录结构">目录结构</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>C:.
│  .gitignore
│  .latexmkrc
│  LICENSE
│  main.tex
│  refs.bib
│
├─contents
├─docs
├─figs
└─settings
    format.tex
    packages.tex
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">main.tex</code>：入口文件，只负责把各部分拼起来。</li>
  <li><code class="language-plaintext highlighter-rouge">refs.bib</code>：参考文献数据库。用 BibTeX 格式存储文献条目，正文里用 <code class="language-plaintext highlighter-rouge">\cite{key}</code> 引用。配合 <code class="language-plaintext highlighter-rouge">biblatex</code> 使用时，在 <code class="language-plaintext highlighter-rouge">format.tex</code> 里用 <code class="language-plaintext highlighter-rouge">\addbibresource{refs.bib}</code> 指定路径。</li>
  <li><code class="language-plaintext highlighter-rouge">contents/</code>：章节内容，按章（<code class="language-plaintext highlighter-rouge">book</code>/<code class="language-plaintext highlighter-rouge">report</code> 类）或按节（<code class="language-plaintext highlighter-rouge">article</code> 类）建子文件夹。</li>
  <li><code class="language-plaintext highlighter-rouge">figs/</code>：图片资源。配合 <code class="language-plaintext highlighter-rouge">\graphicspath</code> 使用，正文里直接写文件名就行。</li>
  <li><code class="language-plaintext highlighter-rouge">docs/</code>：项目文档，比如编译好的 PDF、使用说明等。</li>
  <li><code class="language-plaintext highlighter-rouge">settings/packages.tex</code>：宏包导入。</li>
  <li><code class="language-plaintext highlighter-rouge">settings/format.tex</code>：格式配置（页面布局、标题样式、字体等）。</li>
  <li><code class="language-plaintext highlighter-rouge">.latexmkrc</code>：latexmk 的编译配置，后面会详细讲。</li>
  <li><code class="language-plaintext highlighter-rouge">.gitignore</code>：LaTeX 项目的忽略模板可以参考 <a href="https://github.com/github/gitignore/blob/main/TeX.gitignore">github/gitignore/TeX.gitignore</a>。注意官方模板里没有 <code class="language-plaintext highlighter-rouge">build/</code> 目录，如果你像我一样把编译产物输出到 <code class="language-plaintext highlighter-rouge">build/</code>，记得手动加上。</li>
</ul>

<p><code class="language-plaintext highlighter-rouge">packages.tex</code> 和 <code class="language-plaintext highlighter-rouge">format.tex</code> 的最小示例：</p>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">% settings/packages.tex -- 只管导入，不管配置</span>
<span class="k">\usepackage</span><span class="p">{</span>fontspec<span class="p">}</span>
<span class="k">\usepackage</span><span class="p">{</span>xeCJK<span class="p">}</span>
<span class="k">\usepackage</span><span class="p">{</span>geometry<span class="p">}</span>
<span class="k">\usepackage</span><span class="p">{</span>fancyhdr<span class="p">}</span>
<span class="k">\usepackage</span><span class="p">{</span>amsmath<span class="p">}</span>
<span class="k">\usepackage</span><span class="p">{</span>amssymb<span class="p">}</span>
<span class="k">\usepackage</span><span class="p">{</span>amsfonts<span class="p">}</span>
<span class="k">\usepackage</span><span class="p">{</span>mathtools<span class="p">}</span>
<span class="k">\usepackage</span><span class="p">{</span>graphicx<span class="p">}</span>
<span class="k">\usepackage</span><span class="p">{</span>tabularray<span class="p">}</span>
<span class="k">\usepackage</span><span class="p">{</span>subcaption<span class="p">}</span>
<span class="k">\usepackage</span><span class="p">{</span>listings<span class="p">}</span>
<span class="k">\usepackage</span><span class="p">{</span>enumitem<span class="p">}</span>
<span class="k">\usepackage</span><span class="p">{</span>siunitx<span class="p">}</span>
<span class="k">\usepackage</span><span class="na">[backend=biber, style=gb7714-2015]</span><span class="p">{</span>biblatex<span class="p">}</span>
<span class="k">\usepackage</span><span class="na">[colorlinks=true, linkcolor=blue, citecolor=blue, urlcolor=blue]</span><span class="p">{</span>hyperref<span class="p">}</span>
<span class="k">\usepackage</span><span class="na">[capitalise]</span><span class="p">{</span>cleveref<span class="p">}</span>
</code></pre></div></div>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">% settings/format.tex -- 字体、布局、宏包的后续配置</span>
<span class="k">\setmainfont</span><span class="p">{</span>TeX Gyre Termes<span class="p">}</span>
<span class="k">\setsansfont</span><span class="p">{</span>TeX Gyre Heros<span class="p">}</span>
<span class="k">\setmonofont</span><span class="p">{</span>TeX Gyre Cursor<span class="p">}</span>
<span class="k">\setCJKmainfont</span><span class="p">{</span>Source Han Serif SC<span class="p">}</span>
<span class="k">\setCJKsansfont</span><span class="p">{</span>Source Han Sans SC<span class="p">}</span>
<span class="k">\setCJKmonofont</span><span class="p">{</span>Source Han Mono SC<span class="p">}</span>

<span class="k">\geometry</span><span class="p">{</span>a4paper, margin=2.5cm<span class="p">}</span>

<span class="k">\pagestyle</span><span class="p">{</span>fancy<span class="p">}</span>
<span class="k">\fancyhf</span><span class="p">{}</span>
<span class="k">\fancyhead</span><span class="na">[L]</span><span class="p">{</span><span class="k">\leftmark</span><span class="p">}</span>
<span class="k">\fancyfoot</span><span class="na">[C]</span><span class="p">{</span><span class="k">\thepage</span><span class="p">}</span>
<span class="k">\renewcommand</span><span class="p">{</span><span class="k">\headrulewidth</span><span class="p">}{</span>0.4pt<span class="p">}</span>

<span class="k">\UseTblrLibrary</span><span class="p">{</span>booktabs, siunitx<span class="p">}</span>

<span class="k">\graphicspath</span>
<span class="k">\sisetup</span><span class="p">{</span>range-phrase=<span class="k">\ensuremath</span><span class="p">{</span><span class="k">\sim</span><span class="p">}</span>, range-units=single<span class="p">}</span>
<span class="k">\addbibresource</span><span class="p">{</span>refs.bib<span class="p">}</span>

<span class="c">% tabularray 常用全局设置</span>
<span class="k">\SetTblrStyle</span><span class="p">{</span>firsthead<span class="p">}{</span>font=<span class="k">\bfseries</span><span class="p">}</span>
<span class="k">\SetTblrStyle</span><span class="p">{</span>firstfoot<span class="p">}{</span>font=<span class="k">\small</span><span class="p">}</span>
<span class="k">\SetTblrStyle</span><span class="p">{</span>middlefoot<span class="p">}{</span>font=<span class="k">\small</span><span class="p">}</span>
<span class="k">\SetTblrStyle</span><span class="p">{</span>lastfoot<span class="p">}{</span>font=<span class="k">\small</span><span class="p">}</span>
</code></pre></div></div>

<p>实际使用时按需增减。宏包的加载顺序有讲究，后面”常用宏包”一节会说明。</p>

<h2 id="maintex-模板"><code class="language-plaintext highlighter-rouge">main.tex</code> 模板</h2>

<h3 id="book-类"><code class="language-plaintext highlighter-rouge">book</code> 类</h3>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\documentclass</span><span class="p">{</span>book<span class="p">}</span>

<span class="k">\input</span><span class="p">{</span>settings/packages<span class="p">}</span>
<span class="k">\input</span><span class="p">{</span>settings/format<span class="p">}</span>

<span class="nt">\begin{document}</span>

<span class="k">\maketitle</span>
<span class="k">\frontmatter</span>

<span class="k">\tableofcontents</span>

<span class="k">\mainmatter</span>

<span class="c">% \input{contents/chap_01}</span>

<span class="k">\appendix</span>
<span class="c">% \input{contents/appendix}</span>

<span class="k">\backmatter</span>

<span class="nt">\end{document}</span>
</code></pre></div></div>

<h3 id="article-类"><code class="language-plaintext highlighter-rouge">article</code> 类</h3>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\documentclass</span><span class="p">{</span>article<span class="p">}</span>

<span class="k">\input</span><span class="p">{</span>settings/packages<span class="p">}</span>
<span class="k">\input</span><span class="p">{</span>settings/format<span class="p">}</span>

<span class="nt">\begin{document}</span>

<span class="k">\maketitle</span>

<span class="k">\tableofcontents</span>

<span class="c">% \input{contents/sec_01}</span>

<span class="k">\appendix</span>
<span class="c">% \input{contents/appendix}</span>

<span class="nt">\end{document}</span>
</code></pre></div></div>

<h3 id="report-类"><code class="language-plaintext highlighter-rouge">report</code> 类</h3>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\documentclass</span><span class="p">{</span>report<span class="p">}</span>

<span class="k">\input</span><span class="p">{</span>settings/packages<span class="p">}</span>
<span class="k">\input</span><span class="p">{</span>settings/format<span class="p">}</span>

<span class="nt">\begin{document}</span>

<span class="k">\maketitle</span>

<span class="k">\tableofcontents</span>

<span class="c">% \input{contents/chap_01}</span>

<span class="k">\appendix</span>
<span class="c">% \input{contents/appendix}</span>

<span class="nt">\end{document}</span>
</code></pre></div></div>

<h3 id="三者差在哪">三者差在哪</h3>

<table>
  <thead>
    <tr>
      <th> </th>
      <th><code class="language-plaintext highlighter-rouge">article</code></th>
      <th><code class="language-plaintext highlighter-rouge">report</code></th>
      <th><code class="language-plaintext highlighter-rouge">book</code></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>最高章节</td>
      <td><code class="language-plaintext highlighter-rouge">\section</code></td>
      <td><code class="language-plaintext highlighter-rouge">\chapter</code></td>
      <td><code class="language-plaintext highlighter-rouge">\chapter</code></td>
    </tr>
    <tr>
      <td>默认排版</td>
      <td>单面</td>
      <td>单面</td>
      <td>双面</td>
    </tr>
    <tr>
      <td>标题页</td>
      <td>正文内</td>
      <td>独立一页</td>
      <td>独立一页</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">\frontmatter</code> / <code class="language-plaintext highlighter-rouge">\backmatter</code></td>
      <td>无</td>
      <td>无</td>
      <td>有</td>
    </tr>
    <tr>
      <td>典型用途</td>
      <td>短文、笔记、报告</td>
      <td>课程报告、毕业论文</td>
      <td>书籍、学位论文</td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">report</code> 和 <code class="language-plaintext highlighter-rouge">book</code> 都有 <code class="language-plaintext highlighter-rouge">\chapter</code>，区别主要在排版方式和文档结构命令。<code class="language-plaintext highlighter-rouge">book</code> 多了 <code class="language-plaintext highlighter-rouge">\frontmatter</code>、<code class="language-plaintext highlighter-rouge">\mainmatter</code>、<code class="language-plaintext highlighter-rouge">\backmatter</code>，默认双面排版，适合正式出版物。<code class="language-plaintext highlighter-rouge">report</code> 默认单面，没有前言/正文/后记的划分，适合不需要装订的长文档。</p>

<p><code class="language-plaintext highlighter-rouge">article</code> 没有 <code class="language-plaintext highlighter-rouge">\chapter</code>，从 <code class="language-plaintext highlighter-rouge">\section</code> 开始。写短文、笔记够用了。</p>

<p>三者的标题页也不同。<code class="language-plaintext highlighter-rouge">book</code> 和 <code class="language-plaintext highlighter-rouge">report</code> 的 <code class="language-plaintext highlighter-rouge">\maketitle</code> 都是独立一页，<code class="language-plaintext highlighter-rouge">article</code> 的只是在正文开头插一行。</p>

<h2 id="工具简介">工具简介</h2>

<p>在看 <code class="language-plaintext highlighter-rouge">.latexmkrc</code> 之前，先简单介绍一下涉及的几个工具。</p>

<p><strong>XeLaTeX</strong></p>

<p>LaTeX 的编译引擎之一。相比传统的 pdfLaTeX，XeLaTeX 原生支持 Unicode 和系统字体（通过 fontspec），处理中文要方便得多。如果你主要写中文文档，XeLaTeX（或 LuaLaTeX）基本是必选的。</p>

<p><strong>latexmk</strong></p>

<p>一个 Perl 写的自动化编译工具。LaTeX 的编译经常需要跑多遍（交叉引用、目录、参考文献），手动执行很烦。latexmk 会自动判断需要跑几遍、要不要调用 BibTeX/Biber，直到所有引用都稳定为止。配合 <code class="language-plaintext highlighter-rouge">-pvc</code> 参数还能监控文件变化，保存即编译。</p>

<p><strong>BibTeX 和 Biber</strong></p>

<p>两者都是处理参考文献的工具。BibTeX 是老方案，Biber 是配合 biblatex 宏包的新方案，功能更强（支持 Unicode、更灵活的样式定制）。latexmk 的 <code class="language-plaintext highlighter-rouge">$bibtex_use = 2</code> 会自动检测你用的是哪个，不用手动切换。</p>

<p><strong>SyncTeX</strong></p>

<p>在编辑器和 PDF 阅读器之间做双向跳转的数据格式。加了 <code class="language-plaintext highlighter-rouge">-synctex=1</code> 之后，你可以在编辑器里点一个位置，PDF 阅读器自动翻到对应页面，反过来也行。</p>

<h2 id="latexmkrc"><code class="language-plaintext highlighter-rouge">.latexmkrc</code></h2>

<div class="language-perl highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 主文件</span>
<span class="nv">$root</span> <span class="o">=</span> <span class="p">'</span><span class="s1">main.tex</span><span class="p">';</span>

<span class="c1"># 输出目录</span>
<span class="nv">$out_dir</span> <span class="o">=</span> <span class="p">'</span><span class="s1">build</span><span class="p">';</span>
<span class="nv">$aux_dir</span> <span class="o">=</span> <span class="p">'</span><span class="s1">build</span><span class="p">';</span>

<span class="c1"># 编译引擎: XeLaTeX</span>
<span class="nv">$pdf_mode</span> <span class="o">=</span> <span class="mi">5</span><span class="p">;</span>
<span class="nv">$xelatex</span> <span class="o">=</span> <span class="p">'</span><span class="s1">xelatex -interaction=nonstopmode -file-line-error -synctex=1 %O %S</span><span class="p">';</span>

<span class="c1"># 自动运行 BibTeX 或 Biber</span>
<span class="nv">$bibtex_use</span> <span class="o">=</span> <span class="mi">2</span><span class="p">;</span>

<span class="c1"># 清理时保留 PDF</span>
<span class="nv">$clean_ext</span> <span class="o">=</span> <span class="p">'</span><span class="s1">aux bbl bcf blg fdb_latexmk fls log out run.xml synctex.gz toc</span><span class="p">';</span>

<span class="c1"># 跨平台 PDF 预览器</span>
<span class="k">if</span> <span class="p">(</span><span class="vg">$^O</span> <span class="ow">eq</span> <span class="p">'</span><span class="s1">MSWin32</span><span class="p">')</span> <span class="p">{</span>
    <span class="nv">$pdf_previewer</span> <span class="o">=</span> <span class="p">'</span><span class="s1">start %S</span><span class="p">';</span>
<span class="p">}</span> <span class="k">elsif</span> <span class="p">(</span><span class="vg">$^O</span> <span class="ow">eq</span> <span class="p">'</span><span class="s1">darwin</span><span class="p">')</span> <span class="p">{</span>
    <span class="nv">$pdf_previewer</span> <span class="o">=</span> <span class="p">'</span><span class="s1">open %S</span><span class="p">';</span>
<span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
    <span class="nv">$pdf_previewer</span> <span class="o">=</span> <span class="p">'</span><span class="s1">xdg-open %S 2&gt;/dev/null || evince %S</span><span class="p">';</span>
<span class="p">}</span>

<span class="c1"># Windows 下清理时不删除被占用的文件</span>
<span class="k">if</span> <span class="p">(</span><span class="vg">$^O</span> <span class="ow">eq</span> <span class="p">'</span><span class="s1">MSWin32</span><span class="p">')</span> <span class="p">{</span>
    <span class="nv">$cleanup_includes_generated</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>

<span class="c1"># 自定义依赖示例（取消注释即可用）</span>
<span class="c1"># add_cus_dep('glo', 'gls', 0, 'makeglos');</span>
<span class="c1"># sub makeglos {</span>
<span class="c1">#     system("makeindex -s $_[0].ist -t $_[0].glg -o $_[0].gls $_[0].glo");</span>
<span class="c1"># }</span>
</code></pre></div></div>

<h3 id="逐项说明">逐项说明</h3>

<ul>
  <li><code class="language-plaintext highlighter-rouge">$root</code>：主文件名。latexmk 从这里开始分析依赖。改成你的实际文件名就行。</li>
  <li><code class="language-plaintext highlighter-rouge">$out_dir</code> / <code class="language-plaintext highlighter-rouge">$aux_dir</code>：编译产物放哪。设成 <code class="language-plaintext highlighter-rouge">build</code> 能让根目录干净一些，PDF 会出现在 <code class="language-plaintext highlighter-rouge">build/main.pdf</code>。记得在 <code class="language-plaintext highlighter-rouge">.gitignore</code> 里加上 <code class="language-plaintext highlighter-rouge">build/</code>。</li>
  <li><code class="language-plaintext highlighter-rouge">$pdf_mode</code>：选择编译引擎。1 是 pdfLaTeX，4 是 LuaLaTeX，5 是 XeLaTeX。</li>
  <li><code class="language-plaintext highlighter-rouge">$xelatex</code>：XeLaTeX 的调用参数。<code class="language-plaintext highlighter-rouge">-interaction=nonstopmode</code> 让它遇到错误继续编译而不是停下来等你按回车；<code class="language-plaintext highlighter-rouge">-file-line-error</code> 让报错信息带上文件名和行号；<code class="language-plaintext highlighter-rouge">-synctex=1</code> 生成编辑器跳转用的数据。<code class="language-plaintext highlighter-rouge">%O</code> 和 <code class="language-plaintext highlighter-rouge">%S</code> 是 latexmk 的占位符，分别代表额外选项和源文件。</li>
  <li><code class="language-plaintext highlighter-rouge">$bibtex_use</code>：设为 2 就行，自动检测 BibTeX 还是 Biber。设为 0 关闭自动处理，1 只在 <code class="language-plaintext highlighter-rouge">.bbl</code> 已存在时才跑。</li>
  <li><code class="language-plaintext highlighter-rouge">$clean_ext</code>：<code class="language-plaintext highlighter-rouge">latexmk -c</code> 会删掉这些后缀的文件。PDF 不在里面，所以清理后 PDF 还在。注意区分 <code class="language-plaintext highlighter-rouge">-c</code>（清理中间文件，保留 PDF）和 <code class="language-plaintext highlighter-rouge">-C</code>（连 PDF 一起删），别手滑用了 <code class="language-plaintext highlighter-rouge">-C</code>。</li>
  <li><strong>跨平台预览器</strong>：Perl 的 <code class="language-plaintext highlighter-rouge">$^O</code> 变量存着操作系统类型：Windows 是 <code class="language-plaintext highlighter-rouge">MSWin32</code>，macOS 是 <code class="language-plaintext highlighter-rouge">darwin</code>，Linux 就是 <code class="language-plaintext highlighter-rouge">linux</code>。<code class="language-plaintext highlighter-rouge">start</code>、<code class="language-plaintext highlighter-rouge">open</code>、<code class="language-plaintext highlighter-rouge">xdg-open</code> 分别是三个平台打开文件的命令。<code class="language-plaintext highlighter-rouge">%S</code> 会被替换成 PDF 路径。配合 <code class="language-plaintext highlighter-rouge">latexmk -pvc</code> 使用，保存文件后自动编译并刷新 PDF 预览，不用手动跑命令。</li>
  <li><code class="language-plaintext highlighter-rouge">$cleanup_includes_generated</code>：Windows 上文件锁比较死板，latexmk 清理时可能删不掉正在被占用的文件。设为 0 可以避免这个问题。</li>
  <li><strong>自定义依赖</strong>：<code class="language-plaintext highlighter-rouge">add_cus_dep</code> 用来告诉 latexmk “如果某个 <code class="language-plaintext highlighter-rouge">.glo</code> 文件变了，要重新生成对应的 <code class="language-plaintext highlighter-rouge">.gls</code>“。示例里是术语表的场景，取消注释就能用。</li>
</ul>

<h2 id="常用宏包">常用宏包</h2>

<p>以下是写中文 LaTeX 文档时经常会用到的宏包，以及它们的典型配置。</p>

<p>加载顺序需要注意几点：<code class="language-plaintext highlighter-rouge">fontspec</code> 和 <code class="language-plaintext highlighter-rouge">xeCJK</code> 要在大多数宏包之前加载；<code class="language-plaintext highlighter-rouge">amsmath</code> 要在 <code class="language-plaintext highlighter-rouge">mathtools</code> 之前；<code class="language-plaintext highlighter-rouge">tabularray</code> 的 <code class="language-plaintext highlighter-rouge">\UseTblrLibrary</code> 要在 <code class="language-plaintext highlighter-rouge">siunitx</code> 之后（因为 <code class="language-plaintext highlighter-rouge">siunitx</code> 库需要 siunitx 已加载）；<code class="language-plaintext highlighter-rouge">hyperref</code> 几乎总是放在最后，唯一的例外是 <code class="language-plaintext highlighter-rouge">cleveref</code>，它必须在 <code class="language-plaintext highlighter-rouge">hyperref</code> 之后。上面 <code class="language-plaintext highlighter-rouge">packages.tex</code> 示例中的顺序可以直接用。</p>

<h3 id="字体与编码">字体与编码</h3>

<p><strong>fontspec</strong></p>

<p>配合 XeLaTeX/LuaLaTeX 使用，用来设置系统字体。</p>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\usepackage</span><span class="p">{</span>fontspec<span class="p">}</span>
<span class="k">\setmainfont</span><span class="p">{</span>TeX Gyre Termes<span class="p">}</span>          <span class="c">% 正文衬线字体</span>
<span class="k">\setsansfont</span><span class="p">{</span>TeX Gyre Heros<span class="p">}</span>           <span class="c">% 无衬线字体</span>
<span class="k">\setmonofont</span><span class="p">{</span>TeX Gyre Cursor<span class="p">}</span>          <span class="c">% 等宽字体</span>
</code></pre></div></div>

<p><strong>xeCJK</strong></p>

<p>处理中文排版的核心宏包，自动处理中英文之间的间距、标点挤压等。</p>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\usepackage</span><span class="p">{</span>xeCJK<span class="p">}</span>
<span class="k">\setCJKmainfont</span><span class="p">{</span>Source Han Serif SC<span class="p">}</span>   <span class="c">% 中文衬线（思源宋体）</span>
<span class="k">\setCJKsansfont</span><span class="p">{</span>Source Han Sans SC<span class="p">}</span>    <span class="c">% 中文无衬线（思源黑体）</span>
<span class="k">\setCJKmonofont</span><span class="p">{</span>Source Han Mono SC<span class="p">}</span>    <span class="c">% 中文等宽</span>
</code></pre></div></div>

<h3 id="页面布局">页面布局</h3>

<p><strong>geometry</strong></p>

<p>控制页面尺寸和边距，比 LaTeX 默认的布局参数好用得多。</p>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\usepackage</span><span class="na">[a4paper, margin=2.5cm]</span><span class="p">{</span>geometry<span class="p">}</span>
<span class="c">% 也可以精细控制</span>
<span class="c">% \usepackage[a4paper, top=3cm, bottom=2.5cm, left=2.8cm, right=2.2cm]{geometry}</span>
</code></pre></div></div>

<p><strong>fancyhdr</strong></p>

<p>自定义页眉页脚。</p>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\usepackage</span><span class="p">{</span>fancyhdr<span class="p">}</span>
<span class="k">\pagestyle</span><span class="p">{</span>fancy<span class="p">}</span>
<span class="k">\fancyhf</span><span class="p">{}</span>                             <span class="c">% 清空默认设置</span>
<span class="k">\fancyhead</span><span class="na">[L]</span><span class="p">{</span><span class="k">\leftmark</span><span class="p">}</span>               <span class="c">% 页眉左侧放章节名</span>
<span class="k">\fancyfoot</span><span class="na">[C]</span><span class="p">{</span><span class="k">\thepage</span><span class="p">}</span>                <span class="c">% 页脚中间放页码</span>
<span class="k">\renewcommand</span><span class="p">{</span><span class="k">\headrulewidth</span><span class="p">}{</span>0.4pt<span class="p">}</span>   <span class="c">% 页眉横线粗细</span>
</code></pre></div></div>

<h3 id="数学与代码">数学与代码</h3>

<p><strong>amsmath / amssymb / amsfonts</strong></p>

<p>数学排版的基础三件套，基本上写公式就得用。</p>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\usepackage</span><span class="p">{</span>amsmath<span class="p">}</span>
<span class="k">\usepackage</span><span class="p">{</span>amssymb<span class="p">}</span>
<span class="k">\usepackage</span><span class="p">{</span>amsfonts<span class="p">}</span>
</code></pre></div></div>

<p><strong>mathtools</strong></p>

<p>amsmath 的扩展包，修复了一些设计问题并增加了实用工具。</p>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\usepackage</span><span class="p">{</span>mathtools<span class="p">}</span>
<span class="c">% 例如：可以给公式加编号标签</span>
<span class="c">% \usetagform{default}</span>
</code></pre></div></div>

<p><strong>listings</strong></p>

<p>排版代码块。支持语法高亮，可以指定语言和样式。</p>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\usepackage</span><span class="p">{</span>listings<span class="p">}</span>
<span class="k">\lstset</span><span class="p">{</span>
    basicstyle=<span class="k">\ttfamily\small</span>,
    breaklines=true,
    frame=single,
    numbers=left,
    numberstyle=<span class="k">\tiny\color</span><span class="p">{</span>gray<span class="p">}</span>,
    keywordstyle=<span class="k">\color</span><span class="p">{</span>blue<span class="p">}</span>,
    commentstyle=<span class="k">\color</span><span class="p">{</span>green!60!black<span class="p">}</span>,
    stringstyle=<span class="k">\color</span><span class="p">{</span>red!70!black<span class="p">}</span>,
<span class="p">}</span>
</code></pre></div></div>

<p><strong>minted</strong></p>

<p>基于 Pygments 的代码排版，高亮效果比 listings 好，但需要 Python 环境且编译时要加 <code class="language-plaintext highlighter-rouge">-shell-escape</code>。</p>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\usepackage</span><span class="p">{</span>minted<span class="p">}</span>
<span class="c">% 使用示例：</span>
<span class="c">% \begin{minted}{python}</span>
<span class="c">% def hello():</span>
<span class="c">%     print("world")</span>
<span class="c">% \end{minted}</span>
</code></pre></div></div>

<h3 id="图表与浮动体">图表与浮动体</h3>

<p><strong>graphicx</strong></p>

<p>插入图片。</p>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\usepackage</span><span class="p">{</span>graphicx<span class="p">}</span>
<span class="k">\graphicspath</span>                 <span class="c">% 指定图片搜索路径</span>
</code></pre></div></div>

<p><strong>tabularray</strong></p>

<p>现代表格宏包，统一了表格的样式配置和内容分离。用 <code class="language-plaintext highlighter-rouge">tblr</code> 环境替代传统的 <code class="language-plaintext highlighter-rouge">tabular</code>，样式参数写在 <code class="language-plaintext highlighter-rouge">\begin{tblr}{...}</code> 的花括号里，正文和样式彻底分开。配合 <code class="language-plaintext highlighter-rouge">booktabs</code> 库可以使用 <code class="language-plaintext highlighter-rouge">\toprule</code>、<code class="language-plaintext highlighter-rouge">\midrule</code>、<code class="language-plaintext highlighter-rouge">\bottomrule</code>。</p>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\usepackage</span><span class="p">{</span>tabularray<span class="p">}</span>
<span class="k">\UseTblrLibrary</span><span class="p">{</span>booktabs, siunitx<span class="p">}</span>
<span class="c">% \begin{table}</span>
<span class="c">% \begin{tblr}{</span>
<span class="c">%   colspec = {lrc},</span>
<span class="c">%   row{1} = {font=\bfseries},</span>
<span class="c">% }</span>
<span class="c">%   \toprule</span>
<span class="c">%   姓名 &amp; 分数 &amp; 等级 \\</span>
<span class="c">%   \midrule</span>
<span class="c">%   张三 &amp; 95 &amp; A \\</span>
<span class="c">%   李四 &amp; 82 &amp; B \\</span>
<span class="c">%   \bottomrule</span>
<span class="c">% \end{tblr}</span>
<span class="c">% \end{table}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">booktabs</code> 库提供三线表的线条命令，<code class="language-plaintext highlighter-rouge">siunitx</code> 库让列可以用 <code class="language-plaintext highlighter-rouge">S</code> 类型对齐数字。tabularray 还支持 <code class="language-plaintext highlighter-rouge">longtblr</code>（跨页长表格）和 <code class="language-plaintext highlighter-rouge">talltblr</code>（不跨页但可加脚注的表格），基本覆盖了所有表格场景。</p>

<p><strong>subcaption</strong></p>

<p>在一个浮动体里放多张子图，每个子图独立编号和引用。</p>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\usepackage</span><span class="p">{</span>subcaption<span class="p">}</span>
<span class="c">% \begin{figure}[htbp]</span>
<span class="c">%   \begin{subfigure}{0.48\textwidth}</span>
<span class="c">%     \includegraphics[width=\textwidth]{fig_a}</span>
<span class="c">%     \caption{子图 A}</span>
<span class="c">%   \end{subfigure}</span>
<span class="c">%   \hfill</span>
<span class="c">%   \begin{subfigure}{0.48\textwidth}</span>
<span class="c">%     \includegraphics[width=\textwidth]{fig_b}</span>
<span class="c">%     \caption{子图 B}</span>
<span class="c">%   \end{subfigure}</span>
<span class="c">%   \caption{总标题}</span>
<span class="c">% \end{figure}</span>
</code></pre></div></div>

<h3 id="参考文献">参考文献</h3>

<p><strong>biblatex</strong></p>

<p>现代的参考文献管理方案，配合 Biber 后端使用。比 BibTeX 灵活很多，支持多语言、多文献列表等。</p>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\usepackage</span><span class="na">[backend=biber, style=gb7714-2015]</span><span class="p">{</span>biblatex<span class="p">}</span>
<span class="k">\addbibresource</span><span class="p">{</span>refs.bib<span class="p">}</span>              <span class="c">% 指定 .bib 文件</span>
<span class="c">% 正文中用 \cite{key} 引用</span>
<span class="c">% 文末用 \printbibliography 输出</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">style=gb7714-2015</code> 是中文文献常用的国标样式，需要 <code class="language-plaintext highlighter-rouge">biblatex-gb7714-2015</code> 宏包。英文文档常用 <code class="language-plaintext highlighter-rouge">style=ieee</code>、<code class="language-plaintext highlighter-rouge">style=apa</code> 或 <code class="language-plaintext highlighter-rouge">style=alphabetic</code>。</p>

<p><strong>hyperref</strong></p>

<p>给交叉引用、目录、参考文献加上超链接。通常放在 <code class="language-plaintext highlighter-rouge">\usepackage</code> 的最后（少数例外，如 cleveref 要在 hyperref 之后）。</p>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\usepackage</span>[
    colorlinks=true,
    linkcolor=blue,
    citecolor=blue,
    urlcolor=blue,
]<span class="p">{</span>hyperref<span class="p">}</span>
</code></pre></div></div>

<h3 id="其他实用工具">其他实用工具</h3>

<p><strong>ctex</strong></p>

<p>如果不想手动配 xeCJK，直接用 ctex 宏包或 <code class="language-plaintext highlighter-rouge">ctexart</code>/<code class="language-plaintext highlighter-rouge">ctexbook</code> 文档类，一键搞定中文支持。</p>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\usepackage</span><span class="na">[UTF8, heading=true]</span><span class="p">{</span>ctex<span class="p">}</span>
<span class="c">% 或者直接用 \documentclass{ctexart}</span>
</code></pre></div></div>

<p><strong>enumitem</strong></p>

<p>定制列表环境（enumerate、itemize、description）的间距和标签样式。</p>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\usepackage</span><span class="p">{</span>enumitem<span class="p">}</span>
<span class="k">\setlist</span><span class="p">{</span>nosep<span class="p">}</span>                        <span class="c">% 去掉列表的多余间距</span>
<span class="c">% \begin{enumerate}[label=(\arabic*)]</span>
<span class="c">%   \item 第一项</span>
<span class="c">%   \item 第二项</span>
<span class="c">% \end{enumerate}</span>
</code></pre></div></div>

<p><strong>tikz</strong></p>

<p>画矢量图。学习曲线陡峭，但画出来的东西质量很高，跟 LaTeX 文档浑然一体。</p>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\usepackage</span><span class="p">{</span>tikz<span class="p">}</span>
<span class="k">\usetikzlibrary</span><span class="p">{</span>arrows.meta, positioning<span class="p">}</span>
<span class="c">% 简单示例：画一个带箭头的直线</span>
<span class="c">% \begin{tikzpicture}</span>
<span class="c">%   \draw[-&gt;] (0,0) -- (2,1) node[right] {$y = x/2$};</span>
<span class="c">% \end{tikzpicture}</span>
</code></pre></div></div>

<p><strong>siunitx</strong></p>

<p>处理数值和单位的排版。数字的千分位、科学记数法、单位的正体和间距，它都能管。</p>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\usepackage</span><span class="p">{</span>siunitx<span class="p">}</span>
<span class="k">\sisetup</span><span class="p">{</span>
    range-phrase = <span class="k">\ensuremath</span><span class="p">{</span><span class="k">\sim</span><span class="p">}</span>,
    range-units = single,
<span class="p">}</span>
<span class="c">% \qty{10}{\metre}           -&gt; 10 m</span>
<span class="c">% \qty{1.23e5}{\pascal}      -&gt; 1.23 × 10⁵ Pa</span>
<span class="c">% \qtyrange{1}{10}{\metre}   -&gt; 1~10 m（单位只出现一次，用 ~ 连接）</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">range-units=single</code> 让范围表达中单位只出现一次，而不是”1 m 到 10 m”。<code class="language-plaintext highlighter-rouge">range-phrase</code> 设成 <code class="language-plaintext highlighter-rouge">\ensuremath{\sim}</code> 把分隔符换成 <code class="language-plaintext highlighter-rouge">~</code>（波浪号），<code class="language-plaintext highlighter-rouge">\ensuremath</code> 确保它在数学模式和文本模式下都能正确渲染。</p>

<p><strong>cleveref</strong></p>

<p>智能引用，自动加上”图”“表”“公式”等前缀，不用自己写 <code class="language-plaintext highlighter-rouge">\ref{fig:xxx} 的图</code>。注意要放在 hyperref 之后加载。</p>

<div class="language-tex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\usepackage</span><span class="na">[capitalise]</span><span class="p">{</span>cleveref<span class="p">}</span>
<span class="c">% 使用 \cref{fig:example} 会自动输出"图 1"</span>
<span class="c">% \cref{eq:main,eq:sub} 会输出"公式 (1) 和 (2)"</span>
</code></pre></div></div>]]></content><author><name>bfmhno3</name></author><category term="Note" /><category term="LaTeX" /><summary type="html"><![CDATA[目录结构 C:. │ .gitignore │ .latexmkrc │ LICENSE │ main.tex │ refs.bib │ ├─contents ├─docs ├─figs └─settings format.tex packages.tex main.tex：入口文件，只负责把各部分拼起来。 refs.bib：参考文献数据库。用 BibTeX 格式存储文献条目，正文里用 \cite{key} 引用。配合 biblatex 使用时，在 format.tex 里用 \addbibresource{refs.bib} 指定路径。 contents/：章节内容，按章（book/report 类）或按节（article 类）建子文件夹。 figs/：图片资源。配合 \graphicspath 使用，正文里直接写文件名就行。 docs/：项目文档，比如编译好的 PDF、使用说明等。 settings/packages.tex：宏包导入。 settings/format.tex：格式配置（页面布局、标题样式、字体等）。 .latexmkrc：latexmk 的编译配置，后面会详细讲。 .gitignore：LaTeX 项目的忽略模板可以参考 github/gitignore/TeX.gitignore。注意官方模板里没有 build/ 目录，如果你像我一样把编译产物输出到 build/，记得手动加上。 packages.tex 和 format.tex 的最小示例： % settings/packages.tex -- 只管导入，不管配置 \usepackage{fontspec} \usepackage{xeCJK} \usepackage{geometry} \usepackage{fancyhdr} \usepackage{amsmath} \usepackage{amssymb} \usepackage{amsfonts} \usepackage{mathtools} \usepackage{graphicx} \usepackage{tabularray} \usepackage{subcaption} \usepackage{listings} \usepackage{enumitem} \usepackage{siunitx} \usepackage[backend=biber, style=gb7714-2015]{biblatex} \usepackage[colorlinks=true, linkcolor=blue, citecolor=blue, urlcolor=blue]{hyperref} \usepackage[capitalise]{cleveref} % settings/format.tex -- 字体、布局、宏包的后续配置 \setmainfont{TeX Gyre Termes} \setsansfont{TeX Gyre Heros} \setmonofont{TeX Gyre Cursor} \setCJKmainfont{Source Han Serif SC} \setCJKsansfont{Source Han Sans SC} \setCJKmonofont{Source Han Mono SC} \geometry{a4paper, margin=2.5cm} \pagestyle{fancy} \fancyhf{} \fancyhead[L]{\leftmark} \fancyfoot[C]{\thepage} \renewcommand{\headrulewidth}{0.4pt} \UseTblrLibrary{booktabs, siunitx} \graphicspath \sisetup{range-phrase=\ensuremath{\sim}, range-units=single} \addbibresource{refs.bib} % tabularray 常用全局设置 \SetTblrStyle{firsthead}{font=\bfseries} \SetTblrStyle{firstfoot}{font=\small} \SetTblrStyle{middlefoot}{font=\small} \SetTblrStyle{lastfoot}{font=\small} 实际使用时按需增减。宏包的加载顺序有讲究，后面”常用宏包”一节会说明。 main.tex 模板 book 类 \documentclass{book} \input{settings/packages} \input{settings/format} \begin{document} \maketitle \frontmatter \tableofcontents \mainmatter % \input{contents/chap_01} \appendix % \input{contents/appendix} \backmatter \end{document} article 类 \documentclass{article} \input{settings/packages} \input{settings/format} \begin{document} \maketitle \tableofcontents % \input{contents/sec_01} \appendix % \input{contents/appendix} \end{document} report 类 \documentclass{report} \input{settings/packages} \input{settings/format} \begin{document} \maketitle \tableofcontents % \input{contents/chap_01} \appendix % \input{contents/appendix} \end{document} 三者差在哪   article report book 最高章节 \section \chapter \chapter 默认排版 单面 单面 双面 标题页 正文内 独立一页 独立一页 \frontmatter / \backmatter 无 无 有 典型用途 短文、笔记、报告 课程报告、毕业论文 书籍、学位论文 report 和 book 都有 \chapter，区别主要在排版方式和文档结构命令。book 多了 \frontmatter、\mainmatter、\backmatter，默认双面排版，适合正式出版物。report 默认单面，没有前言/正文/后记的划分，适合不需要装订的长文档。 article 没有 \chapter，从 \section 开始。写短文、笔记够用了。 三者的标题页也不同。book 和 report 的 \maketitle 都是独立一页，article 的只是在正文开头插一行。 工具简介 在看 .latexmkrc 之前，先简单介绍一下涉及的几个工具。 XeLaTeX LaTeX 的编译引擎之一。相比传统的 pdfLaTeX，XeLaTeX 原生支持 Unicode 和系统字体（通过 fontspec），处理中文要方便得多。如果你主要写中文文档，XeLaTeX（或 LuaLaTeX）基本是必选的。 latexmk 一个 Perl 写的自动化编译工具。LaTeX 的编译经常需要跑多遍（交叉引用、目录、参考文献），手动执行很烦。latexmk 会自动判断需要跑几遍、要不要调用 BibTeX/Biber，直到所有引用都稳定为止。配合 -pvc 参数还能监控文件变化，保存即编译。 BibTeX 和 Biber 两者都是处理参考文献的工具。BibTeX 是老方案，Biber 是配合 biblatex 宏包的新方案，功能更强（支持 Unicode、更灵活的样式定制）。latexmk 的 $bibtex_use = 2 会自动检测你用的是哪个，不用手动切换。 SyncTeX 在编辑器和 PDF 阅读器之间做双向跳转的数据格式。加了 -synctex=1 之后，你可以在编辑器里点一个位置，PDF 阅读器自动翻到对应页面，反过来也行。 .latexmkrc # 主文件 $root = 'main.tex'; # 输出目录 $out_dir = 'build'; $aux_dir = 'build'; # 编译引擎: XeLaTeX $pdf_mode = 5; $xelatex = 'xelatex -interaction=nonstopmode -file-line-error -synctex=1 %O %S'; # 自动运行 BibTeX 或 Biber $bibtex_use = 2; # 清理时保留 PDF $clean_ext = 'aux bbl bcf blg fdb_latexmk fls log out run.xml synctex.gz toc'; # 跨平台 PDF 预览器 if ($^O eq 'MSWin32') { $pdf_previewer = 'start %S'; } elsif ($^O eq 'darwin') { $pdf_previewer = 'open %S'; } else { $pdf_previewer = 'xdg-open %S 2&gt;/dev/null || evince %S'; } # Windows 下清理时不删除被占用的文件 if ($^O eq 'MSWin32') { $cleanup_includes_generated = 0; } # 自定义依赖示例（取消注释即可用） # add_cus_dep('glo', 'gls', 0, 'makeglos'); # sub makeglos { # system("makeindex -s $_[0].ist -t $_[0].glg -o $_[0].gls $_[0].glo"); # } 逐项说明 $root：主文件名。latexmk 从这里开始分析依赖。改成你的实际文件名就行。 $out_dir / $aux_dir：编译产物放哪。设成 build 能让根目录干净一些，PDF 会出现在 build/main.pdf。记得在 .gitignore 里加上 build/。 $pdf_mode：选择编译引擎。1 是 pdfLaTeX，4 是 LuaLaTeX，5 是 XeLaTeX。 $xelatex：XeLaTeX 的调用参数。-interaction=nonstopmode 让它遇到错误继续编译而不是停下来等你按回车；-file-line-error 让报错信息带上文件名和行号；-synctex=1 生成编辑器跳转用的数据。%O 和 %S 是 latexmk 的占位符，分别代表额外选项和源文件。 $bibtex_use：设为 2 就行，自动检测 BibTeX 还是 Biber。设为 0 关闭自动处理，1 只在 .bbl 已存在时才跑。 $clean_ext：latexmk -c 会删掉这些后缀的文件。PDF 不在里面，所以清理后 PDF 还在。注意区分 -c（清理中间文件，保留 PDF）和 -C（连 PDF 一起删），别手滑用了 -C。 跨平台预览器：Perl 的 $^O 变量存着操作系统类型：Windows 是 MSWin32，macOS 是 darwin，Linux 就是 linux。start、open、xdg-open 分别是三个平台打开文件的命令。%S 会被替换成 PDF 路径。配合 latexmk -pvc 使用，保存文件后自动编译并刷新 PDF 预览，不用手动跑命令。 $cleanup_includes_generated：Windows 上文件锁比较死板，latexmk 清理时可能删不掉正在被占用的文件。设为 0 可以避免这个问题。 自定义依赖：add_cus_dep 用来告诉 latexmk “如果某个 .glo 文件变了，要重新生成对应的 .gls“。示例里是术语表的场景，取消注释就能用。 常用宏包 以下是写中文 LaTeX 文档时经常会用到的宏包，以及它们的典型配置。 加载顺序需要注意几点：fontspec 和 xeCJK 要在大多数宏包之前加载；amsmath 要在 mathtools 之前；tabularray 的 \UseTblrLibrary 要在 siunitx 之后（因为 siunitx 库需要 siunitx 已加载）；hyperref 几乎总是放在最后，唯一的例外是 cleveref，它必须在 hyperref 之后。上面 packages.tex 示例中的顺序可以直接用。 字体与编码 fontspec 配合 XeLaTeX/LuaLaTeX 使用，用来设置系统字体。 \usepackage{fontspec} \setmainfont{TeX Gyre Termes} % 正文衬线字体 \setsansfont{TeX Gyre Heros} % 无衬线字体 \setmonofont{TeX Gyre Cursor} % 等宽字体 xeCJK 处理中文排版的核心宏包，自动处理中英文之间的间距、标点挤压等。 \usepackage{xeCJK} \setCJKmainfont{Source Han Serif SC} % 中文衬线（思源宋体） \setCJKsansfont{Source Han Sans SC} % 中文无衬线（思源黑体） \setCJKmonofont{Source Han Mono SC} % 中文等宽 页面布局 geometry 控制页面尺寸和边距，比 LaTeX 默认的布局参数好用得多。 \usepackage[a4paper, margin=2.5cm]{geometry} % 也可以精细控制 % \usepackage[a4paper, top=3cm, bottom=2.5cm, left=2.8cm, right=2.2cm]{geometry} fancyhdr 自定义页眉页脚。 \usepackage{fancyhdr} \pagestyle{fancy} \fancyhf{} % 清空默认设置 \fancyhead[L]{\leftmark} % 页眉左侧放章节名 \fancyfoot[C]{\thepage} % 页脚中间放页码 \renewcommand{\headrulewidth}{0.4pt} % 页眉横线粗细 数学与代码 amsmath / amssymb / amsfonts 数学排版的基础三件套，基本上写公式就得用。 \usepackage{amsmath} \usepackage{amssymb} \usepackage{amsfonts} mathtools amsmath 的扩展包，修复了一些设计问题并增加了实用工具。 \usepackage{mathtools} % 例如：可以给公式加编号标签 % \usetagform{default} listings 排版代码块。支持语法高亮，可以指定语言和样式。 \usepackage{listings} \lstset{ basicstyle=\ttfamily\small, breaklines=true, frame=single, numbers=left, numberstyle=\tiny\color{gray}, keywordstyle=\color{blue}, commentstyle=\color{green!60!black}, stringstyle=\color{red!70!black}, } minted 基于 Pygments 的代码排版，高亮效果比 listings 好，但需要 Python 环境且编译时要加 -shell-escape。 \usepackage{minted} % 使用示例： % \begin{minted}{python} % def hello(): % print("world") % \end{minted} 图表与浮动体 graphicx 插入图片。 \usepackage{graphicx} \graphicspath % 指定图片搜索路径 tabularray 现代表格宏包，统一了表格的样式配置和内容分离。用 tblr 环境替代传统的 tabular，样式参数写在 \begin{tblr}{...} 的花括号里，正文和样式彻底分开。配合 booktabs 库可以使用 \toprule、\midrule、\bottomrule。 \usepackage{tabularray} \UseTblrLibrary{booktabs, siunitx} % \begin{table} % \begin{tblr}{ % colspec = {lrc}, % row{1} = {font=\bfseries}, % } % \toprule % 姓名 &amp; 分数 &amp; 等级 \\ % \midrule % 张三 &amp; 95 &amp; A \\ % 李四 &amp; 82 &amp; B \\ % \bottomrule % \end{tblr} % \end{table} booktabs 库提供三线表的线条命令，siunitx 库让列可以用 S 类型对齐数字。tabularray 还支持 longtblr（跨页长表格）和 talltblr（不跨页但可加脚注的表格），基本覆盖了所有表格场景。 subcaption 在一个浮动体里放多张子图，每个子图独立编号和引用。 \usepackage{subcaption} % \begin{figure}[htbp] % \begin{subfigure}{0.48\textwidth} % \includegraphics[width=\textwidth]{fig_a} % \caption{子图 A} % \end{subfigure} % \hfill % \begin{subfigure}{0.48\textwidth} % \includegraphics[width=\textwidth]{fig_b} % \caption{子图 B} % \end{subfigure} % \caption{总标题} % \end{figure} 参考文献 biblatex 现代的参考文献管理方案，配合 Biber 后端使用。比 BibTeX 灵活很多，支持多语言、多文献列表等。 \usepackage[backend=biber, style=gb7714-2015]{biblatex} \addbibresource{refs.bib} % 指定 .bib 文件 % 正文中用 \cite{key} 引用 % 文末用 \printbibliography 输出 style=gb7714-2015 是中文文献常用的国标样式，需要 biblatex-gb7714-2015 宏包。英文文档常用 style=ieee、style=apa 或 style=alphabetic。 hyperref 给交叉引用、目录、参考文献加上超链接。通常放在 \usepackage 的最后（少数例外，如 cleveref 要在 hyperref 之后）。 \usepackage[ colorlinks=true, linkcolor=blue, citecolor=blue, urlcolor=blue, ]{hyperref} 其他实用工具 ctex 如果不想手动配 xeCJK，直接用 ctex 宏包或 ctexart/ctexbook 文档类，一键搞定中文支持。 \usepackage[UTF8, heading=true]{ctex} % 或者直接用 \documentclass{ctexart} enumitem 定制列表环境（enumerate、itemize、description）的间距和标签样式。 \usepackage{enumitem} \setlist{nosep} % 去掉列表的多余间距 % \begin{enumerate}[label=(\arabic*)] % \item 第一项 % \item 第二项 % \end{enumerate} tikz 画矢量图。学习曲线陡峭，但画出来的东西质量很高，跟 LaTeX 文档浑然一体。 \usepackage{tikz} \usetikzlibrary{arrows.meta, positioning} % 简单示例：画一个带箭头的直线 % \begin{tikzpicture} % \draw[-&gt;] (0,0) -- (2,1) node[right] {$y = x/2$}; % \end{tikzpicture} siunitx 处理数值和单位的排版。数字的千分位、科学记数法、单位的正体和间距，它都能管。 \usepackage{siunitx} \sisetup{ range-phrase = \ensuremath{\sim}, range-units = single, } % \qty{10}{\metre} -&gt; 10 m % \qty{1.23e5}{\pascal} -&gt; 1.23 × 10⁵ Pa % \qtyrange{1}{10}{\metre} -&gt; 1~10 m（单位只出现一次，用 ~ 连接） range-units=single 让范围表达中单位只出现一次，而不是”1 m 到 10 m”。range-phrase 设成 \ensuremath{\sim} 把分隔符换成 ~（波浪号），\ensuremath 确保它在数学模式和文本模式下都能正确渲染。 cleveref 智能引用，自动加上”图”“表”“公式”等前缀，不用自己写 \ref{fig:xxx} 的图。注意要放在 hyperref 之后加载。 \usepackage[capitalise]{cleveref} % 使用 \cref{fig:example} 会自动输出"图 1" % \cref{eq:main,eq:sub} 会输出"公式 (1) 和 (2)"]]></summary></entry><entry><title type="html">设备树学习笔记：从基础语法到 DTB</title><link href="https://bfmhno3.github.io/note/devicetree-study-notes/" rel="alternate" type="text/html" title="设备树学习笔记：从基础语法到 DTB" /><published>2026-05-20T03:15:00+00:00</published><updated>2026-05-20T03:15:00+00:00</updated><id>https://bfmhno3.github.io/note/devicetree-study-notes</id><content type="html" xml:base="https://bfmhno3.github.io/note/devicetree-study-notes/"><![CDATA[<h2 id="0-引言为什么我们需要设备树">0. 引言：为什么我们需要设备树？</h2>

<p>想象这样一个场景：你的电脑开机，操作系统启动，它需要知道自己运行在什么硬件之上：有几颗 CPU、多大内存、串口在哪里、网卡是什么型号。操作系统如何获取这些信息？</p>

<p>答案是……看情况。</p>

<h3 id="01-自发现-vs-非自发现总线">0.1 自发现 vs 非自发现总线</h3>

<p>现代计算机系统中，总线大致分为两类。</p>

<p><strong>自发现总线（Self-Discoverable Bus）</strong>：PCIe、USB 这类总线，操作系统可以向总线发送探测请求，插在总线上的设备会主动回答 “我在这里，我是某某设备”。插上一个 USB 键盘，系统立刻就能知道这个键盘的存在及其设备描述符。这是一种<strong>运行时动态发现</strong>机制。</p>

<p><strong>非自发现总线（Non-Discoverable Bus）</strong>：I2C、SPI、CAN、系统总线（Processor Bus）等则完全不是这样。在这些总线上，CPU 无法主动 “问” 谁连接在上面。它只知道有一组内存映射的寄存器地址，但对于这些地址背后是什么设备、什么型号、如何配置，CPU 一无所知。这些信息必须由<strong>人为提供</strong>。</p>

<p>嵌入式系统大量使用<strong>非自发现总线</strong>。一个典型的 ARM SoC 上，UART、I2C 控制器、GPIO 控制器、定时器都是通过内存映射连接到处理器总线的，没有任何动态发现机制。</p>

<h3 id="02-历史的教训linus-的愤怒">0.2 历史的教训：Linus 的愤怒</h3>

<p>在设备树出现之前，Linux 对这些非自发现硬件的处理方式简单粗暴：<strong>硬编码</strong>。</p>

<p>ARM 架构的 Linux 内核曾经有一个庞大的 <code class="language-plaintext highlighter-rouge">arch/arm/mach-*</code> 目录，里面为每一款开发板都写了一份 C 代码，直接在内核源码中描述板上有什么设备、基地址是多少、中断号是多少。如果你买了一块新的开发板，就需要在内核里添加一份新的 <code class="language-plaintext highlighter-rouge">board-*.c</code> 文件。</p>

<p>这导致了几个严重问题：</p>

<ol>
  <li><strong>内核膨胀</strong>：同一份内核镜像无法在多块不同板子上运行。每块板子都需要编译一个专用内核。</li>
  <li><strong>代码冗余</strong>：不同板子之间共享同一个 SoC，但因为板上外设组合不同，代码大量重复。</li>
  <li><strong>维护灾难</strong>：2011 年左右，Linux 内核邮件列表上 Linus Torvalds 公开表达了强烈的<a href="https://lkml.org/lkml/2011/3/17/492">不满</a>——ARM 架构的 <code class="language-plaintext highlighter-rouge">mach-*</code> 目录已经变成一个无法维护的垃圾场。</li>
</ol>

<blockquote>
  <p>Gaah. Guys, this whole ARM thing is a f*cking pain in the ass.</p>
</blockquote>

<p>问题的根源在于：<strong>硬件描述信息（数据）被硬编码在了操作系统的核心逻辑（代码）中</strong>。</p>

<h3 id="03-解决办法数据与代码的剥离">0.3 解决办法：数据与代码的剥离</h3>

<p>设备树的本质思想总结为一句话：</p>

<blockquote>
  <p><strong>将硬件的拓扑结构和物理参数从操作系统的核心代码中彻底剥离，以结构化的数据文件形式独立存在。</strong></p>
</blockquote>

<p>在这个模型中：</p>

<ul>
  <li><strong>引导程序</strong>（Boot Program）如 U-Boot、树莓派固件、QEMU，负责将设备树二进制文件（DTB）加载到内存，并将其地址传递给内核。</li>
  <li><strong>内核</strong>（Client Program）在启动时解析这份数据，按图施工：发现设备、匹配驱动、配置寄存器。</li>
</ul>

<p>这是一种<strong>声明式</strong>（Declarative）的硬件描述方式：你告诉内核 “有什么”，而不是 “怎么做”。内核中的驱动程序则负责 “怎么做”。</p>

<p>这个思想和 Web 前端分离 HTML（结构/数据）与 JavaScript（逻辑）如出一辙，或者类比数据库系统中的 DDL（数据定义）与 DML（数据操作）。</p>

<p>设备树最早来自 Open Firmware（IEEE 1275），用在 PowerPC 和 SPARC 平台上。2005 年，PowerPC Linux 在合并 32 位/64 位支持的清理工作中，将设备树设为强制要求——由此诞生了 “扁平化设备树”（Flattened Device Tree, FDT）格式。此后，FDT 被推广到所有架构。</p>

<p>截至本文写作时，arm、arm64、microblaze、mips、powerpc、sparc、x86、riscv 等主流架构均已支持设备树。</p>

<hr />

<h2 id="1-设备树的基础语法">1. 设备树的基础语法</h2>

<h3 id="11-dts-源码与-dtb-二进制">1.1 DTS 源码与 DTB 二进制</h3>

<p>设备树有两种形态，二者关系类似 C 语言中 <code class="language-plaintext highlighter-rouge">.c</code> 源文件与编译后的二进制可执行文件：</p>

<table>
  <thead>
    <tr>
      <th>形态</th>
      <th>文件名</th>
      <th>本质</th>
      <th>使用者</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>**DTS** (Device Tree Source)</td>
      <td><code class="language-plaintext highlighter-rouge">.dts</code> / <code class="language-plaintext highlighter-rouge">.dtsi</code></td>
      <td>人类可读的文本源码</td>
      <td>开发者</td>
    </tr>
    <tr>
      <td>**DTB** (Device Tree Blob)</td>
      <td><code class="language-plaintext highlighter-rouge">.dtb</code></td>
      <td>二进制的扁平化树结构</td>
      <td>内核</td>
    </tr>
  </tbody>
</table>

<p>DTS 通过<strong>设备树编译器</strong>（DTC, Device Tree Compiler）编译成 DTB。同时，DTC 也支持反编译（<code class="language-plaintext highlighter-rouge">dtc -I dtb -O dts</code>），方便逆向查看既有 DTB 内容。</p>

<p><code class="language-plaintext highlighter-rouge">.dts</code> 文件是设备树源文件，通常一个板子一个。<code class="language-plaintext highlighter-rouge">.dtsi</code> 文件是<strong>可包含的共用文件</strong>（”i” = include），通常描述一个 SoC 的内部结构，然后被多个板子的 <code class="language-plaintext highlighter-rouge">.dts</code> 以 <code class="language-plaintext highlighter-rouge">/include/</code> 指令引用——板级 <code class="language-plaintext highlighter-rouge">.dts</code> 引用 SoC 级 <code class="language-plaintext highlighter-rouge">.dtsi</code>，只在末尾补充板上特有的外设。</p>

<h3 id="12-节点node与路径">1.2 节点（Node）与路径</h3>

<p>设备树是一个<strong>树形数据结构</strong>。每个节点描述一个设备或一个总线，节点可以包含属性和子节点。</p>

<h4 id="节点命名规则">节点命名规则</h4>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">node</span><span class="o">-</span><span class="n">name</span><span class="err">@</span><span class="n">unit</span><span class="o">-</span><span class="n">address</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">node-name</code>：1-31 个字符，只能使用数字、大小写字母和 <code class="language-plaintext highlighter-rouge">,._+-</code>，必须以字母开头。推荐使用描述设备<strong>类别</strong>的通用名称，而非具体型号——例如用 <code class="language-plaintext highlighter-rouge">serial</code> 而不是 <code class="language-plaintext highlighter-rouge">ns16550</code>，用 <code class="language-plaintext highlighter-rouge">ethernet</code> 而不是 <code class="language-plaintext highlighter-rouge">rtl8139</code>。这种”重类别、轻型号”的命名哲学让设备树在不同硬件平台间保持可读性。</li>
  <li><code class="language-plaintext highlighter-rouge">@unit-address</code>：可选的单元地址，必须与 <code class="language-plaintext highlighter-rouge">reg</code> 属性中的第一个地址值匹配。如果节点没有 <code class="language-plaintext highlighter-rouge">reg</code> 属性，则必须省略 <code class="language-plaintext highlighter-rouge">@unit-address</code>。</li>
</ul>

<p>为什么单元地址必须和 <code class="language-plaintext highlighter-rouge">reg</code> 首地址一致？因为 DTC 编译器会校验这种一致性，确保设备树的自我完整性——你在节点名中承诺的物理地址，必须和寄存器基地址真正对上，否则树结构本身就是错的。</p>

<p>举例：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">cpu</span><span class="err">@</span><span class="mi">0</span>          <span class="err">——</span> <span class="mi">0</span> <span class="err">号</span> <span class="n">CPU</span>
<span class="n">serial</span><span class="err">@</span><span class="mi">4600</span>    <span class="err">——</span> <span class="err">基地址</span> <span class="mh">0x4600</span> <span class="err">的串口</span>
<span class="n">i2c</span><span class="err">@</span><span class="mi">7000</span><span class="n">c000</span>   <span class="err">——</span> <span class="err">基地址</span> <span class="mh">0x7000c000</span> <span class="err">的</span> <span class="n">I2C</span> <span class="err">控制器</span>
</code></pre></div></div>

<h4 id="树状层级与绝对路径">树状层级与绝对路径</h4>

<p>每个节点在树中有唯一路径，从根节点 <code class="language-plaintext highlighter-rouge">/</code> 开始：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">/</span><span class="n">cpus</span><span class="o">/</span><span class="n">cpu</span><span class="err">@</span><span class="mi">0</span>
<span class="o">/</span><span class="n">soc</span><span class="o">/</span><span class="n">serial</span><span class="err">@</span><span class="mi">4600</span>
<span class="o">/</span><span class="n">soc</span><span class="o">/</span><span class="n">i2c</span><span class="err">@</span><span class="mi">7000</span><span class="n">c000</span><span class="o">/</span><span class="n">codec</span><span class="err">@</span><span class="mi">1</span><span class="n">a</span>
</code></pre></div></div>

<p>这种层级镜像了硬件的物理拓扑：芯片内部总线（SoC）下挂载着各种外设控制器，而 I2C 控制器下面又挂着连接在 I2C 总线上的设备（如音频编解码器）。</p>

<h3 id="13-属性property数据的载体">1.3 属性（Property）：数据的载体</h3>

<p>每个节点由若干属性定义。属性是键值对结构。</p>

<p>DTSpec 定义了以下几种基本的属性值类型：</p>

<p><strong>〈空值〉（Empty）</strong>：属性的存在本身就是一个布尔信号。例如 <code class="language-plaintext highlighter-rouge">interrupt-controller;</code> 表示不需要任何值，有这个属性就代表”我是中断控制器”。</p>

<p><strong>〈u32〉（32 位整数）</strong>：大端序存储的 32 位无符号整数，在 DTS 源码中用尖括号包裹，例如：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">clock</span><span class="o">-</span><span class="n">frequency</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mi">825000000</span><span class="o">&gt;</span><span class="p">;</span>
</code></pre></div></div>

<p>DTS 支持在尖括号内使用 C 风格的算术、位运算和三元表达式：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="p">(</span><span class="mh">0x1000</span> <span class="o">+</span> <span class="mh">0x200</span><span class="p">)</span> <span class="mh">0x100</span><span class="o">&gt;</span><span class="p">;</span>
</code></pre></div></div>

<p><strong>〈u64〉（64 位整数）</strong>：用两个 <code class="language-plaintext highlighter-rouge">&lt;u32&gt;</code> 表示，高 32 位在前，例如：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">clock</span><span class="o">-</span><span class="n">frequency</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x00000001</span> <span class="mh">0x00000000</span><span class="o">&gt;</span><span class="p">;</span>
</code></pre></div></div>

<p>这表示的是 64 位值 <code class="language-plaintext highlighter-rouge">0x100000000</code>：第一个 cell 是高 32 位（<code class="language-plaintext highlighter-rouge">0x00000001</code>），第二个是低 32 位（<code class="language-plaintext highlighter-rouge">0x00000000</code>）。</p>

<p><strong>〈string〉（字符串）</strong>：双引号包裹，以 null 结尾，例如：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">compatible</span> <span class="o">=</span> <span class="s">"ns16550"</span><span class="p">;</span>
</code></pre></div></div>

<p><strong>〈stringlist〉（字符串列表）</strong>：多个字符串拼接，例如：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">compatible</span> <span class="o">=</span> <span class="s">"fsl,mpc8641"</span><span class="p">,</span> <span class="s">"ns16550"</span><span class="p">;</span>
</code></pre></div></div>

<p><strong>〈phandle〉（节点引用）</strong>：一个 <code class="language-plaintext highlighter-rouge">&lt;u32&gt;</code> 值，指向树中另一个节点。DTC 在编译时自动为有标签的节点分配唯一的 phandle 值。</p>

<p><strong>〈prop-encoded-array〉（自定义编码数组）</strong>：格式取决于具体属性定义，最常见的就是 <code class="language-plaintext highlighter-rouge">reg</code> 中的（地址, 长度）对。</p>

<h3 id="14-语法糖标签与引用">1.4 语法糖：标签与引用</h3>

<p>如果每次都要写 <code class="language-plaintext highlighter-rouge">/soc/i2c@7000c000/codec@1a</code> 这样的绝对路径来引用一个节点，代码会变得难以阅读。DTS 提供了两个机制来解决这个问题。</p>

<p><strong>标签（Label）</strong>：在节点或属性前加 <code class="language-plaintext highlighter-rouge">标签名:</code> 定义，例如：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">wm8903:</span> <span class="n">codec</span><span class="err">@</span><span class="mi">1</span><span class="n">a</span> <span class="p">{</span>
    <span class="n">compatible</span> <span class="o">=</span> <span class="s">"wlf,wm8903"</span><span class="p">;</span>
    <span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x1a</span><span class="o">&gt;</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div>

<p><strong>引用（Reference）</strong>：<code class="language-plaintext highlighter-rouge">&amp;标签名</code> 引用已定义的节点，例如：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">sound</span> <span class="p">{</span>
    <span class="n">compatible</span> <span class="o">=</span> <span class="s">"nvidia,harmony-sound"</span><span class="p">;</span>
    <span class="n">i2s</span><span class="o">-</span><span class="n">controller</span> <span class="o">=</span> <span class="o">&lt;&amp;</span><span class="n">i2s1</span><span class="o">&gt;</span><span class="p">;</span>
    <span class="n">i2s</span><span class="o">-</span><span class="n">codec</span> <span class="o">=</span> <span class="o">&lt;&amp;</span><span class="n">wm8903</span><span class="o">&gt;</span><span class="p">;</span>   <span class="cm">/* 引用上面的 codec 节点 */</span>
<span class="p">};</span>
</code></pre></div></div>

<p>在 DTC 编译时，<code class="language-plaintext highlighter-rouge">&amp;wm8903</code> 会被展开为该节点的 phandle 值，存入 DTB。</p>

<p>引用语法还有一个常见用法：<strong>引用覆盖</strong>。在板级 <code class="language-plaintext highlighter-rouge">.dts</code> 中，你可以这样激活或修改 SoC 级 <code class="language-plaintext highlighter-rouge">.dtsi</code> 中已定义的外设：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cm">/* 在 SoC .dtsi 中，外设默认 disabled */</span>
<span class="o">&amp;</span><span class="n">uart1</span> <span class="p">{</span>
    <span class="n">status</span> <span class="o">=</span> <span class="s">"disabled"</span><span class="p">;</span>
<span class="p">};</span>

<span class="cm">/* 板级 .dts 中，将它启用 */</span>
<span class="o">&amp;</span><span class="n">uart1</span> <span class="p">{</span>
    <span class="n">status</span> <span class="o">=</span> <span class="s">"okay"</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div>

<p>这种 “先定义、后覆盖” 的模式是设备树实际工程中的核心工作流。</p>

<hr />

<h2 id="2-描述硬件的三大核心维度">2. 描述硬件的三大核心维度</h2>

<p>如果设备树是一份硬件档案，那么描述一个设备至少需要回答三个问题：<strong>你是谁、你在哪里、你还活着吗</strong>。</p>

<h3 id="21-身份认同compatible-和-model">2.1 身份认同：<code class="language-plaintext highlighter-rouge">compatible</code> 和 <code class="language-plaintext highlighter-rouge">model</code></h3>

<p><strong><code class="language-plaintext highlighter-rouge">compatible</code></strong> 是设备树中最重要的属性。它定义了一个设备的 “编程模型”，也就是软件（驱动）应该用哪种接口协议来操作这个硬件。</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">compatible</span> <span class="o">=</span> <span class="s">"manufacturer,model"</span><span class="p">;</span>
</code></pre></div></div>

<p>格式严格规定为 <code class="language-plaintext highlighter-rouge">&lt;制造商&gt;,&lt;型号&gt;</code>，全小写，不用下划线。例如：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">compatible</span> <span class="o">=</span> <span class="s">"arm,cortex-a72"</span><span class="p">;</span>
<span class="n">compatible</span> <span class="o">=</span> <span class="s">"nvidia,tegra20-uart"</span><span class="p">;</span>
<span class="n">compatible</span> <span class="o">=</span> <span class="s">"wlf,wm8903"</span><span class="p">;</span>        <span class="cm">/* Wolfson 的 WM8903 音频编解码器 */</span>
</code></pre></div></div>

<p><strong>多重兼容性策略</strong>：<code class="language-plaintext highlighter-rouge">compatible</code> 的值是一个从<strong>最特化</strong>到<strong>最泛化</strong>的字符串列表。内核会按顺序逐一尝试匹配驱动：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">compatible</span> <span class="o">=</span> <span class="s">"fsl,mpc8641"</span><span class="p">,</span> <span class="s">"ns16550"</span><span class="p">;</span>
</code></pre></div></div>

<p>这条配置告诉内核：优先找 Freescale MPC8641 专用驱动；如果没找到，就退而求其次，用通用的 NS16550 UART 驱动。这种”回退”（fallback）策略让同一套内核可以在范围极大的硬件上运行。</p>

<p><strong><code class="language-plaintext highlighter-rouge">model</code></strong> 是一个人类可读的字符串，描述设备的制造商型号：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">model</span> <span class="o">=</span> <span class="s">"fsl,MPC8349EMITX"</span><span class="p">;</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">model</code> 和 <code class="language-plaintext highlighter-rouge">compatible</code> 的核心区别在于使用者不同：<code class="language-plaintext highlighter-rouge">model</code> 是给人（开发者、调试者）看的，<code class="language-plaintext highlighter-rouge">compatible</code> 是给内核的设备匹配算法用的。也就是说，<code class="language-plaintext highlighter-rouge">model</code> 可以由开发者自己确定，随意编写，而 <code class="language-plaintext highlighter-rouge">compatible</code> 则应该遵循一定的规范。</p>

<p>那 <code class="language-plaintext highlighter-rouge">compatible</code> 字符串从哪里来？<code class="language-plaintext highlighter-rouge">&lt;制造商&gt;,&lt;型号&gt;</code> 中的每一对合法取值，都有对应的绑定文档（Binding Document）加以规范。Linux 内核源码树的 <code class="language-plaintext highlighter-rouge">Documentation/devicetree/bindings/</code> 目录就是这份 “字典”：按设备类别分子目录（<code class="language-plaintext highlighter-rouge">serial/</code>、<code class="language-plaintext highlighter-rouge">i2c/</code>、<code class="language-plaintext highlighter-rouge">gpio/</code> 等），每个 <code class="language-plaintext highlighter-rouge">.yaml</code> 文件精确声明该设备支持哪些 <code class="language-plaintext highlighter-rouge">compatible</code> 字符串、哪些属性是必须的、哪些属性是可选的、各属性的合法取值范围。</p>

<p>为设备挑选 <code class="language-plaintext highlighter-rouge">compatible</code> 字符串的正确方式是：先查 <code class="language-plaintext highlighter-rouge">Documentation/devicetree/bindings/</code> 找到对应 binding，再照着 binding 填写，而不是凭经验猜测或照抄其他设备树。</p>

<h3 id="22-寻址空间regaddress-cells-与-size-cells">2.2 寻址空间：<code class="language-plaintext highlighter-rouge">reg</code>、<code class="language-plaintext highlighter-rouge">#address-cells</code> 与 <code class="language-plaintext highlighter-rouge">#size-cells</code></h3>

<p>这三个属性构成了设备树寻址系统的核心。</p>

<p><code class="language-plaintext highlighter-rouge">#address-cells</code> 和 `#size-cells 由<strong>父节点</strong>定义，告诉子节点：描述一个地址需要几个 32 位 cell，描述一个长度需要几个 cell。</p>

<p>这里的 cell 是设备树规范继承自 Open Firmware 的基本数据单元，固定为一个 <strong>32 位无符号整数</strong>。当硬件的地址或长度超过 32 位时，就用多个连续的 cell 拼接：两个 cell 拼接成 64 位，高位在前。</p>

<p>因此，<code class="language-plaintext highlighter-rouge">#address-cells = &lt;2&gt;</code> 意味着子节点的地址用两个 32 位整数（合计 64 位）来描述，<code class="language-plaintext highlighter-rouge">#address-cells = &lt;1&gt;</code> 则是单个 32 位整数就够了。</p>

<p><strong>这两个属性不会被继承，每个有子节点的节点必须自己显式定义</strong>。</p>

<p><strong><code class="language-plaintext highlighter-rouge">reg</code></strong> 属性描述设备的寄存器资源，格式为 <code class="language-plaintext highlighter-rouge">&lt;地址 长度&gt;</code> 对：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">soc</span> <span class="p">{</span>
    <span class="cp">#address-cells = &lt;1&gt;;
</span>    <span class="cp">#size-cells = &lt;1&gt;;
</span>
    <span class="n">serial</span><span class="err">@</span><span class="mi">4600</span> <span class="p">{</span>
        <span class="n">compatible</span> <span class="o">=</span> <span class="s">"ns16550"</span><span class="p">;</span>
        <span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x4600</span> <span class="mh">0x100</span><span class="o">&gt;</span><span class="p">;</span>   <span class="cm">/* 地址 0x4600，长度 0x100 */</span>
    <span class="p">};</span>
<span class="p">};</span>
</code></pre></div></div>

<p>当 <code class="language-plaintext highlighter-rouge">#size-cells = &lt;0&gt;</code> 时，<code class="language-plaintext highlighter-rouge">reg</code> 中只有地址没有长度，这是带独立片选信号的设备（如 I2C 从设备）的常见情况：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">i2c</span><span class="err">@</span><span class="mi">7000</span><span class="n">c000</span> <span class="p">{</span>
    <span class="cp">#address-cells = &lt;1&gt;;
</span>    <span class="cp">#size-cells = &lt;0&gt;;
</span>
    <span class="n">codec</span><span class="err">@</span><span class="mi">1</span><span class="n">a</span> <span class="p">{</span>
        <span class="n">compatible</span> <span class="o">=</span> <span class="s">"wlf,wm8903"</span><span class="p">;</span>
        <span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x1a</span><span class="o">&gt;</span><span class="p">;</span>   <span class="cm">/* 只有 I2C 从机地址，没有长度 */</span>
    <span class="p">};</span>
<span class="p">};</span>
</code></pre></div></div>

<p>一个设备有多个寄存器块时，<code class="language-plaintext highlighter-rouge">reg</code> 可以包含多组 <code class="language-plaintext highlighter-rouge">&lt;地址 长度&gt;</code> 对：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x3000</span> <span class="mh">0x20</span> <span class="mh">0xFE00</span> <span class="mh">0x100</span><span class="o">&gt;</span><span class="p">;</span>
<span class="cm">/*    第一块：offset 0x3000, 长 32 字节
      第二块：offset 0xFE00, 长 256 字节    */</span>
</code></pre></div></div>

<h4 id="进阶地址映射与-dma">进阶：地址映射与 DMA</h4>

<p><strong><code class="language-plaintext highlighter-rouge">ranges</code> 属性</strong>：实现父子总线之间的地址翻译。它定义一个 “映射范围”：子总线地址空间的 X，对应父总线地址空间的 Y，长度 Z。</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">soc</span> <span class="p">{</span>
    <span class="n">compatible</span> <span class="o">=</span> <span class="s">"simple-bus"</span><span class="p">;</span>
    <span class="cp">#address-cells = &lt;1&gt;;
</span>    <span class="cp">#size-cells = &lt;1&gt;;
</span>    <span class="n">ranges</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x0</span> <span class="mh">0xe0000000</span> <span class="mh">0x00100000</span><span class="o">&gt;</span><span class="p">;</span>
    <span class="cm">/* 子地址 0x0 映射到父地址 0xe0000000，范围 1MB */</span>

    <span class="n">serial</span><span class="err">@</span><span class="mi">4600</span> <span class="p">{</span>
        <span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x4600</span> <span class="mh">0x100</span><span class="o">&gt;</span><span class="p">;</span>
        <span class="cm">/* 实际物理地址 = 父地址 0xe0000000 + offset 0x4600 = 0xe0004600 */</span>
    <span class="p">};</span>
<span class="p">};</span>
</code></pre></div></div>

<p>当 <code class="language-plaintext highlighter-rouge">ranges</code> 为空值时，即 <code class="language-plaintext highlighter-rouge">ranges;</code>，意味着父子地址空间完全一致，不需要任何平移。</p>

<p><code class="language-plaintext highlighter-rouge">dma-ranges</code>：与 <code class="language-plaintext highlighter-rouge">ranges</code> 类似，但从 DMA 设备的视角描述，解决的是 “DMA 控制器看到的地址” 和 “CPU 看到的物理地址” 之间的差异（例如 IOMMU 的存在会制造这种差异）。</p>

<p><code class="language-plaintext highlighter-rouge">virtual-reg</code>：当引导程序已经设置了虚拟地址映射时，告知内核虚拟地址到物理地址的转换关系。</p>

<div class="notice--info">
  <p><code class="language-plaintext highlighter-rouge">reg</code> 和 <code class="language-plaintext highlighter-rouge">ranges</code> 是设备树中最容易被误解的概念。简单记忆法则：</p>

  <ul>
    <li><code class="language-plaintext highlighter-rouge">reg</code> = “我的寄存器在<strong>我所在总线地址空间</strong>的什么位置”</li>
    <li><code class="language-plaintext highlighter-rouge">ranges</code> = “<strong>我的总线地址空间</strong>如何映射到<strong>我父亲的总线地址空间</strong>”</li>
    <li>最终 CPU 看到的物理地址 = 层层 ranges 翻译之和</li>
  </ul>
</div>

<h3 id="23-运行状态status">2.3 运行状态：<code class="language-plaintext highlighter-rouge">status</code></h3>

<p><code class="language-plaintext highlighter-rouge">status</code> 属性标记设备当前的操作状态：</p>

<table>
  <thead>
    <tr>
      <th>值</th>
      <th>含义</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">"okay"</code></td>
      <td>设备正常工作</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">"disabled"</code></td>
      <td>设备当前不可用，但可能在未来可用（如模块未插入）</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">"reserved"</code></td>
      <td>设备可用，但不应被本操作系统使用（被固件或其他 OS 控制）</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">"fail"</code></td>
      <td>设备出现严重错误，不可能恢复</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">"fail-sss"</code></td>
      <td>同上，<code class="language-plaintext highlighter-rouge">sss</code> 为设备特定的错误码</td>
    </tr>
  </tbody>
</table>

<p>省略 <code class="language-plaintext highlighter-rouge">status</code> 属性等同于 <code class="language-plaintext highlighter-rouge">status = "okay"</code>。</p>

<p>在实际工程中最常见的场景是：SoC 级 <code class="language-plaintext highlighter-rouge">.dtsi</code> 中所有外设默认 <code class="language-plaintext highlighter-rouge">status = "disabled"</code>，板级 <code class="language-plaintext highlighter-rouge">.dts</code> 中只启用实际使用的外设——设置为 <code class="language-plaintext highlighter-rouge">status = "okay"</code>。</p>

<hr />

<h2 id="3-硬件中断的逻辑连线">3. 硬件中断的逻辑连线</h2>

<p>中断描述是设备树最难理解的部分之一，因为<strong>中断在硬件上的信号流向和在设备树上的描述路径，经常与设备物理层级不同</strong>。</p>

<p>举个典型例子：一颗 I2C 触摸屏控制器挂在 I2C 总线上，所以它在设备树里的物理路径通常是 <code class="language-plaintext highlighter-rouge">soc</code> -&gt; <code class="language-plaintext highlighter-rouge">i2c</code> -&gt; <code class="language-plaintext highlighter-rouge">touchscreen</code>；但它的中断线并不回到 I2C 控制器，而是接到 GPIO 控制器的某个引脚，再由 GPIO 控制器上报给 GIC。于是它的中断路径变成 <code class="language-plaintext highlighter-rouge">touchscreen</code> -&gt; <code class="language-plaintext highlighter-rouge">gpio-controller</code> -&gt; <code class="language-plaintext highlighter-rouge">gic</code>。</p>

<p>这就是 “设备物理层级” 和 “中断描述路径” 不一致的真实场景：你在设备树里看到它在 I2C 下，但它的中断信号却是走的 GPIO/GIC 这条逻辑链路。</p>

<h3 id="31-中断树跳出物理层级的逻辑结构">3.1 中断树：跳出物理层级的逻辑结构</h3>

<p>设备树采用<strong>中断树</strong>（Interrupt Tree）模型来表达中断路由。虽然叫做 “树”，但它更准确的描述是一个<strong>有向无环图</strong>（Directed Acyclic Graph）。</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Device</span> <span class="n">Tree</span> <span class="err">层级</span><span class="o">:</span>             <span class="n">Interrupt</span> <span class="n">Tree</span> <span class="err">层级</span><span class="o">:</span>

<span class="n">soc</span>                             <span class="n">open</span><span class="o">-</span><span class="n">pic</span> <span class="p">(</span><span class="err">根</span><span class="p">)</span>
<span class="err">├──</span> <span class="n">device1</span> <span class="o">------------------&gt;</span> <span class="p">(</span><span class="err">直接连接</span><span class="p">)</span>
<span class="err">├──</span> <span class="n">pci</span><span class="o">-</span><span class="n">host</span> <span class="o">-----------------&gt;</span> <span class="p">(</span><span class="n">nexus</span> <span class="err">节点</span><span class="p">,</span> <span class="err">中转</span><span class="p">)</span>
<span class="err">│</span>   <span class="err">├──</span> <span class="n">slot0</span> <span class="o">----------------&gt;</span> <span class="p">(</span><span class="err">经</span> <span class="n">pci</span><span class="o">-</span><span class="n">host</span> <span class="err">中转</span><span class="p">)</span>
<span class="err">│</span>   <span class="err">└──</span> <span class="n">slot1</span> <span class="o">----------------&gt;</span> <span class="p">(</span><span class="err">经</span> <span class="n">pci</span><span class="o">-</span><span class="n">host</span> <span class="err">中转</span><span class="p">)</span>
<span class="err">└──</span> <span class="n">gpioctrl</span> <span class="o">-----------------&gt;</span> <span class="p">(</span><span class="n">nexus</span> <span class="err">节点</span><span class="p">,</span> <span class="err">中转</span><span class="p">)</span>
    <span class="err">└──</span> <span class="n">device3</span> <span class="o">--------------&gt;</span> <span class="p">(</span><span class="err">经</span> <span class="n">gpioctrl</span> <span class="err">中转</span><span class="p">)</span>
</code></pre></div></div>

<p>关键思想：中断信号在硬件上跨越多层物理总线，设备树通过在逻辑上建立直接的中断父子关系来”压平”这个复杂性。</p>

<h3 id="32-中断信号的属性">3.2 中断信号的属性</h3>

<p><strong>中断产生者（Interrupt-Generating Device）</strong>使用两个核心属性：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">device1</span> <span class="p">{</span>
    <span class="n">interrupts</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0xA</span> <span class="mi">8</span><span class="o">&gt;</span><span class="p">;</span>              <span class="cm">/* 该设备产生的中断规格（specifier） */</span>
    <span class="n">interrupt</span><span class="o">-</span><span class="n">parent</span> <span class="o">=</span> <span class="o">&lt;&amp;</span><span class="n">open</span><span class="o">-</span><span class="n">pic</span><span class="o">&gt;</span><span class="p">;</span>     <span class="cm">/* 中断发往哪个控制器 */</span>
<span class="p">};</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">interrupts</code> 的值格式由目标中断控制器定义的 bindings 决定——对一个 Open PIC 兼容的中断域，两个 cells 分别表示中断号和电平/敏感度信息。</p>

<p>如果设备没有显式的 <code class="language-plaintext highlighter-rouge">interrupt-parent</code>，中断父节点就<strong>默认向上继承</strong>——沿着设备树向上找，使用第一个声明的中断父节点。所以当中断父节点在 SoC 级别统一指定时，设备可以不写 <code class="language-plaintext highlighter-rouge">interrupt-parent</code>：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">soc</span> <span class="p">{</span>
    <span class="n">interrupt</span><span class="o">-</span><span class="n">parent</span> <span class="o">=</span> <span class="o">&lt;&amp;</span><span class="n">intc</span><span class="o">&gt;</span><span class="p">;</span>   <span class="cm">/* 所有子设备统一的中断父节点 */</span>

    <span class="n">serial</span><span class="err">@</span><span class="mi">70006300</span> <span class="p">{</span>
        <span class="n">interrupts</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mi">122</span><span class="o">&gt;</span><span class="p">;</span>       <span class="cm">/* 不需要重复 interrupt-parent */</span>
    <span class="p">};</span>
<span class="p">};</span>
</code></pre></div></div>

<p><strong><code class="language-plaintext highlighter-rouge">interrupts-extended</code></strong> 用于一个设备连接到多个中断控制器的情况：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">interrupts</span><span class="o">-</span><span class="n">extended</span> <span class="o">=</span> <span class="o">&lt;&amp;</span><span class="n">pic</span> <span class="mh">0xA</span> <span class="mi">8</span><span class="o">&gt;</span><span class="p">,</span> <span class="o">&lt;&amp;</span><span class="n">gic</span> <span class="mh">0xda</span><span class="o">&gt;</span><span class="p">;</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">interrupts</code> 和 <code class="language-plaintext highlighter-rouge">interrupts-extended</code> 互斥。当两者共存时，<code class="language-plaintext highlighter-rouge">interrupts-extended</code> 优先。</p>

<h3 id="33-中断控制器">3.3 中断控制器</h3>

<p>一个节点要成为中断控制器，需要两个属性：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">open</span><span class="o">-</span><span class="n">pic</span> <span class="p">{</span>
    <span class="n">interrupt</span><span class="o">-</span><span class="n">controller</span><span class="p">;</span>           <span class="cm">/* 标记：我是中断控制器（空值属性） */</span>
    <span class="cp">#interrupt-cells = &lt;2&gt;;         </span><span class="cm">/* 每个中断 specifier 占几个 cell */</span><span class="cp">
</span><span class="p">};</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">#interrupt-cells</code> 定义了中断规格的”宽度”。对于 Open PIC 这种需要 2 cells（中断号 + 电平/敏感度）的控制器，值就是 <code class="language-plaintext highlighter-rouge">&lt;2&gt;</code>。对于 ARM GIC (Generic Interrupt Controller)，可能是 <code class="language-plaintext highlighter-rouge">&lt;3&gt;</code>（中断类型 + 中断号 + 触发类型）。</p>

<p>中断树的根是遍历中断父链直到找到一个<strong>没有 <code class="language-plaintext highlighter-rouge">interrupts</code> 属性</strong>的中断控制器——这意味着它本身不向更高的控制器报告中断。</p>

<h3 id="34-中继nexus-节点与中断映射">3.4 中继：Nexus 节点与中断映射</h3>

<p>当不同中断域之间需要翻译时（比如 PCI 的 INTA/INTB/INTC/INTD 经过 PCI host bridge 转换到系统中断号），需要一个<strong>nexus 节点</strong>，通过 <code class="language-plaintext highlighter-rouge">interrupt-map</code> 来做这个翻译。</p>

<p>一条映射表行包含五个部分：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">interrupt</span><span class="o">-</span><span class="n">map</span> <span class="o">=</span> <span class="o">&lt;</span>
    <span class="n">child</span><span class="o">-</span><span class="n">unit</span><span class="o">-</span><span class="n">address</span>  <span class="n">child</span><span class="o">-</span><span class="n">interrupt</span><span class="o">-</span><span class="n">specifier</span>
    <span class="n">interrupt</span><span class="o">-</span><span class="n">parent</span><span class="o">-</span><span class="n">phandle</span>
    <span class="n">parent</span><span class="o">-</span><span class="n">unit</span><span class="o">-</span><span class="n">address</span>  <span class="n">parent</span><span class="o">-</span><span class="n">interrupt</span><span class="o">-</span><span class="n">specifier</span>
<span class="o">&gt;</span><span class="p">;</span>
</code></pre></div></div>

<p>例如，PCI slot 1 的 INTA（INT #1）映射到 Open PIC 控制器的中断源 2：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">interrupt</span><span class="o">-</span><span class="n">map</span> <span class="o">=</span> <span class="o">&lt;</span>
    <span class="mh">0x8800</span> <span class="mi">0</span> <span class="mi">0</span>  <span class="mi">1</span>  <span class="o">&amp;</span><span class="n">open</span><span class="o">-</span><span class="n">pic</span>   <span class="mi">2</span> <span class="mi">1</span>
<span class="o">&gt;</span><span class="p">;</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">interrupt-map-mask</code> 在匹配前被 AND 到输入上，作用是屏蔽掉不需要参与匹配的位（如 PCI 总线的 function number 可能不区分）。</p>

<h3 id="35-gpio-等非中断信号的通用映射机制">3.5 GPIO 等非中断信号的通用映射机制</h3>

<p>DTSpec 将 nexus 映射机制推广到了所有类型的 “规格（specifier）”：<code class="language-plaintext highlighter-rouge">&lt;specifier&gt;-map</code>、<code class="language-plaintext highlighter-rouge">&lt;specifier&gt;-map-mask</code>、<code class="language-plaintext highlighter-rouge">&lt;specifier&gt;-map-pass-thru</code> 以及 <code class="language-plaintext highlighter-rouge">#&lt;specifier&gt;-cells</code>。</p>

<p>最常见的例子是 <strong>GPIO 映射</strong>：一个连接器（connector）上的 GPIO 通过 <code class="language-plaintext highlighter-rouge">gpio-map</code> 路由到 SoC 内部的某个 GPIO 控制器：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">connector</span> <span class="p">{</span>
    <span class="cp">#gpio-cells = &lt;2&gt;;
</span>    <span class="n">gpio</span><span class="o">-</span><span class="n">map</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mi">0</span> <span class="mi">0</span> <span class="o">&amp;</span><span class="n">soc_gpio1</span> <span class="mi">1</span> <span class="mi">0</span><span class="o">&gt;</span><span class="p">,</span>
               <span class="o">&lt;</span><span class="mi">1</span> <span class="mi">0</span> <span class="o">&amp;</span><span class="n">soc_gpio2</span> <span class="mi">4</span> <span class="mi">0</span><span class="o">&gt;</span><span class="p">,</span>
               <span class="o">&lt;</span><span class="mi">2</span> <span class="mi">0</span> <span class="o">&amp;</span><span class="n">soc_gpio1</span> <span class="mi">3</span> <span class="mi">0</span><span class="o">&gt;</span><span class="p">,</span>
               <span class="o">&lt;</span><span class="mi">3</span> <span class="mi">0</span> <span class="o">&amp;</span><span class="n">soc_gpio2</span> <span class="mi">2</span> <span class="mi">0</span><span class="o">&gt;</span><span class="p">;</span>
    <span class="n">gpio</span><span class="o">-</span><span class="n">map</span><span class="o">-</span><span class="n">mask</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0xf</span> <span class="mh">0x0</span><span class="o">&gt;</span><span class="p">;</span>
    <span class="n">gpio</span><span class="o">-</span><span class="n">map</span><span class="o">-</span><span class="n">pass</span><span class="o">-</span><span class="n">thru</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x0</span> <span class="mh">0x1</span><span class="o">&gt;</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">gpio-map-pass-thru</code> 的巧妙之处在于允许子 specifier 中的 flags 字段（如 <code class="language-plaintext highlighter-rouge">GPIO_ACTIVE_LOW</code>）穿越映射表，直接透传到父域，避免映射表需要为每种 flag 组合都写一行。</p>

<p class="notice--info">理解设备树中断子系统的最佳思路：中断信号的路径是一张<strong>独立于设备物理层级的有向图</strong>。遍历从 <code class="language-plaintext highlighter-rouge">interrupts</code>/<code class="language-plaintext highlighter-rouge">interrupt-parent</code> 开始，沿链上溯，直到中断树根（没有 <code class="language-plaintext highlighter-rouge">interrupts</code> 的中断控制器）。每一层可能是中断控制器或 nexus 节点——nexus 节点通过 <code class="language-plaintext highlighter-rouge">interrupt-map</code> 表提供域间翻译。这一整套设计的目的在于允许同一个设备树描述中的任何节点都能够表达自己的中断路由，机制统一且可扩展。</p>

<hr />

<h2 id="4-拼装一台计算机系统骨架与必选节点">4. 拼装一台计算机：系统骨架与必选节点</h2>

<p>设备树不只是描述外设。一套完整的设备树必须描述整个系统的骨架 —— CPU 几核、内存多大、控制台在哪。</p>

<h3 id="41-根节点-">4.1 根节点 <code class="language-plaintext highlighter-rouge">/</code></h3>

<p>根节点没有名字，用 <code class="language-plaintext highlighter-rouge">/</code> 表示。它的核心属性包括：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">/</span> <span class="p">{</span>
    <span class="n">model</span> <span class="o">=</span> <span class="s">"fsl,mpc8572ds"</span><span class="p">;</span>
    <span class="n">compatible</span> <span class="o">=</span> <span class="s">"fsl,mpc8572ds"</span><span class="p">;</span>
    <span class="cp">#address-cells = &lt;1&gt;;
</span>    <span class="cp">#size-cells = &lt;1&gt;;
</span>    <span class="n">serial</span><span class="o">-</span><span class="n">number</span> <span class="o">=</span> <span class="s">"PZ123456"</span><span class="p">;</span>      <span class="cm">/* 可选：序列号 */</span>
    <span class="n">chassis</span><span class="o">-</span><span class="n">type</span> <span class="o">=</span> <span class="s">"embedded"</span><span class="p">;</span>       <span class="cm">/* 可选：机箱类型 */</span>
<span class="p">};</span>
</code></pre></div></div>

<p>根节点的 <code class="language-plaintext highlighter-rouge">#address-cells</code> 和 <code class="language-plaintext highlighter-rouge">#size-cells</code> 定义了内存地址的编码规则，影响 <code class="language-plaintext highlighter-rouge">/memory</code> 节点的 <code class="language-plaintext highlighter-rouge">reg</code> 格式。</p>

<p><code class="language-plaintext highlighter-rouge">chassis-type</code> 可取 <code class="language-plaintext highlighter-rouge">"desktop"</code>、<code class="language-plaintext highlighter-rouge">"laptop"</code>、<code class="language-plaintext highlighter-rouge">"convertible"</code>、<code class="language-plaintext highlighter-rouge">"server"</code>、<code class="language-plaintext highlighter-rouge">"tablet"</code>、<code class="language-plaintext highlighter-rouge">"handset"</code>、<code class="language-plaintext highlighter-rouge">"embedded"</code> 等值。</p>

<h3 id="42-大脑cpus">4.2 大脑：<code class="language-plaintext highlighter-rouge">/cpus</code></h3>

<p><code class="language-plaintext highlighter-rouge">/cpus</code> 节点是所有 CPU 节点的容器：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">cpus</span> <span class="p">{</span>
    <span class="cp">#address-cells = &lt;1&gt;;
</span>    <span class="cp">#size-cells = &lt;0&gt;;
</span>
    <span class="n">cpu</span><span class="err">@</span><span class="mi">0</span> <span class="p">{</span>
        <span class="n">device_type</span> <span class="o">=</span> <span class="s">"cpu"</span><span class="p">;</span>
        <span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mi">0</span><span class="o">&gt;</span><span class="p">;</span>
        <span class="n">clock</span><span class="o">-</span><span class="n">frequency</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mi">825000000</span><span class="o">&gt;</span><span class="p">;</span>      <span class="cm">/* 825 MHz */</span>
        <span class="n">timebase</span><span class="o">-</span><span class="n">frequency</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mi">82500000</span><span class="o">&gt;</span><span class="p">;</span>    <span class="cm">/* 82.5 MHz 时基 */</span>
    <span class="p">};</span>

    <span class="n">cpu</span><span class="err">@</span><span class="mi">1</span> <span class="p">{</span>
        <span class="n">device_type</span> <span class="o">=</span> <span class="s">"cpu"</span><span class="p">;</span>
        <span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mi">1</span><span class="o">&gt;</span><span class="p">;</span>
        <span class="n">clock</span><span class="o">-</span><span class="n">frequency</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mi">825000000</span><span class="o">&gt;</span><span class="p">;</span>
    <span class="p">};</span>
<span class="p">};</span>
</code></pre></div></div>

<p>多核 SMP 系统中，<code class="language-plaintext highlighter-rouge">status</code> 属性标记每颗 CPU 的运行状态：</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">"okay"</code>：该 CPU 正常运行</li>
  <li><code class="language-plaintext highlighter-rouge">"disabled"</code>：该 CPU 处于<strong>静止状态</strong>（quiescent state），可通过特定方法唤醒</li>
  <li><code class="language-plaintext highlighter-rouge">"fail"</code>：该 CPU 不可用或不存在</li>
</ul>

<p>对于 <code class="language-plaintext highlighter-rouge">disabled</code> 的 CPU，需要 <code class="language-plaintext highlighter-rouge">enable-method</code> 属性指定唤醒方式。最常见的是 <code class="language-plaintext highlighter-rouge">"spin-table"</code>：CPU 在指定物理地址（<code class="language-plaintext highlighter-rouge">cpu-release-addr</code>）上自旋等待，直到引导 CPU 写入释放信号。</p>

<h4 id="缓存层级">缓存层级</h4>

<p>CPU 的缓存层级通过 <code class="language-plaintext highlighter-rouge">next-level-cache</code> 属性和 phandle 引用串联：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">cpu</span><span class="err">@</span><span class="mi">0</span> <span class="p">{</span>
    <span class="n">next</span><span class="o">-</span><span class="n">level</span><span class="o">-</span><span class="n">cache</span> <span class="o">=</span> <span class="o">&lt;&amp;</span><span class="n">L2_0</span><span class="o">&gt;</span><span class="p">;</span>
    <span class="nl">L2_0:</span> <span class="n">l2</span><span class="o">-</span><span class="n">cache</span> <span class="p">{</span>
        <span class="n">compatible</span> <span class="o">=</span> <span class="s">"cache"</span><span class="p">;</span>
        <span class="n">cache</span><span class="o">-</span><span class="n">level</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mi">2</span><span class="o">&gt;</span><span class="p">;</span>
        <span class="n">cache</span><span class="o">-</span><span class="n">size</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x40000</span><span class="o">&gt;</span><span class="p">;</span>   <span class="cm">/* 256 KB */</span>
        <span class="n">next</span><span class="o">-</span><span class="n">level</span><span class="o">-</span><span class="n">cache</span> <span class="o">=</span> <span class="o">&lt;&amp;</span><span class="n">L3</span><span class="o">&gt;</span><span class="p">;</span>
    <span class="p">};</span>
<span class="p">};</span>
</code></pre></div></div>

<p>这种基于 phandle 的多级缓存描述方式让缓存拓扑可以灵活表达各种共享结构（两核共享 L2、全芯片共享 L3 等）。</p>

<h3 id="43-物理记忆memory-和-reserved-memory">4.3 物理记忆：<code class="language-plaintext highlighter-rouge">/memory</code> 和 <code class="language-plaintext highlighter-rouge">/reserved-memory</code></h3>

<p><strong><code class="language-plaintext highlighter-rouge">/memory</code> 节点</strong>宣告系统的可用物理 RAM。这是每个设备树必须具备的节点之一——所有设备树都必须有 <code class="language-plaintext highlighter-rouge">/cpus</code> 和至少一个 <code class="language-plaintext highlighter-rouge">/memory</code> 节点。</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">memory</span><span class="err">@</span><span class="mi">0</span> <span class="p">{</span>
    <span class="n">device_type</span> <span class="o">=</span> <span class="s">"memory"</span><span class="p">;</span>
    <span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x00000000</span> <span class="mh">0x80000000</span><span class="o">&gt;</span><span class="p">;</span>   <span class="cm">/* 0x0 起 2 GB */</span>
<span class="p">};</span>
</code></pre></div></div>

<p>对于 64 位系统（<code class="language-plaintext highlighter-rouge">#address-cells = &lt;2&gt;</code>, <code class="language-plaintext highlighter-rouge">#size-cells = &lt;2&gt;</code>）：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">memory</span><span class="err">@</span><span class="mi">0</span> <span class="p">{</span>
    <span class="n">device_type</span> <span class="o">=</span> <span class="s">"memory"</span><span class="p">;</span>
    <span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x00000000</span> <span class="mh">0x00000000</span>  <span class="mh">0x00000000</span> <span class="mh">0x80000000</span><span class="o">&gt;</span><span class="p">;</span>  <span class="cm">/* 低 2GB */</span>
<span class="p">};</span>
<span class="n">memory</span><span class="err">@</span><span class="mi">100000000</span> <span class="p">{</span>
    <span class="n">device_type</span> <span class="o">=</span> <span class="s">"memory"</span><span class="p">;</span>
    <span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x00000001</span> <span class="mh">0x00000000</span>  <span class="mh">0x00000001</span> <span class="mh">0x00000000</span><span class="o">&gt;</span><span class="p">;</span>  <span class="cm">/* 高 4GB */</span>
<span class="p">};</span>
</code></pre></div></div>

<p><strong><code class="language-plaintext highlighter-rouge">/reserved-memory</code> 节点</strong>划定<strong>不可被内核随意使用的内存禁区</strong>：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">reserved</span><span class="o">-</span><span class="n">memory</span> <span class="p">{</span>
    <span class="cp">#address-cells = &lt;1&gt;;
</span>    <span class="cp">#size-cells = &lt;1&gt;;
</span>    <span class="n">ranges</span><span class="p">;</span>

    <span class="nl">display_reserved:</span> <span class="n">framebuffer</span><span class="err">@</span><span class="mi">78000000</span> <span class="p">{</span>
        <span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x78000000</span> <span class="mh">0x800000</span><span class="o">&gt;</span><span class="p">;</span>          <span class="cm">/* 8 MB 静态预留 */</span>
    <span class="p">};</span>

    <span class="n">linux</span><span class="p">,</span><span class="n">cma</span> <span class="p">{</span>
        <span class="n">compatible</span> <span class="o">=</span> <span class="s">"shared-dma-pool"</span><span class="p">;</span>
        <span class="n">reusable</span><span class="p">;</span>
        <span class="n">size</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x4000000</span><span class="o">&gt;</span><span class="p">;</span>                    <span class="cm">/* 64 MB 动态分配 */</span>
        <span class="n">alignment</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x2000</span><span class="o">&gt;</span><span class="p">;</span>                  <span class="cm">/* 8 KB 对齐 */</span>
    <span class="p">};</span>
<span class="p">};</span>
</code></pre></div></div>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">no-map</code></strong>：操作系统<strong>禁止</strong>为这段区域创建虚拟映射——用于安全隔离（如 TEE/安全飞地）。</li>
  <li><strong><code class="language-plaintext highlighter-rouge">reusable</code></strong>：系统可临时将这片内存挪作他用（如页面缓存），但当设备驱动需要时，能将其要回来。</li>
  <li><code class="language-plaintext highlighter-rouge">no-map</code> 和 <code class="language-plaintext highlighter-rouge">reusable</code> 是互斥的。</li>
</ul>

<p>设备通过 <code class="language-plaintext highlighter-rouge">memory-region</code> 属性引用专属内存：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">fb0:</span> <span class="n">video</span><span class="err">@</span><span class="mi">12300000</span> <span class="p">{</span>
    <span class="n">memory</span><span class="o">-</span><span class="n">region</span> <span class="o">=</span> <span class="o">&lt;&amp;</span><span class="n">display_reserved</span><span class="o">&gt;</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div>

<p>关于与 UEFI 的协作：当通过 UEFI 启动时，<code class="language-plaintext highlighter-rouge">/memory</code> 和静态 <code class="language-plaintext highlighter-rouge">/reserved-memory</code> 的内容也必须在 UEFI 内存映射表中体现：带 <code class="language-plaintext highlighter-rouge">no-map</code> 的区域标记为 <code class="language-plaintext highlighter-rouge">EfiReservedMemoryType</code>，其他区域标记为 <code class="language-plaintext highlighter-rouge">EfiBootServicesData</code>。动态预留区域则不应在 UEFI 内存映射表中列出——它们在固件退出后才由操作系统分配。</p>

<h3 id="44-运行时通道chosen-和-aliases">4.4 运行时通道：<code class="language-plaintext highlighter-rouge">/chosen</code> 和 <code class="language-plaintext highlighter-rouge">/aliases</code></h3>

<p><strong><code class="language-plaintext highlighter-rouge">/chosen</code></strong> 节点不代表任何物理设备。它是引导程序向内核传递<strong>运行时配置</strong>的通道：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">chosen</span> <span class="p">{</span>
    <span class="n">bootargs</span> <span class="o">=</span> <span class="s">"console=ttyS0,115200 root=/dev/mmcblk0p2 rw"</span><span class="p">;</span>
    <span class="n">stdout</span><span class="o">-</span><span class="n">path</span> <span class="o">=</span> <span class="s">"/soc/serial@70006300:115200"</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">bootargs</code> 就是内核命令行参数，<code class="language-plaintext highlighter-rouge">stdout-path</code> 指定启动控制台的输出设备。</p>

<p><strong><code class="language-plaintext highlighter-rouge">/aliases</code></strong> 为冗长的路径起短名：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">aliases</span> <span class="p">{</span>
    <span class="n">serial0</span> <span class="o">=</span> <span class="s">"/soc/serial@70006300"</span><span class="p">;</span>
    <span class="n">ethernet0</span> <span class="o">=</span> <span class="s">"/soc/ethernet@31c000"</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div>

<p>内核中的 <code class="language-plaintext highlighter-rouge">of_alias_get_id()</code> 等函数使用这些别名定位设备或标准化设备编号（如 <code class="language-plaintext highlighter-rouge">/dev/ttyS0</code> 对应 <code class="language-plaintext highlighter-rouge">serial0</code>）。</p>

<h3 id="45-典型设备绑定">4.5 典型设备绑定</h3>

<h4 id="451-不可探测的系统总线simple-bus">4.5.1 不可探测的系统总线：<code class="language-plaintext highlighter-rouge">simple-bus</code></h4>

<p><code class="language-plaintext highlighter-rouge">compatible = "simple-bus"</code> 是设备树中的一个重要特殊值。它描述一类最简单的总线：<strong>内存映射、不可探测、子设备的寄存器直接在 CPU 地址空间可见</strong>。</p>

<p>SoC 内部总线通常就是 <code class="language-plaintext highlighter-rouge">simple-bus</code>。当内核看到 <code class="language-plaintext highlighter-rouge">simple-bus</code> 兼容的节点时，会自动遍历其子节点并注册 platform_device：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">soc</span> <span class="p">{</span>
    <span class="n">compatible</span> <span class="o">=</span> <span class="s">"simple-bus"</span><span class="p">;</span>
    <span class="cp">#address-cells = &lt;1&gt;;
</span>    <span class="cp">#size-cells = &lt;1&gt;;
</span>    <span class="n">ranges</span><span class="p">;</span>

    <span class="cm">/* 子节点自动被创建为 platform_device */</span>
<span class="p">};</span>
</code></pre></div></div>

<h4 id="452-串口以-ns16550-为例">4.5.2 串口：以 NS16550 为例</h4>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">serial</span><span class="err">@</span><span class="mi">70006300</span> <span class="p">{</span>
    <span class="n">compatible</span> <span class="o">=</span> <span class="s">"nvidia,tegra20-uart"</span><span class="p">,</span> <span class="s">"ns16550"</span><span class="p">;</span>
    <span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x70006300</span> <span class="mh">0x100</span><span class="o">&gt;</span><span class="p">;</span>
    <span class="n">clock</span><span class="o">-</span><span class="n">frequency</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mi">408000000</span><span class="o">&gt;</span><span class="p">;</span>
    <span class="n">interrupts</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mi">122</span><span class="o">&gt;</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">clock-frequency</code> 是 UART 波特率发生器所需的时钟频率——内核 UART 驱动用这个值计算正确的分频系数。</p>

<h4 id="453-网络以太网">4.5.3 网络：以太网</h4>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">ethernet</span><span class="err">@</span><span class="mi">31</span><span class="n">c000</span> <span class="p">{</span>
    <span class="n">compatible</span> <span class="o">=</span> <span class="s">"nvidia,tegra20-ehci"</span><span class="p">;</span>
    <span class="n">reg</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x31c000</span> <span class="mh">0x100</span><span class="o">&gt;</span><span class="p">;</span>
    <span class="n">interrupts</span> <span class="o">=</span> <span class="o">&lt;</span><span class="mh">0x6d</span><span class="o">&gt;</span><span class="p">;</span>
    <span class="n">local</span><span class="o">-</span><span class="n">mac</span><span class="o">-</span><span class="n">address</span> <span class="o">=</span> <span class="p">[</span><span class="mo">00</span> <span class="mi">11</span> <span class="mi">22</span> <span class="mi">33</span> <span class="mi">44</span> <span class="mi">55</span><span class="p">];</span>
    <span class="n">phy</span><span class="o">-</span><span class="n">connection</span><span class="o">-</span><span class="n">type</span> <span class="o">=</span> <span class="s">"rgmii"</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">local-mac-address</code> 是 6 字节的 MAC 地址，用方括号格式的字节串表示。<code class="language-plaintext highlighter-rouge">phy-connection-type</code> 指定 PHY 和 MAC 之间的总线类型（RGMII、RMII、MII 等）——这决定了内核如何配置 MAC 侧的时序和管脚。</p>

<hr />

<h2 id="5-庖丁解牛dtb-二进制格式的底层设计">5. 庖丁解牛：DTB 二进制格式的底层设计</h2>

<p>虽然大部分开发者只接触 DTS 源码，但理解 DTB 的二进制结构有助于理解设备树的<strong>设计哲学</strong>——为什么某些奇怪的限制存在。</p>

<h3 id="51-扁平化的哲学为什么没有-c-风格的指针">5.1 扁平化的哲学：为什么没有 C 风格的指针？</h3>

<p>DTSpec 规范开篇第一句就点明了关键前提：</p>

<blockquote>
  <p>A boot program loads a devicetree into a client program’s memory and passes a pointer to the devicetree to the client.</p>
</blockquote>

<p>在这个时刻，MMU（内存管理单元）可能还没开启，虚拟地址的概念不存在。DTB 必须是一块<strong>可以被任意复制、重定位到任意物理地址</strong>的数据块。</p>

<p>如果 DTB 内部使用 C 指针——那在重定位后就全是悬垂指针了。因此 DTB 设计为<strong>完全自包含、位置无关</strong>的格式：所有的跨区块引用都用偏移量而非指针表达。</p>

<p>这就是”扁平化”（Flattened）的核心含义——一个可以平移到任意内存位置、无需解析指针就能直接读取的数据结构。</p>

<h3 id="52-dtb-的四大内存区块">5.2 DTB 的四大内存区块</h3>

<p>一个 DTB 文件由四个连续的内存区块组成：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">┌───────────────────────┐</span>  <span class="err">←</span> <span class="err">低地址</span>
<span class="err">│</span>  <span class="n">Header</span> <span class="p">(</span><span class="err">头部</span><span class="p">)</span>         <span class="err">│</span>  <span class="mi">40</span> <span class="err">字节</span> <span class="p">(</span><span class="mi">10</span> <span class="err">×</span> <span class="kt">uint32_t</span><span class="p">)</span>
<span class="err">├───────────────────────┤</span>
<span class="err">│</span>  <span class="n">Memory</span> <span class="n">Reservation</span>    <span class="err">│</span>  <span class="err">预留内存列表</span>
<span class="err">│</span>  <span class="n">Block</span>                 <span class="err">│</span>
<span class="err">├───────────────────────┤</span>
<span class="err">│</span>  <span class="n">Structure</span> <span class="n">Block</span>       <span class="err">│</span>  <span class="err">树的线性</span> <span class="n">Token</span> <span class="err">序列</span>
<span class="err">│</span>  <span class="p">(</span><span class="n">FDT_BEGIN_NODE</span> <span class="err">等</span><span class="p">)</span>  <span class="err">│</span>
<span class="err">├───────────────────────┤</span>
<span class="err">│</span>  <span class="n">Free</span> <span class="n">Space</span> <span class="p">(</span><span class="err">可选</span><span class="p">)</span>     <span class="err">│</span>
<span class="err">├───────────────────────┤</span>
<span class="err">│</span>  <span class="n">Strings</span> <span class="n">Block</span>         <span class="err">│</span>  <span class="err">属性名字符串池</span>
<span class="err">└───────────────────────┘</span>  <span class="err">←</span> <span class="err">高地址</span>
</code></pre></div></div>

<h4 id="521-header头部设备的身份证">5.2.1 Header（头部）：设备的身份证</h4>

<p>Header 是 40 字节的固定结构，所有字段都是大端 32 位整数：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">struct</span> <span class="n">fdt_header</span> <span class="p">{</span>
    <span class="kt">uint32_t</span> <span class="n">magic</span><span class="p">;</span>              <span class="cm">/* 0xd00dfeed — 魔数 */</span>
    <span class="kt">uint32_t</span> <span class="n">totalsize</span><span class="p">;</span>          <span class="cm">/* DTB 总大小 */</span>
    <span class="kt">uint32_t</span> <span class="n">off_dt_struct</span><span class="p">;</span>      <span class="cm">/* Structure Block 偏移 */</span>
    <span class="kt">uint32_t</span> <span class="n">off_dt_strings</span><span class="p">;</span>     <span class="cm">/* Strings Block 偏移 */</span>
    <span class="kt">uint32_t</span> <span class="n">off_mem_rsvmap</span><span class="p">;</span>     <span class="cm">/* 内存预留块偏移 */</span>
    <span class="kt">uint32_t</span> <span class="n">version</span><span class="p">;</span>            <span class="cm">/* 格式版本 (当前为 17) */</span>
    <span class="kt">uint32_t</span> <span class="n">last_comp_version</span><span class="p">;</span>  <span class="cm">/* 向后兼容的最低版本 (16) */</span>
    <span class="kt">uint32_t</span> <span class="n">boot_cpuid_phys</span><span class="p">;</span>    <span class="cm">/* 引导 CPU 的物理 ID */</span>
    <span class="kt">uint32_t</span> <span class="n">size_dt_strings</span><span class="p">;</span>    <span class="cm">/* Strings Block 大小 */</span>
    <span class="kt">uint32_t</span> <span class="n">size_dt_struct</span><span class="p">;</span>     <span class="cm">/* Structure Block 大小 */</span>
<span class="p">};</span>
</code></pre></div></div>

<p>几个关键字段：</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">magic = 0xd00dfeed</code></strong>：这个魔数的英文读音就是 “dude feed” —— 是 Open Firmware 工作组留下的一个幽默彩蛋。内核通过比对它来确认”这确实是一个 DTB”。</li>
  <li><strong><code class="language-plaintext highlighter-rouge">version = 17</code></strong>：当前 DTSpec 标准格式版本。向后兼容的最低版本是 16。</li>
  <li><strong><code class="language-plaintext highlighter-rouge">totalsize</code></strong>：涵盖所有区块及之间的空格，但不包含 DTB 前后的额外内存。这个字段让解析器提前知道数据边界。</li>
  <li><strong><code class="language-plaintext highlighter-rouge">boot_cpuid_phys</code></strong>：必须与 <code class="language-plaintext highlighter-rouge">/cpus</code> 中对应 CPU 节点的 <code class="language-plaintext highlighter-rouge">reg</code> 值一致。内核通过它确定哪个 CPU 是引导 CPU。</li>
</ul>

<h4 id="522-memory-reservation-block硬核预留">5.2.2 Memory Reservation Block：硬核预留</h4>

<p>内核初始化过程的脆弱时刻——代码在未成熟的地址空间中一步步构建完整的虚拟地址系统——必须确保引导程序的关键内存不被覆写。Memory Reservation Block 就是为此而建立的”禁止访问”清单。</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">struct</span> <span class="n">fdt_reserve_entry</span> <span class="p">{</span>
    <span class="kt">uint64_t</span> <span class="n">address</span><span class="p">;</span>
    <span class="kt">uint64_t</span> <span class="n">size</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div>

<p>每个条目描述一段（物理地址, 大小）的保留区域。列表以 <code class="language-plaintext highlighter-rouge">(0, 0)</code> 条目终止。这比 <code class="language-plaintext highlighter-rouge">/reserved-memory</code> 节点更底层——在内核还没解析设备树结构之前，引导程序就已确认这些区域不能被触碰。</p>

<h4 id="523-structure-block用-token-线性展开的树">5.2.3 Structure Block：用 Token 线性展开的树</h4>

<p>这是 DTB 最核心的部分——如何将一个树状结构编码为平直化的字节序列？</p>

<p>结构区块由 5 种 Token 组成，每个 Token 是 32 位大端整数：</p>

<table>
  <thead>
    <tr>
      <th>Token</th>
      <th>值</th>
      <th>含义</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">FDT_BEGIN_NODE</code></td>
      <td><code class="language-plaintext highlighter-rouge">0x00000001</code></td>
      <td>节点开始，后跟节点名（null-terminated string）</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">FDT_END_NODE</code></td>
      <td><code class="language-plaintext highlighter-rouge">0x00000002</code></td>
      <td>节点结束</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">FDT_PROP</code></td>
      <td><code class="language-plaintext highlighter-rouge">0x00000003</code></td>
      <td>属性，后跟 len + nameoff + 值</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">FDT_NOP</code></td>
      <td><code class="language-plaintext highlighter-rouge">0x00000004</code></td>
      <td>空操作（用于覆盖已删除的节点/属性）</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">FDT_END</code></td>
      <td><code class="language-plaintext highlighter-rouge">0x00000009</code></td>
      <td>结构区块结束标记</td>
    </tr>
  </tbody>
</table>

<p><strong><code class="language-plaintext highlighter-rouge">FDT_PROP</code> 的编码</strong>：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">struct</span> <span class="p">{</span>
    <span class="kt">uint32_t</span> <span class="n">len</span><span class="p">;</span>       <span class="cm">/* 属性值的字节长度 */</span>
    <span class="kt">uint32_t</span> <span class="n">nameoff</span><span class="p">;</span>   <span class="cm">/* 属性名字在 Strings Block 中的偏移量 */</span>
<span class="p">};</span>
<span class="cm">/* 后跟 len 字节的属性值，然后 0 填充至 4 字节对齐 */</span>
</code></pre></div></div>

<p><strong>树的线性化编码</strong>：</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">FDT_BEGIN_NODE</span>  <span class="s">"/"</span>
  <span class="n">FDT_PROP</span>        <span class="err">属性</span><span class="mi">1</span>
  <span class="n">FDT_PROP</span>        <span class="err">属性</span><span class="mi">2</span>
  <span class="n">FDT_BEGIN_NODE</span>  <span class="s">"soc"</span>
    <span class="n">FDT_PROP</span>      <span class="err">属性</span>
  <span class="n">FDT_END_NODE</span>
<span class="n">FDT_END_NODE</span>
<span class="n">FDT_END</span>           <span class="err">全局结束</span>
</code></pre></div></div>

<p>这种结构的巧妙处：</p>

<ul>
  <li>前端解析器从头到尾扫描，遇到 <code class="language-plaintext highlighter-rouge">FDT_BEGIN_NODE</code> 就是”进入子节点”，遇到 <code class="language-plaintext highlighter-rouge">FDT_END_NODE</code> 就是”回到父节点”——一个栈结构自然处理整个解析。</li>
  <li>通过 <code class="language-plaintext highlighter-rouge">FDT_NOP</code>（覆盖被删除块的开头），DTB 可以在不移动其他数据的情况下”删除”节点或属性——这在 DT overlay 和运行时修改的场景中非常重要。</li>
  <li>属性名通过偏移量引用 Strings Block，同一个名字字符串被全局共享——这正是下一节要讲的极致压缩策略。</li>
</ul>

<h4 id="524-strings-block极致压缩的字符串复用池">5.2.4 Strings Block：极致压缩的字符串复用池</h4>

<p>设备树里有大量相同的属性名——”compatible” 可能出现上百次，”reg” 可能出现几百次。Strings Block 通过<strong>所有属性名统一存储</strong>，只保存一份字符串，然后用偏移量引用来实现极致的空间压缩。</p>

<p>本质上，这就是一个以 null 字节分隔的字符串列表。查找过程：取 <code class="language-plaintext highlighter-rouge">nameoff</code> 值 → 跳到 Strings Block 开始处 + nameoff 的位置 → 读到 null 为止。这个简单的设计让 20 KB 的 DTS 编译后可能只有 5 KB 的 DTB。</p>

<h3 id="53-内存对齐使用-32-位单元但以-8-字节整体看待">5.3 内存对齐：使用 32 位单元但以 8 字节整体看待</h3>

<p>DTB 的每一处对齐都有其物理上的现实原因：</p>

<ul>
  <li><strong>Structure Block</strong> 按要求 4 字节对齐——每个 Token 是 <code class="language-plaintext highlighter-rouge">uint32_t</code></li>
  <li><strong>Memory Reservation Block</strong> 8 字节对齐——条目是 <code class="language-plaintext highlighter-rouge">uint64_t</code> 对</li>
  <li><strong>整个 DTB</strong> 按 8 字节对齐加载——这是所有子块对齐要求的最大公约数</li>
</ul>

<p>这个 8 字节整体对齐要求意味着引导程序从任何可以 8 字节对齐的地址上加载的 DTB 都能确保在不做额外内存搬运的情况下，被内核安全地按 4 或 8 字节访问。</p>

<p class="notice--info">DTB 的设计体现的是嵌入式引导阶段的现实约束：无 MMU、无动态分配、物理内存可能非连续、引导程序的内存在”交接时刻”不可以被覆盖。那些看似奇怪的限制——没有指针只有偏移量、所有数据平坦排列、必须整体 8 字节对齐——全都是从这个唯一约束推导出来的：<strong>在 MMU 开启前，内核必须安全地解析一个可以被任意重定位的硬件描述块</strong>。</p>

<hr />

<h2 id="结语">结语</h2>

<p>设备树，归根结底，是一份交给操作系统的”施工图纸”。引导程序拿着蓝图说：”这就是这个机器的样子。”操作系统回答说：”好的，我知道该怎么做了。”</p>

<h2 id="参考资料">参考资料</h2>

<ol>
  <li><a href="https://docs.kernel.org/devicetree/usage-model.html">Linux Kernel Documentation: Devicetree Usage Model</a></li>
  <li><a href="https://github.com/devicetree-org/devicetree-specification">DeviceTree Specification（devicetree-org）</a></li>
</ol>]]></content><author><name>bfmhno3</name></author><category term="Note" /><category term="Linux" /><summary type="html"><![CDATA[0. 引言：为什么我们需要设备树？ 想象这样一个场景：你的电脑开机，操作系统启动，它需要知道自己运行在什么硬件之上：有几颗 CPU、多大内存、串口在哪里、网卡是什么型号。操作系统如何获取这些信息？ 答案是……看情况。 0.1 自发现 vs 非自发现总线 现代计算机系统中，总线大致分为两类。 自发现总线（Self-Discoverable Bus）：PCIe、USB 这类总线，操作系统可以向总线发送探测请求，插在总线上的设备会主动回答 “我在这里，我是某某设备”。插上一个 USB 键盘，系统立刻就能知道这个键盘的存在及其设备描述符。这是一种运行时动态发现机制。 非自发现总线（Non-Discoverable Bus）：I2C、SPI、CAN、系统总线（Processor Bus）等则完全不是这样。在这些总线上，CPU 无法主动 “问” 谁连接在上面。它只知道有一组内存映射的寄存器地址，但对于这些地址背后是什么设备、什么型号、如何配置，CPU 一无所知。这些信息必须由人为提供。 嵌入式系统大量使用非自发现总线。一个典型的 ARM SoC 上，UART、I2C 控制器、GPIO 控制器、定时器都是通过内存映射连接到处理器总线的，没有任何动态发现机制。 0.2 历史的教训：Linus 的愤怒 在设备树出现之前，Linux 对这些非自发现硬件的处理方式简单粗暴：硬编码。 ARM 架构的 Linux 内核曾经有一个庞大的 arch/arm/mach-* 目录，里面为每一款开发板都写了一份 C 代码，直接在内核源码中描述板上有什么设备、基地址是多少、中断号是多少。如果你买了一块新的开发板，就需要在内核里添加一份新的 board-*.c 文件。 这导致了几个严重问题： 内核膨胀：同一份内核镜像无法在多块不同板子上运行。每块板子都需要编译一个专用内核。 代码冗余：不同板子之间共享同一个 SoC，但因为板上外设组合不同，代码大量重复。 维护灾难：2011 年左右，Linux 内核邮件列表上 Linus Torvalds 公开表达了强烈的不满——ARM 架构的 mach-* 目录已经变成一个无法维护的垃圾场。 Gaah. Guys, this whole ARM thing is a f*cking pain in the ass. 问题的根源在于：硬件描述信息（数据）被硬编码在了操作系统的核心逻辑（代码）中。 0.3 解决办法：数据与代码的剥离 设备树的本质思想总结为一句话： 将硬件的拓扑结构和物理参数从操作系统的核心代码中彻底剥离，以结构化的数据文件形式独立存在。 在这个模型中： 引导程序（Boot Program）如 U-Boot、树莓派固件、QEMU，负责将设备树二进制文件（DTB）加载到内存，并将其地址传递给内核。 内核（Client Program）在启动时解析这份数据，按图施工：发现设备、匹配驱动、配置寄存器。 这是一种声明式（Declarative）的硬件描述方式：你告诉内核 “有什么”，而不是 “怎么做”。内核中的驱动程序则负责 “怎么做”。 这个思想和 Web 前端分离 HTML（结构/数据）与 JavaScript（逻辑）如出一辙，或者类比数据库系统中的 DDL（数据定义）与 DML（数据操作）。 设备树最早来自 Open Firmware（IEEE 1275），用在 PowerPC 和 SPARC 平台上。2005 年，PowerPC Linux 在合并 32 位/64 位支持的清理工作中，将设备树设为强制要求——由此诞生了 “扁平化设备树”（Flattened Device Tree, FDT）格式。此后，FDT 被推广到所有架构。 截至本文写作时，arm、arm64、microblaze、mips、powerpc、sparc、x86、riscv 等主流架构均已支持设备树。 1. 设备树的基础语法 1.1 DTS 源码与 DTB 二进制 设备树有两种形态，二者关系类似 C 语言中 .c 源文件与编译后的二进制可执行文件： 形态 文件名 本质 使用者 **DTS** (Device Tree Source) .dts / .dtsi 人类可读的文本源码 开发者 **DTB** (Device Tree Blob) .dtb 二进制的扁平化树结构 内核 DTS 通过设备树编译器（DTC, Device Tree Compiler）编译成 DTB。同时，DTC 也支持反编译（dtc -I dtb -O dts），方便逆向查看既有 DTB 内容。 .dts 文件是设备树源文件，通常一个板子一个。.dtsi 文件是可包含的共用文件（”i” = include），通常描述一个 SoC 的内部结构，然后被多个板子的 .dts 以 /include/ 指令引用——板级 .dts 引用 SoC 级 .dtsi，只在末尾补充板上特有的外设。 1.2 节点（Node）与路径 设备树是一个树形数据结构。每个节点描述一个设备或一个总线，节点可以包含属性和子节点。 节点命名规则 node-name@unit-address node-name：1-31 个字符，只能使用数字、大小写字母和 ,._+-，必须以字母开头。推荐使用描述设备类别的通用名称，而非具体型号——例如用 serial 而不是 ns16550，用 ethernet 而不是 rtl8139。这种”重类别、轻型号”的命名哲学让设备树在不同硬件平台间保持可读性。 @unit-address：可选的单元地址，必须与 reg 属性中的第一个地址值匹配。如果节点没有 reg 属性，则必须省略 @unit-address。 为什么单元地址必须和 reg 首地址一致？因为 DTC 编译器会校验这种一致性，确保设备树的自我完整性——你在节点名中承诺的物理地址，必须和寄存器基地址真正对上，否则树结构本身就是错的。 举例： cpu@0 —— 0 号 CPU serial@4600 —— 基地址 0x4600 的串口 i2c@7000c000 —— 基地址 0x7000c000 的 I2C 控制器 树状层级与绝对路径 每个节点在树中有唯一路径，从根节点 / 开始： /cpus/cpu@0 /soc/serial@4600 /soc/i2c@7000c000/codec@1a 这种层级镜像了硬件的物理拓扑：芯片内部总线（SoC）下挂载着各种外设控制器，而 I2C 控制器下面又挂着连接在 I2C 总线上的设备（如音频编解码器）。 1.3 属性（Property）：数据的载体 每个节点由若干属性定义。属性是键值对结构。 DTSpec 定义了以下几种基本的属性值类型： 〈空值〉（Empty）：属性的存在本身就是一个布尔信号。例如 interrupt-controller; 表示不需要任何值，有这个属性就代表”我是中断控制器”。 〈u32〉（32 位整数）：大端序存储的 32 位无符号整数，在 DTS 源码中用尖括号包裹，例如： clock-frequency = &lt;825000000&gt;; DTS 支持在尖括号内使用 C 风格的算术、位运算和三元表达式： reg = &lt;(0x1000 + 0x200) 0x100&gt;; 〈u64〉（64 位整数）：用两个 &lt;u32&gt; 表示，高 32 位在前，例如： clock-frequency = &lt;0x00000001 0x00000000&gt;; 这表示的是 64 位值 0x100000000：第一个 cell 是高 32 位（0x00000001），第二个是低 32 位（0x00000000）。 〈string〉（字符串）：双引号包裹，以 null 结尾，例如： compatible = "ns16550"; 〈stringlist〉（字符串列表）：多个字符串拼接，例如： compatible = "fsl,mpc8641", "ns16550"; 〈phandle〉（节点引用）：一个 &lt;u32&gt; 值，指向树中另一个节点。DTC 在编译时自动为有标签的节点分配唯一的 phandle 值。 〈prop-encoded-array〉（自定义编码数组）：格式取决于具体属性定义，最常见的就是 reg 中的（地址, 长度）对。 1.4 语法糖：标签与引用 如果每次都要写 /soc/i2c@7000c000/codec@1a 这样的绝对路径来引用一个节点，代码会变得难以阅读。DTS 提供了两个机制来解决这个问题。 标签（Label）：在节点或属性前加 标签名: 定义，例如： wm8903: codec@1a { compatible = "wlf,wm8903"; reg = &lt;0x1a&gt;; }; 引用（Reference）：&amp;标签名 引用已定义的节点，例如： sound { compatible = "nvidia,harmony-sound"; i2s-controller = &lt;&amp;i2s1&gt;; i2s-codec = &lt;&amp;wm8903&gt;; /* 引用上面的 codec 节点 */ }; 在 DTC 编译时，&amp;wm8903 会被展开为该节点的 phandle 值，存入 DTB。 引用语法还有一个常见用法：引用覆盖。在板级 .dts 中，你可以这样激活或修改 SoC 级 .dtsi 中已定义的外设： /* 在 SoC .dtsi 中，外设默认 disabled */ &amp;uart1 { status = "disabled"; }; /* 板级 .dts 中，将它启用 */ &amp;uart1 { status = "okay"; }; 这种 “先定义、后覆盖” 的模式是设备树实际工程中的核心工作流。 2. 描述硬件的三大核心维度 如果设备树是一份硬件档案，那么描述一个设备至少需要回答三个问题：你是谁、你在哪里、你还活着吗。 2.1 身份认同：compatible 和 model compatible 是设备树中最重要的属性。它定义了一个设备的 “编程模型”，也就是软件（驱动）应该用哪种接口协议来操作这个硬件。 compatible = "manufacturer,model"; 格式严格规定为 &lt;制造商&gt;,&lt;型号&gt;，全小写，不用下划线。例如： compatible = "arm,cortex-a72"; compatible = "nvidia,tegra20-uart"; compatible = "wlf,wm8903"; /* Wolfson 的 WM8903 音频编解码器 */ 多重兼容性策略：compatible 的值是一个从最特化到最泛化的字符串列表。内核会按顺序逐一尝试匹配驱动： compatible = "fsl,mpc8641", "ns16550"; 这条配置告诉内核：优先找 Freescale MPC8641 专用驱动；如果没找到，就退而求其次，用通用的 NS16550 UART 驱动。这种”回退”（fallback）策略让同一套内核可以在范围极大的硬件上运行。 model 是一个人类可读的字符串，描述设备的制造商型号： model = "fsl,MPC8349EMITX"; model 和 compatible 的核心区别在于使用者不同：model 是给人（开发者、调试者）看的，compatible 是给内核的设备匹配算法用的。也就是说，model 可以由开发者自己确定，随意编写，而 compatible 则应该遵循一定的规范。 那 compatible 字符串从哪里来？&lt;制造商&gt;,&lt;型号&gt; 中的每一对合法取值，都有对应的绑定文档（Binding Document）加以规范。Linux 内核源码树的 Documentation/devicetree/bindings/ 目录就是这份 “字典”：按设备类别分子目录（serial/、i2c/、gpio/ 等），每个 .yaml 文件精确声明该设备支持哪些 compatible 字符串、哪些属性是必须的、哪些属性是可选的、各属性的合法取值范围。 为设备挑选 compatible 字符串的正确方式是：先查 Documentation/devicetree/bindings/ 找到对应 binding，再照着 binding 填写，而不是凭经验猜测或照抄其他设备树。 2.2 寻址空间：reg、#address-cells 与 #size-cells 这三个属性构成了设备树寻址系统的核心。 #address-cells 和 `#size-cells 由父节点定义，告诉子节点：描述一个地址需要几个 32 位 cell，描述一个长度需要几个 cell。 这里的 cell 是设备树规范继承自 Open Firmware 的基本数据单元，固定为一个 32 位无符号整数。当硬件的地址或长度超过 32 位时，就用多个连续的 cell 拼接：两个 cell 拼接成 64 位，高位在前。 因此，#address-cells = &lt;2&gt; 意味着子节点的地址用两个 32 位整数（合计 64 位）来描述，#address-cells = &lt;1&gt; 则是单个 32 位整数就够了。 这两个属性不会被继承，每个有子节点的节点必须自己显式定义。 reg 属性描述设备的寄存器资源，格式为 &lt;地址 长度&gt; 对： soc { #address-cells = &lt;1&gt;; #size-cells = &lt;1&gt;; serial@4600 { compatible = "ns16550"; reg = &lt;0x4600 0x100&gt;; /* 地址 0x4600，长度 0x100 */ }; }; 当 #size-cells = &lt;0&gt; 时，reg 中只有地址没有长度，这是带独立片选信号的设备（如 I2C 从设备）的常见情况： i2c@7000c000 { #address-cells = &lt;1&gt;; #size-cells = &lt;0&gt;; codec@1a { compatible = "wlf,wm8903"; reg = &lt;0x1a&gt;; /* 只有 I2C 从机地址，没有长度 */ }; }; 一个设备有多个寄存器块时，reg 可以包含多组 &lt;地址 长度&gt; 对： reg = &lt;0x3000 0x20 0xFE00 0x100&gt;; /* 第一块：offset 0x3000, 长 32 字节 第二块：offset 0xFE00, 长 256 字节 */ 进阶：地址映射与 DMA ranges 属性：实现父子总线之间的地址翻译。它定义一个 “映射范围”：子总线地址空间的 X，对应父总线地址空间的 Y，长度 Z。 soc { compatible = "simple-bus"; #address-cells = &lt;1&gt;; #size-cells = &lt;1&gt;; ranges = &lt;0x0 0xe0000000 0x00100000&gt;; /* 子地址 0x0 映射到父地址 0xe0000000，范围 1MB */ serial@4600 { reg = &lt;0x4600 0x100&gt;; /* 实际物理地址 = 父地址 0xe0000000 + offset 0x4600 = 0xe0004600 */ }; }; 当 ranges 为空值时，即 ranges;，意味着父子地址空间完全一致，不需要任何平移。 dma-ranges：与 ranges 类似，但从 DMA 设备的视角描述，解决的是 “DMA 控制器看到的地址” 和 “CPU 看到的物理地址” 之间的差异（例如 IOMMU 的存在会制造这种差异）。 virtual-reg：当引导程序已经设置了虚拟地址映射时，告知内核虚拟地址到物理地址的转换关系。 reg 和 ranges 是设备树中最容易被误解的概念。简单记忆法则： reg = “我的寄存器在我所在总线地址空间的什么位置” ranges = “我的总线地址空间如何映射到我父亲的总线地址空间” 最终 CPU 看到的物理地址 = 层层 ranges 翻译之和 2.3 运行状态：status status 属性标记设备当前的操作状态： 值 含义 "okay" 设备正常工作 "disabled" 设备当前不可用，但可能在未来可用（如模块未插入） "reserved" 设备可用，但不应被本操作系统使用（被固件或其他 OS 控制） "fail" 设备出现严重错误，不可能恢复 "fail-sss" 同上，sss 为设备特定的错误码 省略 status 属性等同于 status = "okay"。 在实际工程中最常见的场景是：SoC 级 .dtsi 中所有外设默认 status = "disabled"，板级 .dts 中只启用实际使用的外设——设置为 status = "okay"。 3. 硬件中断的逻辑连线 中断描述是设备树最难理解的部分之一，因为中断在硬件上的信号流向和在设备树上的描述路径，经常与设备物理层级不同。 举个典型例子：一颗 I2C 触摸屏控制器挂在 I2C 总线上，所以它在设备树里的物理路径通常是 soc -&gt; i2c -&gt; touchscreen；但它的中断线并不回到 I2C 控制器，而是接到 GPIO 控制器的某个引脚，再由 GPIO 控制器上报给 GIC。于是它的中断路径变成 touchscreen -&gt; gpio-controller -&gt; gic。 这就是 “设备物理层级” 和 “中断描述路径” 不一致的真实场景：你在设备树里看到它在 I2C 下，但它的中断信号却是走的 GPIO/GIC 这条逻辑链路。 3.1 中断树：跳出物理层级的逻辑结构 设备树采用中断树（Interrupt Tree）模型来表达中断路由。虽然叫做 “树”，但它更准确的描述是一个有向无环图（Directed Acyclic Graph）。 Device Tree 层级: Interrupt Tree 层级: soc open-pic (根) ├── device1 ------------------&gt; (直接连接) ├── pci-host -----------------&gt; (nexus 节点, 中转) │ ├── slot0 ----------------&gt; (经 pci-host 中转) │ └── slot1 ----------------&gt; (经 pci-host 中转) └── gpioctrl -----------------&gt; (nexus 节点, 中转) └── device3 --------------&gt; (经 gpioctrl 中转) 关键思想：中断信号在硬件上跨越多层物理总线，设备树通过在逻辑上建立直接的中断父子关系来”压平”这个复杂性。 3.2 中断信号的属性 中断产生者（Interrupt-Generating Device）使用两个核心属性： device1 { interrupts = &lt;0xA 8&gt;; /* 该设备产生的中断规格（specifier） */ interrupt-parent = &lt;&amp;open-pic&gt;; /* 中断发往哪个控制器 */ }; interrupts 的值格式由目标中断控制器定义的 bindings 决定——对一个 Open PIC 兼容的中断域，两个 cells 分别表示中断号和电平/敏感度信息。 如果设备没有显式的 interrupt-parent，中断父节点就默认向上继承——沿着设备树向上找，使用第一个声明的中断父节点。所以当中断父节点在 SoC 级别统一指定时，设备可以不写 interrupt-parent： soc { interrupt-parent = &lt;&amp;intc&gt;; /* 所有子设备统一的中断父节点 */ serial@70006300 { interrupts = &lt;122&gt;; /* 不需要重复 interrupt-parent */ }; }; interrupts-extended 用于一个设备连接到多个中断控制器的情况： interrupts-extended = &lt;&amp;pic 0xA 8&gt;, &lt;&amp;gic 0xda&gt;; interrupts 和 interrupts-extended 互斥。当两者共存时，interrupts-extended 优先。 3.3 中断控制器 一个节点要成为中断控制器，需要两个属性： open-pic { interrupt-controller; /* 标记：我是中断控制器（空值属性） */ #interrupt-cells = &lt;2&gt;; /* 每个中断 specifier 占几个 cell */ }; #interrupt-cells 定义了中断规格的”宽度”。对于 Open PIC 这种需要 2 cells（中断号 + 电平/敏感度）的控制器，值就是 &lt;2&gt;。对于 ARM GIC (Generic Interrupt Controller)，可能是 &lt;3&gt;（中断类型 + 中断号 + 触发类型）。 中断树的根是遍历中断父链直到找到一个没有 interrupts 属性的中断控制器——这意味着它本身不向更高的控制器报告中断。 3.4 中继：Nexus 节点与中断映射 当不同中断域之间需要翻译时（比如 PCI 的 INTA/INTB/INTC/INTD 经过 PCI host bridge 转换到系统中断号），需要一个nexus 节点，通过 interrupt-map 来做这个翻译。 一条映射表行包含五个部分： interrupt-map = &lt; child-unit-address child-interrupt-specifier interrupt-parent-phandle parent-unit-address parent-interrupt-specifier &gt;; 例如，PCI slot 1 的 INTA（INT #1）映射到 Open PIC 控制器的中断源 2： interrupt-map = &lt; 0x8800 0 0 1 &amp;open-pic 2 1 &gt;; interrupt-map-mask 在匹配前被 AND 到输入上，作用是屏蔽掉不需要参与匹配的位（如 PCI 总线的 function number 可能不区分）。 3.5 GPIO 等非中断信号的通用映射机制 DTSpec 将 nexus 映射机制推广到了所有类型的 “规格（specifier）”：&lt;specifier&gt;-map、&lt;specifier&gt;-map-mask、&lt;specifier&gt;-map-pass-thru 以及 #&lt;specifier&gt;-cells。 最常见的例子是 GPIO 映射：一个连接器（connector）上的 GPIO 通过 gpio-map 路由到 SoC 内部的某个 GPIO 控制器： connector { #gpio-cells = &lt;2&gt;; gpio-map = &lt;0 0 &amp;soc_gpio1 1 0&gt;, &lt;1 0 &amp;soc_gpio2 4 0&gt;, &lt;2 0 &amp;soc_gpio1 3 0&gt;, &lt;3 0 &amp;soc_gpio2 2 0&gt;; gpio-map-mask = &lt;0xf 0x0&gt;; gpio-map-pass-thru = &lt;0x0 0x1&gt;; }; gpio-map-pass-thru 的巧妙之处在于允许子 specifier 中的 flags 字段（如 GPIO_ACTIVE_LOW）穿越映射表，直接透传到父域，避免映射表需要为每种 flag 组合都写一行。 理解设备树中断子系统的最佳思路：中断信号的路径是一张独立于设备物理层级的有向图。遍历从 interrupts/interrupt-parent 开始，沿链上溯，直到中断树根（没有 interrupts 的中断控制器）。每一层可能是中断控制器或 nexus 节点——nexus 节点通过 interrupt-map 表提供域间翻译。这一整套设计的目的在于允许同一个设备树描述中的任何节点都能够表达自己的中断路由，机制统一且可扩展。 4. 拼装一台计算机：系统骨架与必选节点 设备树不只是描述外设。一套完整的设备树必须描述整个系统的骨架 —— CPU 几核、内存多大、控制台在哪。 4.1 根节点 / 根节点没有名字，用 / 表示。它的核心属性包括： / { model = "fsl,mpc8572ds"; compatible = "fsl,mpc8572ds"; #address-cells = &lt;1&gt;; #size-cells = &lt;1&gt;; serial-number = "PZ123456"; /* 可选：序列号 */ chassis-type = "embedded"; /* 可选：机箱类型 */ }; 根节点的 #address-cells 和 #size-cells 定义了内存地址的编码规则，影响 /memory 节点的 reg 格式。 chassis-type 可取 "desktop"、"laptop"、"convertible"、"server"、"tablet"、"handset"、"embedded" 等值。 4.2 大脑：/cpus /cpus 节点是所有 CPU 节点的容器： cpus { #address-cells = &lt;1&gt;; #size-cells = &lt;0&gt;; cpu@0 { device_type = "cpu"; reg = &lt;0&gt;; clock-frequency = &lt;825000000&gt;; /* 825 MHz */ timebase-frequency = &lt;82500000&gt;; /* 82.5 MHz 时基 */ }; cpu@1 { device_type = "cpu"; reg = &lt;1&gt;; clock-frequency = &lt;825000000&gt;; }; }; 多核 SMP 系统中，status 属性标记每颗 CPU 的运行状态： "okay"：该 CPU 正常运行 "disabled"：该 CPU 处于静止状态（quiescent state），可通过特定方法唤醒 "fail"：该 CPU 不可用或不存在 对于 disabled 的 CPU，需要 enable-method 属性指定唤醒方式。最常见的是 "spin-table"：CPU 在指定物理地址（cpu-release-addr）上自旋等待，直到引导 CPU 写入释放信号。 缓存层级 CPU 的缓存层级通过 next-level-cache 属性和 phandle 引用串联： cpu@0 { next-level-cache = &lt;&amp;L2_0&gt;; L2_0: l2-cache { compatible = "cache"; cache-level = &lt;2&gt;; cache-size = &lt;0x40000&gt;; /* 256 KB */ next-level-cache = &lt;&amp;L3&gt;; }; }; 这种基于 phandle 的多级缓存描述方式让缓存拓扑可以灵活表达各种共享结构（两核共享 L2、全芯片共享 L3 等）。 4.3 物理记忆：/memory 和 /reserved-memory /memory 节点宣告系统的可用物理 RAM。这是每个设备树必须具备的节点之一——所有设备树都必须有 /cpus 和至少一个 /memory 节点。 memory@0 { device_type = "memory"; reg = &lt;0x00000000 0x80000000&gt;; /* 0x0 起 2 GB */ }; 对于 64 位系统（#address-cells = &lt;2&gt;, #size-cells = &lt;2&gt;）： memory@0 { device_type = "memory"; reg = &lt;0x00000000 0x00000000 0x00000000 0x80000000&gt;; /* 低 2GB */ }; memory@100000000 { device_type = "memory"; reg = &lt;0x00000001 0x00000000 0x00000001 0x00000000&gt;; /* 高 4GB */ }; /reserved-memory 节点划定不可被内核随意使用的内存禁区： reserved-memory { #address-cells = &lt;1&gt;; #size-cells = &lt;1&gt;; ranges; display_reserved: framebuffer@78000000 { reg = &lt;0x78000000 0x800000&gt;; /* 8 MB 静态预留 */ }; linux,cma { compatible = "shared-dma-pool"; reusable; size = &lt;0x4000000&gt;; /* 64 MB 动态分配 */ alignment = &lt;0x2000&gt;; /* 8 KB 对齐 */ }; }; no-map：操作系统禁止为这段区域创建虚拟映射——用于安全隔离（如 TEE/安全飞地）。 reusable：系统可临时将这片内存挪作他用（如页面缓存），但当设备驱动需要时，能将其要回来。 no-map 和 reusable 是互斥的。 设备通过 memory-region 属性引用专属内存： fb0: video@12300000 { memory-region = &lt;&amp;display_reserved&gt;; }; 关于与 UEFI 的协作：当通过 UEFI 启动时，/memory 和静态 /reserved-memory 的内容也必须在 UEFI 内存映射表中体现：带 no-map 的区域标记为 EfiReservedMemoryType，其他区域标记为 EfiBootServicesData。动态预留区域则不应在 UEFI 内存映射表中列出——它们在固件退出后才由操作系统分配。 4.4 运行时通道：/chosen 和 /aliases /chosen 节点不代表任何物理设备。它是引导程序向内核传递运行时配置的通道： chosen { bootargs = "console=ttyS0,115200 root=/dev/mmcblk0p2 rw"; stdout-path = "/soc/serial@70006300:115200"; }; bootargs 就是内核命令行参数，stdout-path 指定启动控制台的输出设备。 /aliases 为冗长的路径起短名： aliases { serial0 = "/soc/serial@70006300"; ethernet0 = "/soc/ethernet@31c000"; }; 内核中的 of_alias_get_id() 等函数使用这些别名定位设备或标准化设备编号（如 /dev/ttyS0 对应 serial0）。 4.5 典型设备绑定 4.5.1 不可探测的系统总线：simple-bus compatible = "simple-bus" 是设备树中的一个重要特殊值。它描述一类最简单的总线：内存映射、不可探测、子设备的寄存器直接在 CPU 地址空间可见。 SoC 内部总线通常就是 simple-bus。当内核看到 simple-bus 兼容的节点时，会自动遍历其子节点并注册 platform_device： soc { compatible = "simple-bus"; #address-cells = &lt;1&gt;; #size-cells = &lt;1&gt;; ranges; /* 子节点自动被创建为 platform_device */ }; 4.5.2 串口：以 NS16550 为例 serial@70006300 { compatible = "nvidia,tegra20-uart", "ns16550"; reg = &lt;0x70006300 0x100&gt;; clock-frequency = &lt;408000000&gt;; interrupts = &lt;122&gt;; }; clock-frequency 是 UART 波特率发生器所需的时钟频率——内核 UART 驱动用这个值计算正确的分频系数。 4.5.3 网络：以太网 ethernet@31c000 { compatible = "nvidia,tegra20-ehci"; reg = &lt;0x31c000 0x100&gt;; interrupts = &lt;0x6d&gt;; local-mac-address = [00 11 22 33 44 55]; phy-connection-type = "rgmii"; }; local-mac-address 是 6 字节的 MAC 地址，用方括号格式的字节串表示。phy-connection-type 指定 PHY 和 MAC 之间的总线类型（RGMII、RMII、MII 等）——这决定了内核如何配置 MAC 侧的时序和管脚。 5. 庖丁解牛：DTB 二进制格式的底层设计 虽然大部分开发者只接触 DTS 源码，但理解 DTB 的二进制结构有助于理解设备树的设计哲学——为什么某些奇怪的限制存在。 5.1 扁平化的哲学：为什么没有 C 风格的指针？ DTSpec 规范开篇第一句就点明了关键前提： A boot program loads a devicetree into a client program’s memory and passes a pointer to the devicetree to the client. 在这个时刻，MMU（内存管理单元）可能还没开启，虚拟地址的概念不存在。DTB 必须是一块可以被任意复制、重定位到任意物理地址的数据块。 如果 DTB 内部使用 C 指针——那在重定位后就全是悬垂指针了。因此 DTB 设计为完全自包含、位置无关的格式：所有的跨区块引用都用偏移量而非指针表达。 这就是”扁平化”（Flattened）的核心含义——一个可以平移到任意内存位置、无需解析指针就能直接读取的数据结构。 5.2 DTB 的四大内存区块 一个 DTB 文件由四个连续的内存区块组成： ┌───────────────────────┐ ← 低地址 │ Header (头部) │ 40 字节 (10 × uint32_t) ├───────────────────────┤ │ Memory Reservation │ 预留内存列表 │ Block │ ├───────────────────────┤ │ Structure Block │ 树的线性 Token 序列 │ (FDT_BEGIN_NODE 等) │ ├───────────────────────┤ │ Free Space (可选) │ ├───────────────────────┤ │ Strings Block │ 属性名字符串池 └───────────────────────┘ ← 高地址 5.2.1 Header（头部）：设备的身份证 Header 是 40 字节的固定结构，所有字段都是大端 32 位整数： struct fdt_header { uint32_t magic; /* 0xd00dfeed — 魔数 */ uint32_t totalsize; /* DTB 总大小 */ uint32_t off_dt_struct; /* Structure Block 偏移 */ uint32_t off_dt_strings; /* Strings Block 偏移 */ uint32_t off_mem_rsvmap; /* 内存预留块偏移 */ uint32_t version; /* 格式版本 (当前为 17) */ uint32_t last_comp_version; /* 向后兼容的最低版本 (16) */ uint32_t boot_cpuid_phys; /* 引导 CPU 的物理 ID */ uint32_t size_dt_strings; /* Strings Block 大小 */ uint32_t size_dt_struct; /* Structure Block 大小 */ }; 几个关键字段： magic = 0xd00dfeed：这个魔数的英文读音就是 “dude feed” —— 是 Open Firmware 工作组留下的一个幽默彩蛋。内核通过比对它来确认”这确实是一个 DTB”。 version = 17：当前 DTSpec 标准格式版本。向后兼容的最低版本是 16。 totalsize：涵盖所有区块及之间的空格，但不包含 DTB 前后的额外内存。这个字段让解析器提前知道数据边界。 boot_cpuid_phys：必须与 /cpus 中对应 CPU 节点的 reg 值一致。内核通过它确定哪个 CPU 是引导 CPU。 5.2.2 Memory Reservation Block：硬核预留 内核初始化过程的脆弱时刻——代码在未成熟的地址空间中一步步构建完整的虚拟地址系统——必须确保引导程序的关键内存不被覆写。Memory Reservation Block 就是为此而建立的”禁止访问”清单。 struct fdt_reserve_entry { uint64_t address; uint64_t size; }; 每个条目描述一段（物理地址, 大小）的保留区域。列表以 (0, 0) 条目终止。这比 /reserved-memory 节点更底层——在内核还没解析设备树结构之前，引导程序就已确认这些区域不能被触碰。 5.2.3 Structure Block：用 Token 线性展开的树 这是 DTB 最核心的部分——如何将一个树状结构编码为平直化的字节序列？ 结构区块由 5 种 Token 组成，每个 Token 是 32 位大端整数： Token 值 含义 FDT_BEGIN_NODE 0x00000001 节点开始，后跟节点名（null-terminated string） FDT_END_NODE 0x00000002 节点结束 FDT_PROP 0x00000003 属性，后跟 len + nameoff + 值 FDT_NOP 0x00000004 空操作（用于覆盖已删除的节点/属性） FDT_END 0x00000009 结构区块结束标记 FDT_PROP 的编码： struct { uint32_t len; /* 属性值的字节长度 */ uint32_t nameoff; /* 属性名字在 Strings Block 中的偏移量 */ }; /* 后跟 len 字节的属性值，然后 0 填充至 4 字节对齐 */ 树的线性化编码： FDT_BEGIN_NODE "/" FDT_PROP 属性1 FDT_PROP 属性2 FDT_BEGIN_NODE "soc" FDT_PROP 属性 FDT_END_NODE FDT_END_NODE FDT_END 全局结束 这种结构的巧妙处： 前端解析器从头到尾扫描，遇到 FDT_BEGIN_NODE 就是”进入子节点”，遇到 FDT_END_NODE 就是”回到父节点”——一个栈结构自然处理整个解析。 通过 FDT_NOP（覆盖被删除块的开头），DTB 可以在不移动其他数据的情况下”删除”节点或属性——这在 DT overlay 和运行时修改的场景中非常重要。 属性名通过偏移量引用 Strings Block，同一个名字字符串被全局共享——这正是下一节要讲的极致压缩策略。 5.2.4 Strings Block：极致压缩的字符串复用池 设备树里有大量相同的属性名——”compatible” 可能出现上百次，”reg” 可能出现几百次。Strings Block 通过所有属性名统一存储，只保存一份字符串，然后用偏移量引用来实现极致的空间压缩。 本质上，这就是一个以 null 字节分隔的字符串列表。查找过程：取 nameoff 值 → 跳到 Strings Block 开始处 + nameoff 的位置 → 读到 null 为止。这个简单的设计让 20 KB 的 DTS 编译后可能只有 5 KB 的 DTB。 5.3 内存对齐：使用 32 位单元但以 8 字节整体看待 DTB 的每一处对齐都有其物理上的现实原因： Structure Block 按要求 4 字节对齐——每个 Token 是 uint32_t Memory Reservation Block 8 字节对齐——条目是 uint64_t 对 整个 DTB 按 8 字节对齐加载——这是所有子块对齐要求的最大公约数 这个 8 字节整体对齐要求意味着引导程序从任何可以 8 字节对齐的地址上加载的 DTB 都能确保在不做额外内存搬运的情况下，被内核安全地按 4 或 8 字节访问。 DTB 的设计体现的是嵌入式引导阶段的现实约束：无 MMU、无动态分配、物理内存可能非连续、引导程序的内存在”交接时刻”不可以被覆盖。那些看似奇怪的限制——没有指针只有偏移量、所有数据平坦排列、必须整体 8 字节对齐——全都是从这个唯一约束推导出来的：在 MMU 开启前，内核必须安全地解析一个可以被任意重定位的硬件描述块。 结语 设备树，归根结底，是一份交给操作系统的”施工图纸”。引导程序拿着蓝图说：”这就是这个机器的样子。”操作系统回答说：”好的，我知道该怎么做了。” 参考资料 Linux Kernel Documentation: Devicetree Usage Model DeviceTree Specification（devicetree-org）]]></summary></entry><entry><title type="html">BibTeX 与 BibLaTeX 入门：.bib 文件结构、引用与编译流程</title><link href="https://bfmhno3.github.io/note/bibtex-and-biblatex-guide/" rel="alternate" type="text/html" title="BibTeX 与 BibLaTeX 入门：.bib 文件结构、引用与编译流程" /><published>2026-05-19T14:00:00+00:00</published><updated>2026-05-19T14:00:00+00:00</updated><id>https://bfmhno3.github.io/note/bibtex-and-biblatex-guide</id><content type="html" xml:base="https://bfmhno3.github.io/note/bibtex-and-biblatex-guide/"><![CDATA[<p>最近恰逢本科阶段的倒数第二个课程设计，初步估计又得三四十页，并且老师不发模板只发排版要求，美其名曰让我们提前适应毕业论文写作（但是我记得毕业论文也是有 Word 模板的吧😂）。</p>

<p>我并非是某一方的狂热爱好者，在我看来两者都是工具，只要能解决我的问题，那就是好工具。显然这种比较专业的排版需求还是使用 LaTeX 比较合适，而我发现我对 LaTeX 的引用数据库还是不太了解，就大概学习了一并下整理了这篇博客，不一定足够专业，但是应该能够应付我的课程设计了。</p>

<h2 id="1-bib-文件的基本结构">1. <code class="language-plaintext highlighter-rouge">.bib</code> 文件的基本结构</h2>

<p><code class="language-plaintext highlighter-rouge">.bib</code> 文件可以理解成一个文献数据库，由多条文献条目组成。每条条目一般是下面这种形式：</p>

<div class="language-bibtex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">@entrytype</span><span class="p">{</span><span class="nl">citekey</span><span class="p">,</span>
  <span class="na">field1</span> <span class="p">=</span> <span class="s">{value1}</span><span class="p">,</span>
  <span class="na">field2</span> <span class="p">=</span> <span class="s">{value2}</span><span class="p">,</span>
  <span class="na">...</span>
<span class="err">}</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">@entrytype</code>：文献类型，例如 <code class="language-plaintext highlighter-rouge">@article</code>、<code class="language-plaintext highlighter-rouge">@book</code>、<code class="language-plaintext highlighter-rouge">@inproceedings</code>。</li>
  <li><code class="language-plaintext highlighter-rouge">citekey</code>：引用键（citation key），在正文中通过 <code class="language-plaintext highlighter-rouge">\cite{citekey}</code> 调用。</li>
  <li><code class="language-plaintext highlighter-rouge">field = {value}</code>：字段和值，如 <code class="language-plaintext highlighter-rouge">author</code>、<code class="language-plaintext highlighter-rouge">title</code>、<code class="language-plaintext highlighter-rouge">year</code>。</li>
</ul>

<h3 id="11-常见条目类型与常用字段">1.1 常见条目类型与常用字段</h3>

<table>
  <thead>
    <tr>
      <th>条目类型</th>
      <th>说明</th>
      <th>常用字段</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@article</code></td>
      <td>期刊论文</td>
      <td><code class="language-plaintext highlighter-rouge">author</code>, <code class="language-plaintext highlighter-rouge">title</code>, <code class="language-plaintext highlighter-rouge">journal</code>, <code class="language-plaintext highlighter-rouge">year</code>, <code class="language-plaintext highlighter-rouge">volume</code>, <code class="language-plaintext highlighter-rouge">number</code>, <code class="language-plaintext highlighter-rouge">pages</code>, <code class="language-plaintext highlighter-rouge">doi</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@inproceedings</code></td>
      <td>会议论文</td>
      <td><code class="language-plaintext highlighter-rouge">author</code>, <code class="language-plaintext highlighter-rouge">title</code>, <code class="language-plaintext highlighter-rouge">booktitle</code>, <code class="language-plaintext highlighter-rouge">year</code>, <code class="language-plaintext highlighter-rouge">pages</code>, <code class="language-plaintext highlighter-rouge">organization</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@book</code></td>
      <td>书籍</td>
      <td><code class="language-plaintext highlighter-rouge">author</code> 或 <code class="language-plaintext highlighter-rouge">editor</code>, <code class="language-plaintext highlighter-rouge">title</code>, <code class="language-plaintext highlighter-rouge">publisher</code>, <code class="language-plaintext highlighter-rouge">year</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@phdthesis</code> / <code class="language-plaintext highlighter-rouge">@mastersthesis</code></td>
      <td>学位论文</td>
      <td><code class="language-plaintext highlighter-rouge">author</code>, <code class="language-plaintext highlighter-rouge">title</code>, <code class="language-plaintext highlighter-rouge">school</code>, <code class="language-plaintext highlighter-rouge">year</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">@misc</code> / <code class="language-plaintext highlighter-rouge">@online</code>（BibLaTeX）</td>
      <td>网页、预印本</td>
      <td><code class="language-plaintext highlighter-rouge">author</code>, <code class="language-plaintext highlighter-rouge">title</code>, <code class="language-plaintext highlighter-rouge">year</code>, <code class="language-plaintext highlighter-rouge">howpublished</code> 或 <code class="language-plaintext highlighter-rouge">url</code>, <code class="language-plaintext highlighter-rouge">note</code></td>
    </tr>
  </tbody>
</table>

<p class="notice--info">不同条目在 BibTeX 与 BibLaTeX 中的必填字段会有差异，建议以所用样式文档为准。可以通过 <code class="language-plaintext highlighter-rouge">texdoc biblatex</code> 或 <code class="language-plaintext highlighter-rouge">texdoc bibtex</code> 查看官方说明。</p>

<hr />

<h2 id="2-一个最小可用的-bib-示例">2. 一个最小可用的 <code class="language-plaintext highlighter-rouge">.bib</code> 示例</h2>

<p>假设你新建一个文件 <code class="language-plaintext highlighter-rouge">references.bib</code>，内容如下：</p>

<div class="language-bibtex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">@article</span><span class="p">{</span><span class="nl">einstein1905</span><span class="p">,</span>
  <span class="na">author</span>  <span class="p">=</span> <span class="s">{Albert Einstein}</span><span class="p">,</span>
  <span class="na">title</span>   <span class="p">=</span> <span class="s">{On the Electrodynamics of Moving Bodies}</span><span class="p">,</span>
  <span class="na">journal</span> <span class="p">=</span> <span class="s">{Annalen der Physik}</span><span class="p">,</span>
  <span class="na">year</span>    <span class="p">=</span> <span class="s">{1905}</span><span class="p">,</span>
  <span class="na">volume</span>  <span class="p">=</span> <span class="s">{17}</span><span class="p">,</span>
  <span class="na">pages</span>   <span class="p">=</span> <span class="s">{891--921}</span><span class="p">,</span>
<span class="p">}</span>

<span class="nc">@book</span><span class="p">{</span><span class="nl">knuth1984</span><span class="p">,</span>
  <span class="na">author</span>    <span class="p">=</span> <span class="s">{Donald E. Knuth}</span><span class="p">,</span>
  <span class="na">title</span>     <span class="p">=</span> <span class="s">{The {TeX}book}</span><span class="p">,</span>
  <span class="na">publisher</span> <span class="p">=</span> <span class="s">{Addison-Wesley}</span><span class="p">,</span>
  <span class="na">year</span>      <span class="p">=</span> <span class="s">{1984}</span><span class="p">,</span>
<span class="p">}</span>

<span class="nc">@inproceedings</span><span class="p">{</span><span class="nl">he2016resnet</span><span class="p">,</span>
  <span class="na">author</span>    <span class="p">=</span> <span class="s">{Kaiming He and Xiangyu Zhang and Shaoqing Ren and Jian Sun}</span><span class="p">,</span>
  <span class="na">title</span>     <span class="p">=</span> <span class="s">{Deep Residual Learning for Image Recognition}</span><span class="p">,</span>
  <span class="na">booktitle</span> <span class="p">=</span> <span class="s">{Proceedings of the IEEE Conference on Computer Vision and Pattern Recognition (CVPR)}</span><span class="p">,</span>
  <span class="na">year</span>      <span class="p">=</span> <span class="s">{2016}</span><span class="p">,</span>
  <span class="na">pages</span>     <span class="p">=</span> <span class="s">{770--778}</span><span class="p">,</span>
<span class="p">}</span>
</code></pre></div></div>

<p>几个常用写法：</p>

<ul>
  <li><strong>多个作者用 <code class="language-plaintext highlighter-rouge">and</code> 分隔</strong>（BibTeX/BibLaTeX 会自动识别作者列表）：</li>
</ul>

<div class="language-bibtex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">author = {First Author and Second Author and Third Author},</span>
</code></pre></div></div>

<ul>
  <li><strong>页码用 <code class="language-plaintext highlighter-rouge">--</code> 而不是 <code class="language-plaintext highlighter-rouge">-</code></strong>：</li>
</ul>

<div class="language-bibtex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">pages = {770--778},</span>
</code></pre></div></div>

<ul>
  <li><strong>大小写保护</strong>：专有名词或缩写（如 <code class="language-plaintext highlighter-rouge">TeX</code>、<code class="language-plaintext highlighter-rouge">CNN</code>）可用 <code class="language-plaintext highlighter-rouge">{}</code> 包起来，避免被样式改写：</li>
</ul>

<div class="language-bibtex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">title = {Deep Residual Learning for {CNN} Image Recognition},</span>
</code></pre></div></div>

<hr />

<h2 id="3-在-latex-里如何使用-bib两种主流工作流">3. 在 LaTeX 里如何使用 .bib（两种主流工作流）</h2>

<ol>
  <li><strong>传统路线</strong>：LaTeX + BibTeX</li>
  <li><strong>现代路线</strong>：LaTeX + BibLaTeX + biber</li>
</ol>

<hr />

<h3 id="31-传统latex--bibtex-工作流">3.1 传统：LaTeX + BibTeX 工作流</h3>

<p><strong>文档示例（<code class="language-plaintext highlighter-rouge">main.tex</code>）：</strong></p>

<div class="language-latex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\documentclass</span><span class="p">{</span>article<span class="p">}</span>
<span class="k">\usepackage</span><span class="na">[utf8]</span><span class="p">{</span>inputenc<span class="p">}</span> <span class="c">% 使用 XeLaTeX/LuaLaTeX 时通常不需要</span>

<span class="nt">\begin{document}</span>

这是正文中的一条引用 <span class="k">\cite</span><span class="p">{</span>einstein1905<span class="p">}</span>，再来一条 <span class="k">\cite</span><span class="p">{</span>knuth1984<span class="p">}</span>。

<span class="k">\bibliographystyle</span><span class="p">{</span>ieeetr<span class="p">}</span>   <span class="c">% 选择参考文献样式，如 plain, unsrt, alpha, ieeetr 等</span>
<span class="k">\bibliography</span><span class="p">{</span>references<span class="p">}</span>    <span class="c">% 不写 .bib 后缀</span>

<span class="nt">\end{document}</span>
</code></pre></div></div>

<p><strong>编译顺序（非常关键）：</strong></p>

<ol>
  <li><code class="language-plaintext highlighter-rouge">pdflatex main</code></li>
  <li><code class="language-plaintext highlighter-rouge">bibtex main</code></li>
  <li><code class="language-plaintext highlighter-rouge">pdflatex main</code></li>
  <li><code class="language-plaintext highlighter-rouge">pdflatex main</code></li>
</ol>

<p>很多编辑器可以自动完成这些步骤，但底层顺序仍然是这 4 步。</p>

<p><strong>特点</strong>：</p>

<ul>
  <li><strong>优点</strong>：历史悠久、兼容性好，很多旧模板和投稿模板仍要求 BibTeX。</li>
  <li><strong>缺点</strong>：对 UTF-8、多语种姓名支持相对有限；<code class="language-plaintext highlighter-rouge">.bst</code> 样式定制门槛较高；对 URL、DOI、在线资源的处理不如 BibLaTeX 灵活。</li>
</ul>

<hr />

<h3 id="32-现代biblatex--biber-工作流">3.2 现代：BibLaTeX + biber 工作流</h3>

<p><strong>文档示例（<code class="language-plaintext highlighter-rouge">main.tex</code>）：</strong></p>

<div class="language-latex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\documentclass</span><span class="p">{</span>article<span class="p">}</span>
<span class="k">\usepackage</span><span class="na">[utf8]</span><span class="p">{</span>inputenc<span class="p">}</span> <span class="c">% 使用 XeLaTeX/LuaLaTeX 时可省略</span>
<span class="k">\usepackage</span><span class="na">[backend=biber,style=authoryear,sorting=nyt]</span><span class="p">{</span>biblatex<span class="p">}</span>

<span class="k">\addbibresource</span><span class="p">{</span>references.bib<span class="p">}</span> <span class="c">% 指定 .bib 文件</span>

<span class="nt">\begin{document}</span>

这是正文中的一条引用 <span class="k">\parencite</span><span class="p">{</span>einstein1905<span class="p">}</span>，
以及一条行文引用 <span class="k">\textcite</span><span class="p">{</span>knuth1984<span class="p">}</span>。

<span class="k">\printbibliography</span>

<span class="nt">\end{document}</span>
</code></pre></div></div>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">backend=biber</code>：</strong>指定后端为 <code class="language-plaintext highlighter-rouge">biber</code>（而不是 <code class="language-plaintext highlighter-rouge">bibtex</code>）。</li>
  <li><strong><code class="language-plaintext highlighter-rouge">style=authoryear</code>：</strong>作者-年份格式（如 “Einstein 1905”），也可改为 <code class="language-plaintext highlighter-rouge">numeric</code>、<code class="language-plaintext highlighter-rouge">ieee</code> 等。</li>
  <li><strong><code class="language-plaintext highlighter-rouge">sorting=nyt</code>：</strong>按 <code class="language-plaintext highlighter-rouge">name-year-title</code> 排序。</li>
</ul>

<p><strong>编译顺序：</strong></p>

<ol>
  <li><code class="language-plaintext highlighter-rouge">pdflatex main</code></li>
  <li><code class="language-plaintext highlighter-rouge">biber main</code></li>
  <li><code class="language-plaintext highlighter-rouge">pdflatex main</code></li>
  <li><code class="language-plaintext highlighter-rouge">pdflatex main</code></li>
</ol>

<p>如果你用的是 XeLaTeX/LuaLaTeX，则把 <code class="language-plaintext highlighter-rouge">pdflatex</code> 换成 <code class="language-plaintext highlighter-rouge">xelatex</code> 或 <code class="language-plaintext highlighter-rouge">lualatex</code> 即可。</p>

<p><strong>特点</strong>：</p>

<ul>
  <li><strong>优点</strong>：对 UTF-8 与多语言支持更好；字段更丰富（如 <code class="language-plaintext highlighter-rouge">@online</code>、<code class="language-plaintext highlighter-rouge">url</code>、<code class="language-plaintext highlighter-rouge">doi</code>、<code class="language-plaintext highlighter-rouge">eprint</code>）；样式可通过 LaTeX 宏定制，可维护性更高；支持按章节、按类型分组等高级功能。</li>
  <li><strong>缺点</strong>：某些期刊/会议模板仍明确要求 BibTeX。</li>
</ul>

<hr />

<h2 id="4-该如何选择">4. 该如何选择？</h2>

<p class="notice--important">如果模板没有明确限制，通常可以优先考虑 <code class="language-plaintext highlighter-rouge">XeLaTeX/LuaLaTeX + BibLaTeX + biber</code>。</p>

<hr />

<h2 id="5-写-bib-时的一些实用小技巧">5. 写 <code class="language-plaintext highlighter-rouge">.bib</code> 时的一些实用小技巧</h2>

<ul>
  <li><strong>优先从数据库导出 BibTeX</strong>：例如 Google Scholar、arXiv、ACM、IEEE、Springer，可减少手写错误。</li>
  <li><strong>统一命名规则（citekey）</strong>：</li>
</ul>

<p>比如 <code class="language-plaintext highlighter-rouge">authorYearShortTitle</code>：</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vaswani2017attention
he2016resnet
knuth1984texbook
</code></pre></div></div>

<ul>
  <li><strong>中文文献的处理</strong>：在 BibLaTeX + biber + UTF-8 工作流中，可直接使用中文 <code class="language-plaintext highlighter-rouge">title</code>、<code class="language-plaintext highlighter-rouge">author</code>；若投稿要求英文参考文献，可将英文信息写入主要字段，中文信息放在 <code class="language-plaintext highlighter-rouge">note</code> 或 <code class="language-plaintext highlighter-rouge">language</code>。</li>
  <li><strong>注意特殊字符转义</strong>：<code class="language-plaintext highlighter-rouge">&amp;</code>, <code class="language-plaintext highlighter-rouge">%</code>, <code class="language-plaintext highlighter-rouge">_</code>, <code class="language-plaintext highlighter-rouge">#</code> 等应转义为 <code class="language-plaintext highlighter-rouge">\&amp;</code>, <code class="language-plaintext highlighter-rouge">\%</code>, <code class="language-plaintext highlighter-rouge">\_</code>, <code class="language-plaintext highlighter-rouge">\#</code>。</li>
</ul>

<hr />

<h2 id="6-完整示例biblatex--biber">6. 完整示例（BibLaTeX + biber）</h2>

<p>下面给出一套可直接实验的最小流程。</p>

<h3 id="61-新建-referencesbib">6.1 新建 <code class="language-plaintext highlighter-rouge">references.bib</code></h3>

<div class="language-bibtex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">@article</span><span class="p">{</span><span class="nl">vaswani2017attention</span><span class="p">,</span>
  <span class="na">author</span>  <span class="p">=</span> <span class="s">{Ashish Vaswani and Noam Shazeer and Niki Parmar and Jakob Uszkoreit
             and Llion Jones and Aidan N. Gomez and Lukasz Kaiser and Illia Polosukhin}</span><span class="p">,</span>
  <span class="na">title</span>   <span class="p">=</span> <span class="s">{Attention Is All You Need}</span><span class="p">,</span>
  <span class="na">journal</span> <span class="p">=</span> <span class="s">{Advances in Neural Information Processing Systems}</span><span class="p">,</span>
  <span class="na">volume</span>  <span class="p">=</span> <span class="s">{30}</span><span class="p">,</span>
  <span class="na">year</span>    <span class="p">=</span> <span class="s">{2017}</span><span class="p">,</span>
<span class="p">}</span>

<span class="nc">@book</span><span class="p">{</span><span class="nl">knuth1984texbook</span><span class="p">,</span>
  <span class="na">author</span>    <span class="p">=</span> <span class="s">{Donald E. Knuth}</span><span class="p">,</span>
  <span class="na">title</span>     <span class="p">=</span> <span class="s">{The {TeX}book}</span><span class="p">,</span>
  <span class="na">publisher</span> <span class="p">=</span> <span class="s">{Addison-Wesley}</span><span class="p">,</span>
  <span class="na">year</span>      <span class="p">=</span> <span class="s">{1984}</span><span class="p">,</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="62-新建-maintex">6.2 新建 <code class="language-plaintext highlighter-rouge">main.tex</code></h3>

<div class="language-latex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">\documentclass</span><span class="p">{</span>article<span class="p">}</span>
<span class="k">\usepackage</span><span class="na">[utf8]</span><span class="p">{</span>inputenc<span class="p">}</span>
<span class="k">\usepackage</span><span class="na">[backend=biber,style=authoryear,sorting=nyt]</span><span class="p">{</span>biblatex<span class="p">}</span>

<span class="k">\addbibresource</span><span class="p">{</span>references.bib<span class="p">}</span>

<span class="nt">\begin{document}</span>

这是对 Transformer 的引用 <span class="k">\parencite</span><span class="p">{</span>vaswani2017attention<span class="p">}</span>，
这是对 <span class="k">\textcite</span><span class="p">{</span>knuth1984texbook<span class="p">}</span> 的引用。

<span class="k">\printbibliography</span>

<span class="nt">\end{document}</span>
</code></pre></div></div>

<h3 id="63-编译顺序">6.3 编译顺序</h3>

<ol>
  <li><code class="language-plaintext highlighter-rouge">pdflatex main</code></li>
  <li><code class="language-plaintext highlighter-rouge">biber main</code></li>
  <li><code class="language-plaintext highlighter-rouge">pdflatex main</code></li>
  <li><code class="language-plaintext highlighter-rouge">pdflatex main</code></li>
</ol>

<p>如果你用的是 XeLaTeX，就把 <code class="language-plaintext highlighter-rouge">pdflatex</code> 换成 <code class="language-plaintext highlighter-rouge">xelatex</code>。</p>]]></content><author><name>bfmhno3</name></author><category term="Note" /><category term="LaTeX" /><summary type="html"><![CDATA[最近恰逢本科阶段的倒数第二个课程设计，初步估计又得三四十页，并且老师不发模板只发排版要求，美其名曰让我们提前适应毕业论文写作（但是我记得毕业论文也是有 Word 模板的吧😂）。 我并非是某一方的狂热爱好者，在我看来两者都是工具，只要能解决我的问题，那就是好工具。显然这种比较专业的排版需求还是使用 LaTeX 比较合适，而我发现我对 LaTeX 的引用数据库还是不太了解，就大概学习了一并下整理了这篇博客，不一定足够专业，但是应该能够应付我的课程设计了。 1. .bib 文件的基本结构 .bib 文件可以理解成一个文献数据库，由多条文献条目组成。每条条目一般是下面这种形式： @entrytype{citekey, field1 = {value1}, field2 = {value2}, ... } @entrytype：文献类型，例如 @article、@book、@inproceedings。 citekey：引用键（citation key），在正文中通过 \cite{citekey} 调用。 field = {value}：字段和值，如 author、title、year。 1.1 常见条目类型与常用字段 条目类型 说明 常用字段 @article 期刊论文 author, title, journal, year, volume, number, pages, doi @inproceedings 会议论文 author, title, booktitle, year, pages, organization @book 书籍 author 或 editor, title, publisher, year @phdthesis / @mastersthesis 学位论文 author, title, school, year @misc / @online（BibLaTeX） 网页、预印本 author, title, year, howpublished 或 url, note 不同条目在 BibTeX 与 BibLaTeX 中的必填字段会有差异，建议以所用样式文档为准。可以通过 texdoc biblatex 或 texdoc bibtex 查看官方说明。 2. 一个最小可用的 .bib 示例 假设你新建一个文件 references.bib，内容如下： @article{einstein1905, author = {Albert Einstein}, title = {On the Electrodynamics of Moving Bodies}, journal = {Annalen der Physik}, year = {1905}, volume = {17}, pages = {891--921}, } @book{knuth1984, author = {Donald E. Knuth}, title = {The {TeX}book}, publisher = {Addison-Wesley}, year = {1984}, } @inproceedings{he2016resnet, author = {Kaiming He and Xiangyu Zhang and Shaoqing Ren and Jian Sun}, title = {Deep Residual Learning for Image Recognition}, booktitle = {Proceedings of the IEEE Conference on Computer Vision and Pattern Recognition (CVPR)}, year = {2016}, pages = {770--778}, } 几个常用写法： 多个作者用 and 分隔（BibTeX/BibLaTeX 会自动识别作者列表）： author = {First Author and Second Author and Third Author}, 页码用 -- 而不是 -： pages = {770--778}, 大小写保护：专有名词或缩写（如 TeX、CNN）可用 {} 包起来，避免被样式改写： title = {Deep Residual Learning for {CNN} Image Recognition}, 3. 在 LaTeX 里如何使用 .bib（两种主流工作流） 传统路线：LaTeX + BibTeX 现代路线：LaTeX + BibLaTeX + biber 3.1 传统：LaTeX + BibTeX 工作流 文档示例（main.tex）： \documentclass{article} \usepackage[utf8]{inputenc} % 使用 XeLaTeX/LuaLaTeX 时通常不需要 \begin{document} 这是正文中的一条引用 \cite{einstein1905}，再来一条 \cite{knuth1984}。 \bibliographystyle{ieeetr} % 选择参考文献样式，如 plain, unsrt, alpha, ieeetr 等 \bibliography{references} % 不写 .bib 后缀 \end{document} 编译顺序（非常关键）： pdflatex main bibtex main pdflatex main pdflatex main 很多编辑器可以自动完成这些步骤，但底层顺序仍然是这 4 步。 特点： 优点：历史悠久、兼容性好，很多旧模板和投稿模板仍要求 BibTeX。 缺点：对 UTF-8、多语种姓名支持相对有限；.bst 样式定制门槛较高；对 URL、DOI、在线资源的处理不如 BibLaTeX 灵活。 3.2 现代：BibLaTeX + biber 工作流 文档示例（main.tex）： \documentclass{article} \usepackage[utf8]{inputenc} % 使用 XeLaTeX/LuaLaTeX 时可省略 \usepackage[backend=biber,style=authoryear,sorting=nyt]{biblatex} \addbibresource{references.bib} % 指定 .bib 文件 \begin{document} 这是正文中的一条引用 \parencite{einstein1905}， 以及一条行文引用 \textcite{knuth1984}。 \printbibliography \end{document} backend=biber：指定后端为 biber（而不是 bibtex）。 style=authoryear：作者-年份格式（如 “Einstein 1905”），也可改为 numeric、ieee 等。 sorting=nyt：按 name-year-title 排序。 编译顺序： pdflatex main biber main pdflatex main pdflatex main 如果你用的是 XeLaTeX/LuaLaTeX，则把 pdflatex 换成 xelatex 或 lualatex 即可。 特点： 优点：对 UTF-8 与多语言支持更好；字段更丰富（如 @online、url、doi、eprint）；样式可通过 LaTeX 宏定制，可维护性更高；支持按章节、按类型分组等高级功能。 缺点：某些期刊/会议模板仍明确要求 BibTeX。 4. 该如何选择？ 如果模板没有明确限制，通常可以优先考虑 XeLaTeX/LuaLaTeX + BibLaTeX + biber。 5. 写 .bib 时的一些实用小技巧 优先从数据库导出 BibTeX：例如 Google Scholar、arXiv、ACM、IEEE、Springer，可减少手写错误。 统一命名规则（citekey）： 比如 authorYearShortTitle： vaswani2017attention he2016resnet knuth1984texbook 中文文献的处理：在 BibLaTeX + biber + UTF-8 工作流中，可直接使用中文 title、author；若投稿要求英文参考文献，可将英文信息写入主要字段，中文信息放在 note 或 language。 注意特殊字符转义：&amp;, %, _, # 等应转义为 \&amp;, \%, \_, \#。 6. 完整示例（BibLaTeX + biber） 下面给出一套可直接实验的最小流程。 6.1 新建 references.bib @article{vaswani2017attention, author = {Ashish Vaswani and Noam Shazeer and Niki Parmar and Jakob Uszkoreit and Llion Jones and Aidan N. Gomez and Lukasz Kaiser and Illia Polosukhin}, title = {Attention Is All You Need}, journal = {Advances in Neural Information Processing Systems}, volume = {30}, year = {2017}, } @book{knuth1984texbook, author = {Donald E. Knuth}, title = {The {TeX}book}, publisher = {Addison-Wesley}, year = {1984}, } 6.2 新建 main.tex \documentclass{article} \usepackage[utf8]{inputenc} \usepackage[backend=biber,style=authoryear,sorting=nyt]{biblatex} \addbibresource{references.bib} \begin{document} 这是对 Transformer 的引用 \parencite{vaswani2017attention}， 这是对 \textcite{knuth1984texbook} 的引用。 \printbibliography \end{document} 6.3 编译顺序 pdflatex main biber main pdflatex main pdflatex main 如果你用的是 XeLaTeX，就把 pdflatex 换成 xelatex。]]></summary></entry><entry><title type="html">Tmux 使用笔记</title><link href="https://bfmhno3.github.io/note/note-of-tmux/" rel="alternate" type="text/html" title="Tmux 使用笔记" /><published>2026-04-25T13:55:00+00:00</published><updated>2026-04-25T13:55:00+00:00</updated><id>https://bfmhno3.github.io/note/note-of-tmux</id><content type="html" xml:base="https://bfmhno3.github.io/note/note-of-tmux/"><![CDATA[<h2 id="为什么我们需要-tmux">为什么我们需要 tmux？</h2>

<h3 id="1-终端的物理与逻辑基础">1. 终端的物理与逻辑基础</h3>

<p>在计算机世界里，我们通过 “终端（Terminal）” 与 “Shell（如 bash, zsh）” 进行交互。</p>

<p>当你打开一个终端窗口，或者通过 SSH 连接到远程服务器时，操作系统为你分配了一个 <strong>TTY（伪终端）</strong>，并在这个 TTY 上启动了一个 Shell 进程。你在终端里运行的所有程序（跑脚本、开服务器、编辑代码），都是这个 Shell 的<strong>子进程</strong>。</p>

<h3 id="2-致命的耦合痛点所在">2. 致命的耦合（痛点所在）</h3>

<p>这就产生了一个致命的 “生命周期耦合” 问题：</p>

<ul>
  <li><strong>规则</strong>：当你的终端窗口关闭，或者由于网络波动导致 SSH 连接断开时，系统会向该 TTY 上的 Shell 发送一个 <code class="language-plaintext highlighter-rouge">SIGHUP</code>（挂断信号）。</li>
  <li><strong>结果</strong>：Shell 收到信号后会立即终止，并带着它所有的子进程一起退出。</li>
  <li><strong>痛点</strong>：如果你正在服务器上训练一个需要跑 3 天的深度学习模型，或者正在编译一个巨大的项目，只要你的网络断开一秒钟，或者你不小心关掉了窗口，前功尽弃。</li>
</ul>

<h3 id="3-需求推演">3. 需求推演</h3>

<p>为了解决这个问题，我们需要在 “显示界面（你的屏幕/SSH连接）” 和 “后台进程（Shell 及你的任务）” 之间，插入一个<strong>中间层</strong>。</p>

<p>这个中间层需要具备两个核心能力：</p>

<ol>
  <li><strong>进程解耦（持久化）</strong>：它可以接管所有的 Shell 进程，并且无视网络断开和窗口关闭，永远在后台运行。</li>
  <li><strong>多路复用（分屏）</strong>：既然它接管了终端，那它最好能把一个屏幕切分成多个区域，让我们可以同时看代码、看日志、跑服务，而不需要开一堆 SSH 连接。</li>
</ol>

<p>这就是 <strong>tmux</strong> 诞生的原因。</p>

<h2 id="tmux-是什么核心架构">tmux 是什么？（核心架构）</h2>

<p><strong>tmux (Terminal Multiplexer，终端复用器)</strong> 就是我们刚刚推导出的那个“中间层”。</p>

<p>它的底层本质是一个 <strong>C/S 架构（Client-Server）</strong>：</p>

<ul>
  <li><strong>Server（服务端）</strong>：当你第一次运行 tmux 时，它会在后台默默启动一个 Server。这个 Server 负责管理你所有的运行环境。只要服务器不重启，它就一直活着。</li>
  <li><strong>Client（客户端）</strong>：当你在终端敲下 <code class="language-plaintext highlighter-rouge">tmux</code> 时，你实际上是启动了一个 Client，连接到了后台的 Server。把 Client 关掉，只是断开了 “显示器”，后台的 Server 和进程依旧在运转。</li>
</ul>

<h3 id="tmux-的层级逻辑非常重要">tmux 的层级逻辑（非常重要）</h3>

<p>为了方便管理，tmux 将你的工作区抽象为三个层级：</p>

<ol>
  <li><strong>Session（会话）</strong>：最大的单位。一个 Session 通常代表一个 “项目”。比如你可以建一个名叫 <code class="language-plaintext highlighter-rouge">work</code> 的 Session，里面跑你的代码；再建一个名叫 <code class="language-plaintext highlighter-rouge">download</code> 的 Session，里面专门用来挂机下载。</li>
  <li><strong>Window（窗口）</strong>：Session 里面的 “标签页”。类似于浏览器的 Tab，一个屏幕只能显示一个 Window。</li>
  <li><strong>Pane（窗格）</strong>：Window 里的 “分屏”。你可以把一个 Window 切分成左边看代码、右边看日志。</li>
</ol>

<p><img class="mermaid" src="https://mermaid.-TkSBXaW5kb3cgMTxici8-KOe8lui-keS7o-eggSlcIl1cblNlc3Npb24xIC0tPiBXaW4yW1wi8J-TkSBXaW5kb3cgMjxici8-KOi_kOihjOa1i-ivlSlcIl1cbiUlLVxuU2Vzc2lvbjIgLS0-IFdpbjNbXCLwn5ORIFdpbmRvdyAzPGJyLz4o5LiL6L29566h55CGKVwiXVxuJSUtXG5XaW4xIC0tPiBQYW5lMVtcIvCflrzvuI8gUGFuZSAxLjFcIl1cbldpbjEgLS0-IFBhbmUyW1wi8J-WvO-4jyBQYW5lIDEuMlwiXVxuV2luMSAtLT4gUGFuZTNbXCLwn5a877iPIFBhbmUgMS4zXCJdXG4lJS1cbldpbjIgLS0-IFBhbmU0W1wi8J-WvO-4jyBQYW5lIDIuMVwiXSIsIm1lcm1haWQiOm51bGx9" /></p>

<h2 id="tmux-的使用方法">tmux 的使用方法</h2>

<p>理解了上面的逻辑，使用 tmux 就只是死记硬背几个命令而已了。</p>

<h3 id="1-前置概念prefix-key前缀键">1. 前置概念：Prefix Key（前缀键）</h3>

<p>在 tmux 中，所有的快捷键都必须配合一个<strong>前缀键</strong>使用，默认是 <code class="language-plaintext highlighter-rouge">Ctrl + b</code>。</p>

<p><strong>操作逻辑：</strong> 先同时按下 <code class="language-plaintext highlighter-rouge">Ctrl</code> 和 <code class="language-plaintext highlighter-rouge">b</code>，<strong>松开双手</strong>，然后再按下一个键。</p>

<p class="notice--info"><em>(下文中我将 <code class="language-plaintext highlighter-rouge">Ctrl + b</code> 简称为 <code class="language-plaintext highlighter-rouge">Prefix</code>)</em></p>

<h3 id="2-session-管理在普通的-linux-终端打字">2. Session 管理（在普通的 Linux 终端打字）</h3>

<p>这是你和 Server 交互的命令：</p>

<ul>
  <li><strong>新建会话</strong>：<code class="language-plaintext highlighter-rouge">tmux new -s myproject</code> （新建一个名为 myproject 的会话并进入）</li>
  <li><strong>脱离会话（后台运行）：</strong> <code class="language-plaintext highlighter-rouge">Prefix</code> 然后按 <code class="language-plaintext highlighter-rouge">d</code> (detach)。你的界面会退回普通终端，但里面的程序还在跑！</li>
  <li><strong>查看现有会话</strong>：<code class="language-plaintext highlighter-rouge">tmux ls</code></li>
  <li><strong>重新连接会话</strong>：<code class="language-plaintext highlighter-rouge">tmux attach -t myproject</code> (attach)</li>
  <li><strong>彻底杀死会话</strong>：<code class="language-plaintext highlighter-rouge">tmux kill-session -t myproject</code></li>
</ul>

<h3 id="3-window-管理在-tmux-内部使用快捷键">3. Window 管理（在 tmux 内部使用快捷键）</h3>

<ul>
  <li><strong>新建窗口</strong>：<code class="language-plaintext highlighter-rouge">Prefix</code> 然后按 <code class="language-plaintext highlighter-rouge">c</code> (create)</li>
  <li><strong>切换窗口</strong>：<code class="language-plaintext highlighter-rouge">Prefix</code> 然后按 <code class="language-plaintext highlighter-rouge">n</code> (next，下一个)
    <ul>
      <li><code class="language-plaintext highlighter-rouge">Prefix</code> 然后按 <code class="language-plaintext highlighter-rouge">p</code> (previous，上一个)</li>
      <li><code class="language-plaintext highlighter-rouge">Prefix</code> 然后按 <code class="language-plaintext highlighter-rouge">数字键</code> (直接跳转到指定窗口)</li>
    </ul>
  </li>
  <li><strong>关闭当前窗口</strong>：<code class="language-plaintext highlighter-rouge">Prefix</code> 然后按 <code class="language-plaintext highlighter-rouge">&amp;</code> (会弹出确认提示)</li>
</ul>

<h3 id="4-pane-管理在-tmux-内部使用快捷键">4. Pane 管理（在 tmux 内部使用快捷键）</h3>

<p>这是日常写代码最常用的分屏功能：</p>

<ul>
  <li><strong>左右分屏</strong>：<code class="language-plaintext highlighter-rouge">Prefix</code> 然后按 <code class="language-plaintext highlighter-rouge">%</code></li>
  <li><strong>上下分屏</strong>：<code class="language-plaintext highlighter-rouge">Prefix</code> 然后按 <code class="language-plaintext highlighter-rouge">"</code> (双引号)</li>
  <li><strong>在分屏间移动光标</strong>：<code class="language-plaintext highlighter-rouge">Prefix</code> 然后按 <code class="language-plaintext highlighter-rouge">方向键</code> (上下左右)</li>
  <li><strong>全屏/取消全屏当前分屏</strong>：<code class="language-plaintext highlighter-rouge">Prefix</code> 然后按 <code class="language-plaintext highlighter-rouge">z</code> (zoom，非常实用，看长报错信息时按一下放大，看完再按一下缩小)</li>
  <li><strong>关闭当前分屏</strong>：<code class="language-plaintext highlighter-rouge">Prefix</code> 然后按 <code class="language-plaintext highlighter-rouge">x</code> 或者直接输入 <code class="language-plaintext highlighter-rouge">exit</code></li>
</ul>

<h2 id="进阶让-tmux-变得更好用">进阶：让 tmux 变得更好用</h2>

<p>可以参考开源项目 <a href="https://github.com/gpakosz/.tmux">gpakosz/.tmux</a>，这里仅列举出开源项目中使用的快捷键绑定。</p>

<h3 id="格式说明">格式说明</h3>

<ul>
  <li><code class="language-plaintext highlighter-rouge">&lt;prefix&gt;</code> 表示需要按下 <kbd>Ctrl</kbd> + <kbd>a</kbd> 或者 <kbd>Ctrl</kbd> + <kbd>b</kbd>。</li>
  <li><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; c</code> 表示需要按下 <kbd>Ctrl</kbd> + <kbd>a</kbd> 或者 <kbd>Ctrl</kbd> + <kbd>b</kbd> 加上 <kbd>c</kbd>。</li>
  <li><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; C-c</code> 表示需要按下 <kbd>Ctrl</kbd> + <kbd>a</kbd> 或者 <kbd>Ctrl</kbd> + <kbd>b</kbd> 加上 <kbd>Ctrl</kbd> + <kbd>c</kbd>。</li>
</ul>

<h3 id="快捷键分类说明">快捷键分类说明</h3>

<p>下表按<strong>使用频率和优先级</strong>分组，建议按顺序学习：</p>

<ul>
  <li><strong>📌 必学</strong>：日常工作最核心的 5 个操作，学会了就能满足基本需求</li>
  <li><strong>⭐ 常用</strong>：提升工作效率的 15 个快捷键，逐步熟悉即可</li>
  <li><strong>🔧 进阶</strong>：高级功能和特殊场景，按需学习</li>
</ul>

<h3 id="快捷键速查表">快捷键速查表</h3>

<h4 id="-会话与窗口基础">📌 会话与窗口基础</h4>

<table>
  <thead>
    <tr>
      <th>快捷键</th>
      <th>功能</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">tmux new -s myproject</code></td>
      <td>**新建会话** myproject 并进入（普通终端执行）</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">tmux ls</code></td>
      <td>列出所有会话（普通终端执行）</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">tmux attach -t myproject</code></td>
      <td>重新连接会话（普通终端执行）</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; d</code></td>
      <td>**脱离会话**，回到普通终端，后台保持运行</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; c</code></td>
      <td>**新建窗口**</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; n</code></td>
      <td>切换到 **下一个** 窗口</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; p</code></td>
      <td>切换到 **上一个** 窗口</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; 数字</code></td>
      <td>直接切换到指定窗口</td>
    </tr>
  </tbody>
</table>

<h4 id="-分屏基础">📌 分屏基础</h4>

<table>
  <thead>
    <tr>
      <th>快捷键</th>
      <th>功能</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; %</code></td>
      <td>**左右分屏**</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; "</code></td>
      <td>**上下分屏**（双引号）</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; 方向键</code></td>
      <td>在分屏间 **移动光标**</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; z</code></td>
      <td>**放大/缩小** 当前分屏</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; x</code></td>
      <td>**关闭** 当前分屏</td>
    </tr>
  </tbody>
</table>

<h4 id="-常用操作">⭐ 常用操作</h4>

<table>
  <thead>
    <tr>
      <th>快捷键</th>
      <th>功能</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; e</code></td>
      <td>打开 .local 配置文件（使用 $VISUAL 或 $EDITOR 变量定义的编辑器，默认 vim）</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; r</code></td>
      <td>重新加载配置</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; C-c</code></td>
      <td>新建会话</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; C-f</code></td>
      <td>按名称切换会话</td>
    </tr>
  </tbody>
</table>

<h4 id="-窗口与分屏导航">⭐ 窗口与分屏导航</h4>

<table>
  <thead>
    <tr>
      <th>快捷键</th>
      <th>功能</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">C-l</code></td>
      <td>清空屏幕和 tmux 历史记录</td>
    </tr>
  </tbody>
</table>

<table>
  <thead>
    <tr>
      <th>快捷键</th>
      <th>功能</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; C-h</code></td>
      <td>导航到上一个窗口</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; C-l</code></td>
      <td>导航到下一个窗口</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; Tab</code></td>
      <td>切换到最后活跃的窗口</td>
    </tr>
  </tbody>
</table>

<table>
  <thead>
    <tr>
      <th>快捷键</th>
      <th>功能</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; -</code></td>
      <td>垂直分屏</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; _</code></td>
      <td>水平分屏</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; h</code></td>
      <td>向左导航窗格（Vim 风格）</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; j</code></td>
      <td>向下导航窗格（Vim 风格）</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; k</code></td>
      <td>向上导航窗格（Vim 风格）</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; l</code></td>
      <td>向右导航窗格（Vim 风格）</td>
    </tr>
  </tbody>
</table>

<h4 id="-进阶操作---窗格调整与交换">🔧 进阶操作 - 窗格调整与交换</h4>

<table>
  <thead>
    <tr>
      <th>快捷键</th>
      <th>功能</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; H</code></td>
      <td>向左调整窗格大小</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; J</code></td>
      <td>向下调整窗格大小</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; K</code></td>
      <td>向上调整窗格大小</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; L</code></td>
      <td>向右调整窗格大小</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; &lt;</code></td>
      <td>与左边的窗格交换</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; &gt;</code></td>
      <td>与右边的窗格交换</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; +</code></td>
      <td>最大化当前窗格到新窗口</td>
    </tr>
  </tbody>
</table>

<h4 id="-进阶操作---鼠标与复制粘贴">🔧 进阶操作 - 鼠标与复制粘贴</h4>

<table>
  <thead>
    <tr>
      <th>快捷键</th>
      <th>功能</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; m</code></td>
      <td>切换鼠标模式（开启/关闭）</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; Enter</code></td>
      <td>进入复制模式</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; b</code></td>
      <td>查看粘贴缓冲区列表</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; p</code></td>
      <td>从顶部粘贴缓冲区粘贴</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; P</code></td>
      <td>选择粘贴缓冲区进行粘贴</td>
    </tr>
  </tbody>
</table>

<h4 id="-进阶操作---工具集成">🔧 进阶操作 - 工具集成</h4>

<table>
  <thead>
    <tr>
      <th>快捷键</th>
      <th>功能</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; U</code></td>
      <td>启动 Urlscan（如果可用）或 Urlview</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">&lt;prefix&gt; F</code></td>
      <td>启动 Facebook PathPicker（如果可用）</td>
    </tr>
  </tbody>
</table>

<h3 id="-进阶操作---copy-mode-vi-绑定">🔧 进阶操作 - Copy-mode-vi 绑定</h3>

<p>配置文件中的 <code class="language-plaintext highlighter-rouge">copy-mode-vi</code> 设定与 Vim 配置相匹配，以下为复制模式下的快捷键：</p>

<table>
  <thead>
    <tr>
      <th>快捷键</th>
      <th>功能</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">v</code></td>
      <td>开始选择 / 可视模式</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">C-v</code></td>
      <td>在块状可视模式和可视模式之间切换</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">H</code></td>
      <td>跳转到行首</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">L</code></td>
      <td>跳转到行尾</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">y</code></td>
      <td>复制选择内容到顶部粘贴缓冲区</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Escape</code></td>
      <td>取消当前操作</td>
    </tr>
  </tbody>
</table>

<p class="notice--info"><strong>提示</strong>：你也可以通过在 .local 自定义配置文件中设置 <code class="language-plaintext highlighter-rouge">tmux_conf_preserve_stock_bindings</code> 变量为 true，来保留 tmux 的原始按键绑定。</p>

<h2 id="总结">总结</h2>

<p>掌握 tmux 的关键不在于背熟所有的快捷键，而是在脑海中建立起 <strong>Server -&gt; Session -&gt; Window -&gt; Pane</strong> 的空间模型，以及理解它<strong>将显示与进程解耦</strong>的核心价值。习惯它之后，你会发现它是服务器端开发的终极神器。</p>]]></content><author><name>bfmhno3</name></author><category term="Note" /><category term="Linux" /><summary type="html"><![CDATA[为什么我们需要 tmux？ 1. 终端的物理与逻辑基础 在计算机世界里，我们通过 “终端（Terminal）” 与 “Shell（如 bash, zsh）” 进行交互。 当你打开一个终端窗口，或者通过 SSH 连接到远程服务器时，操作系统为你分配了一个 TTY（伪终端），并在这个 TTY 上启动了一个 Shell 进程。你在终端里运行的所有程序（跑脚本、开服务器、编辑代码），都是这个 Shell 的子进程。 2. 致命的耦合（痛点所在） 这就产生了一个致命的 “生命周期耦合” 问题： 规则：当你的终端窗口关闭，或者由于网络波动导致 SSH 连接断开时，系统会向该 TTY 上的 Shell 发送一个 SIGHUP（挂断信号）。 结果：Shell 收到信号后会立即终止，并带着它所有的子进程一起退出。 痛点：如果你正在服务器上训练一个需要跑 3 天的深度学习模型，或者正在编译一个巨大的项目，只要你的网络断开一秒钟，或者你不小心关掉了窗口，前功尽弃。 3. 需求推演 为了解决这个问题，我们需要在 “显示界面（你的屏幕/SSH连接）” 和 “后台进程（Shell 及你的任务）” 之间，插入一个中间层。 这个中间层需要具备两个核心能力： 进程解耦（持久化）：它可以接管所有的 Shell 进程，并且无视网络断开和窗口关闭，永远在后台运行。 多路复用（分屏）：既然它接管了终端，那它最好能把一个屏幕切分成多个区域，让我们可以同时看代码、看日志、跑服务，而不需要开一堆 SSH 连接。 这就是 tmux 诞生的原因。 tmux 是什么？（核心架构） tmux (Terminal Multiplexer，终端复用器) 就是我们刚刚推导出的那个“中间层”。 它的底层本质是一个 C/S 架构（Client-Server）： Server（服务端）：当你第一次运行 tmux 时，它会在后台默默启动一个 Server。这个 Server 负责管理你所有的运行环境。只要服务器不重启，它就一直活着。 Client（客户端）：当你在终端敲下 tmux 时，你实际上是启动了一个 Client，连接到了后台的 Server。把 Client 关掉，只是断开了 “显示器”，后台的 Server 和进程依旧在运转。 tmux 的层级逻辑（非常重要） 为了方便管理，tmux 将你的工作区抽象为三个层级： Session（会话）：最大的单位。一个 Session 通常代表一个 “项目”。比如你可以建一个名叫 work 的 Session，里面跑你的代码；再建一个名叫 download 的 Session，里面专门用来挂机下载。 Window（窗口）：Session 里面的 “标签页”。类似于浏览器的 Tab，一个屏幕只能显示一个 Window。 Pane（窗格）：Window 里的 “分屏”。你可以把一个 Window 切分成左边看代码、右边看日志。 tmux 的使用方法 理解了上面的逻辑，使用 tmux 就只是死记硬背几个命令而已了。 1. 前置概念：Prefix Key（前缀键） 在 tmux 中，所有的快捷键都必须配合一个前缀键使用，默认是 Ctrl + b。 操作逻辑： 先同时按下 Ctrl 和 b，松开双手，然后再按下一个键。 (下文中我将 Ctrl + b 简称为 Prefix) 2. Session 管理（在普通的 Linux 终端打字） 这是你和 Server 交互的命令： 新建会话：tmux new -s myproject （新建一个名为 myproject 的会话并进入） 脱离会话（后台运行）： Prefix 然后按 d (detach)。你的界面会退回普通终端，但里面的程序还在跑！ 查看现有会话：tmux ls 重新连接会话：tmux attach -t myproject (attach) 彻底杀死会话：tmux kill-session -t myproject 3. Window 管理（在 tmux 内部使用快捷键） 新建窗口：Prefix 然后按 c (create) 切换窗口：Prefix 然后按 n (next，下一个) Prefix 然后按 p (previous，上一个) Prefix 然后按 数字键 (直接跳转到指定窗口) 关闭当前窗口：Prefix 然后按 &amp; (会弹出确认提示) 4. Pane 管理（在 tmux 内部使用快捷键） 这是日常写代码最常用的分屏功能： 左右分屏：Prefix 然后按 % 上下分屏：Prefix 然后按 " (双引号) 在分屏间移动光标：Prefix 然后按 方向键 (上下左右) 全屏/取消全屏当前分屏：Prefix 然后按 z (zoom，非常实用，看长报错信息时按一下放大，看完再按一下缩小) 关闭当前分屏：Prefix 然后按 x 或者直接输入 exit 进阶：让 tmux 变得更好用 可以参考开源项目 gpakosz/.tmux，这里仅列举出开源项目中使用的快捷键绑定。 格式说明 &lt;prefix&gt; 表示需要按下 Ctrl + a 或者 Ctrl + b。 &lt;prefix&gt; c 表示需要按下 Ctrl + a 或者 Ctrl + b 加上 c。 &lt;prefix&gt; C-c 表示需要按下 Ctrl + a 或者 Ctrl + b 加上 Ctrl + c。 快捷键分类说明 下表按使用频率和优先级分组，建议按顺序学习： 📌 必学：日常工作最核心的 5 个操作，学会了就能满足基本需求 ⭐ 常用：提升工作效率的 15 个快捷键，逐步熟悉即可 🔧 进阶：高级功能和特殊场景，按需学习 快捷键速查表 📌 会话与窗口基础 快捷键 功能 tmux new -s myproject **新建会话** myproject 并进入（普通终端执行） tmux ls 列出所有会话（普通终端执行） tmux attach -t myproject 重新连接会话（普通终端执行） &lt;prefix&gt; d **脱离会话**，回到普通终端，后台保持运行 &lt;prefix&gt; c **新建窗口** &lt;prefix&gt; n 切换到 **下一个** 窗口 &lt;prefix&gt; p 切换到 **上一个** 窗口 &lt;prefix&gt; 数字 直接切换到指定窗口 📌 分屏基础 快捷键 功能 &lt;prefix&gt; % **左右分屏** &lt;prefix&gt; " **上下分屏**（双引号） &lt;prefix&gt; 方向键 在分屏间 **移动光标** &lt;prefix&gt; z **放大/缩小** 当前分屏 &lt;prefix&gt; x **关闭** 当前分屏 ⭐ 常用操作 快捷键 功能 &lt;prefix&gt; e 打开 .local 配置文件（使用 $VISUAL 或 $EDITOR 变量定义的编辑器，默认 vim） &lt;prefix&gt; r 重新加载配置 &lt;prefix&gt; C-c 新建会话 &lt;prefix&gt; C-f 按名称切换会话 ⭐ 窗口与分屏导航 快捷键 功能 C-l 清空屏幕和 tmux 历史记录 快捷键 功能 &lt;prefix&gt; C-h 导航到上一个窗口 &lt;prefix&gt; C-l 导航到下一个窗口 &lt;prefix&gt; Tab 切换到最后活跃的窗口 快捷键 功能 &lt;prefix&gt; - 垂直分屏 &lt;prefix&gt; _ 水平分屏 &lt;prefix&gt; h 向左导航窗格（Vim 风格） &lt;prefix&gt; j 向下导航窗格（Vim 风格） &lt;prefix&gt; k 向上导航窗格（Vim 风格） &lt;prefix&gt; l 向右导航窗格（Vim 风格） 🔧 进阶操作 - 窗格调整与交换 快捷键 功能 &lt;prefix&gt; H 向左调整窗格大小 &lt;prefix&gt; J 向下调整窗格大小 &lt;prefix&gt; K 向上调整窗格大小 &lt;prefix&gt; L 向右调整窗格大小 &lt;prefix&gt; &lt; 与左边的窗格交换 &lt;prefix&gt; &gt; 与右边的窗格交换 &lt;prefix&gt; + 最大化当前窗格到新窗口 🔧 进阶操作 - 鼠标与复制粘贴 快捷键 功能 &lt;prefix&gt; m 切换鼠标模式（开启/关闭） &lt;prefix&gt; Enter 进入复制模式 &lt;prefix&gt; b 查看粘贴缓冲区列表 &lt;prefix&gt; p 从顶部粘贴缓冲区粘贴 &lt;prefix&gt; P 选择粘贴缓冲区进行粘贴 🔧 进阶操作 - 工具集成 快捷键 功能 &lt;prefix&gt; U 启动 Urlscan（如果可用）或 Urlview &lt;prefix&gt; F 启动 Facebook PathPicker（如果可用） 🔧 进阶操作 - Copy-mode-vi 绑定 配置文件中的 copy-mode-vi 设定与 Vim 配置相匹配，以下为复制模式下的快捷键： 快捷键 功能 v 开始选择 / 可视模式 C-v 在块状可视模式和可视模式之间切换 H 跳转到行首 L 跳转到行尾 y 复制选择内容到顶部粘贴缓冲区 Escape 取消当前操作 提示：你也可以通过在 .local 自定义配置文件中设置 tmux_conf_preserve_stock_bindings 变量为 true，来保留 tmux 的原始按键绑定。 总结 掌握 tmux 的关键不在于背熟所有的快捷键，而是在脑海中建立起 Server -&gt; Session -&gt; Window -&gt; Pane 的空间模型，以及理解它将显示与进程解耦的核心价值。习惯它之后，你会发现它是服务器端开发的终极神器。]]></summary></entry><entry><title type="html">从 FHS 到 Linux 运行时目录体系</title><link href="https://bfmhno3.github.io/note/fhs-of-linux/" rel="alternate" type="text/html" title="从 FHS 到 Linux 运行时目录体系" /><published>2026-04-21T07:00:00+00:00</published><updated>2026-04-21T07:00:00+00:00</updated><id>https://bfmhno3.github.io/note/fhs-of-linux</id><content type="html" xml:base="https://bfmhno3.github.io/note/fhs-of-linux/"><![CDATA[<p>要真正理解 Linux 的 <strong>FHS (Filesystem Hierarchy Standard，文件系统层次结构标准)</strong>，不能只靠死记硬背“哪个目录放什么文件”。更有效的办法，是剥开表象，回到操作系统设计的起点，看看在构建一个多用户、网络化、可扩展的系统时，我们究竟面临了哪些根本问题，以及 FHS 又是如何作为一套“解决框架”出现的。</p>

<p>不过，本文并不准备停在 FHS 的条文本身。更准确地说，行文会分成两步：先把 <strong>FHS 作为规范</strong> 讲明白，再顺着这套规范继续往外延伸，看看现代 Linux 是如何在这棵目录树上接入运行时机制、容器隔离、嵌入式约束和内核接口的。也正因为如此，标题与其叫“Linux 的 FHS”，不如更直接一点，叫“从 FHS 到 Linux 运行时目录体系”。</p>

<h2 id="推演起点我们到底要存储什么">推演起点：我们到底要存储什么？</h2>

<p>假设你从零开始设计一个操作系统。系统需要运行，用户需要存数据，程序需要产生日志。如果你把所有东西都塞进一个大文件夹里，系统马上就会面临灾难：</p>

<ol>
  <li><strong>权限混乱</strong>： 普通用户可能会不小心删掉系统的核心启动文件。</li>
  <li><strong>状态混淆</strong>： 你无法区分哪些文件是只读的系统核心，哪些是随时变动的日志，导致备份极其困难。</li>
  <li><strong>共享灾难</strong>： 如果你想在局域网内让多台机器共享一套程序，你无法分离 “机器特有的配置” 和 “可以共享的程序本体”。</li>
</ol>

<p>因此，FHS 的本质，是基于<strong>数据属性的二维分类矩阵</strong>。Linux 的文件系统布局，本质上就是围绕着这两个核心维度展开的：</p>

<p><strong>维度一：是否可变 (Static vs. Variable)</strong></p>

<ul>
  <li><strong>静态数据 (Static)：</strong> 系统运行期间不需要修改的文件（如可执行程序、库文件、默认系统配置）。</li>
  <li><strong>可变数据 (Variable)：</strong> 系统运行期间会不断变化的文件（如日志、用户数据、临时文件、缓存）。</li>
</ul>

<p><strong>维度二：是否可共享 (Shareable vs. Unshareable)</strong></p>

<ul>
  <li><strong>可共享 (Shareable)：</strong> 可以在多台主机之间共享的数据（如二进制应用程序、用户主目录）。</li>
  <li><strong>不可共享 (Unshareable)：</strong> 特定于某台主机的本地数据（如本机的网络配置、设备节点、本机的运行状态）。</li>
</ul>

<p>通过这个 2x2 的矩阵，FHS 建立了一个严密的逻辑体系。</p>

<h2 id="目录拆解">目录拆解</h2>

<p>有了上面这两个维度，再回头看 Linux 根目录 <code class="language-plaintext highlighter-rouge">/</code> 下的各个分支，很多事情就会变得顺理成章。</p>

<h3 id="1-系统的-地基-与-骨架不可共享--静态">1. 系统的 “地基” 与 “骨架”（不可共享 / 静态）</h3>

<p>系统要能够开机，或者在崩溃时能够进入单用户模式进行急救，它需要一个自给自足的最基础环境。</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">/boot</code> (启动引导)：</strong> 系统启动的第一步。这里存放着内核（vmlinuz）和引导程序（GRUB）。它是极其特殊的不可共享静态数据。</li>
  <li><strong><code class="language-plaintext highlighter-rouge">/bin</code> (Basic Binaries)：</strong> 系统最核心、最基础的用户级命令（如 <code class="language-plaintext highlighter-rouge">ls</code>, <code class="language-plaintext highlighter-rouge">cp</code>, <code class="language-plaintext highlighter-rouge">cat</code>）。<strong>原则：</strong> 即使所有其他分区（如 <code class="language-plaintext highlighter-rouge">/usr</code>）都损坏或未挂载，这里的命令也必须能用，以保证管理员能修复系统。</li>
  <li><strong><code class="language-plaintext highlighter-rouge">/sbin</code> (System Binaries)：</strong> 系统级的核心命令（如 <code class="language-plaintext highlighter-rouge">fdisk</code>, <code class="language-plaintext highlighter-rouge">reboot</code>, <code class="language-plaintext highlighter-rouge">ip</code>）。与 <code class="language-plaintext highlighter-rouge">/bin</code> 的区别在于，这里的工具主要是给 <strong>Root（超级用户）</strong> 用的。</li>
  <li><strong><code class="language-plaintext highlighter-rouge">/etc</code> (Host-specific system configuration)：</strong> 按照 FHS 的官方说法，这里是 <strong>host-specific system configuration</strong>，也就是“特定于本机的系统配置”。你可以把它理解为本机的“规则字典”。<strong>原则：</strong> 这里面应该全是配置，而不应该放可执行二进制文件。</li>
  <li><strong><code class="language-plaintext highlighter-rouge">/lib</code> &amp; <code class="language-plaintext highlighter-rouge">/lib64</code> (Libraries)：</strong> <code class="language-plaintext highlighter-rouge">/bin</code> 和 <code class="language-plaintext highlighter-rouge">/sbin</code> 中的基础命令在运行时需要调用的共享库（C语言库等），以及内核模块。相当于程序的“公共零件库”。</li>
</ul>

<h3 id="2-一切皆文件-的虚拟抽象层不可共享--实时动态">2. “一切皆文件” 的虚拟抽象层（不可共享 / 实时动态）</h3>

<p>Linux 继承了 UNIX 最伟大的哲学：“一切皆文件”。硬件设备和系统运行时的内存状态，都被抽象成了文件，方便统一管理。这些目录在硬盘上往往是不占空间的（存在于内存中）。</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">/dev</code> (Devices)：</strong> 所有的硬件设备（硬盘、键盘、鼠标、终端）都在这里有一个映射文件。对这些文件的读写，就是对硬件的读写。</li>
  <li><strong><code class="language-plaintext highlighter-rouge">/proc</code> (Processes)：</strong> 内核的“监视器”。里面全是虚拟文件，记录了当前运行的进程信息和内核状态。比如查 CPU 信息就是去读 <code class="language-plaintext highlighter-rouge">/proc/cpuinfo</code>。</li>
  <li><strong><code class="language-plaintext highlighter-rouge">/sys</code> (Sysfs)：</strong> 对 <code class="language-plaintext highlighter-rouge">/proc</code> 的现代化补充，更结构化地展示内核、设备驱动和总线的信息。</li>
</ul>

<h3 id="3-系统的-扩展应用程序可共享--静态">3. 系统的 “扩展应用程序”（可共享 / 静态）</h3>

<p>当系统完成了基础启动，我们需要安装大量的第三方软件（办公软件、开发工具等）。这些软件体积庞大，且在不同主机间是<strong>可以共享</strong>的。</p>

<h4 id="usr"><code class="language-plaintext highlighter-rouge">/usr</code></h4>

<p><strong><code class="language-plaintext highlighter-rouge">/usr</code>：</strong> FHS 对它的定位，本质上是系统中 <strong>shareable, read-only data</strong> 的主要存放地。你可以把它理解为系统的“第二层级”：基础系统先靠 <code class="language-plaintext highlighter-rouge">/bin</code>、<code class="language-plaintext highlighter-rouge">/sbin</code>、<code class="language-plaintext highlighter-rouge">/lib</code> 站起来，而更完整、更庞大的用户空间资源则放在 <code class="language-plaintext highlighter-rouge">/usr</code> 下。</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">/usr/bin</code> 和 <code class="language-plaintext highlighter-rouge">/usr/sbin</code>：绝大多数非核心的用户和管理员命令。</li>
  <li><code class="language-plaintext highlighter-rouge">/usr/lib</code>：非核心的库文件。</li>
  <li><code class="language-plaintext highlighter-rouge">/usr/local</code>：系统管理员本地手动编译安装的软件存放地（不受包管理器如 apt/yum 管辖，保证了系统的纯净）。</li>
</ul>

<h3 id="4-系统的新陈代谢与状态记录不可共享--可变">4. 系统的“新陈代谢”与状态记录（不可共享 / 可变）</h3>

<p>系统在运行过程中，会产生大量的过程数据和历史记录。</p>

<h4 id="var"><code class="language-plaintext highlighter-rouge">/var</code></h4>

<p><strong><code class="language-plaintext highlighter-rouge">/var</code> (Variable)：</strong> 它是系统的“消化系统”。<strong>原则：</strong> 任何大小会随时间不断变化的数据，都必须放在这里，绝对不能放在只读的 <code class="language-plaintext highlighter-rouge">/usr</code> 下。</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">/var/log</code>：系统和程序的日志（排错必备）。</li>
  <li><code class="language-plaintext highlighter-rouge">/var/cache</code>：应用程序缓存。</li>
  <li><code class="language-plaintext highlighter-rouge">/var/spool</code>：队列数据（如打印队列、未发送的邮件）。</li>
</ul>

<h3 id="5-用户与临时数据可变">5. 用户与临时数据（可变）</h3>

<p>隔离系统和用户，是保障系统安全的基石。</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">/home</code>：</strong> 普通用户的“私人领地”。无论系统怎么重装或升级，只要单独挂载 <code class="language-plaintext highlighter-rouge">/home</code> 分区，用户的数据就不会丢失。</li>
  <li><strong><code class="language-plaintext highlighter-rouge">/root</code>：</strong> 超级用户 root 的主目录。为什么不放在 <code class="language-plaintext highlighter-rouge">/home/root</code>？因为如果 <code class="language-plaintext highlighter-rouge">/home</code> 所在的磁盘挂载失败，root 仍然需要登录系统进行修复。</li>
  <li><strong><code class="language-plaintext highlighter-rouge">/tmp</code> (Temporary)：</strong> 系统的“草稿纸”。任何程序或用户都可以把临时文件扔在这里。系统重启时（或定期），这里的数据会被自动清空。</li>
</ul>

<h3 id="6-额外与外载资源">6. 额外与外载资源</h3>
<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">/opt</code> (Optional)：</strong> 附加的应用程序软件包。通常用于大型闭源软件或第三方商业软件（如 Google Chrome, Oracle 数据库），它们喜欢把所有文件（二进制、库、配置）打包放在自己的一个独立目录下，互不干扰。</li>
  <li><strong><code class="language-plaintext highlighter-rouge">/mnt</code> (Mount) / <code class="language-plaintext highlighter-rouge">/media</code>：</strong> 挂载点。<code class="language-plaintext highlighter-rouge">/mnt</code> 通常用于管理员手动临时挂载额外的文件系统，而 <code class="language-plaintext highlighter-rouge">/media</code> 通常用于系统自动挂载的可移动介质（如 U盘、光驱）。</li>
</ul>

<h2 id="现代-linux-的变化">现代 Linux 的变化</h2>

<p>随着技术的发展，早期的某些约束（比如硬盘太小，不得不把核心 <code class="language-plaintext highlighter-rouge">/bin</code> 放在一个小根分区，庞大的 <code class="language-plaintext highlighter-rouge">/usr/bin</code> 放在另一个大分区）已经不复存在。</p>

<p>现在的 Linux 初始化流程（通过 <code class="language-plaintext highlighter-rouge">initramfs</code>）可以在挂载真正的根目录之前，就把需要的东西加载进内存。因此，现代发行版（如 Arch, Fedora, Ubuntu）正在推行 <strong>“usr-merge”（<code class="language-plaintext highlighter-rouge">/usr</code> 合并）</strong>：</p>

<p class="notice--info">现在的 <code class="language-plaintext highlighter-rouge">/bin</code>, <code class="language-plaintext highlighter-rouge">/sbin</code>, <code class="language-plaintext highlighter-rouge">/lib</code> 往往只是指向 <code class="language-plaintext highlighter-rouge">/usr/bin</code>, <code class="language-plaintext highlighter-rouge">/usr/sbin</code>, <code class="language-plaintext highlighter-rouge">/usr/lib</code> 的<strong>软链接（Symlinks）</strong>。</p>

<p>这并不是背离了 FHS，而是在<strong>物理条件改变后</strong>的自然进化：既然底层硬件不再受限，那么在逻辑上将 “系统核心二进制” 和 “系统扩展二进制” 统一管理（全部归入静态可共享的 <code class="language-plaintext highlighter-rouge">/usr</code>），能让系统结构更加干净利落。</p>

<h2 id="补全-fhs-拼图从系统开发和编译链路视角再看一遍">补全 FHS 拼图：从系统开发和编译链路视角再看一遍</h2>

<p>前面的重点，在于搭建 FHS 背后“可变/静态”与“共享/不可共享”的底层逻辑框架。顺着这条主线继续往下推，会发现还有一些对开发者，特别是做 C/C++ 编译、驱动、工具链和底层系统开发的人来说极其关键的目录，值得单独补齐。</p>

<h3 id="1-深度拆解-usr-子目录开发者的主战场">1. 深度拆解 <code class="language-plaintext highlighter-rouge">/usr</code> 子目录（开发者的主战场）</h3>

<p><code class="language-plaintext highlighter-rouge">/usr</code> (Universal System Resources) 是静态且可共享的资源库。除了前面提到的 <code class="language-plaintext highlighter-rouge">bin</code> 和 <code class="language-plaintext highlighter-rouge">lib</code>，它内部的细分目录是 C/C++ 工具链和系统构建的核心。</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">/usr/include</code> (C/C++ 标准头文件)</strong>
    <ul>
      <li><strong>属性：</strong> 静态，可共享。</li>
      <li><strong>本质：</strong> 它是系统级 API 的“契约”。当你写 C/C++ 代码并使用 <code class="language-plaintext highlighter-rouge">#include &lt;stdio.h&gt;</code> 或 <code class="language-plaintext highlighter-rouge">#include &lt;linux/i2c.h&gt;</code> 时，GCC/Clang 编译器默认会来这里寻找对应的 <code class="language-plaintext highlighter-rouge">.h</code> 文件。如果你在交叉编译环境（比如为 ARM 架构编译代码）中，你的交叉编译工具链也会有一个结构完全平行的 <code class="language-plaintext highlighter-rouge">include</code> 目录。</li>
    </ul>
  </li>
  <li><strong><code class="language-plaintext highlighter-rouge">/usr/src</code> (源代码)</strong>
    <ul>
      <li><strong>属性：</strong> 静态，可共享。</li>
      <li><strong>本质：</strong> 存放系统级软件的源代码。最常见的是<strong>内核源码头文件（Kernel Headers）</strong>。如果你需要为某个特定的硬件（比如一块自定义的驱动板）编写并编译外部内核模块（Out-of-tree module），你的 Makefile 必须引用这里的内核头文件，以确保你的驱动和当前运行的内核版本 ABI 兼容。</li>
    </ul>
  </li>
  <li><strong><code class="language-plaintext highlighter-rouge">/usr/share</code> (架构无关的共享数据)</strong>
    <ul>
      <li><strong>属性：</strong> 静态，可共享，<strong>且与 CPU 架构无关</strong>。</li>
      <li><strong>本质：</strong> 按照 FHS 的说法，这里存放的是 <strong>architecture-independent data</strong>。无论你是 x86 还是 ARM，这里的数据都是通用的。这包括 <code class="language-plaintext highlighter-rouge">man</code> 帮助文档（<code class="language-plaintext highlighter-rouge">/usr/share/man</code>）、时区数据（<code class="language-plaintext highlighter-rouge">/usr/share/zoneinfo</code>）、以及构建工具的配置文件（例如 CMake 的默认模块通常存放在 <code class="language-plaintext highlighter-rouge">/usr/share/cmake/Modules/</code> 下）。</li>
    </ul>
  </li>
  <li><strong><code class="language-plaintext highlighter-rouge">/usr/libexec</code> (系统内部调用的可执行文件)</strong>
    <ul>
      <li><strong>属性：</strong> 静态，可共享。</li>
      <li><strong>本质：</strong> FHS 对它的表述很直接：<strong>binaries run by other programs</strong>。这里存放的二进制程序<strong>不是给用户在终端里直接敲击运行的</strong>，而是给其他程序调用的后台工具。比如系统的认证模块守护进程、某些网络配置的辅助脚本等。它们没有被放进 <code class="language-plaintext highlighter-rouge">/usr/bin</code>，就是为了防止污染用户的环境变量 <code class="language-plaintext highlighter-rouge">PATH</code>。</li>
    </ul>
  </li>
</ul>

<h3 id="2-现代-linux-的内存文件系统补充">2. 现代 Linux 的内存文件系统补充</h3>

<p>随着 systemd 的普及和系统启动流程的优化，FHS 引入了更高效的运行时目录。</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">/run</code> (运行时状态数据 - 现代 tmpfs)</strong>
    <ul>
      <li><strong>属性：</strong> 实时动态，不可共享，<strong>纯内存存储</strong>。</li>
      <li><strong>本质：</strong> FHS 3.0 对它的定义就是 <strong>run-time variable data</strong>。它记录系统自本次开机以来的状态数据（如守护进程的 PID 文件、Socket 文件等）。在早期的 FHS 中，这些数据放在 <code class="language-plaintext highlighter-rouge">/var/run</code>。但因为 <code class="language-plaintext highlighter-rouge">/var</code> 通常在硬盘上，读写慢且可能在启动早期尚未挂载，所以现代 Linux 创建了 <code class="language-plaintext highlighter-rouge">/run</code> 并将其挂载为 <code class="language-plaintext highlighter-rouge">tmpfs</code>（内存文件系统）。每次重启，这里自然清空。</li>
    </ul>
  </li>
  <li><strong><code class="language-plaintext highlighter-rouge">/dev/shm</code> (POSIX 共享内存)</strong>
    <ul>
      <li><strong>属性：</strong> 实时动态，纯内存存储。</li>
      <li><strong>本质：</strong> 也是一个 <code class="language-plaintext highlighter-rouge">tmpfs</code> 挂载点。它提供了一种极高效率的进程间通信（IPC）机制。如果有两个高实时性要求的进程需要交换海量数据，通过在这里创建内存映射文件进行读写，速度远超 Socket 或管道。</li>
    </ul>
  </li>
</ul>

<h3 id="3-驱动与服务的特定目录">3. 驱动与服务的特定目录</h3>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">/lib/modules</code> (内核模块)</strong>
    <ul>
      <li><strong>属性：</strong> 静态（通常伴随内核更新而更新），不可共享。</li>
      <li><strong>本质：</strong> 虽然归属在 <code class="language-plaintext highlighter-rouge">/lib</code> 下，但必须单独拿出来强调。这里存放的是以 <code class="language-plaintext highlighter-rouge">.ko</code> 结尾的内核驱动模块。当系统通过 <code class="language-plaintext highlighter-rouge">udev</code> 检测到新硬件插入时，<code class="language-plaintext highlighter-rouge">modprobe</code> 工具就是在这个目录（具体路径为 <code class="language-plaintext highlighter-rouge">/lib/modules/$(uname -r)/</code>）下寻找并加载对应的驱动。</li>
    </ul>
  </li>
  <li><strong><code class="language-plaintext highlighter-rouge">/srv</code> (服务数据 Service Data)</strong>
    <ul>
      <li><strong>属性：</strong> 可变或静态（视具体服务而定），不可共享。</li>
      <li><strong>本质：</strong> FHS 的官方说法是 <strong>data for services provided by this system</strong>。也就是说，这里存放的是“由这台机器对外提供的服务所使用的数据”。比如你在这台机器上跑了一个静态博客系统或 Web 服务器，网站的根目录数据（如 HTML、CSS、Markdown 渲染后的文件）就更适合放在 <code class="language-plaintext highlighter-rouge">/srv/www/</code> 或 <code class="language-plaintext highlighter-rouge">/srv/http/</code> 中，而不是混进 <code class="language-plaintext highlighter-rouge">/home</code> 或 <code class="language-plaintext highlighter-rouge">/var</code> 的其他语义里。</li>
    </ul>
  </li>
</ul>

<h3 id="4-灾难恢复机制">4. 灾难恢复机制</h3>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">/lost+found</code> (文件系统孤儿院)</strong>
    <ul>
      <li><strong>属性：</strong> 动态。</li>
      <li><strong>本质：</strong> 这是 Ext 系列（Ext2/3/4）文件系统特有的目录。当系统遭遇突然断电或内核崩溃，导致文件系统损坏时，在下次重启运行 <code class="language-plaintext highlighter-rouge">fsck</code>（文件系统检查）时，那些在底层 inode 存在但失去了目录引用的“无头文件”，就会被抢救并存放在这里，供管理员后续手工分析和恢复。</li>
    </ul>
  </li>
</ul>

<h3 id="全景视图first-principles-矩阵最终版">全景视图（First Principles 矩阵最终版）</h3>

<p>把这些补充的目录重新放回前面的二维分类矩阵里，FHS 的逻辑骨架就会清楚很多。这里要特别说明一点：<strong>下面这张表是为了帮助理解而做的教学化归纳，并不是 FHS 标准原文中逐格给出的官方表格。</strong> 不过，目录本身的定义和归属思路，会尽量贴近 FHS 的官方说法。</p>

<table>
  <thead>
    <tr>
      <th style="text-align: left">数据属性</th>
      <th style="text-align: left">静态 (Static) <br /> *系统运行不改变，可只读挂载*</th>
      <th style="text-align: left">可变 (Variable) <br /> *系统运行持续写入*</th>
      <th style="text-align: left">实时内存抽象 <br /> *不占硬盘，描述状态或提供机制*</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: left">**不可共享 (Unshareable)** <br /> *特定于本机*</td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">/boot</code>, <code class="language-plaintext highlighter-rouge">/etc</code>, <code class="language-plaintext highlighter-rouge">/bin</code>, <code class="language-plaintext highlighter-rouge">/sbin</code>, <code class="language-plaintext highlighter-rouge">/lib</code>, <code class="language-plaintext highlighter-rouge">/lib/modules</code></td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">/var</code>, <code class="language-plaintext highlighter-rouge">/run</code>, <code class="language-plaintext highlighter-rouge">/tmp</code>, <code class="language-plaintext highlighter-rouge">/lost+found</code>, <code class="language-plaintext highlighter-rouge">/srv</code></td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">/dev</code>, <code class="language-plaintext highlighter-rouge">/proc</code>, <code class="language-plaintext highlighter-rouge">/sys</code></td>
    </tr>
    <tr>
      <td style="text-align: left">**可共享 (Shareable)** <br /> *可跨主机复用*</td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">/usr</code>, <code class="language-plaintext highlighter-rouge">/usr/include</code>, <code class="language-plaintext highlighter-rouge">/usr/src</code>, <code class="language-plaintext highlighter-rouge">/usr/share</code>, <code class="language-plaintext highlighter-rouge">/usr/libexec</code>, <code class="language-plaintext highlighter-rouge">/opt</code></td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">/home</code></td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">/dev/shm</code></td>
    </tr>
  </tbody>
</table>

<p>FHS 的设计从来不是为了限制开发者，而是为了在混乱的多任务环境中划定清晰的边界。明白 <code class="language-plaintext highlighter-rouge">/usr/include</code> 提供接口、<code class="language-plaintext highlighter-rouge">/lib</code> 提供实现、<code class="language-plaintext highlighter-rouge">/usr/src</code> 提供底层编译依赖，整个系统的编译与运行逻辑就能彻底串联起来了。</p>

<h2 id="从-fhs-到-linux-运行时目录体系">从 FHS 到 Linux 运行时目录体系</h2>

<p>如果说前半篇讲的是“FHS 这套目录分层规范本身”，那么从这里开始，视角就要再往外扩一层。也就是说，下面讨论的很多目录，已经不只是在回答“标准要求你怎么放文件”，而是在回答另一个问题：<strong>现代 Linux 又是如何把硬件、容器、云实例、嵌入式约束和内核机制继续投影进这棵目录树里的。</strong></p>

<p>换句话说，前半部分偏“规范”，后半部分偏“演化”。两者不能混为一谈，但放在同一篇文章里连起来看，恰好能把 Linux 的目录体系从静态分层一路看到动态运行。</p>

<h3 id="嵌入式与底层硬件开发">嵌入式与底层硬件开发</h3>

<p>标准 FHS 是为通用服务器设计的。当 Linux 运行在没有标准 BIOS/UEFI 的 ARM 开发板、路由器或嵌入式设备上时，文件系统的职责会发生微妙偏移，衍生出一些普通用户很少接触的 “隐藏版图”。</p>

<h4 id="1-硬件描述的真理之源sysfirmwaredevicetree">1. 硬件描述的真理之源：<code class="language-plaintext highlighter-rouge">/sys/firmware/devicetree</code></h4>

<ul>
  <li><strong>底层逻辑：</strong> 与 x86 电脑不同，大部分 ARM 架构的主板（无论是开发板还是路由器）没有一套标准的机制让内核在开机时 “自动发现” 所有硬件（比如哪个引脚连着 LED，哪个 I2C 总线上挂着传感器）。</li>
  <li><strong>文件系统映射：</strong> Linux 引入了设备树（Device Tree, <code class="language-plaintext highlighter-rouge">.dtb</code> 文件）。在系统启动后，内核会将它解析到的底层硬件拓扑结构，原封不动地映射到 <code class="language-plaintext highlighter-rouge">/sys/firmware/devicetree/base</code> 目录下。</li>
  <li><strong>开发意义：</strong> 当你发现自己写的驱动或者配置没有生效时，第一步不是去查代码，而是直接 <code class="language-plaintext highlighter-rouge">cat</code> 这个目录下的节点，看看内核到底有没有正确加载你的硬件描述。这里是硬件状态的 “第一现场”。</li>
</ul>

<h4 id="2-用户空间的硬件操控面板sysclass">2. 用户空间的硬件操控面板：<code class="language-plaintext highlighter-rouge">/sys/class/</code></h4>

<ul>
  <li><strong>底层逻辑：</strong> 按照传统的 UNIX 哲学，操作硬件必须写内核态的驱动程序（<code class="language-plaintext highlighter-rouge">.ko</code>），然后通过 <code class="language-plaintext highlighter-rouge">/dev</code> 下的设备节点进行交互（如 <code class="language-plaintext highlighter-rouge">ioctl</code>）。但这对于简单的硬件控制来说太重了。</li>
  <li><strong>文件系统映射：</strong> Linux 内核提供了一套子系统，将底层硬件的控制权以<strong>纯文本文件</strong>的形式暴露在 <code class="language-plaintext highlighter-rouge">/sys/class/</code> 目录下。
    <ul>
      <li><code class="language-plaintext highlighter-rouge">/sys/class/gpio/</code>：你可以通过向特定文件写入 <code class="language-plaintext highlighter-rouge">1</code> 或 <code class="language-plaintext highlighter-rouge">0</code> 来直接翻转微控制器的 GPIO 引脚电平。</li>
      <li><code class="language-plaintext highlighter-rouge">/sys/class/pwm/</code>：直接通过修改文本文件里的数值来调整 PWM 波的占空比（比如控制风扇转速或 TEC 散热功率）。</li>
      <li><code class="language-plaintext highlighter-rouge">/sys/class/leds/</code>：直接控制板载指示灯的亮灭和触发模式。</li>
    </ul>
  </li>
  <li><strong>开发意义：</strong> 极大地降低了调试成本。你可以直接用 Shell 脚本（如 <code class="language-plaintext highlighter-rouge">echo 1 &gt; /sys/class/gpio/gpio10/value</code>）来做硬件点检，而不需要写一行 C 代码编译驱动。</li>
</ul>

<h4 id="3-交叉编译与根文件系统构建sysroot">3. 交叉编译与根文件系统构建（Sysroot）</h4>

<p>当你为另一台不同架构的机器（比如给 ARM 开发板或路由器）编译系统和软件时，你会在宿主机上看到 FHS 的 “套娃” 现象。</p>

<ul>
  <li><strong>工具链目录（通常在 <code class="language-plaintext highlighter-rouge">/opt</code> 或独立目录下）：</strong> 你可能会看到像 <code class="language-plaintext highlighter-rouge">aarch64-linux-gnu</code> 这样的目录。在这个目录下，会<strong>完整地再现一套</strong> <code class="language-plaintext highlighter-rouge">bin</code>, <code class="language-plaintext highlighter-rouge">lib</code>, <code class="language-plaintext highlighter-rouge">include</code>, <code class="language-plaintext highlighter-rouge">sys</code>。</li>
  <li><strong>底层逻辑：</strong> 这个目录被称为 <strong>Sysroot（系统根）</strong>。编译器在编译代码时，不会去包含宿主机（你当前的电脑）的 <code class="language-plaintext highlighter-rouge">/usr/include</code>，而是去这个 Sysroot 里的 <code class="language-plaintext highlighter-rouge">include</code> 找头文件，去 Sysroot 里的 <code class="language-plaintext highlighter-rouge">lib</code> 找动态库。这是保证“跨平台编译”不出错的物理隔离带。</li>
</ul>

<h4 id="4-深度定制系统的变异以-openwrtimmortalwrt-为例">4. 深度定制系统的变异：以 OpenWrt/ImmortalWrt 为例</h4>

<p>如果你接触过网络路由器的底层系统（如基于 Linux 的 OpenWrt 及其分支 ImmortalWrt），你会发现它们虽然遵循 FHS，但为了适应极其有限的 Flash 存储（比如只有 16MB 或 32MB），在 <code class="language-plaintext highlighter-rouge">/etc</code> 的管理上发展出了一套独特的哲学：</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">/etc/config/</code> (UCI 配置系统)：</strong> 在标准的 Ubuntu 或 Debian 中，各种软件的配置文件散落在 <code class="language-plaintext highlighter-rouge">/etc</code> 下，格式五花八门（json, yaml, ini, 纯文本）。但在路由器系统中，为了统一管理和提供 Web 界面（LuCI），所有的核心配置都被抽象并统一存放在 <code class="language-plaintext highlighter-rouge">/etc/config/</code> 下，采用完全一致的语法。</li>
  <li><strong>底层逻辑：</strong> 通过高度统一的配置范式，降低系统的解析开销，并使得网络状态、防火墙规则、代理转发的联动变得可通过脚本高效调度。</li>
</ul>

<h4 id="5-架构的妥协lib32-lib64-libx86_64-linux-gnu">5. 架构的妥协：<code class="language-plaintext highlighter-rouge">/lib32</code>, <code class="language-plaintext highlighter-rouge">/lib64</code>, <code class="language-plaintext highlighter-rouge">/lib/x86_64-linux-gnu</code></h4>

<ul>
  <li><strong>底层逻辑：</strong> 当计算机从 32 位向 64 位过渡时，面临着兼容性问题：64 位的系统可能需要运行 32 位的旧程序，这就需要系统里同时存在两套共享库。</li>
  <li><strong>文件系统映射：</strong> FHS 规范允许建立 <code class="language-plaintext highlighter-rouge">/lib32</code> 和 <code class="language-plaintext highlighter-rouge">/lib64</code>（或相应的 <code class="language-plaintext highlighter-rouge">/usr/lib32</code>）。但在现代的 Debian/Ubuntu 体系中，引入了更高级的 <strong>Multiarch（多架构）</strong> 机制。它们不再简单粗暴地用 32/64 区分，而是在 <code class="language-plaintext highlighter-rouge">/lib</code> 或 <code class="language-plaintext highlighter-rouge">/usr/lib</code> 下建立了按处理器架构命名的目录，例如 <code class="language-plaintext highlighter-rouge">/lib/x86_64-linux-gnu</code>。</li>
</ul>

<h3 id="云原生与现代架构">云原生与现代架构</h3>

<p>传统的 FHS 是为<strong>“单机、多用户、共享物理资源”</strong>的时代设计的。但在过去的十几年里，云计算、容器化、智能手机和不可变基础设施的崛起，带来了全新的挑战：<strong>隔离性、可重复性、以及极致的安全。</strong> 为了应对这些挑战，现代 Linux 衍生出了若干对 FHS 的 “变异” 与 “延伸” 。</p>

<h4 id="1-容器与资源调度的基石sysfscgroup">1. 容器与资源调度的基石：<code class="language-plaintext highlighter-rouge">/sys/fs/cgroup</code></h4>

<ul>
  <li><strong>底层逻辑：</strong> 传统的 Linux 里，所有进程都在抢占同一个 CPU 和内存池，最多靠 <code class="language-plaintext highlighter-rouge">nice</code> 值调换一下优先级。但在云时代，我们需要把资源切块卖给不同的租户，或者让不同的服务（比如 Web 服务和数据库）绝对互不干扰。</li>
  <li><strong>文件系统映射：</strong> Control Groups (cgroups)。内核又一次使用了“一切皆文件”的魔法。在 <code class="language-plaintext highlighter-rouge">/sys/fs/cgroup</code> 下，你会看到 <code class="language-plaintext highlighter-rouge">cpu</code>, <code class="language-plaintext highlighter-rouge">memory</code>, <code class="language-plaintext highlighter-rouge">blkio</code> 等目录。</li>
  <li><strong>开发意义：</strong> Docker 或 Kubernetes 怎么限制一个容器只能用 2G 内存和 1 个 CPU 核心？根本不需要什么复杂的系统调用，只需要在这个目录下建一个文件夹（代表一个进程组），然后往 <code class="language-plaintext highlighter-rouge">memory.max</code> 这个文本文件里写入 <code class="language-plaintext highlighter-rouge">2147483648</code>（2GB），内核的调度器就会死死掐住这个进程。这是整个现代云原生底座的物理控制面板。</li>
</ul>

<h4 id="2-打破共享法则的依赖沙盒snap-与-docker-联合文件系统">2. 打破“共享”法则的依赖沙盒：<code class="language-plaintext highlighter-rouge">/snap</code> 与 Docker 联合文件系统</h4>

<ul>
  <li><strong>底层逻辑：</strong> FHS 的核心设计之一是 <code class="language-plaintext highlighter-rouge">/usr/lib</code> 作为全局共享库（大家共用一份 glibc 或 OpenSSL 节省空间）。但这导致了臭名昭著的 “依赖地狱” —— 软件 A 需要 OpenSSL 1.0，软件 B 需要 1.1，两者无法在同一个系统中共存。</li>
  <li><strong>文件系统映射：</strong>
    <ul>
      <li><strong><code class="language-plaintext highlighter-rouge">/snap</code> 或 <code class="language-plaintext highlighter-rouge">/var/lib/flatpak</code>：</strong> 现代桌面 Linux 为了解决这个问题，干脆放弃了共享库。一个 App 把自己需要的所有底层库全部打包在一起，挂载成一个只读的虚拟磁盘。</li>
      <li><strong><code class="language-plaintext highlighter-rouge">/var/lib/docker/overlay2/</code>：</strong> Docker 更是把这种隔离推向了极致。它使用 OverlayFS（联合文件系统），把底层的操作系统（LowerDir）设为只读，然后在上面铺一层透明的读写层（UpperDir）。</li>
    </ul>
  </li>
  <li><strong>思考：</strong> 存储空间变得极其廉价，而工程师排查依赖冲突的时间变得极其昂贵。所以现代系统宁愿在硬盘上存 10 份冗余的相同库文件，也要换取应用环境的绝对隔离和稳定。</li>
</ul>

<h4 id="3-不可变系统-的激进实验nixstore-与-ostree">3. “不可变系统” 的激进实验：<code class="language-plaintext highlighter-rouge">/nix/store</code> 与 <code class="language-plaintext highlighter-rouge">/ostree</code></h4>

<ul>
  <li><strong>底层逻辑：</strong> 传统的 <code class="language-plaintext highlighter-rouge">/</code> 根目录是“可变的”（Mutable），你用 <code class="language-plaintext highlighter-rouge">apt</code> 或 <code class="language-plaintext highlighter-rouge">yum</code> 安装软件，文件系统就被修改了，一旦中断或出错，系统可能就崩了，且无法回滚。</li>
  <li><strong>文件系统映射：</strong>
    <ul>
      <li><strong><code class="language-plaintext highlighter-rouge">/nix/store</code> (NixOS)：</strong> 这可以说是对 FHS 最彻底的颠覆。系统里所有的软件、库甚至配置文件，全部存放在这里，目录名是一长串哈希值（如 <code class="language-plaintext highlighter-rouge">/nix/store/x8z...-glibc-2.32/</code>）。系统的 <code class="language-plaintext highlighter-rouge">/bin</code> 和 <code class="language-plaintext highlighter-rouge">/etc</code> 全部变成了指向这里的软链接。</li>
      <li><strong><code class="language-plaintext highlighter-rouge">/ostree</code> (如 Fedora Silverblue)：</strong> 类似于操作系统的 Git 仓库。整个系统的核心文件被按版本管理，更新系统就像 <code class="language-plaintext highlighter-rouge">git pull</code>，出错了一键切回上一个 commit。</li>
    </ul>
  </li>
  <li><strong>开发意义：</strong> 实现了绝对的 “环境可复现”。无论你在哪台机器上部署，只要配置清单一样，构建出来的文件树在哈希级别完全一致。</li>
</ul>

<h4 id="4-彻底抛弃-gnu-工具链的变种android-的目录体系">4. 彻底抛弃 GNU 工具链的变种：Android 的目录体系</h4>

<p>虽然 Android 的内核是 Linux，但它完全抛弃了标准的 FHS（因为它不需要支持传统的 GNU 工具链），重构了一套为 “App 沙盒” 和 “硬件厂商解耦” 而生的目录：</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">/system</code>：</strong> 相当于 Linux 的 <code class="language-plaintext highlighter-rouge">/usr</code>，但是由 Google 和手机厂商打包的只读系统镜像。</li>
  <li><strong><code class="language-plaintext highlighter-rouge">/vendor</code>：</strong> 专属硬件驱动层（高通、联发科的闭源驱动包）。这让 Google 可以单独升级 <code class="language-plaintext highlighter-rouge">/system</code> 而不需要硬件厂商配合重新编译驱动（Project Treble）。</li>
  <li><strong><code class="language-plaintext highlighter-rouge">/data</code>：</strong> 相当于高度隔离的 <code class="language-plaintext highlighter-rouge">/home</code> 和 <code class="language-plaintext highlighter-rouge">/var</code> 的结合体。每个 App 都有严格分配的 <code class="language-plaintext highlighter-rouge">/data/data/com.xxx.app/</code> 目录，相互之间即使通过绝对路径也无法跨越权限读取。</li>
</ul>

<h4 id="5-主板固件的物理桥梁bootefi">5. 主板固件的物理桥梁：<code class="language-plaintext highlighter-rouge">/boot/efi</code></h4>

<ul>
  <li><strong>底层逻辑：</strong> 早期的 BIOS 只要读硬盘前 512 字节（MBR）就能引导系统。现代的 UEFI 标准要求硬盘必须有一个专门的 FAT32 格式分区，主板固件只能读懂这个分区的格式。</li>
  <li><strong>文件系统映射：</strong> <code class="language-plaintext highlighter-rouge">/boot/efi</code>。这是 Linux 向主板妥协的产物。无论你用多么高级的 EXT4、BTRFS 还是 ZFS 格式化 Linux，你都必须留一个小小的 FAT32 分区挂载在这里，把引导程序（如 GRUB 的 <code class="language-plaintext highlighter-rouge">.efi</code> 文件）放在这，主板通电后才能找到操作系统的大门。</li>
</ul>

<h3 id="桌面跨系统与云端">桌面、跨系统与云端</h3>

<p>在<strong>现代桌面软件开发</strong>、<strong>跨系统混合开发</strong>以及<strong>云服务器自动化部署</strong>的场景中，还存在几个绕不开的核心目录规范。这些规范由开源社区或云厂商为了解决实际痛点而建立，是 FHS 在特定领域的自然延伸。</p>

<h4 id="1-桌面级应用的数据归宿xdg-base-directory-规范">1. 桌面级应用的数据归宿：XDG Base Directory 规范</h4>

<ul>
  <li><strong>底层逻辑：</strong> 早期的 Linux 系统里，各种软件喜欢在用户的 <code class="language-plaintext highlighter-rouge">/home/user/</code> 目录下到处留下隐藏文件（比如 <code class="language-plaintext highlighter-rouge">.bashrc</code>, <code class="language-plaintext highlighter-rouge">.viminfo</code>, <code class="language-plaintext highlighter-rouge">.nvm</code>）。这导致用户的家目录极其混乱。为了解决这个问题，freedesktop.org 提出了 XDG 标准，规定用户级应用的数据必须分类存放。</li>
  <li><strong>文件系统映射：</strong>
    <ul>
      <li><strong><code class="language-plaintext highlighter-rouge">~/.config/</code> (用户级配置)：</strong> 如果你用 Qt 等框架开发了一个带有复杂 GUI 的桌面应用程序（比如一个炫酷的音乐播放器），它的用户偏好设置、自定义的主题 QSS 样式文件，就不应该直接扔在 <code class="language-plaintext highlighter-rouge">~</code> 下，而应该存放在 <code class="language-plaintext highlighter-rouge">~/.config/你的应用名/</code> 里。</li>
      <li><strong><code class="language-plaintext highlighter-rouge">~/.local/share/</code> (用户级数据)：</strong> 存放该应用产生的、特定于用户的数据。比如你的程序扫描本地音乐库生成的 SQLite 数据库文件、或者用户自定义的播放列表。</li>
      <li><strong><code class="language-plaintext highlighter-rouge">~/.cache/</code> (用户级缓存)：</strong> 存放可以随时丢弃、不影响核心功能的数据。比如音乐播放过程中实时计算出的 FFT 频谱缓存，或者是从网络上拉取的专辑封面缩略图。</li>
    </ul>
  </li>
  <li><strong>开发意义：</strong> 遵循这个规范开发软件，能让用户的家目录保持整洁，同时在做系统备份时，用户可以放心地忽略 <code class="language-plaintext highlighter-rouge">~/.cache/</code> 目录，极大地节省存储空间和时间。</li>
</ul>

<h4 id="2-跨系统融合的魔法桥梁wsl2-与-9p-协议挂载">2. 跨系统融合的魔法桥梁：WSL2 与 <code class="language-plaintext highlighter-rouge">9P</code> 协议挂载</h4>

<ul>
  <li><strong>底层逻辑：</strong> 当系统不再是纯粹的物理机，而是在宿主操作系统（Windows）内部运行的高级轻量级虚拟机时，如何让两个完全不同的文件系统（NTFS 和 Ext4）高效互通？</li>
  <li><strong>文件系统映射：</strong>
    <ul>
      <li><strong><code class="language-plaintext highlighter-rouge">/mnt/c</code>, <code class="language-plaintext highlighter-rouge">/mnt/d</code> 等：</strong> 在使用 WSL2 进行开发时，Windows 的物理磁盘会被自动挂载到 Linux 的 <code class="language-plaintext highlighter-rouge">/mnt</code> 目录下。</li>
      <li><strong>原理：</strong> 这里并不是简单的磁盘共享。Windows 使用的是 DrvFs 插件，底层通过 <strong>9P 网络协议</strong>（一个源自贝尔实验室 Plan 9 系统的分布式文件系统协议）来实现跨 OS 的文件访问。它需要在 Linux 的 POSIX 权限模型（rwx，所有者/组）和 Windows 的 ACL（访问控制列表）之间做复杂的实时翻译。这也是为什么在 WSL2 里跨文件系统（比如在 Linux 里编译 <code class="language-plaintext highlighter-rouge">/mnt/c</code> 下的 C++ 项目）进行高频 I/O 操作会比较慢的原因：每一次读写，都要经过协议层的翻译。</li>
    </ul>
  </li>
</ul>

<h4 id="3-云端实例的诞生印记cloud-init-与-varlibcloud">3. 云端实例的诞生印记：<code class="language-plaintext highlighter-rouge">cloud-init</code> 与 <code class="language-plaintext highlighter-rouge">/var/lib/cloud</code></h4>

<ul>
  <li><strong>底层逻辑：</strong> 当你在云服务商（比如 GCP、AWS）那里点击 “创建一台 Ubuntu 服务器” 时，云厂商并不是现场给你安装系统，而是直接克隆一个标准的、一模一样的只读系统镜像。那么问题来了：这个标准镜像开机后，是怎么知道你的主机名、网络配置，以及最关键的——<strong>怎么把你的 SSH 公钥注入进系统的</strong>？</li>
  <li><strong>文件系统映射：</strong>
    <ul>
      <li><strong><code class="language-plaintext highlighter-rouge">/var/lib/cloud/</code>：</strong> 这是现代云服务器自动初始化的“核心现场”。</li>
    </ul>
  </li>
  <li><strong>工作机制：</strong> 系统第一次开机时，一个叫 <code class="language-plaintext highlighter-rouge">cloud-init</code> 的核心服务会抢先运行。它会通过内部网络去访问云厂商的元数据服务器（Metadata Server），拉取你在这台实例上配置的专属数据（比如 SSH Key），然后把这些状态和执行日志保存在 <code class="language-plaintext highlighter-rouge">/var/lib/cloud/instances/</code> 下，最后再把 Key 写入到你的 <code class="language-plaintext highlighter-rouge">~/.ssh/authorized_keys</code> 里。当你成功通过 SSH 连接上这台服务器时，这套复杂的“认主”流程其实已经在文件系统的这个角落里悄悄走完了。</li>
</ul>

<h3 id="内核机制与底层调试">内核机制与底层调试</h3>

<p>到了这个深度，我们已经脱离了传统意义上的 “文件系统规范”，触及到了 <strong>Linux 内核机制在用户态的强行投影</strong>。这里是底层驱动开发者、固件极客和内核调试专家的领域。</p>

<h4 id="1-路由器不死之身的秘密rom-与-overlay">1. 路由器“不死之身”的秘密：<code class="language-plaintext highlighter-rouge">/rom</code> 与 <code class="language-plaintext highlighter-rouge">/overlay</code></h4>

<p>在高度定制的嵌入式网络设备（如 OpenWrt / ImmortalWrt）中，你可以在根目录 <code class="language-plaintext highlighter-rouge">/</code> 下任意修改配置文件，但如果系统崩溃，只要执行“恢复出厂设置”，一切又能完美如初。</p>

<ul>
  <li><strong>第一性原理：</strong> 嵌入式设备的 Flash 闪存寿命有限，且出厂固件必须保证绝对安全，不能被意外写坏。</li>
  <li><strong>文件系统映射：</strong>
    <ul>
      <li><strong><code class="language-plaintext highlighter-rouge">/rom</code> (只读层)：</strong> 这里存放的是系统刷入时的高压缩比只读文件系统（通常是 SquashFS）。它是系统的“底片”，绝对不可写。</li>
      <li><strong><code class="language-plaintext highlighter-rouge">/overlay</code> (可写层)：</strong> 这是一个极小的可读写分区（通常是 JFFS2 或 F2FS）。</li>
    </ul>
  </li>
  <li><strong>底层机制（联合挂载）：</strong> 当系统启动时，内核会将 <code class="language-plaintext highlighter-rouge">/rom</code> 和 <code class="language-plaintext highlighter-rouge">/overlay</code> 像千层饼一样叠在一起，合并成你看到的根目录 <code class="language-plaintext highlighter-rouge">/</code>。如果你修改了 <code class="language-plaintext highlighter-rouge">/etc/config/network</code>，系统其实并没有修改 <code class="language-plaintext highlighter-rouge">/rom</code> 里的原始文件，而是在 <code class="language-plaintext highlighter-rouge">/overlay/etc/config/</code> 下新建了一个同名文件把它 “遮盖” 住了。所谓 “恢复出厂设置”，在物理层面上仅仅是执行了一句 <code class="language-plaintext highlighter-rouge">rm -rf /overlay/*</code>。</li>
</ul>

<h4 id="2-裸机闪存的真面目devmtd-与-devmtdblock">2. 裸机闪存的真面目：<code class="language-plaintext highlighter-rouge">/dev/mtd</code> 与 <code class="language-plaintext highlighter-rouge">/dev/mtdblock</code></h4>

<p>当你脱离了电脑的 SATA/NVMe 硬盘，去给 ARM 开发板或路由器编写底层固件时，你会发现 <code class="language-plaintext highlighter-rouge">/dev/sda</code> 这种块设备消失了。</p>

<ul>
  <li><strong>第一性原理：</strong> 原始的 NAND/NOR Flash 闪存芯片与普通硬盘有着本质的物理差异。硬盘可以按扇区随意读写，但原始 Flash 必须<strong>“先擦除（Erase），后写入（Write）”</strong>，且具有坏块（Bad Block）特性。</li>
  <li><strong>文件系统映射：</strong>
    <ul>
      <li>Linux 专门引入了 <strong>MTD (Memory Technology Device)</strong> 子系统。</li>
      <li><strong><code class="language-plaintext highlighter-rouge">/dev/mtdX</code>（字符设备）：</strong> 允许底层工具直接对物理闪存的 Block 进行擦除和读写操作（比如用 <code class="language-plaintext highlighter-rouge">flashcp</code> 烧录 Bootloader）。</li>
      <li><strong><code class="language-plaintext highlighter-rouge">/dev/mtdblockX</code>（块设备）：</strong> 内核在这个接口上封装了一层软件逻辑（如磨损均衡），让上层应用能勉强把它当成普通硬盘来挂载。</li>
    </ul>
  </li>
</ul>

<h4 id="3-容器视界的物理边界procpidns">3. 容器“视界”的物理边界：<code class="language-plaintext highlighter-rouge">/proc/[pid]/ns/</code></h4>

<p>我们前面提到了 Docker 用 <code class="language-plaintext highlighter-rouge">/sys/fs/cgroup</code> 来限制 CPU 和内存，用 OverlayFS 来隔离文件。但这还不够，如何让容器里的进程认为自己是“系统里唯一的进程”，且拥有独立的网卡？</p>

<ul>
  <li><strong>第一性原理：</strong> 容器并不是真正的虚拟机，它只是宿主机上的一个普通进程。我们需要一种机制，在内核层面对这个进程进行“感官剥夺”和“视觉欺骗”。</li>
  <li><strong>文件系统映射：</strong> <strong>Namespaces（命名空间）</strong>。
    <ul>
      <li>如果你进入 <code class="language-plaintext highlighter-rouge">/proc/某个进程PID/ns/</code> 目录，你会看到一堆神奇的软链接：<code class="language-plaintext highlighter-rouge">mnt</code> (挂载点隔离), <code class="language-plaintext highlighter-rouge">net</code> (网络隔离), <code class="language-plaintext highlighter-rouge">pid</code> (进程号隔离), <code class="language-plaintext highlighter-rouge">ipc</code> (进程通信隔离)。</li>
    </ul>
  </li>
  <li><strong>开发意义：</strong> 当 Docker 启动一个容器时，它其实是通过系统调用，为这个进程创建了一套全新的 Namespace 文件链接。只要两个进程的 <code class="language-plaintext highlighter-rouge">net</code> 命名空间指向不同的哈希值，它们就处在物理隔离的两个网络平行宇宙中。这也是现代云原生虚拟化技术的绝对基石。</li>
</ul>

<h4 id="4-内核开发者的-后门syskerneldebug-debugfs">4. 内核开发者的 “后门”：<code class="language-plaintext highlighter-rouge">/sys/kernel/debug/</code> (DebugFS)</h4>

<ul>
  <li><strong>第一性原理：</strong> 如果你正在为一块新板子写 I2C 或 SPI 驱动，代码跑在内核态（Ring 0），一旦出错系统直接 Kernel Panic（死机）。你没法用 <code class="language-plaintext highlighter-rouge">printf</code> 打印日志，也没法用普通的 GDB 调试。</li>
  <li><strong>文件系统映射：</strong> Linux 提供了 DebugFS（通常挂载在 <code class="language-plaintext highlighter-rouge">/sys/kernel/debug/</code>）。</li>
  <li><strong>底层机制：</strong> 这是一个完全没有规矩、不受任何标准约束的 RAM 文件系统。内核开发者可以在驱动代码里写几行简单的 API，就能把内核深处极其敏感的变量（比如当前寄存器的原始状态、内存碎片的分布图、电源管理芯片的实时微伏电压）直接以文本文件的形式暴露在这个目录下。系统一旦发布到生产环境，这个目录通常会被严密封锁或卸载。</li>
</ul>

<h4 id="5-死亡瞬间的黑匣子sysfspstore-persistent-storage">5. 死亡瞬间的黑匣子：<code class="language-plaintext highlighter-rouge">/sys/fs/pstore</code> (Persistent Storage)</h4>

<ul>
  <li><strong>第一性原理：</strong> 当系统发生极其严重的致命错误（Kernel Panic）时，内核会瞬间崩溃。此时，硬盘驱动可能已经失效，中断已经被屏蔽，系统根本无法将崩溃日志写进常规的 <code class="language-plaintext highlighter-rouge">/var/log/messages</code> 中。那重启后，我们怎么知道死机前一毫秒发生了什么？</li>
  <li><strong>底层机制：</strong> Linux 引入了 <code class="language-plaintext highlighter-rouge">pstore</code> 文件系统。当内核判定自己即将死亡时，它会绕过所有复杂的 IO 堆栈，将最后遗言（oops 报错、寄存器状态）直接写入一段<strong>特殊的非易失性内存（NVRAM）</strong>或主板上即使热重启也不会断电的保留 RAM 区域。</li>
  <li><strong>物理映射：</strong> 系统重启后，你会在 <code class="language-plaintext highlighter-rouge">/sys/fs/pstore/</code> 下看到名为 <code class="language-plaintext highlighter-rouge">dmesg-efi-xxx</code> 或 <code class="language-plaintext highlighter-rouge">console-ramoops</code> 的文件。读取它，你就能看到系统上一次死亡瞬间的“走马灯”。这是底层嵌入式设备和服务器排查玄学死机问题的终极武器。</li>
</ul>

<h4 id="6-现代内核的上帝视角sysfsbpf-ebpf-maps">6. 现代内核的上帝视角：<code class="language-plaintext highlighter-rouge">/sys/fs/bpf</code> (eBPF Maps)</h4>

<ul>
  <li><strong>第一性原理：</strong> 传统的 Linux 网络和安全过滤（比如 iptables）是在内核里写死逻辑，用户只能配规则。但现代 Linux 引入了 eBPF 技术，允许用户把一段自己写的 C 代码，经过极其严格的安全审查后，<strong>直接注入到内核的运行沙箱中执行</strong>（极其恐怖的特权）。</li>
  <li><strong>底层机制：</strong> 注入到内核的代码怎么和用户态的用户程序交换庞大的数据（比如实时的网络流量统计）？靠普通的系统调用太慢了。</li>
  <li><strong>物理映射：</strong> eBPF 引入了 <code class="language-plaintext highlighter-rouge">Map</code>（映射表）的概念，并将其挂载在 <code class="language-plaintext highlighter-rouge">/sys/fs/bpf</code> 下。这表面上是个目录，但里面的“文件”其实是<strong>用户态和内核态共享的高速内存数据结构</strong>（如 Hash 表、数组）。通过读写这里，你的程序就像是直接插了一根导管到了内核的大动脉上。</li>
</ul>

<h4 id="7-强制访问控制的终极监狱sysfsselinux-或-smacksapparmor">7. 强制访问控制的终极监狱：<code class="language-plaintext highlighter-rouge">/sys/fs/selinux</code> (或 smacks/apparmor)</h4>

<ul>
  <li><strong>第一性原理：</strong> 标准的 FHS 只有 rwx（读写执行）三种权限，只要你是 Root，系统就对你完全敞开。但在高安防环境（如国防、金融或 Android 手机底层）中，这太危险了。</li>
  <li><strong>底层机制：</strong> 也就是所谓的 MAC（强制访问控制）。即使你是 Root 用户，如果你运行的进程（比如 Web 服务器）没有被赋予读取特定标签文件的权限，内核也会在底层直接把你的操作斩断。</li>
  <li><strong>物理映射：</strong> <code class="language-plaintext highlighter-rouge">/sys/fs/selinux</code> 是这套庞大安全引擎的控制面板。里面的文件控制着内核的安全策略布尔值。比如 <code class="language-plaintext highlighter-rouge">enforce</code> 文件，写入 1 就是开启强制拦截，写入 0 就是只记录不拦截。这里定义了系统里每一根发丝的访问规则。</li>
</ul>

<h4 id="8-幽灵文件系统net-或-misc-autofs">8. 幽灵文件系统：<code class="language-plaintext highlighter-rouge">/net</code> 或 <code class="language-plaintext highlighter-rouge">/misc</code> (AutoFS)</h4>

<ul>
  <li><strong>第一性原理：</strong> 挂载一个局域网内的 NFS（网络文件系统）是极其消耗资源的。如果我们有 100 个网络存储，不能一开机全挂载上，但又希望用户想用的时候随时能找到。</li>
  <li><strong>底层机制：</strong> 按需挂载。</li>
  <li><strong>物理映射：</strong> 当你 <code class="language-plaintext highlighter-rouge">ls /net/server_ip/share</code> 的时候，这个目录原本在物理上是<strong>不存在</strong>的。但是当你敲下回车触发访问异常的瞬间，内核的 <code class="language-plaintext highlighter-rouge">autofs</code> 模块会瞬间拦截这个请求，在后台光速完成网络握手和挂载，然后再把结果返回给你。从你的视角看，这个文件系统就像幽灵一样，你注视它时它就存在，你离开时它就消失。</li>
</ul>

<h2 id="真正的绝对底部vfs-virtual-file-system">真正的绝对底部：VFS (Virtual File System)</h2>

<p>如果我们要进行最后一次第一性原理的推演，那就是：<strong>整个根目录 <code class="language-plaintext highlighter-rouge">/</code> 本身，就是一个虚假的幻觉。</strong></p>

<p>在 Linux 极早期的源代码中，并没有所谓的目录树。所有的硬盘、内存映射、设备节点，之所以能呈现出统一的层级结构，完全是因为 Linux 内核中存在一个极其伟大的抽象层 —— <strong>VFS（虚拟文件系统）</strong>。</p>

<p>你在终端里敲下 <code class="language-plaintext highlighter-rouge">cd /</code> 和 <code class="language-plaintext highlighter-rouge">ls</code> 看到的所有东西，本质上都是内核在内存中维护的一张巨大的<strong>结构体图（<code class="language-plaintext highlighter-rouge">struct dentry</code> 和 <code class="language-plaintext highlighter-rouge">struct inode</code>）</strong>。</p>

<ul>
  <li>Ext4 硬盘文件是 VFS 的子类。</li>
  <li><code class="language-plaintext highlighter-rouge">/sys</code> 设备树是 VFS 的子类。</li>
  <li><code class="language-plaintext highlighter-rouge">/proc</code> 进程信息是 VFS 的子类。</li>
  <li><code class="language-plaintext highlighter-rouge">/run</code> 内存状态也是 VFS 的子类。</li>
</ul>

<p>走到这里，文件系统这座迷宫已经到了尽头。再往前迈一步，就已经没有 “文件” 和 “目录” 的概念了——前方是纯粹的 C 语言指针、内存分页、自旋锁，以及 Linux 内核的源代码汪洋。</p>

<p>回头再看，FHS 的意义也就更清楚了。它并不是要替 Linux 的一切目录现象提供最终答案，而是先给出一套最基础、也最耐用的分层原则：哪些是系统骨架，哪些是运行状态，哪些是用户数据，哪些是共享资源，哪些是本机专属。</p>

<p>而现代 Linux 做的事情，则是在这套骨架之上不断往前延伸：把启动过程、容器隔离、嵌入式存储、云端初始化、内核调试乃至运行时安全策略，继续投影到这棵目录树上。于是你看到的，就不再只是一个“文件该放哪”的标准，而是一整套从静态布局延伸到动态运行的目录体系。</p>]]></content><author><name>bfmhno3</name></author><category term="Note" /><category term="Linux" /><summary type="html"><![CDATA[要真正理解 Linux 的 FHS (Filesystem Hierarchy Standard，文件系统层次结构标准)，不能只靠死记硬背“哪个目录放什么文件”。更有效的办法，是剥开表象，回到操作系统设计的起点，看看在构建一个多用户、网络化、可扩展的系统时，我们究竟面临了哪些根本问题，以及 FHS 又是如何作为一套“解决框架”出现的。 不过，本文并不准备停在 FHS 的条文本身。更准确地说，行文会分成两步：先把 FHS 作为规范 讲明白，再顺着这套规范继续往外延伸，看看现代 Linux 是如何在这棵目录树上接入运行时机制、容器隔离、嵌入式约束和内核接口的。也正因为如此，标题与其叫“Linux 的 FHS”，不如更直接一点，叫“从 FHS 到 Linux 运行时目录体系”。 推演起点：我们到底要存储什么？ 假设你从零开始设计一个操作系统。系统需要运行，用户需要存数据，程序需要产生日志。如果你把所有东西都塞进一个大文件夹里，系统马上就会面临灾难： 权限混乱： 普通用户可能会不小心删掉系统的核心启动文件。 状态混淆： 你无法区分哪些文件是只读的系统核心，哪些是随时变动的日志，导致备份极其困难。 共享灾难： 如果你想在局域网内让多台机器共享一套程序，你无法分离 “机器特有的配置” 和 “可以共享的程序本体”。 因此，FHS 的本质，是基于数据属性的二维分类矩阵。Linux 的文件系统布局，本质上就是围绕着这两个核心维度展开的： 维度一：是否可变 (Static vs. Variable) 静态数据 (Static)： 系统运行期间不需要修改的文件（如可执行程序、库文件、默认系统配置）。 可变数据 (Variable)： 系统运行期间会不断变化的文件（如日志、用户数据、临时文件、缓存）。 维度二：是否可共享 (Shareable vs. Unshareable) 可共享 (Shareable)： 可以在多台主机之间共享的数据（如二进制应用程序、用户主目录）。 不可共享 (Unshareable)： 特定于某台主机的本地数据（如本机的网络配置、设备节点、本机的运行状态）。 通过这个 2x2 的矩阵，FHS 建立了一个严密的逻辑体系。 目录拆解 有了上面这两个维度，再回头看 Linux 根目录 / 下的各个分支，很多事情就会变得顺理成章。 1. 系统的 “地基” 与 “骨架”（不可共享 / 静态） 系统要能够开机，或者在崩溃时能够进入单用户模式进行急救，它需要一个自给自足的最基础环境。 /boot (启动引导)： 系统启动的第一步。这里存放着内核（vmlinuz）和引导程序（GRUB）。它是极其特殊的不可共享静态数据。 /bin (Basic Binaries)： 系统最核心、最基础的用户级命令（如 ls, cp, cat）。原则： 即使所有其他分区（如 /usr）都损坏或未挂载，这里的命令也必须能用，以保证管理员能修复系统。 /sbin (System Binaries)： 系统级的核心命令（如 fdisk, reboot, ip）。与 /bin 的区别在于，这里的工具主要是给 Root（超级用户） 用的。 /etc (Host-specific system configuration)： 按照 FHS 的官方说法，这里是 host-specific system configuration，也就是“特定于本机的系统配置”。你可以把它理解为本机的“规则字典”。原则： 这里面应该全是配置，而不应该放可执行二进制文件。 /lib &amp; /lib64 (Libraries)： /bin 和 /sbin 中的基础命令在运行时需要调用的共享库（C语言库等），以及内核模块。相当于程序的“公共零件库”。 2. “一切皆文件” 的虚拟抽象层（不可共享 / 实时动态） Linux 继承了 UNIX 最伟大的哲学：“一切皆文件”。硬件设备和系统运行时的内存状态，都被抽象成了文件，方便统一管理。这些目录在硬盘上往往是不占空间的（存在于内存中）。 /dev (Devices)： 所有的硬件设备（硬盘、键盘、鼠标、终端）都在这里有一个映射文件。对这些文件的读写，就是对硬件的读写。 /proc (Processes)： 内核的“监视器”。里面全是虚拟文件，记录了当前运行的进程信息和内核状态。比如查 CPU 信息就是去读 /proc/cpuinfo。 /sys (Sysfs)： 对 /proc 的现代化补充，更结构化地展示内核、设备驱动和总线的信息。 3. 系统的 “扩展应用程序”（可共享 / 静态） 当系统完成了基础启动，我们需要安装大量的第三方软件（办公软件、开发工具等）。这些软件体积庞大，且在不同主机间是可以共享的。 /usr /usr： FHS 对它的定位，本质上是系统中 shareable, read-only data 的主要存放地。你可以把它理解为系统的“第二层级”：基础系统先靠 /bin、/sbin、/lib 站起来，而更完整、更庞大的用户空间资源则放在 /usr 下。 /usr/bin 和 /usr/sbin：绝大多数非核心的用户和管理员命令。 /usr/lib：非核心的库文件。 /usr/local：系统管理员本地手动编译安装的软件存放地（不受包管理器如 apt/yum 管辖，保证了系统的纯净）。 4. 系统的“新陈代谢”与状态记录（不可共享 / 可变） 系统在运行过程中，会产生大量的过程数据和历史记录。 /var /var (Variable)： 它是系统的“消化系统”。原则： 任何大小会随时间不断变化的数据，都必须放在这里，绝对不能放在只读的 /usr 下。 /var/log：系统和程序的日志（排错必备）。 /var/cache：应用程序缓存。 /var/spool：队列数据（如打印队列、未发送的邮件）。 5. 用户与临时数据（可变） 隔离系统和用户，是保障系统安全的基石。 /home： 普通用户的“私人领地”。无论系统怎么重装或升级，只要单独挂载 /home 分区，用户的数据就不会丢失。 /root： 超级用户 root 的主目录。为什么不放在 /home/root？因为如果 /home 所在的磁盘挂载失败，root 仍然需要登录系统进行修复。 /tmp (Temporary)： 系统的“草稿纸”。任何程序或用户都可以把临时文件扔在这里。系统重启时（或定期），这里的数据会被自动清空。 6. 额外与外载资源 /opt (Optional)： 附加的应用程序软件包。通常用于大型闭源软件或第三方商业软件（如 Google Chrome, Oracle 数据库），它们喜欢把所有文件（二进制、库、配置）打包放在自己的一个独立目录下，互不干扰。 /mnt (Mount) / /media： 挂载点。/mnt 通常用于管理员手动临时挂载额外的文件系统，而 /media 通常用于系统自动挂载的可移动介质（如 U盘、光驱）。 现代 Linux 的变化 随着技术的发展，早期的某些约束（比如硬盘太小，不得不把核心 /bin 放在一个小根分区，庞大的 /usr/bin 放在另一个大分区）已经不复存在。 现在的 Linux 初始化流程（通过 initramfs）可以在挂载真正的根目录之前，就把需要的东西加载进内存。因此，现代发行版（如 Arch, Fedora, Ubuntu）正在推行 “usr-merge”（/usr 合并）： 现在的 /bin, /sbin, /lib 往往只是指向 /usr/bin, /usr/sbin, /usr/lib 的软链接（Symlinks）。 这并不是背离了 FHS，而是在物理条件改变后的自然进化：既然底层硬件不再受限，那么在逻辑上将 “系统核心二进制” 和 “系统扩展二进制” 统一管理（全部归入静态可共享的 /usr），能让系统结构更加干净利落。 补全 FHS 拼图：从系统开发和编译链路视角再看一遍 前面的重点，在于搭建 FHS 背后“可变/静态”与“共享/不可共享”的底层逻辑框架。顺着这条主线继续往下推，会发现还有一些对开发者，特别是做 C/C++ 编译、驱动、工具链和底层系统开发的人来说极其关键的目录，值得单独补齐。 1. 深度拆解 /usr 子目录（开发者的主战场） /usr (Universal System Resources) 是静态且可共享的资源库。除了前面提到的 bin 和 lib，它内部的细分目录是 C/C++ 工具链和系统构建的核心。 /usr/include (C/C++ 标准头文件) 属性： 静态，可共享。 本质： 它是系统级 API 的“契约”。当你写 C/C++ 代码并使用 #include &lt;stdio.h&gt; 或 #include &lt;linux/i2c.h&gt; 时，GCC/Clang 编译器默认会来这里寻找对应的 .h 文件。如果你在交叉编译环境（比如为 ARM 架构编译代码）中，你的交叉编译工具链也会有一个结构完全平行的 include 目录。 /usr/src (源代码) 属性： 静态，可共享。 本质： 存放系统级软件的源代码。最常见的是内核源码头文件（Kernel Headers）。如果你需要为某个特定的硬件（比如一块自定义的驱动板）编写并编译外部内核模块（Out-of-tree module），你的 Makefile 必须引用这里的内核头文件，以确保你的驱动和当前运行的内核版本 ABI 兼容。 /usr/share (架构无关的共享数据) 属性： 静态，可共享，且与 CPU 架构无关。 本质： 按照 FHS 的说法，这里存放的是 architecture-independent data。无论你是 x86 还是 ARM，这里的数据都是通用的。这包括 man 帮助文档（/usr/share/man）、时区数据（/usr/share/zoneinfo）、以及构建工具的配置文件（例如 CMake 的默认模块通常存放在 /usr/share/cmake/Modules/ 下）。 /usr/libexec (系统内部调用的可执行文件) 属性： 静态，可共享。 本质： FHS 对它的表述很直接：binaries run by other programs。这里存放的二进制程序不是给用户在终端里直接敲击运行的，而是给其他程序调用的后台工具。比如系统的认证模块守护进程、某些网络配置的辅助脚本等。它们没有被放进 /usr/bin，就是为了防止污染用户的环境变量 PATH。 2. 现代 Linux 的内存文件系统补充 随着 systemd 的普及和系统启动流程的优化，FHS 引入了更高效的运行时目录。 /run (运行时状态数据 - 现代 tmpfs) 属性： 实时动态，不可共享，纯内存存储。 本质： FHS 3.0 对它的定义就是 run-time variable data。它记录系统自本次开机以来的状态数据（如守护进程的 PID 文件、Socket 文件等）。在早期的 FHS 中，这些数据放在 /var/run。但因为 /var 通常在硬盘上，读写慢且可能在启动早期尚未挂载，所以现代 Linux 创建了 /run 并将其挂载为 tmpfs（内存文件系统）。每次重启，这里自然清空。 /dev/shm (POSIX 共享内存) 属性： 实时动态，纯内存存储。 本质： 也是一个 tmpfs 挂载点。它提供了一种极高效率的进程间通信（IPC）机制。如果有两个高实时性要求的进程需要交换海量数据，通过在这里创建内存映射文件进行读写，速度远超 Socket 或管道。 3. 驱动与服务的特定目录 /lib/modules (内核模块) 属性： 静态（通常伴随内核更新而更新），不可共享。 本质： 虽然归属在 /lib 下，但必须单独拿出来强调。这里存放的是以 .ko 结尾的内核驱动模块。当系统通过 udev 检测到新硬件插入时，modprobe 工具就是在这个目录（具体路径为 /lib/modules/$(uname -r)/）下寻找并加载对应的驱动。 /srv (服务数据 Service Data) 属性： 可变或静态（视具体服务而定），不可共享。 本质： FHS 的官方说法是 data for services provided by this system。也就是说，这里存放的是“由这台机器对外提供的服务所使用的数据”。比如你在这台机器上跑了一个静态博客系统或 Web 服务器，网站的根目录数据（如 HTML、CSS、Markdown 渲染后的文件）就更适合放在 /srv/www/ 或 /srv/http/ 中，而不是混进 /home 或 /var 的其他语义里。 4. 灾难恢复机制 /lost+found (文件系统孤儿院) 属性： 动态。 本质： 这是 Ext 系列（Ext2/3/4）文件系统特有的目录。当系统遭遇突然断电或内核崩溃，导致文件系统损坏时，在下次重启运行 fsck（文件系统检查）时，那些在底层 inode 存在但失去了目录引用的“无头文件”，就会被抢救并存放在这里，供管理员后续手工分析和恢复。 全景视图（First Principles 矩阵最终版） 把这些补充的目录重新放回前面的二维分类矩阵里，FHS 的逻辑骨架就会清楚很多。这里要特别说明一点：下面这张表是为了帮助理解而做的教学化归纳，并不是 FHS 标准原文中逐格给出的官方表格。 不过，目录本身的定义和归属思路，会尽量贴近 FHS 的官方说法。 数据属性 静态 (Static) *系统运行不改变，可只读挂载* 可变 (Variable) *系统运行持续写入* 实时内存抽象 *不占硬盘，描述状态或提供机制* **不可共享 (Unshareable)** *特定于本机* /boot, /etc, /bin, /sbin, /lib, /lib/modules /var, /run, /tmp, /lost+found, /srv /dev, /proc, /sys **可共享 (Shareable)** *可跨主机复用* /usr, /usr/include, /usr/src, /usr/share, /usr/libexec, /opt /home /dev/shm FHS 的设计从来不是为了限制开发者，而是为了在混乱的多任务环境中划定清晰的边界。明白 /usr/include 提供接口、/lib 提供实现、/usr/src 提供底层编译依赖，整个系统的编译与运行逻辑就能彻底串联起来了。 从 FHS 到 Linux 运行时目录体系 如果说前半篇讲的是“FHS 这套目录分层规范本身”，那么从这里开始，视角就要再往外扩一层。也就是说，下面讨论的很多目录，已经不只是在回答“标准要求你怎么放文件”，而是在回答另一个问题：现代 Linux 又是如何把硬件、容器、云实例、嵌入式约束和内核机制继续投影进这棵目录树里的。 换句话说，前半部分偏“规范”，后半部分偏“演化”。两者不能混为一谈，但放在同一篇文章里连起来看，恰好能把 Linux 的目录体系从静态分层一路看到动态运行。 嵌入式与底层硬件开发 标准 FHS 是为通用服务器设计的。当 Linux 运行在没有标准 BIOS/UEFI 的 ARM 开发板、路由器或嵌入式设备上时，文件系统的职责会发生微妙偏移，衍生出一些普通用户很少接触的 “隐藏版图”。 1. 硬件描述的真理之源：/sys/firmware/devicetree 底层逻辑： 与 x86 电脑不同，大部分 ARM 架构的主板（无论是开发板还是路由器）没有一套标准的机制让内核在开机时 “自动发现” 所有硬件（比如哪个引脚连着 LED，哪个 I2C 总线上挂着传感器）。 文件系统映射： Linux 引入了设备树（Device Tree, .dtb 文件）。在系统启动后，内核会将它解析到的底层硬件拓扑结构，原封不动地映射到 /sys/firmware/devicetree/base 目录下。 开发意义： 当你发现自己写的驱动或者配置没有生效时，第一步不是去查代码，而是直接 cat 这个目录下的节点，看看内核到底有没有正确加载你的硬件描述。这里是硬件状态的 “第一现场”。 2. 用户空间的硬件操控面板：/sys/class/ 底层逻辑： 按照传统的 UNIX 哲学，操作硬件必须写内核态的驱动程序（.ko），然后通过 /dev 下的设备节点进行交互（如 ioctl）。但这对于简单的硬件控制来说太重了。 文件系统映射： Linux 内核提供了一套子系统，将底层硬件的控制权以纯文本文件的形式暴露在 /sys/class/ 目录下。 /sys/class/gpio/：你可以通过向特定文件写入 1 或 0 来直接翻转微控制器的 GPIO 引脚电平。 /sys/class/pwm/：直接通过修改文本文件里的数值来调整 PWM 波的占空比（比如控制风扇转速或 TEC 散热功率）。 /sys/class/leds/：直接控制板载指示灯的亮灭和触发模式。 开发意义： 极大地降低了调试成本。你可以直接用 Shell 脚本（如 echo 1 &gt; /sys/class/gpio/gpio10/value）来做硬件点检，而不需要写一行 C 代码编译驱动。 3. 交叉编译与根文件系统构建（Sysroot） 当你为另一台不同架构的机器（比如给 ARM 开发板或路由器）编译系统和软件时，你会在宿主机上看到 FHS 的 “套娃” 现象。 工具链目录（通常在 /opt 或独立目录下）： 你可能会看到像 aarch64-linux-gnu 这样的目录。在这个目录下，会完整地再现一套 bin, lib, include, sys。 底层逻辑： 这个目录被称为 Sysroot（系统根）。编译器在编译代码时，不会去包含宿主机（你当前的电脑）的 /usr/include，而是去这个 Sysroot 里的 include 找头文件，去 Sysroot 里的 lib 找动态库。这是保证“跨平台编译”不出错的物理隔离带。 4. 深度定制系统的变异：以 OpenWrt/ImmortalWrt 为例 如果你接触过网络路由器的底层系统（如基于 Linux 的 OpenWrt 及其分支 ImmortalWrt），你会发现它们虽然遵循 FHS，但为了适应极其有限的 Flash 存储（比如只有 16MB 或 32MB），在 /etc 的管理上发展出了一套独特的哲学： /etc/config/ (UCI 配置系统)： 在标准的 Ubuntu 或 Debian 中，各种软件的配置文件散落在 /etc 下，格式五花八门（json, yaml, ini, 纯文本）。但在路由器系统中，为了统一管理和提供 Web 界面（LuCI），所有的核心配置都被抽象并统一存放在 /etc/config/ 下，采用完全一致的语法。 底层逻辑： 通过高度统一的配置范式，降低系统的解析开销，并使得网络状态、防火墙规则、代理转发的联动变得可通过脚本高效调度。 5. 架构的妥协：/lib32, /lib64, /lib/x86_64-linux-gnu 底层逻辑： 当计算机从 32 位向 64 位过渡时，面临着兼容性问题：64 位的系统可能需要运行 32 位的旧程序，这就需要系统里同时存在两套共享库。 文件系统映射： FHS 规范允许建立 /lib32 和 /lib64（或相应的 /usr/lib32）。但在现代的 Debian/Ubuntu 体系中，引入了更高级的 Multiarch（多架构） 机制。它们不再简单粗暴地用 32/64 区分，而是在 /lib 或 /usr/lib 下建立了按处理器架构命名的目录，例如 /lib/x86_64-linux-gnu。 云原生与现代架构 传统的 FHS 是为“单机、多用户、共享物理资源”的时代设计的。但在过去的十几年里，云计算、容器化、智能手机和不可变基础设施的崛起，带来了全新的挑战：隔离性、可重复性、以及极致的安全。 为了应对这些挑战，现代 Linux 衍生出了若干对 FHS 的 “变异” 与 “延伸” 。 1. 容器与资源调度的基石：/sys/fs/cgroup 底层逻辑： 传统的 Linux 里，所有进程都在抢占同一个 CPU 和内存池，最多靠 nice 值调换一下优先级。但在云时代，我们需要把资源切块卖给不同的租户，或者让不同的服务（比如 Web 服务和数据库）绝对互不干扰。 文件系统映射： Control Groups (cgroups)。内核又一次使用了“一切皆文件”的魔法。在 /sys/fs/cgroup 下，你会看到 cpu, memory, blkio 等目录。 开发意义： Docker 或 Kubernetes 怎么限制一个容器只能用 2G 内存和 1 个 CPU 核心？根本不需要什么复杂的系统调用，只需要在这个目录下建一个文件夹（代表一个进程组），然后往 memory.max 这个文本文件里写入 2147483648（2GB），内核的调度器就会死死掐住这个进程。这是整个现代云原生底座的物理控制面板。 2. 打破“共享”法则的依赖沙盒：/snap 与 Docker 联合文件系统 底层逻辑： FHS 的核心设计之一是 /usr/lib 作为全局共享库（大家共用一份 glibc 或 OpenSSL 节省空间）。但这导致了臭名昭著的 “依赖地狱” —— 软件 A 需要 OpenSSL 1.0，软件 B 需要 1.1，两者无法在同一个系统中共存。 文件系统映射： /snap 或 /var/lib/flatpak： 现代桌面 Linux 为了解决这个问题，干脆放弃了共享库。一个 App 把自己需要的所有底层库全部打包在一起，挂载成一个只读的虚拟磁盘。 /var/lib/docker/overlay2/： Docker 更是把这种隔离推向了极致。它使用 OverlayFS（联合文件系统），把底层的操作系统（LowerDir）设为只读，然后在上面铺一层透明的读写层（UpperDir）。 思考： 存储空间变得极其廉价，而工程师排查依赖冲突的时间变得极其昂贵。所以现代系统宁愿在硬盘上存 10 份冗余的相同库文件，也要换取应用环境的绝对隔离和稳定。 3. “不可变系统” 的激进实验：/nix/store 与 /ostree 底层逻辑： 传统的 / 根目录是“可变的”（Mutable），你用 apt 或 yum 安装软件，文件系统就被修改了，一旦中断或出错，系统可能就崩了，且无法回滚。 文件系统映射： /nix/store (NixOS)： 这可以说是对 FHS 最彻底的颠覆。系统里所有的软件、库甚至配置文件，全部存放在这里，目录名是一长串哈希值（如 /nix/store/x8z...-glibc-2.32/）。系统的 /bin 和 /etc 全部变成了指向这里的软链接。 /ostree (如 Fedora Silverblue)： 类似于操作系统的 Git 仓库。整个系统的核心文件被按版本管理，更新系统就像 git pull，出错了一键切回上一个 commit。 开发意义： 实现了绝对的 “环境可复现”。无论你在哪台机器上部署，只要配置清单一样，构建出来的文件树在哈希级别完全一致。 4. 彻底抛弃 GNU 工具链的变种：Android 的目录体系 虽然 Android 的内核是 Linux，但它完全抛弃了标准的 FHS（因为它不需要支持传统的 GNU 工具链），重构了一套为 “App 沙盒” 和 “硬件厂商解耦” 而生的目录： /system： 相当于 Linux 的 /usr，但是由 Google 和手机厂商打包的只读系统镜像。 /vendor： 专属硬件驱动层（高通、联发科的闭源驱动包）。这让 Google 可以单独升级 /system 而不需要硬件厂商配合重新编译驱动（Project Treble）。 /data： 相当于高度隔离的 /home 和 /var 的结合体。每个 App 都有严格分配的 /data/data/com.xxx.app/ 目录，相互之间即使通过绝对路径也无法跨越权限读取。 5. 主板固件的物理桥梁：/boot/efi 底层逻辑： 早期的 BIOS 只要读硬盘前 512 字节（MBR）就能引导系统。现代的 UEFI 标准要求硬盘必须有一个专门的 FAT32 格式分区，主板固件只能读懂这个分区的格式。 文件系统映射： /boot/efi。这是 Linux 向主板妥协的产物。无论你用多么高级的 EXT4、BTRFS 还是 ZFS 格式化 Linux，你都必须留一个小小的 FAT32 分区挂载在这里，把引导程序（如 GRUB 的 .efi 文件）放在这，主板通电后才能找到操作系统的大门。 桌面、跨系统与云端 在现代桌面软件开发、跨系统混合开发以及云服务器自动化部署的场景中，还存在几个绕不开的核心目录规范。这些规范由开源社区或云厂商为了解决实际痛点而建立，是 FHS 在特定领域的自然延伸。 1. 桌面级应用的数据归宿：XDG Base Directory 规范 底层逻辑： 早期的 Linux 系统里，各种软件喜欢在用户的 /home/user/ 目录下到处留下隐藏文件（比如 .bashrc, .viminfo, .nvm）。这导致用户的家目录极其混乱。为了解决这个问题，freedesktop.org 提出了 XDG 标准，规定用户级应用的数据必须分类存放。 文件系统映射： ~/.config/ (用户级配置)： 如果你用 Qt 等框架开发了一个带有复杂 GUI 的桌面应用程序（比如一个炫酷的音乐播放器），它的用户偏好设置、自定义的主题 QSS 样式文件，就不应该直接扔在 ~ 下，而应该存放在 ~/.config/你的应用名/ 里。 ~/.local/share/ (用户级数据)： 存放该应用产生的、特定于用户的数据。比如你的程序扫描本地音乐库生成的 SQLite 数据库文件、或者用户自定义的播放列表。 ~/.cache/ (用户级缓存)： 存放可以随时丢弃、不影响核心功能的数据。比如音乐播放过程中实时计算出的 FFT 频谱缓存，或者是从网络上拉取的专辑封面缩略图。 开发意义： 遵循这个规范开发软件，能让用户的家目录保持整洁，同时在做系统备份时，用户可以放心地忽略 ~/.cache/ 目录，极大地节省存储空间和时间。 2. 跨系统融合的魔法桥梁：WSL2 与 9P 协议挂载 底层逻辑： 当系统不再是纯粹的物理机，而是在宿主操作系统（Windows）内部运行的高级轻量级虚拟机时，如何让两个完全不同的文件系统（NTFS 和 Ext4）高效互通？ 文件系统映射： /mnt/c, /mnt/d 等： 在使用 WSL2 进行开发时，Windows 的物理磁盘会被自动挂载到 Linux 的 /mnt 目录下。 原理： 这里并不是简单的磁盘共享。Windows 使用的是 DrvFs 插件，底层通过 9P 网络协议（一个源自贝尔实验室 Plan 9 系统的分布式文件系统协议）来实现跨 OS 的文件访问。它需要在 Linux 的 POSIX 权限模型（rwx，所有者/组）和 Windows 的 ACL（访问控制列表）之间做复杂的实时翻译。这也是为什么在 WSL2 里跨文件系统（比如在 Linux 里编译 /mnt/c 下的 C++ 项目）进行高频 I/O 操作会比较慢的原因：每一次读写，都要经过协议层的翻译。 3. 云端实例的诞生印记：cloud-init 与 /var/lib/cloud 底层逻辑： 当你在云服务商（比如 GCP、AWS）那里点击 “创建一台 Ubuntu 服务器” 时，云厂商并不是现场给你安装系统，而是直接克隆一个标准的、一模一样的只读系统镜像。那么问题来了：这个标准镜像开机后，是怎么知道你的主机名、网络配置，以及最关键的——怎么把你的 SSH 公钥注入进系统的？ 文件系统映射： /var/lib/cloud/： 这是现代云服务器自动初始化的“核心现场”。 工作机制： 系统第一次开机时，一个叫 cloud-init 的核心服务会抢先运行。它会通过内部网络去访问云厂商的元数据服务器（Metadata Server），拉取你在这台实例上配置的专属数据（比如 SSH Key），然后把这些状态和执行日志保存在 /var/lib/cloud/instances/ 下，最后再把 Key 写入到你的 ~/.ssh/authorized_keys 里。当你成功通过 SSH 连接上这台服务器时，这套复杂的“认主”流程其实已经在文件系统的这个角落里悄悄走完了。 内核机制与底层调试 到了这个深度，我们已经脱离了传统意义上的 “文件系统规范”，触及到了 Linux 内核机制在用户态的强行投影。这里是底层驱动开发者、固件极客和内核调试专家的领域。 1. 路由器“不死之身”的秘密：/rom 与 /overlay 在高度定制的嵌入式网络设备（如 OpenWrt / ImmortalWrt）中，你可以在根目录 / 下任意修改配置文件，但如果系统崩溃，只要执行“恢复出厂设置”，一切又能完美如初。 第一性原理： 嵌入式设备的 Flash 闪存寿命有限，且出厂固件必须保证绝对安全，不能被意外写坏。 文件系统映射： /rom (只读层)： 这里存放的是系统刷入时的高压缩比只读文件系统（通常是 SquashFS）。它是系统的“底片”，绝对不可写。 /overlay (可写层)： 这是一个极小的可读写分区（通常是 JFFS2 或 F2FS）。 底层机制（联合挂载）： 当系统启动时，内核会将 /rom 和 /overlay 像千层饼一样叠在一起，合并成你看到的根目录 /。如果你修改了 /etc/config/network，系统其实并没有修改 /rom 里的原始文件，而是在 /overlay/etc/config/ 下新建了一个同名文件把它 “遮盖” 住了。所谓 “恢复出厂设置”，在物理层面上仅仅是执行了一句 rm -rf /overlay/*。 2. 裸机闪存的真面目：/dev/mtd 与 /dev/mtdblock 当你脱离了电脑的 SATA/NVMe 硬盘，去给 ARM 开发板或路由器编写底层固件时，你会发现 /dev/sda 这种块设备消失了。 第一性原理： 原始的 NAND/NOR Flash 闪存芯片与普通硬盘有着本质的物理差异。硬盘可以按扇区随意读写，但原始 Flash 必须“先擦除（Erase），后写入（Write）”，且具有坏块（Bad Block）特性。 文件系统映射： Linux 专门引入了 MTD (Memory Technology Device) 子系统。 /dev/mtdX（字符设备）： 允许底层工具直接对物理闪存的 Block 进行擦除和读写操作（比如用 flashcp 烧录 Bootloader）。 /dev/mtdblockX（块设备）： 内核在这个接口上封装了一层软件逻辑（如磨损均衡），让上层应用能勉强把它当成普通硬盘来挂载。 3. 容器“视界”的物理边界：/proc/[pid]/ns/ 我们前面提到了 Docker 用 /sys/fs/cgroup 来限制 CPU 和内存，用 OverlayFS 来隔离文件。但这还不够，如何让容器里的进程认为自己是“系统里唯一的进程”，且拥有独立的网卡？ 第一性原理： 容器并不是真正的虚拟机，它只是宿主机上的一个普通进程。我们需要一种机制，在内核层面对这个进程进行“感官剥夺”和“视觉欺骗”。 文件系统映射： Namespaces（命名空间）。 如果你进入 /proc/某个进程PID/ns/ 目录，你会看到一堆神奇的软链接：mnt (挂载点隔离), net (网络隔离), pid (进程号隔离), ipc (进程通信隔离)。 开发意义： 当 Docker 启动一个容器时，它其实是通过系统调用，为这个进程创建了一套全新的 Namespace 文件链接。只要两个进程的 net 命名空间指向不同的哈希值，它们就处在物理隔离的两个网络平行宇宙中。这也是现代云原生虚拟化技术的绝对基石。 4. 内核开发者的 “后门”：/sys/kernel/debug/ (DebugFS) 第一性原理： 如果你正在为一块新板子写 I2C 或 SPI 驱动，代码跑在内核态（Ring 0），一旦出错系统直接 Kernel Panic（死机）。你没法用 printf 打印日志，也没法用普通的 GDB 调试。 文件系统映射： Linux 提供了 DebugFS（通常挂载在 /sys/kernel/debug/）。 底层机制： 这是一个完全没有规矩、不受任何标准约束的 RAM 文件系统。内核开发者可以在驱动代码里写几行简单的 API，就能把内核深处极其敏感的变量（比如当前寄存器的原始状态、内存碎片的分布图、电源管理芯片的实时微伏电压）直接以文本文件的形式暴露在这个目录下。系统一旦发布到生产环境，这个目录通常会被严密封锁或卸载。 5. 死亡瞬间的黑匣子：/sys/fs/pstore (Persistent Storage) 第一性原理： 当系统发生极其严重的致命错误（Kernel Panic）时，内核会瞬间崩溃。此时，硬盘驱动可能已经失效，中断已经被屏蔽，系统根本无法将崩溃日志写进常规的 /var/log/messages 中。那重启后，我们怎么知道死机前一毫秒发生了什么？ 底层机制： Linux 引入了 pstore 文件系统。当内核判定自己即将死亡时，它会绕过所有复杂的 IO 堆栈，将最后遗言（oops 报错、寄存器状态）直接写入一段特殊的非易失性内存（NVRAM）或主板上即使热重启也不会断电的保留 RAM 区域。 物理映射： 系统重启后，你会在 /sys/fs/pstore/ 下看到名为 dmesg-efi-xxx 或 console-ramoops 的文件。读取它，你就能看到系统上一次死亡瞬间的“走马灯”。这是底层嵌入式设备和服务器排查玄学死机问题的终极武器。 6. 现代内核的上帝视角：/sys/fs/bpf (eBPF Maps) 第一性原理： 传统的 Linux 网络和安全过滤（比如 iptables）是在内核里写死逻辑，用户只能配规则。但现代 Linux 引入了 eBPF 技术，允许用户把一段自己写的 C 代码，经过极其严格的安全审查后，直接注入到内核的运行沙箱中执行（极其恐怖的特权）。 底层机制： 注入到内核的代码怎么和用户态的用户程序交换庞大的数据（比如实时的网络流量统计）？靠普通的系统调用太慢了。 物理映射： eBPF 引入了 Map（映射表）的概念，并将其挂载在 /sys/fs/bpf 下。这表面上是个目录，但里面的“文件”其实是用户态和内核态共享的高速内存数据结构（如 Hash 表、数组）。通过读写这里，你的程序就像是直接插了一根导管到了内核的大动脉上。 7. 强制访问控制的终极监狱：/sys/fs/selinux (或 smacks/apparmor) 第一性原理： 标准的 FHS 只有 rwx（读写执行）三种权限，只要你是 Root，系统就对你完全敞开。但在高安防环境（如国防、金融或 Android 手机底层）中，这太危险了。 底层机制： 也就是所谓的 MAC（强制访问控制）。即使你是 Root 用户，如果你运行的进程（比如 Web 服务器）没有被赋予读取特定标签文件的权限，内核也会在底层直接把你的操作斩断。 物理映射： /sys/fs/selinux 是这套庞大安全引擎的控制面板。里面的文件控制着内核的安全策略布尔值。比如 enforce 文件，写入 1 就是开启强制拦截，写入 0 就是只记录不拦截。这里定义了系统里每一根发丝的访问规则。 8. 幽灵文件系统：/net 或 /misc (AutoFS) 第一性原理： 挂载一个局域网内的 NFS（网络文件系统）是极其消耗资源的。如果我们有 100 个网络存储，不能一开机全挂载上，但又希望用户想用的时候随时能找到。 底层机制： 按需挂载。 物理映射： 当你 ls /net/server_ip/share 的时候，这个目录原本在物理上是不存在的。但是当你敲下回车触发访问异常的瞬间，内核的 autofs 模块会瞬间拦截这个请求，在后台光速完成网络握手和挂载，然后再把结果返回给你。从你的视角看，这个文件系统就像幽灵一样，你注视它时它就存在，你离开时它就消失。 真正的绝对底部：VFS (Virtual File System) 如果我们要进行最后一次第一性原理的推演，那就是：整个根目录 / 本身，就是一个虚假的幻觉。 在 Linux 极早期的源代码中，并没有所谓的目录树。所有的硬盘、内存映射、设备节点，之所以能呈现出统一的层级结构，完全是因为 Linux 内核中存在一个极其伟大的抽象层 —— VFS（虚拟文件系统）。 你在终端里敲下 cd / 和 ls 看到的所有东西，本质上都是内核在内存中维护的一张巨大的结构体图（struct dentry 和 struct inode）。 Ext4 硬盘文件是 VFS 的子类。 /sys 设备树是 VFS 的子类。 /proc 进程信息是 VFS 的子类。 /run 内存状态也是 VFS 的子类。 走到这里，文件系统这座迷宫已经到了尽头。再往前迈一步，就已经没有 “文件” 和 “目录” 的概念了——前方是纯粹的 C 语言指针、内存分页、自旋锁，以及 Linux 内核的源代码汪洋。 回头再看，FHS 的意义也就更清楚了。它并不是要替 Linux 的一切目录现象提供最终答案，而是先给出一套最基础、也最耐用的分层原则：哪些是系统骨架，哪些是运行状态，哪些是用户数据，哪些是共享资源，哪些是本机专属。 而现代 Linux 做的事情，则是在这套骨架之上不断往前延伸：把启动过程、容器隔离、嵌入式存储、云端初始化、内核调试乃至运行时安全策略，继续投影到这棵目录树上。于是你看到的，就不再只是一个“文件该放哪”的标准，而是一整套从静态布局延伸到动态运行的目录体系。]]></summary></entry><entry><title type="html">英语语法综述</title><link href="https://bfmhno3.github.io/note/overview-of-english-grammar/" rel="alternate" type="text/html" title="英语语法综述" /><published>2026-04-06T09:00:00+00:00</published><updated>2026-04-06T09:00:00+00:00</updated><id>https://bfmhno3.github.io/note/overview-of-english-grammar</id><content type="html" xml:base="https://bfmhno3.github.io/note/overview-of-english-grammar/"><![CDATA[<p>我们为什么学语法？</p>

<p class="notice--info"><del>当然是为了考高分啊😂</del></p>

<p>其实，所有的英语语法都只有一个目的：<strong>造句</strong>。</p>

<p>英语和中文一样，我们说话写文章有的时候用短句子，有的时候用长句子。不同的短句子又可以组合成新的长句。但这里说的是句子的长度，并不适合语法讨论。</p>

<p>我们只有把句子拆开来，直到拆到不能再继续拆了，否则句意会不完整的 “基本句”，才好研究语法。因为如果 “基本句” 研究透了，长句子就是它们的组合。</p>

<h2 id="1-简单句simple-sentence">1. 简单句（Simple Sentence）</h2>

<p>我们把这样的不能再拆的 “基本句” 称作<strong>简单句</strong>（Simple Sentence）。而这样的简单句，除去那些<strong>感叹句</strong>（“嗯”，“哦”，“啊”等）、<strong>省略句</strong>、<strong>（向整句提问的）问句</strong>，几乎全部都是在说：</p>

<p class="notice--info"><strong>什么</strong> + <strong>怎么样</strong></p>

<p>绝大多数句子粗略地分一共就只有这两个部分，而这个 “什么” 和 “怎么样” 分别对应了两个最基本的<strong>句子成分</strong>：</p>

<ul>
  <li>“什么”： <strong>主语</strong>（Subject）。</li>
  <li>“怎么样”：<strong>谓语</strong>（Predicate）。</li>
</ul>

<p>换句话说，几乎所有的英语句子结构都是：</p>

<p class="notice--info"><strong>主语</strong> + <strong>谓语</strong></p>

<p>这里的<strong>主语</strong>一般是人或物，不管是抽象还是具体。而这里的<strong>谓语</strong>则是指一个广义的动作或发生的事情。</p>

<p class="notice--warning">这个 “动作” 不是我们平时狭义说的要动起来的 “动作”，而这个广义的 “动作” 也就是我们语法上说的<strong>动词</strong>（Verb）。</p>

<h2 id="2-谓语动词">2. 谓语动词</h2>

<table>
  <thead>
    <tr>
      <th style="text-align: center">序号</th>
      <th style="text-align: left">动词类型</th>
      <th style="text-align: left">解释</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">1</td>
      <td style="text-align: left">不及物动词</td>
      <td style="text-align: left">可以独立完成的动作</td>
    </tr>
    <tr>
      <td style="text-align: center">2</td>
      <td style="text-align: left">单及物动词</td>
      <td style="text-align: left">有一个动作的承受者</td>
    </tr>
    <tr>
      <td style="text-align: center">3</td>
      <td style="text-align: left">双及物动词</td>
      <td style="text-align: left">有两个动作承受者</td>
    </tr>
    <tr>
      <td style="text-align: center">4</td>
      <td style="text-align: left">复杂及物动词</td>
      <td style="text-align: left">只有一个动作承受者</td>
    </tr>
    <tr>
      <td style="text-align: center">5</td>
      <td style="text-align: left">系动词</td>
      <td style="text-align: left">非 “动作”</td>
    </tr>
  </tbody>
</table>

<h3 id="21-不及物动词intransitive-verb">2.1 不及物动词（Intransitive Verb）</h3>

<p><strong>不及物动词</strong>（Intransitive Verb）就是指<strong>可以独立完成的动作</strong>，例如：</p>

<blockquote>
  <p>Papa Rabbit sleeps.</p>

  <p>兔老爹睡觉。</p>
</blockquote>

<p>类似 “sleeps” 这样的没有承受者的动词就是<strong>不及物动词</strong>（Intransitive Verb）。</p>

<p>不及物动词所对应的基本句子结构就是：</p>

<p class="notice--success"><strong>主语</strong> + <strong>不及物动词</strong></p>

<h3 id="22-单及物动词monotransitive-verb">2.2 单及物动词（Monotransitive Verb）</h3>

<blockquote>
  <p>Papa Rabbit likes you.</p>

  <p>兔老爹喜欢你。</p>
</blockquote>

<p>如果这里只说 “兔老爹喜欢”，你肯定觉得意思不完整，因为像 “喜欢” 这样的动词没有承受者就没有什么实际意义，这样的动词就属于<strong>及物动词</strong>（Transitive Verb），而这个动作承受者就是<strong>宾语</strong>（Object）。</p>

<p>具体而言，对于只有一个宾语及物动词，我们称为<strong>单及物动词</strong>（Monotransitive Verb）。</p>

<p>单及物动词对应的基本句子结构就是：</p>

<p class="notice--success"><strong>主语</strong> + <strong>单及物动词</strong> + <strong>宾语</strong></p>

<h3 id="23-双及物动词ditransitive-verbs">2.3 双及物动词（Ditransitive Verbs）</h3>

<blockquote>
  <p>Papa Rabbit teaches you English.</p>

  <p>兔老爹教你英语。</p>
</blockquote>

<p>这里的核心动词是 “teach”（教），但是教授的知识是 “英语”，而知识的接收对象是 “你”。在语法上，我们把这样的动词的两个承受者分别称为<strong>直接宾语</strong>（Direct Object）和<strong>间接宾语</strong>（Indirect Object）。</p>

<p>如果只说 “兔老爹教英语”，其实语义已经完整了，所以 “English” 这里就是<strong>直接宾语</strong>，而如果只说 “兔老爹教你”，而又没有上下文的话，你肯定觉得还缺了什么，所以 “you” 在这里是间接宾语。</p>

<p>这样的往往既有直接宾语又有间接宾语的动词，也是及物动词的一种，更具体地说，它属于<strong>双及物动词</strong>（Ditransitive Verbs）。</p>

<p>双及物动词对应的基本句子结构就是：</p>

<p class="notice--success"><strong>主语</strong> + <strong>双及物动词</strong> + <strong>间接宾语</strong> + <strong>直接宾语</strong></p>

<h3 id="24-复杂及物动词complex-transitive-verb">2.4 复杂及物动词（Complex-transitive Verb）</h3>

<blockquote>
  <p>Papa Rabbit considers you smart.</p>

  <p>兔老爹认为你聪明。</p>
</blockquote>

<p>这里虽然只有一个动作的承受者 “you”，但如果只说 “兔老爹认为你”，你肯定会觉得话没有说完，但是宾语 “you” 后面的 “smart” 却也不像双及物动词那样是另一个动作承受者。</p>

<p>这样的动词，必须要有个补充承受者的信息才意义完整，而这个补充的信息，我们在语法上称为<strong>补足语</strong>或<strong>补语</strong>（Complement）。更明确也可以说是<strong>宾语补语</strong>（Object Complement）。</p>

<p>这样的需要<strong>补语</strong>的动词，我们称之为<strong>复杂及物动词</strong>（Complex-transitive Verb）</p>

<p>复杂及物动词对应的基本句子结构就是：</p>

<p class="notice--success"><strong>主语</strong> + <strong>复杂及物动词</strong> + <strong>宾语</strong> + <strong>（宾语）补语</strong></p>

<h3 id="25-系动词linking-verb">2.5 系动词（Linking Verb）</h3>

<p>这种动作所表示的含义与狭义上的 “动作” 不太一样：</p>

<blockquote>
  <p>Papa Rabbit is tall.</p>

  <p>兔老爹是高的。</p>
</blockquote>

<p>这里的 “is” 在中文里经常翻译成 “是”，但它实际上的作用很简单：</p>

<p class="notice--danger">就是把这个动词之后的信息<strong>赋予</strong>前面的主语。</p>

<p>这里的 “is” 说白了就是把 “Papa Rabbit” 和 “tall” 连在一起，划上等号而已。或者说，把后者信息赋予前者。如果把 “tall” 换成 “in the room”：</p>

<blockquote>
  <p>Papa Rabbit is in the room.</p>

  <p>兔老爹在房间里。</p>
</blockquote>

<p>其实也是把 “in the room” 这个状态性质赋予 “Papa Rabbit” 而已。</p>

<blockquote>
  <p>Papa Rabbit looks tall.</p>

  <p>兔老爹看起来高。</p>
</blockquote>

<p>这个 “look” 通常意义是 “看”，但是这里到底是谁在看？其实不是 “Papa Rabbit” 在看，”Papa Rabbit” 在这里是<strong>被看</strong>。这里的 “looks” 其实也是把后面的 “tall” 赋予前面的主语 “Papa Rabbit” 而已，也算是划等号，只不过这个划等号比之前的 “is” 多了个 “看上去”。</p>

<blockquote>
  <p>Papa Rabbit smells nice.</p>

  <p>兔老爹闻上去香。</p>
</blockquote>

<p>这里的 “smells” 还是把 “Papa Rabbit” 和 “nice” 划等号，只不过还有 “闻上去” 这层意思。</p>

<p>像这样的赋予主语某种性质状态的 “划等号” 的动词，我们称之为<strong>连系动词</strong>（Linking Verb），也称为<strong>系动词</strong>。</p>

<p>系动词后面的补充信息自然就是<strong>补足语</strong>或<strong>补语</strong>，更精确地说是<strong>主语补语</strong>，而<strong>主语补语</strong>在中国的英语教学中又被称为<strong>表语</strong>（Predicative）。</p>

<p>系动词对应的基本句子结构就是：</p>

<p class="notice--success"><strong>主语</strong> + <strong>系动词</strong> + <strong>（主语）补语/表语</strong></p>

<h2 id="3-五大基本句型">3. 五大基本句型</h2>

<table>
  <thead>
    <tr>
      <th style="text-align: center">序号</th>
      <th style="text-align: left">句型</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">1</td>
      <td style="text-align: left">主语 + 不及物动词</td>
    </tr>
    <tr>
      <td style="text-align: center">2</td>
      <td style="text-align: left">主语 + 单及物动词 + 宾语</td>
    </tr>
    <tr>
      <td style="text-align: center">3</td>
      <td style="text-align: left">主语 + 双及物动词 + 间接宾语 + 直接宾语</td>
    </tr>
    <tr>
      <td style="text-align: center">4</td>
      <td style="text-align: left">主语 + 复杂及物动词 + 宾语 + 宾语补语</td>
    </tr>
    <tr>
      <td style="text-align: center">5</td>
      <td style="text-align: left">主语 + 系动词 + 主语补语/表语</td>
    </tr>
  </tbody>
</table>

<div class="notice">
  <p>还有一种 “八大句型” 的分类，即在 “五大句型” 基础上增加：</p>

  <ol start="6">
  <li><strong>there be</strong> 句型，如：There is a rabbit. 可理解为第 5 种句型 “主语 + 系动词 + 主语补语/表语” 的倒装。</li>
  <li><strong>主语</strong> + <strong>谓语</strong> + <strong>状语</strong>，如：I live in China. 可理解为第 1 种句型 “主语 + 不及物动词” 的延伸（虽然这里的状语很重要）。</li>
  <li><strong>主语</strong> + <strong>谓语动词</strong> + <strong>宾语</strong> + <strong>状语</strong>，如：I put the carrot on the table. 可理解为第 4 种句型 “主语 + 复杂及物动词 + 宾语 + 宾语补语” 的延伸。</li>
</ol>
</div>

<div class="notice--warning">
  <p>严格说来，谓语和谓语动词是有区别的。句子主语后面余下的部分就是谓语，谓语动词只是谓语的一部分。</p>

  <p>但是在平时，通俗地，为了方便，很多人会直接称呼谓语动词为谓语，而把 “主语 + 谓语动词 + 宾语” 这样的句子结构直接称呼为 “主语 + 谓语 + 宾语”（或 “主谓宾”），这也不是什么大问题。</p>
</div>

<h2 id="4-句子基本成分">4. 句子基本成分</h2>

<table>
  <thead>
    <tr>
      <th style="text-align: center">序号</th>
      <th style="text-align: left">成分</th>
      <th style="text-align: left">英文名</th>
      <th style="text-align: left">解释</th>
      <th style="text-align: left">例句</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">1</td>
      <td style="text-align: left">主语</td>
      <td style="text-align: left">Subject</td>
      <td style="text-align: left">动作的发出者或被讨论的对象</td>
      <td style="text-align: left">**Papa Rabbit** eats carrots.</td>
    </tr>
    <tr>
      <td style="text-align: center">2</td>
      <td style="text-align: left">谓语动词</td>
      <td style="text-align: left">Predicate Verb</td>
      <td style="text-align: left">表示具体的动作或状态</td>
      <td style="text-align: left">Papa Rabbit **eats** carrots.</td>
    </tr>
    <tr>
      <td style="text-align: center">3</td>
      <td style="text-align: left">宾语</td>
      <td style="text-align: left">Object</td>
      <td style="text-align: left">动作的承受者</td>
      <td style="text-align: left">Papa Rabbit eats **carrots**.</td>
    </tr>
    <tr>
      <td style="text-align: center">4</td>
      <td style="text-align: left">宾语补语</td>
      <td style="text-align: left">Object Complement</td>
      <td style="text-align: left">补充说明宾语的状态或特征</td>
      <td style="text-align: left">Papa Rabbit considers you **smart**.</td>
    </tr>
    <tr>
      <td style="text-align: center">5</td>
      <td style="text-align: left">主语补语</td>
      <td style="text-align: left">Subject Complement</td>
      <td style="text-align: left">补充说明主语的性质状态（即表语）</td>
      <td style="text-align: left">Papa Rabbit is **tall**.</td>
    </tr>
    <tr>
      <td style="text-align: center">6</td>
      <td style="text-align: left">定语</td>
      <td style="text-align: left">Attributive</td>
      <td style="text-align: left">修饰主语或宾语</td>
      <td style="text-align: left">**The little** white rabbit ate **a large** carrot.</td>
    </tr>
    <tr>
      <td style="text-align: center">7</td>
      <td style="text-align: left">状语</td>
      <td style="text-align: left">Adverbial</td>
      <td style="text-align: left">修饰谓语动词</td>
      <td style="text-align: left">The rabbit ate **quickly**.</td>
    </tr>
    <tr>
      <td style="text-align: center">8</td>
      <td style="text-align: left">同位语</td>
      <td style="text-align: left">Appositive|</td>
      <td style="text-align: left">Papa Rabbit, **an English teacher**, eats carrots.</td>
      <td style="text-align: left"> </td>
    </tr>
  </tbody>
</table>

<p class="notice--info">你可能听过 “<strong>插入语</strong>”，如：This carrot, I think(插入语), is very tasty. 但是插入语是独立的，不作句子成分。</p>

<h2 id="5-复杂句">5. 复杂句</h2>

<p>直到现在所说的都是<strong>简单句</strong>（Simple Sentence），说白了就是没法再拆成更多句子了。但是我们说话写文章可不全是简单句，而是经常会把这些简单句互相组合形成所谓<strong>复合句</strong>（Compound Sentence）和<strong>复杂句</strong>（Complex Sentence）。</p>

<p><strong>复合句</strong>说白了就是句子简单的叠加，句子和句子之间是<strong>并列关系</strong>，因此也被称为<strong>并列句</strong>。</p>

<p><strong>复杂句</strong>则是把一个句子套在另一个句子里，允许不断嵌套，是<strong>从属关系</strong>。</p>

<p>一个句子套另一个句子，在英语语法上分别叫做<strong>主句</strong>（Main Clause）和<strong>从句</strong>（Subordinate Clause）。</p>

<p>从句说白了就是把简单句修改一下来充当另一个句子的句子成分。</p>

<div style="max-width: 520px; margin: 1rem auto;">

  <p><img class="mermaid" src="https://mermaid.ink/svg/eyJjb2RlIjoiZmxvd2NoYXJ0IFRCXG5zdWJncmFwaCBBW1wi5Li75Y-lPGJyLz5NYWluIENsYXVzZVwiXVxuc3BhY2VyW1wiIFwiXTo6OmludmlzXG5CW1wi5LuO5Y-" /></p>

</div>

<h3 id="51-从句类型">5.1 从句类型</h3>

<table>
  <thead>
    <tr>
      <th style="text-align: center">序号</th>
      <th style="text-align: left">从句</th>
      <th style="text-align: left">作用</th>
      <th style="text-align: left">补充</th>
      <th style="text-align: left">例句</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">1</td>
      <td style="text-align: left">主语从句</td>
      <td style="text-align: left">作主语</td>
      <td style="text-align: left">名词性从句</td>
      <td style="text-align: left">**What Papa Rabbit ate** is a carrot.</td>
    </tr>
    <tr>
      <td style="text-align: center">2</td>
      <td style="text-align: left">宾语从句</td>
      <td style="text-align: left">作宾语</td>
      <td style="text-align: left">名词性从句</td>
      <td style="text-align: left">I know **that Papa Rabbit likes carrots**.</td>
    </tr>
    <tr>
      <td style="text-align: center">3</td>
      <td style="text-align: left">表语从句</td>
      <td style="text-align: left">作表语</td>
      <td style="text-align: left">名词性从句</td>
      <td style="text-align: left">The truth is **that he likes you**.</td>
    </tr>
    <tr>
      <td style="text-align: center">4</td>
      <td style="text-align: left">同位语从句</td>
      <td style="text-align: left">作同位语</td>
      <td style="text-align: left">名词性从句</td>
      <td style="text-align: left">The fact **that he ate the carrot** is obvious.</td>
    </tr>
    <tr>
      <td style="text-align: center">5</td>
      <td style="text-align: left">定语从句</td>
      <td style="text-align: left">作定语|</td>
      <td style="text-align: left">The rabbit **who ate the carrot** is my papa.</td>
      <td style="text-align: left"> </td>
    </tr>
    <tr>
      <td style="text-align: center">6</td>
      <td style="text-align: left">状语从句</td>
      <td style="text-align: left">作状语|</td>
      <td style="text-align: left">I will leave **when Papa Rabbit comes**.</td>
      <td style="text-align: left"> </td>
    </tr>
  </tbody>
</table>

<h2 id="6-词性">6. 词性</h2>

<p>但是现在问题来了，两个句子也许句子组成的方式相同，比如都是 “主语 + 谓语动词 + 宾语”，可是句中包含的词的类型并不一定相同，比如：</p>

<blockquote>
  <p><em>The rabbit</em> <strong>ate</strong> <del>a carrot</del>.</p>

  <p><em>He</em> <strong>saw</strong> <del>something over there</del>.</p>
</blockquote>

<p>很显然，这两句话 “句型” 相同，但是包含的词却不太一样。也就是说，同一类句子成分里可能有不同的词类（或词类），这个概念中文中也有。</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">序号</th>
      <th style="text-align: left">词性</th>
      <th style="text-align: left">英文名</th>
      <th style="text-align: left">解释</th>
      <th style="text-align: left">例句</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">1</td>
      <td style="text-align: left">动词</td>
      <td style="text-align: left">verb</td>
      <td style="text-align: left">表示动作或状态</td>
      <td style="text-align: left">Papa Rabbit **eats** a carrot.</td>
    </tr>
    <tr>
      <td style="text-align: center">2</td>
      <td style="text-align: left">名词</td>
      <td style="text-align: left">noun</td>
      <td style="text-align: left">表示人或物</td>
      <td style="text-align: left">Papa Rabbit is a **rabbit**.</td>
    </tr>
    <tr>
      <td style="text-align: center">3</td>
      <td style="text-align: left">冠词</td>
      <td style="text-align: left">article</td>
      <td style="text-align: left">用于说明人和事物</td>
      <td style="text-align: left">Papa Rabbit is **a** rabbit.</td>
    </tr>
    <tr>
      <td style="text-align: center">4</td>
      <td style="text-align: left">代词</td>
      <td style="text-align: left">pronoun</td>
      <td style="text-align: left">替代人和物</td>
      <td style="text-align: left">**I** am a rabbit.</td>
    </tr>
    <tr>
      <td style="text-align: center">5</td>
      <td style="text-align: left">形容词</td>
      <td style="text-align: left">adjective</td>
      <td style="text-align: left">形容人和物</td>
      <td style="text-align: left">I am a **smart** rabbit.</td>
    </tr>
    <tr>
      <td style="text-align: center">6</td>
      <td style="text-align: left">数词</td>
      <td style="text-align: left">numeral</td>
      <td style="text-align: left">表数量</td>
      <td style="text-align: left">I ate **two** carrots.</td>
    </tr>
    <tr>
      <td style="text-align: center">7</td>
      <td style="text-align: left">副词</td>
      <td style="text-align: left">adverb</td>
      <td style="text-align: left">修饰动作或形容词</td>
      <td style="text-align: left">I ate two carrots **quickly**.</td>
    </tr>
    <tr>
      <td style="text-align: center">8</td>
      <td style="text-align: left">介词</td>
      <td style="text-align: left">preposition</td>
      <td style="text-align: left">表示和其他词关系的词</td>
      <td style="text-align: left">I ate two carrots **with** chopsticks.</td>
    </tr>
    <tr>
      <td style="text-align: center">9</td>
      <td style="text-align: left">叹词</td>
      <td style="text-align: left">interjection</td>
      <td style="text-align: left">表感叹</td>
      <td style="text-align: left">**Ah**, the carrot is tasty!</td>
    </tr>
    <tr>
      <td style="text-align: center">10</td>
      <td style="text-align: left">连词</td>
      <td style="text-align: left">conjunction</td>
      <td style="text-align: left">连接词和句</td>
      <td style="text-align: left">I ate two carrots and a potato, **because** I was hungry.</td>
    </tr>
  </tbody>
</table>

<p class="notice--danger"><strong>句子成分</strong>（Clause Element）和句子中的<strong>词类</strong>（Parts of Speech）是完全不同的概念。</p>

<p>前文所述的每一种句子成分，除了<strong>谓语动词</strong>只能是动词，其他成分都可能包含不同的词类。</p>

<h2 id="7-谓语动词的-三大本领">7. 谓语动词的 “三大本领”</h2>

<p>英语中的谓语动词有很多厉害的本领，而这些本领，中文中的动词都没有，是英语语法的难点之一：</p>

<ol>
  <li>动作时间</li>
  <li>动作状态</li>
  <li>动作假设，情感等</li>
</ol>

<p class="notice--warning">所谓 “中文动词没有这些本领” 是说中文动词本身没有 “时、体、气” 的概念，但是中文当然有相同或相似意思的表达。</p>

<h3 id="71-时态">7.1 时态</h3>

<p>英语的时态就是<strong>时间</strong> + <strong>状态</strong>合在一起，而并非只有时间。</p>

<table>
  <thead>
    <tr>
      <th style="text-align: left">|</th>
      <th style="text-align: left">**现在**</th>
      <th style="text-align: left">**过去**</th>
      <th style="text-align: left">**将来**</th>
      <th style="text-align: left">**过去将来**</th>
      <th> </th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: left"> </td>
      <td style="text-align: left">*一般*</td>
      <td style="text-align: left">*一般***现在**</td>
      <td style="text-align: left">*一般***过去**</td>
      <td style="text-align: left">*一般***将来**</td>
      <td>*一般***过去将来**</td>
    </tr>
    <tr>
      <td style="text-align: left"> </td>
      <td style="text-align: left">*完成*</td>
      <td style="text-align: left">**现在***完成*</td>
      <td style="text-align: left">**过去***完成*</td>
      <td style="text-align: left">**将来***完成*</td>
      <td>**过去将来***完成*</td>
    </tr>
    <tr>
      <td style="text-align: left"> </td>
      <td style="text-align: left">*进行*</td>
      <td style="text-align: left">**现在***进行*</td>
      <td style="text-align: left">**过去***进行*</td>
      <td style="text-align: left">**将来***进行*</td>
      <td>**过去将来***进行*</td>
    </tr>
    <tr>
      <td style="text-align: left"> </td>
      <td style="text-align: left">*完成进行*</td>
      <td style="text-align: left">**现在***完成进行*</td>
      <td style="text-align: left">**过去***完成进行*</td>
      <td style="text-align: left">**将来***完成进行*</td>
      <td>**过去将来***完成进行*</td>
    </tr>
  </tbody>
</table>

<p class="notice--info">时态是时间的排列组合，并不是一个线性的列表，更不是只和时间有关。</p>

<h3 id="72-语气">7.2 语气</h3>

<table>
  <thead>
    <tr>
      <th style="text-align: center">序号</th>
      <th style="text-align: left">语气</th>
      <th style="text-align: left">解释</th>
      <th style="text-align: left">例句</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">1</td>
      <td style="text-align: left">虚拟语气</td>
      <td style="text-align: left">用来表示意愿，与事实相反的假设等</td>
      <td style="text-align: left">If I **were** a rabbit…</td>
    </tr>
    <tr>
      <td style="text-align: center">2</td>
      <td style="text-align: left">陈述语气|</td>
      <td style="text-align: left">I ate a carrot and…</td>
      <td style="text-align: left"> </td>
    </tr>
    <tr>
      <td style="text-align: center">3</td>
      <td style="text-align: left">祈使语气|</td>
      <td style="text-align: left">Eat this carrot and…</td>
      <td style="text-align: left"> </td>
    </tr>
  </tbody>
</table>

<h3 id="73-助动词auxiliary-verb">7.3 助动词（Auxiliary Verb）</h3>

<p>英语的谓语动词本身往往是不能够独立完成刚才说的上述的这些本领，而且谓语动词也无法完成比如表示否定、可能性、必须性等意思。</p>

<p>想让谓语动词充分发挥，我们必须用到另一类经常和动词一起用的词，来帮助完成任务，它们就是<strong>助动词</strong>（Auxiliary Verb）</p>

<p>例如对于动词 eat：</p>

<table>
  <thead>
    <tr>
      <th style="text-align: left">协助</th>
      <th style="text-align: left">助动词</th>
      <th style="text-align: left">组合</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: left">表示 “吃过了（完成）”</td>
      <td style="text-align: left">have</td>
      <td style="text-align: left">have eaten</td>
    </tr>
    <tr>
      <td style="text-align: left">表示 “正在吃（进行）”</td>
      <td style="text-align: left">be</td>
      <td style="text-align: left">is eating</td>
    </tr>
    <tr>
      <td style="text-align: left">表示 “被吃”</td>
      <td style="text-align: left">be</td>
      <td style="text-align: left">is eaten</td>
    </tr>
    <tr>
      <td style="text-align: left">表示 “有能力吃”</td>
      <td style="text-align: left">can</td>
      <td style="text-align: left">can eat</td>
    </tr>
    <tr>
      <td style="text-align: left">表示 “有可能吃”</td>
      <td style="text-align: left">might</td>
      <td style="text-align: left">might eat</td>
    </tr>
    <tr>
      <td style="text-align: left">表示 “有义务吃”</td>
      <td style="text-align: left">must</td>
      <td style="text-align: left">must eat</td>
    </tr>
    <tr>
      <td style="text-align: left">表示 “不吃（否定）”</td>
      <td style="text-align: left">do</td>
      <td style="text-align: left">do not eat</td>
    </tr>
  </tbody>
</table>

<p class="notice--info">以上的 can, might, must 也被称作 “情态动词”，可归类在 “助动词” 里，但是也有些语法体系把情态动词单独归类，这是分类的不同，不影响理解。</p>

<p class="notice--danger">有些动词，既可以是助动词（无实义），也可以做实义动词，比如 have 也可以表示 “拥有”。千万不要把助动词和它的其他身份（作实义动词时）弄混淆。</p>

<h2 id="8-非谓语动词">8. 非谓语动词</h2>

<p>之所以需要把动词叫做谓语动词，是因为还有所谓<strong>非谓语动词</strong>，这听起来似乎是一句废话。</p>

<p>不过，之所以这么叫，是因为动词除了充当句子中的谓语动词，还有可能充当：主语、宾语、宾语补语、主语补语、定语等，只不过需要变成非谓语动词。</p>

<p>不仅如此，非谓语动词几乎可以取代所有的从句，从而简化句子。只不过非谓语动词就不再具有表示动作时间、状态、语态、语气的功能，也就是它们丧失了时态、语气、语态这些本领了。</p>

<p>非谓语动词以不同的形式出现在句中，包括：</p>

<ul>
  <li><strong>动词不定式</strong>：常表将来或具体的单次行为。如作主语：<strong>To eat</strong> a carrot is Papa Rabbit’s dream.</li>
  <li><strong>现在分词</strong>：常表主动或正在进行。如作定语：The rabbit <strong>eating</strong> a carrot is my papa.</li>
  <li><strong>动名词</strong>：将动词名词化，常表抽象概念或习惯。如作宾语：Papa Rabbit enjoys <strong>eating</strong> carrots.</li>
  <li><strong>过去分词</strong>：常表被动或已经完成。如作宾语补语：Papa Rabbit wants the carrot <strong>eaten</strong>.</li>
</ul>

<h2 id="9-总结">9. 总结</h2>

<p>上述这么多一直在讲动词，这是因为：</p>

<p class="notice--danger">英语语法的核心就是动词，动词几乎可以串联起所有英语语法的核心概念。</p>

<figure class=" centered-75-figure"><a href="/assets/images/english_grammar_system.png" class="image-popup" title="英语语法体系
"><img src="/assets/images/english_grammar_system.png" alt="English Grammar System" /></a><figcaption>
      英语语法体系

    </figcaption></figure>

<p>以上就是英语语法体系的主体框架，绝大多数的语法规则，都逃不出这个框架。而无数的规则和特例也不过就是让这个核心体系更加丰富而已。</p>

<h2 id="10-参考资料">10. 参考资料</h2>

<ol>
  <li><a href="https://www.bilibili.com/video/BV1XY411J7aG?t=0&amp;p=2">英语兔：英语语法综述</a></li>
  <li><a href="https://mmistakes.github.io/minimal-mistakes/docs/layouts">Minimal Mistakes: Layouts</a></li>
</ol>]]></content><author><name>bfmhno3</name></author><category term="Note" /><category term="English" /><summary type="html"><![CDATA[我们为什么学语法？ 当然是为了考高分啊😂 其实，所有的英语语法都只有一个目的：造句。 英语和中文一样，我们说话写文章有的时候用短句子，有的时候用长句子。不同的短句子又可以组合成新的长句。但这里说的是句子的长度，并不适合语法讨论。 我们只有把句子拆开来，直到拆到不能再继续拆了，否则句意会不完整的 “基本句”，才好研究语法。因为如果 “基本句” 研究透了，长句子就是它们的组合。 1. 简单句（Simple Sentence） 我们把这样的不能再拆的 “基本句” 称作简单句（Simple Sentence）。而这样的简单句，除去那些感叹句（“嗯”，“哦”，“啊”等）、省略句、（向整句提问的）问句，几乎全部都是在说： 什么 + 怎么样 绝大多数句子粗略地分一共就只有这两个部分，而这个 “什么” 和 “怎么样” 分别对应了两个最基本的句子成分： “什么”： 主语（Subject）。 “怎么样”：谓语（Predicate）。 换句话说，几乎所有的英语句子结构都是： 主语 + 谓语 这里的主语一般是人或物，不管是抽象还是具体。而这里的谓语则是指一个广义的动作或发生的事情。 这个 “动作” 不是我们平时狭义说的要动起来的 “动作”，而这个广义的 “动作” 也就是我们语法上说的动词（Verb）。 2. 谓语动词 序号 动词类型 解释 1 不及物动词 可以独立完成的动作 2 单及物动词 有一个动作的承受者 3 双及物动词 有两个动作承受者 4 复杂及物动词 只有一个动作承受者 5 系动词 非 “动作” 2.1 不及物动词（Intransitive Verb） 不及物动词（Intransitive Verb）就是指可以独立完成的动作，例如： Papa Rabbit sleeps. 兔老爹睡觉。 类似 “sleeps” 这样的没有承受者的动词就是不及物动词（Intransitive Verb）。 不及物动词所对应的基本句子结构就是： 主语 + 不及物动词 2.2 单及物动词（Monotransitive Verb） Papa Rabbit likes you. 兔老爹喜欢你。 如果这里只说 “兔老爹喜欢”，你肯定觉得意思不完整，因为像 “喜欢” 这样的动词没有承受者就没有什么实际意义，这样的动词就属于及物动词（Transitive Verb），而这个动作承受者就是宾语（Object）。 具体而言，对于只有一个宾语及物动词，我们称为单及物动词（Monotransitive Verb）。 单及物动词对应的基本句子结构就是： 主语 + 单及物动词 + 宾语 2.3 双及物动词（Ditransitive Verbs） Papa Rabbit teaches you English. 兔老爹教你英语。 这里的核心动词是 “teach”（教），但是教授的知识是 “英语”，而知识的接收对象是 “你”。在语法上，我们把这样的动词的两个承受者分别称为直接宾语（Direct Object）和间接宾语（Indirect Object）。 如果只说 “兔老爹教英语”，其实语义已经完整了，所以 “English” 这里就是直接宾语，而如果只说 “兔老爹教你”，而又没有上下文的话，你肯定觉得还缺了什么，所以 “you” 在这里是间接宾语。 这样的往往既有直接宾语又有间接宾语的动词，也是及物动词的一种，更具体地说，它属于双及物动词（Ditransitive Verbs）。 双及物动词对应的基本句子结构就是： 主语 + 双及物动词 + 间接宾语 + 直接宾语 2.4 复杂及物动词（Complex-transitive Verb） Papa Rabbit considers you smart. 兔老爹认为你聪明。 这里虽然只有一个动作的承受者 “you”，但如果只说 “兔老爹认为你”，你肯定会觉得话没有说完，但是宾语 “you” 后面的 “smart” 却也不像双及物动词那样是另一个动作承受者。 这样的动词，必须要有个补充承受者的信息才意义完整，而这个补充的信息，我们在语法上称为补足语或补语（Complement）。更明确也可以说是宾语补语（Object Complement）。 这样的需要补语的动词，我们称之为复杂及物动词（Complex-transitive Verb） 复杂及物动词对应的基本句子结构就是： 主语 + 复杂及物动词 + 宾语 + （宾语）补语 2.5 系动词（Linking Verb） 这种动作所表示的含义与狭义上的 “动作” 不太一样： Papa Rabbit is tall. 兔老爹是高的。 这里的 “is” 在中文里经常翻译成 “是”，但它实际上的作用很简单： 就是把这个动词之后的信息赋予前面的主语。 这里的 “is” 说白了就是把 “Papa Rabbit” 和 “tall” 连在一起，划上等号而已。或者说，把后者信息赋予前者。如果把 “tall” 换成 “in the room”： Papa Rabbit is in the room. 兔老爹在房间里。 其实也是把 “in the room” 这个状态性质赋予 “Papa Rabbit” 而已。 Papa Rabbit looks tall. 兔老爹看起来高。 这个 “look” 通常意义是 “看”，但是这里到底是谁在看？其实不是 “Papa Rabbit” 在看，”Papa Rabbit” 在这里是被看。这里的 “looks” 其实也是把后面的 “tall” 赋予前面的主语 “Papa Rabbit” 而已，也算是划等号，只不过这个划等号比之前的 “is” 多了个 “看上去”。 Papa Rabbit smells nice. 兔老爹闻上去香。 这里的 “smells” 还是把 “Papa Rabbit” 和 “nice” 划等号，只不过还有 “闻上去” 这层意思。 像这样的赋予主语某种性质状态的 “划等号” 的动词，我们称之为连系动词（Linking Verb），也称为系动词。 系动词后面的补充信息自然就是补足语或补语，更精确地说是主语补语，而主语补语在中国的英语教学中又被称为表语（Predicative）。 系动词对应的基本句子结构就是： 主语 + 系动词 + （主语）补语/表语 3. 五大基本句型 序号 句型 1 主语 + 不及物动词 2 主语 + 单及物动词 + 宾语 3 主语 + 双及物动词 + 间接宾语 + 直接宾语 4 主语 + 复杂及物动词 + 宾语 + 宾语补语 5 主语 + 系动词 + 主语补语/表语 还有一种 “八大句型” 的分类，即在 “五大句型” 基础上增加： there be 句型，如：There is a rabbit. 可理解为第 5 种句型 “主语 + 系动词 + 主语补语/表语” 的倒装。 主语 + 谓语 + 状语，如：I live in China. 可理解为第 1 种句型 “主语 + 不及物动词” 的延伸（虽然这里的状语很重要）。 主语 + 谓语动词 + 宾语 + 状语，如：I put the carrot on the table. 可理解为第 4 种句型 “主语 + 复杂及物动词 + 宾语 + 宾语补语” 的延伸。 严格说来，谓语和谓语动词是有区别的。句子主语后面余下的部分就是谓语，谓语动词只是谓语的一部分。 但是在平时，通俗地，为了方便，很多人会直接称呼谓语动词为谓语，而把 “主语 + 谓语动词 + 宾语” 这样的句子结构直接称呼为 “主语 + 谓语 + 宾语”（或 “主谓宾”），这也不是什么大问题。 4. 句子基本成分 序号 成分 英文名 解释 例句 1 主语 Subject 动作的发出者或被讨论的对象 **Papa Rabbit** eats carrots. 2 谓语动词 Predicate Verb 表示具体的动作或状态 Papa Rabbit **eats** carrots. 3 宾语 Object 动作的承受者 Papa Rabbit eats **carrots**. 4 宾语补语 Object Complement 补充说明宾语的状态或特征 Papa Rabbit considers you **smart**. 5 主语补语 Subject Complement 补充说明主语的性质状态（即表语） Papa Rabbit is **tall**. 6 定语 Attributive 修饰主语或宾语 **The little** white rabbit ate **a large** carrot. 7 状语 Adverbial 修饰谓语动词 The rabbit ate **quickly**. 8 同位语 Appositive| Papa Rabbit, **an English teacher**, eats carrots.   你可能听过 “插入语”，如：This carrot, I think(插入语), is very tasty. 但是插入语是独立的，不作句子成分。 5. 复杂句 直到现在所说的都是简单句（Simple Sentence），说白了就是没法再拆成更多句子了。但是我们说话写文章可不全是简单句，而是经常会把这些简单句互相组合形成所谓复合句（Compound Sentence）和复杂句（Complex Sentence）。 复合句说白了就是句子简单的叠加，句子和句子之间是并列关系，因此也被称为并列句。 复杂句则是把一个句子套在另一个句子里，允许不断嵌套，是从属关系。 一个句子套另一个句子，在英语语法上分别叫做主句（Main Clause）和从句（Subordinate Clause）。 从句说白了就是把简单句修改一下来充当另一个句子的句子成分。 5.1 从句类型 序号 从句 作用 补充 例句 1 主语从句 作主语 名词性从句 **What Papa Rabbit ate** is a carrot. 2 宾语从句 作宾语 名词性从句 I know **that Papa Rabbit likes carrots**. 3 表语从句 作表语 名词性从句 The truth is **that he likes you**. 4 同位语从句 作同位语 名词性从句 The fact **that he ate the carrot** is obvious. 5 定语从句 作定语| The rabbit **who ate the carrot** is my papa.   6 状语从句 作状语| I will leave **when Papa Rabbit comes**.   6. 词性 但是现在问题来了，两个句子也许句子组成的方式相同，比如都是 “主语 + 谓语动词 + 宾语”，可是句中包含的词的类型并不一定相同，比如： The rabbit ate a carrot. He saw something over there. 很显然，这两句话 “句型” 相同，但是包含的词却不太一样。也就是说，同一类句子成分里可能有不同的词类（或词类），这个概念中文中也有。 序号 词性 英文名 解释 例句 1 动词 verb 表示动作或状态 Papa Rabbit **eats** a carrot. 2 名词 noun 表示人或物 Papa Rabbit is a **rabbit**. 3 冠词 article 用于说明人和事物 Papa Rabbit is **a** rabbit. 4 代词 pronoun 替代人和物 **I** am a rabbit. 5 形容词 adjective 形容人和物 I am a **smart** rabbit. 6 数词 numeral 表数量 I ate **two** carrots. 7 副词 adverb 修饰动作或形容词 I ate two carrots **quickly**. 8 介词 preposition 表示和其他词关系的词 I ate two carrots **with** chopsticks. 9 叹词 interjection 表感叹 **Ah**, the carrot is tasty! 10 连词 conjunction 连接词和句 I ate two carrots and a potato, **because** I was hungry. 句子成分（Clause Element）和句子中的词类（Parts of Speech）是完全不同的概念。 前文所述的每一种句子成分，除了谓语动词只能是动词，其他成分都可能包含不同的词类。 7. 谓语动词的 “三大本领” 英语中的谓语动词有很多厉害的本领，而这些本领，中文中的动词都没有，是英语语法的难点之一： 动作时间 动作状态 动作假设，情感等 所谓 “中文动词没有这些本领” 是说中文动词本身没有 “时、体、气” 的概念，但是中文当然有相同或相似意思的表达。 7.1 时态 英语的时态就是时间 + 状态合在一起，而并非只有时间。 | **现在** **过去** **将来** **过去将来**     *一般* *一般***现在** *一般***过去** *一般***将来** *一般***过去将来**   *完成* **现在***完成* **过去***完成* **将来***完成* **过去将来***完成*   *进行* **现在***进行* **过去***进行* **将来***进行* **过去将来***进行*   *完成进行* **现在***完成进行* **过去***完成进行* **将来***完成进行* **过去将来***完成进行* 时态是时间的排列组合，并不是一个线性的列表，更不是只和时间有关。 7.2 语气 序号 语气 解释 例句 1 虚拟语气 用来表示意愿，与事实相反的假设等 If I **were** a rabbit… 2 陈述语气| I ate a carrot and…   3 祈使语气| Eat this carrot and…   7.3 助动词（Auxiliary Verb） 英语的谓语动词本身往往是不能够独立完成刚才说的上述的这些本领，而且谓语动词也无法完成比如表示否定、可能性、必须性等意思。 想让谓语动词充分发挥，我们必须用到另一类经常和动词一起用的词，来帮助完成任务，它们就是助动词（Auxiliary Verb） 例如对于动词 eat： 协助 助动词 组合 表示 “吃过了（完成）” have have eaten 表示 “正在吃（进行）” be is eating 表示 “被吃” be is eaten 表示 “有能力吃” can can eat 表示 “有可能吃” might might eat 表示 “有义务吃” must must eat 表示 “不吃（否定）” do do not eat 以上的 can, might, must 也被称作 “情态动词”，可归类在 “助动词” 里，但是也有些语法体系把情态动词单独归类，这是分类的不同，不影响理解。 有些动词，既可以是助动词（无实义），也可以做实义动词，比如 have 也可以表示 “拥有”。千万不要把助动词和它的其他身份（作实义动词时）弄混淆。 8. 非谓语动词 之所以需要把动词叫做谓语动词，是因为还有所谓非谓语动词，这听起来似乎是一句废话。 不过，之所以这么叫，是因为动词除了充当句子中的谓语动词，还有可能充当：主语、宾语、宾语补语、主语补语、定语等，只不过需要变成非谓语动词。 不仅如此，非谓语动词几乎可以取代所有的从句，从而简化句子。只不过非谓语动词就不再具有表示动作时间、状态、语态、语气的功能，也就是它们丧失了时态、语气、语态这些本领了。 非谓语动词以不同的形式出现在句中，包括： 动词不定式：常表将来或具体的单次行为。如作主语：To eat a carrot is Papa Rabbit’s dream. 现在分词：常表主动或正在进行。如作定语：The rabbit eating a carrot is my papa. 动名词：将动词名词化，常表抽象概念或习惯。如作宾语：Papa Rabbit enjoys eating carrots. 过去分词：常表被动或已经完成。如作宾语补语：Papa Rabbit wants the carrot eaten. 9. 总结 上述这么多一直在讲动词，这是因为： 英语语法的核心就是动词，动词几乎可以串联起所有英语语法的核心概念。 英语语法体系 以上就是英语语法体系的主体框架，绝大多数的语法规则，都逃不出这个框架。而无数的规则和特例也不过就是让这个核心体系更加丰富而已。 10. 参考资料 英语兔：英语语法综述 Minimal Mistakes: Layouts]]></summary></entry><entry><title type="html">GNU 汇编（GAS）通用语法与伪指令指南</title><link href="https://bfmhno3.github.io/note/gnu-assembly/" rel="alternate" type="text/html" title="GNU 汇编（GAS）通用语法与伪指令指南" /><published>2026-04-05T08:35:00+00:00</published><updated>2026-04-05T08:35:00+00:00</updated><id>https://bfmhno3.github.io/note/gnu-assembly</id><content type="html" xml:base="https://bfmhno3.github.io/note/gnu-assembly/"><![CDATA[<p>由于机器指令及助记符高度依赖于芯片的具体架构，本文不讨论特定指令集的全部细节，而是聚焦于 GAS（GNU Assembler）通用的伪指令系统及其基本语法规则。</p>

<h2 id="1-汇编器的本质与工作流">1. 汇编器的本质与工作流</h2>

<p>GNU Assembler（简称 <code class="language-plaintext highlighter-rouge">as</code>）的核心工作流可以用以下公式概括：</p>

<p>$$
\text{源代码 (.s)} \xrightarrow{\text{汇编器 (as)}} \text{可重定位目标文件 (.o)}
$$</p>

<p>生成的 <code class="language-plaintext highlighter-rouge">.o</code> 文件主要由 <strong>Section</strong>（段）和 <strong>Symbol Table</strong>（符号表）组成。我们常用的 GAS 伪指令（Directives/Pseudo-ops），其根本目的就是为了控制这两个核心要素的生成。</p>

<p>从本质上来说，GNU Assembler 的核心职能并非简单的 “代码翻译”，而是<strong>内存空间的规划</strong>与<strong>符号地址的解析</strong>。</p>

<p>汇编器内部维护着一个至关重要的变量：<strong>地址计数器</strong>（Location Counter，通常记为 <code class="language-plaintext highlighter-rouge">.</code>）。当汇编器读取源代码时，它实际上是在执行一个状态机循环：</p>

<ol>
  <li><strong>解析伪指令</strong>：根据指令调整地址计数器的位置（例如跳转到新的段、预留空白空间）或修改符号的属性（如设置为全局可见）。</li>
  <li><strong>发射字节流</strong>：将翻译好的机器指令或定义的静态数据 “填充” 到地址计数器当前指向的内存单元，随后让计数器自增，指向下一个空闲位置。</li>
  <li><strong>构建符号表</strong>：记录所有标号（Label）对应的最终物理地址或相对于段的偏移量。</li>
</ol>

<p>因此，当我们编写汇编代码时，实际上是在做两件事：</p>

<ol>
  <li><strong>填充内存</strong>：明确告诉汇编器，在当前的内存地址上应当放置什么数据（注意：机器指令在内存中本质上也是数据）。</li>
  <li><strong>标记位置</strong>：给特定的内存地址起一个人类可读的名字（Label/Symbol），以便后续进行跳转或读写操作。</li>
</ol>

<h2 id="2-指令语法">2. 指令语法</h2>

<p>GAS 语法的核心难点在于不同架构下对<strong>操作数方向</strong>的处理，以及如何消除符号的<strong>歧义</strong>。</p>

<h3 id="21-注释风格">2.1 注释风格</h3>

<p>不同的 CPU 架构在 GAS 中通常使用不同的单行注释符号。这里仅列举常见架构的规范，更详细的列表可查阅 <a href="https://sourceware.org/binutils/docs/as/Machine-Dependencies.html">Binutils Docs: Machine Dependent Features</a>。</p>

<p class="notice--info">推荐直接查阅 “<a href="https://sourceware.org/binutils/docs/as/Machine-Dependencies.html">Machine Dependent Features</a>” 文档，官方通用的 “<a href="https://sourceware.org/binutils/docs/as/Comments.html">Comments</a>” 文档包含大量历史遗留信息，较为混乱。</p>

<p><strong>多行注释</strong>在所有架构中是通用的，沿用了 C 语言风格：</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">/*</span>
 <span class="nf">The</span> <span class="nv">only</span> <span class="nv">way</span> <span class="nv">to</span> <span class="nv">include</span> <span class="nv">a</span> <span class="nv">newline</span> <span class="o">(</span><span class="sc">'\n'</span><span class="o">)</span> <span class="nv">in</span> <span class="nv">a</span> <span class="nv">comment</span>
 <span class="nb">is</span> <span class="nv">to</span> <span class="nv">use</span> <span class="nv">this</span> <span class="nv">sort</span> <span class="nv">of</span> <span class="nv">comment</span><span class="mf">.</span>
 <span class="err">*/</span>

<span class="err">/*</span> <span class="nf">This</span> <span class="nv">sort</span> <span class="nv">of</span> <span class="nv">comment</span> <span class="nv">does</span> <span class="nv">not</span> <span class="nv">nest</span><span class="mf">.</span> <span class="o">*/</span>
</code></pre></div></div>

<p><strong>单行注释</strong>则因架构而异：</p>

<h4 id="211-x86-64">2.1.1 x86-64</h4>

<p>根据 <a href="https://sourceware.org/binutils/docs/as/i386_002dChars.html">Binutils Docs: i386_002dChars</a> 的说明：</p>

<blockquote>
  <p>The presence of a <code class="language-plaintext highlighter-rouge">#</code> appearing anywhere on a line indicates the start of a comment that extends to the end of that line.</p>

  <p>If a <code class="language-plaintext highlighter-rouge">#</code> appears as the first character of a line then the whole line is treated as a comment, but in this case the line can also be a logical line number directive…</p>

  <p>If the –divide command line option has not been specified then the `/’ character appearing anywhere on a line also introduces a line comment.</p>

  <p>The <code class="language-plaintext highlighter-rouge">;</code> character can be used to separate statements on the same line.</p>
</blockquote>

<p><strong>核心解读：</strong></p>

<ul>
  <li><strong>首选注释符</strong>：使用 <code class="language-plaintext highlighter-rouge">#</code>。只要行内出现 <code class="language-plaintext highlighter-rouge">#</code>，其后内容均为注释。</li>
  <li><strong>特殊情况</strong>：如果 <code class="language-plaintext highlighter-rouge">#</code> 出现在行首，汇编器会优先检查它是否为预处理命令（如 <code class="language-plaintext highlighter-rouge">#include</code>）或逻辑行号。</li>
  <li><strong>避坑指南</strong>：虽然默认情况下 <code class="language-plaintext highlighter-rouge">/</code> 也可以作为注释开始，但这极易产生歧义。例如在指令 <code class="language-plaintext highlighter-rouge">movq $(10/2), %rax</code> 中，汇编器可能会把 <code class="language-plaintext highlighter-rouge">/2</code> 误判为注释。除非指定了 <code class="language-plaintext highlighter-rouge">--divide</code> 选项，否则<strong>严禁使用 <code class="language-plaintext highlighter-rouge">/</code> 写注释</strong>。</li>
  <li><strong>语句分隔</strong>：分号 <code class="language-plaintext highlighter-rouge">;</code> 用于在同一行写多条指令，类似 C 语言。</li>
</ul>

<h4 id="212-arm-32-bit">2.1.2 ARM (32-bit)</h4>

<p>根据 <a href="https://sourceware.org/binutils/docs/as/ARM_002dChars.html">Binutils Docs: ARM_002dChars</a> 的说明：</p>

<blockquote>
  <p>The presence of a <code class="language-plaintext highlighter-rouge">@</code> anywhere on a line indicates the start of a comment that extends to the end of that line.</p>

  <p>If a <code class="language-plaintext highlighter-rouge">#</code> appears as the first character of a line then the whole line is treated as a comment…</p>

  <p>The <code class="language-plaintext highlighter-rouge">;</code> character can be used instead of a newline to separate statements.</p>

  <p>Either <code class="language-plaintext highlighter-rouge">#</code> or <code class="language-plaintext highlighter-rouge">$</code> can be used to indicate immediate operands.</p>
</blockquote>

<p><strong>核心解读：</strong></p>

<ul>
  <li><strong>首选注释符</strong>：使用 <code class="language-plaintext highlighter-rouge">@</code>。这是最安全的方式。</li>
  <li><strong>分号的作用</strong>：在 GNU ARM 汇编中，<code class="language-plaintext highlighter-rouge">;</code> 是语句分隔符（注意：这与 ARM 官方汇编器如 Keil/armasm 不同，后者用 <code class="language-plaintext highlighter-rouge">;</code> 做注释）。</li>
  <li><strong><code class="language-plaintext highlighter-rouge">#</code> 的双重身份</strong>：
    <ul>
      <li>如果在<strong>行首</strong>，它是注释。</li>
      <li>如果在<strong>指令中</strong>，它表示立即数（例如 <code class="language-plaintext highlighter-rouge">MOV R0, #10</code>）。</li>
      <li><strong>警告</strong>：切勿在指令行尾使用 <code class="language-plaintext highlighter-rouge">#</code> 做注释，汇编器会将其误认为是非法的立即数格式。</li>
    </ul>
  </li>
  <li><strong>立即数</strong>：虽然支持 x86 风格的 <code class="language-plaintext highlighter-rouge">$</code>，但为了代码移植性，<strong>强烈建议使用 <code class="language-plaintext highlighter-rouge">#</code> 表示立即数</strong>。</li>
</ul>

<h4 id="213-aarch64-arm64">2.1.3 AArch64 (ARM64)</h4>

<p>GNU 对 32 位 ARM 和 64 位 AArch64 做了严格区分，语法有所不同。</p>

<p><a href="https://sourceware.org/binutils/docs/as/AArch64_002dChars.html">Binutils Docs: AArch64_002dChars</a> 说明如下：</p>

<blockquote>
  <p>The presence of a <code class="language-plaintext highlighter-rouge">//</code> on a line indicates the start of a comment that extends to the end of the current line.</p>

  <p>If a <code class="language-plaintext highlighter-rouge">#</code> appears as the first character of a line, the whole line is treated as a comment.</p>

  <p>The <code class="language-plaintext highlighter-rouge">#</code> can be optionally used to indicate immediate operands.</p>
</blockquote>

<p><strong>核心解读：</strong></p>

<ul>
  <li><strong>首选注释符</strong>：使用 <code class="language-plaintext highlighter-rouge">//</code>（C++ 风格）。</li>
  <li><strong>立即数</strong>：<code class="language-plaintext highlighter-rouge">#</code> 是可选的，但在阅读习惯上保留它更为清晰。</li>
</ul>

<h4 id="214-risc-v">2.1.4 RISC-V</h4>

<p>RISC-V 的 GAS 实现较为现代，通常兼容性较好：</p>

<ul>
  <li>支持行首 <code class="language-plaintext highlighter-rouge">#</code> 注释。</li>
  <li>通常支持 <code class="language-plaintext highlighter-rouge">//</code> 或 <code class="language-plaintext highlighter-rouge">#</code> 作为行尾注释（具体取决于工具链版本，建议测试确认）。</li>
</ul>

<p class="notice--warning">建议以 <code class="language-plaintext highlighter-rouge">#</code> 为主，不建议使用 <code class="language-plaintext highlighter-rouge">//</code> 作为通用写法。</p>

<hr />

<h3 id="22-最大的分歧数据流向direction">2.2 最大的分歧：数据流向（Direction）</h3>

<p>阅读 GAS 代码的第一道门槛是<strong>源操作数与目的操作数的顺序</strong>。GAS 历史上继承了 <strong>AT&amp;T</strong> 语法（源自 Unix），而芯片厂商文档通常使用 <strong>Intel/Native</strong> 语法。</p>

<h4 id="221-x86-环境下的-gasatt-风格">2.2.1 x86 环境下的 GAS（AT&amp;T 风格）</h4>

<p>x86 GAS 遵循 “<strong>源在前，目的在后</strong>” 的原则。读代码时请默念“把左边的值给右边”。</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">operation</span> <span class="nb">source</span><span class="err">,</span> <span class="nv">destination</span>
</code></pre></div></div>

<p>例如：</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">movl</span> <span class="err">%</span><span class="nb">eax</span><span class="err">,</span> <span class="o">%</span><span class="nv">ebx</span>  <span class="o">#</span> <span class="err">含义：</span><span class="nv">EBX</span> <span class="o">=</span> <span class="nv">EAX</span> <span class="o">(</span><span class="err">将</span> <span class="nv">EAX</span> <span class="err">的值搬运到</span> <span class="nv">EBX</span><span class="o">)</span>
</code></pre></div></div>

<h4 id="222-armrisc-v-环境下的-gas官方风格">2.2.2 ARM/RISC-V 环境下的 GAS（官方风格）</h4>

<p>为了减少混乱，GNU 针对 ARM 和 RISC-V 做了妥协，采用了更符合官方文档的 “<strong>目的在前，源在后</strong>” 顺序：</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">operation</span> <span class="nb">destination</span><span class="err">,</span> <span class="nv">source</span>
</code></pre></div></div>

<p>例如：</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">mov</span> <span class="nb">r0</span><span class="err">,</span> <span class="nv">r1</span>       <span class="o">#</span> <span class="err">含义：</span><span class="nv">r0</span> <span class="o">=</span> <span class="nv">r1</span> <span class="o">(</span><span class="err">将</span> <span class="nv">r1</span> <span class="err">的值搬运到</span> <span class="nv">r0</span><span class="o">)</span>
</code></pre></div></div>

<h3 id="23-消除歧义前缀与修饰符">2.3 消除歧义：前缀与修饰符</h3>

<p>汇编器必须通过“前缀”来区分一个数字到底是<strong>数值</strong>、<strong>地址</strong>还是<strong>寄存器</strong>。</p>

<h4 id="231-寄存器">2.3.1 寄存器</h4>

<ul>
  <li><strong>x86 (AT&amp;T)</strong>：必须加 <code class="language-plaintext highlighter-rouge">%</code>，例如 <code class="language-plaintext highlighter-rouge">%eax</code>, <code class="language-plaintext highlighter-rouge">%esp</code>。</li>
  <li><strong>ARM/RISC-V</strong>：通常不加前缀，直接写 <code class="language-plaintext highlighter-rouge">r0</code>, <code class="language-plaintext highlighter-rouge">sp</code>, <code class="language-plaintext highlighter-rouge">pc</code>。为了避免与 C 语言变量名冲突，偶尔可见 <code class="language-plaintext highlighter-rouge">%r0</code> 写法，但不常见。</li>
</ul>

<h4 id="232-立即数">2.3.2 立即数</h4>

<p>汇编器如何知道 <code class="language-plaintext highlighter-rouge">10</code> 是指“内存地址 10”还是“数字 10”？</p>

<ul>
  <li><strong>x86 (AT&amp;T)</strong>：数字前必须加 <code class="language-plaintext highlighter-rouge">$</code>。</li>
  <li><strong>ARM</strong>：官方推荐加 <code class="language-plaintext highlighter-rouge">#</code>。</li>
  <li><strong>RISC-V</strong>：极其精简，通常不加前缀，依靠指令名（如 <code class="language-plaintext highlighter-rouge">li</code>, <code class="language-plaintext highlighter-rouge">addi</code>）的上下文自动推断。</li>
</ul>

<p><strong>对比示例：</strong></p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">#</span> <span class="nb">x86</span> <span class="o">(</span><span class="nv">AT</span><span class="o">&amp;</span><span class="nv">T</span><span class="o">)</span>
<span class="nl">movl</span> <span class="err">$10,</span> <span class="o">%</span><span class="nv">eax</span>   <span class="o">#</span> <span class="nv">EAX</span> <span class="o">=</span> <span class="mi">10</span> <span class="o">(</span><span class="err">立即数</span><span class="o">)</span>
<span class="nl">movl</span> <span class="err">10,</span> <span class="o">%</span><span class="nv">eax</span>    <span class="o">#</span> <span class="nv">EAX</span> <span class="o">=</span> <span class="o">*(</span><span class="nv">int</span><span class="o">*)(</span><span class="mh">0x0A</span><span class="o">)</span> <span class="o">(</span><span class="err">读取内存地址</span> <span class="mi">10</span> <span class="err">的内容</span><span class="o">)</span>

<span class="err">#</span> <span class="k">ARM</span>
<span class="nl">MOV</span> <span class="nb">R0</span><span class="err">,</span> <span class="o">#</span><span class="mi">10</span>      <span class="no">@</span> <span class="nv">R0</span> <span class="o">=</span> <span class="mi">10</span>

<span class="err">#</span> <span class="nb">RISC</span><span class="err">-</span><span class="nb">V</span>
<span class="nl">li</span> <span class="nb">a0</span><span class="err">,</span> <span class="mi">10</span>        <span class="o">#</span> <span class="nv">Load</span> <span class="nv">Immediate</span><span class="err">:</span> <span class="nv">a0</span> <span class="o">=</span> <span class="mi">10</span>
</code></pre></div></div>

<h3 id="24-内存寻址模式">2.4 内存寻址模式</h3>

<p>在 64 位系统中，内存地址空间高达 $2^{64}$，需要 8 字节才能完整表达。然而，一条机器指令通常只有 4 字节（RISC）或变长（x86）。如何在有限的指令长度内精确指向遥远的内存地址？这是寻址模式设计的核心挑战。</p>

<h4 id="241-x86-att">2.4.1 x86 (AT&amp;T)</h4>

<p>x86 是 CISC（复杂指令集），其最大特点是<strong>允许运算指令直接操作内存</strong>。硬件内建了强大的 SIB（Scale-Index-Base）寻址单元，将地址计算视为一个多项式：</p>

<p><strong>通用格式</strong>：</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">offset</span><span class="err">(</span><span class="nl">base</span><span class="err">,</span> <span class="nb">index</span><span class="err">,</span> <span class="nv">scale</span><span class="o">)</span>
</code></pre></div></div>

<p><strong>计算公式</strong>：</p>

\[\text{Address} = \text{base} + (\text{index} \times \text{scale}) + \text{offset}\]

<p><strong>应用场景</strong>：这非常适合访问数组。例如 <code class="language-plaintext highlighter-rouge">array[i]</code>，其中 <code class="language-plaintext highlighter-rouge">base</code> 是数组首地址，<code class="language-plaintext highlighter-rouge">index</code> 是变量 <code class="language-plaintext highlighter-rouge">i</code>，<code class="language-plaintext highlighter-rouge">scale</code> 是元素大小（如 <code class="language-plaintext highlighter-rouge">int</code> 为 4）。</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">movl</span> <span class="err">4(%</span><span class="nb">ebp</span><span class="err">),</span> <span class="o">%</span><span class="nv">eax</span>    <span class="o">#</span> <span class="err">取地址</span> <span class="o">(</span><span class="nv">ebp</span> <span class="o">+</span> <span class="mi">4</span><span class="o">)</span> <span class="err">处的数据</span> <span class="o">-&gt;</span> <span class="nv">eax</span>
<span class="nl">movl</span> <span class="nb">array</span><span class="err">(,%</span><span class="nb">edi</span><span class="err">,4),</span> <span class="o">%</span><span class="nv">eax</span> <span class="o">#</span> <span class="err">取地址</span> <span class="o">(</span><span class="nv">array</span> <span class="o">+</span> <span class="nv">edi</span> <span class="o">*</span> <span class="mi">4</span><span class="o">)</span> <span class="err">处的数据</span> <span class="o">-&gt;</span> <span class="nv">eax</span>
</code></pre></div></div>

<h4 id="242-arm">2.4.2 ARM</h4>

<p>ARM 是 RISC 且为 <strong>Load/Store 架构</strong>。这意味着不能直接对内存里的数进行 <code class="language-plaintext highlighter-rouge">add</code> 等运算，必须先 <code class="language-plaintext highlighter-rouge">ldr</code>（Load）到寄存器，运算后再 <code class="language-plaintext highlighter-rouge">str</code>（Store）回去。</p>

<p>ARM 的特色在于 <strong>Address Write-back（地址回写）</strong>。为了方便遍历数组，它允许在读写内存的同时，自动更新基址寄存器的值。</p>

<p>GAS 中使用中括号 <code class="language-plaintext highlighter-rouge">[]</code> 表示内存引用：</p>

<ul>
  <li><strong>常规偏移</strong>：<code class="language-plaintext highlighter-rouge">[Base, Offset]</code></li>
  <li><strong>寄存器偏移</strong>：<code class="language-plaintext highlighter-rouge">[Base, Register]</code>，甚至支持移位 <code class="language-plaintext highlighter-rouge">[Base, Reg, lsl #3]</code>。</li>
  <li><strong>前索引（Pre-indexed）</strong>：<code class="language-plaintext highlighter-rouge">[r1, #8]!</code> —— 访问前先加 8，且<strong>修改</strong> x1 的值（x1 += 8）。</li>
  <li><strong>后索引（Post-indexed）</strong>：<code class="language-plaintext highlighter-rouge">[r1], #8</code> —— 访问后再加 8，且<strong>修改</strong> x1 的值。</li>
</ul>

<h4 id="243-risc-v">2.4.3 RISC-V</h4>

<p>RISC-V 追求极致的硬件精简。它不支持 ARM 那种复杂的自动回写，也不支持 x86 的复杂比例因子。</p>

<p>RISC-V 的基础指令多为 32 bit（但启用压缩扩展后也有 16 bit 指令），除去操作码和寄存器位，留给立即数偏移的空间只有 <strong>12 位</strong>（即 $\pm 2\text{KB}$）。</p>

<p><strong>通用格式</strong>：</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">offset</span><span class="err">(</span><span class="nl">base</span><span class="err">)</span>
</code></pre></div></div>

<p><strong>计算公式</strong>：</p>

\[\text{Address} = \text{base} + \text{offset}\]

<p><strong>如何访问大范围地址？</strong></p>

<p>由于 12 位无法容纳 32/64 位地址，必须将大地址拆分为“高位”和“低位”两条指令处理：</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">#</span> <span class="err">小范围寻址</span> <span class="o">(</span><span class="nv">Offset</span> <span class="o">&lt;</span> <span class="mi">2048</span><span class="o">)</span>
<span class="nl">lw</span> <span class="nb">a0</span><span class="err">,</span> <span class="mi">8</span><span class="o">(</span><span class="nv">sp</span><span class="o">)</span>        <span class="o">#</span> <span class="err">从</span> <span class="nv">sp</span> <span class="o">+</span> <span class="mi">8</span> <span class="err">处加载一个</span> <span class="nv">word</span>
<span class="nl">sw</span> <span class="nb">a0</span><span class="err">,</span> <span class="mi">0</span><span class="o">(</span><span class="nv">t1</span><span class="o">)</span>        <span class="o">#</span> <span class="err">存入</span> <span class="nv">t1</span> <span class="o">+</span> <span class="mi">0</span> <span class="err">处</span>

<span class="err">#</span> <span class="err">大范围/全局变量寻址</span> <span class="o">(</span><span class="nv">Hi</span><span class="o">/</span><span class="nv">Lo</span> <span class="err">拆分</span><span class="o">)</span>
<span class="nl">lui</span> <span class="nb">t0</span><span class="err">,</span> <span class="o">%</span><span class="nb">hi</span><span class="o">(</span><span class="nv">VAL</span><span class="o">)</span>      <span class="o">#</span> <span class="err">加载</span> <span class="nv">VAL</span> <span class="err">地址的高</span> <span class="mi">20</span> <span class="err">位到</span> <span class="nv">t0</span>
<span class="nl">lw</span>  <span class="nb">a0</span><span class="err">,</span> <span class="o">%</span><span class="nb">lo</span><span class="o">(</span><span class="nv">VAL</span><span class="o">)(</span><span class="nv">t0</span><span class="o">)</span>  <span class="o">#</span> <span class="err">加载</span> <span class="nv">VAL</span> <span class="err">地址的低</span> <span class="mi">12</span> <span class="err">位，作为偏移加到</span> <span class="nv">t0</span> <span class="err">上读取</span>
</code></pre></div></div>

<h3 id="25-指令后缀操作数宽度-size-suffixes">2.5 指令后缀：操作数宽度 (Size Suffixes)</h3>

<p>在机器层面，CPU 处理数据时必须明确一个核心参数：<strong>位宽 (Width)</strong>。硬件需要知道它是应该只驱动数据总线上的 8 根线，还是激活全部 64 根线？是将数据放入寄存器的低 8 位（如 <code class="language-plaintext highlighter-rouge">AL</code>），还是覆盖整个 64 位（如 <code class="language-plaintext highlighter-rouge">RAX</code>）？</p>

<p>GAS 通过在指令助记符后添加<strong>后缀</strong>来显式控制这一行为。</p>

<h4 id="251-gas-通用命名规则">2.5.1 GAS 通用命名规则</h4>

<p>GAS 使用一套单字母后缀来表示数据宽度。需要特别警惕的是，不同架构对 “Word” 的定义存在历史分歧。</p>

<table>
  <thead>
    <tr>
      <th><strong>后缀</strong></th>
      <th><strong>全称</strong></th>
      <th><strong>大小 (Bits)</strong></th>
      <th><strong>字节数</strong></th>
      <th><strong>说明</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>b</strong></td>
      <td><strong>B</strong>yte</td>
      <td>8-bit</td>
      <td>1</td>
      <td>通用。</td>
    </tr>
    <tr>
      <td><strong>w</strong></td>
      <td><strong>W</strong>ord</td>
      <td><strong>16-bit</strong></td>
      <td>2</td>
      <td><strong>注意！</strong> 在 x86 语境下，Word 是 16 位（源于 8086）。但在 ARM/RISC-V 文档中，Word 通常指 32 位。GAS 的 <code class="language-plaintext highlighter-rouge">w</code> 后缀专指 16 位。</td>
    </tr>
    <tr>
      <td><strong>l</strong></td>
      <td><strong>L</strong>ong</td>
      <td>32-bit</td>
      <td>4</td>
      <td>x86 的标准 32 位后缀。</td>
    </tr>
    <tr>
      <td><strong>q</strong></td>
      <td><strong>Q</strong>uad</td>
      <td>64-bit</td>
      <td>8</td>
      <td>四字（4 x 16-bit），即 64 位。</td>
    </tr>
  </tbody>
</table>

<h4 id="252-x86-架构显式后缀-explicit-suffixes">2.5.2 x86 架构：显式后缀 (Explicit Suffixes)</h4>

<p>x86 是变长指令集，且寄存器存在嵌套关系（<code class="language-plaintext highlighter-rouge">RAX</code> 包含 <code class="language-plaintext highlighter-rouge">EAX</code> 包含 <code class="language-plaintext highlighter-rouge">AX</code> 包含 <code class="language-plaintext highlighter-rouge">AL</code>）。为了消除歧义，GAS <strong>强烈推荐</strong>在所有操作码后加上后缀。</p>

<p>虽然现代汇编器有时能根据寄存器名字（如 <code class="language-plaintext highlighter-rouge">%eax</code>）推断出你是想操作 32 位，但在涉及内存操作数（无法看出大小）或立即数时，不写后缀极易报错。</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">movb</span> <span class="err">$0</span><span class="nf">xFF</span><span class="err">,</span> <span class="o">%</span><span class="nb">al</span>      	<span class="o">#</span> <span class="err">移动</span> <span class="mi">1</span> <span class="err">个字节</span> <span class="o">(</span><span class="mi">8</span><span class="o">-</span><span class="nv">bit</span><span class="o">)</span>
<span class="nl">movw</span> <span class="err">$0</span><span class="nf">xFFFF</span><span class="err">,</span> <span class="o">%</span><span class="nv">ax</span>    	<span class="o">#</span> <span class="err">移动</span> <span class="mi">1</span> <span class="err">个字</span>   <span class="o">(</span><span class="mi">16</span><span class="o">-</span><span class="nv">bit</span><span class="o">)</span>
<span class="nl">movl</span> <span class="err">$0</span><span class="nf">xFFFFFFFF</span><span class="err">,</span> <span class="o">%</span><span class="nv">eax</span>	<span class="o">#</span> <span class="err">移动</span> <span class="mi">1</span> <span class="err">个长字</span> <span class="o">(</span><span class="mi">32</span><span class="o">-</span><span class="nv">bit</span><span class="o">)</span>
<span class="nl">movq</span> <span class="err">$1,</span> <span class="o">%</span><span class="nv">rax</span>        	<span class="o">#</span> <span class="err">移动</span> <span class="mi">1</span> <span class="err">个四字</span> <span class="o">(</span><span class="mi">64</span><span class="o">-</span><span class="nv">bit</span><span class="o">)</span>

<span class="err">#</span> <span class="err">歧义示例：</span>
<span class="err">#</span> <span class="nb">mov</span> <span class="err">$</span><span class="mi">0</span><span class="o">,</span> <span class="o">(%</span><span class="nv">rbx</span><span class="o">)</span>     <span class="o">#</span> <span class="err">错误！汇编器不知道你想把</span> <span class="nv">rb</span> <span class="err">指向的内存清零</span> <span class="mi">1</span> <span class="err">个字节还是</span> <span class="mi">8</span> <span class="err">个字节。</span>
<span class="nl">movq</span> <span class="err">$0,</span> <span class="o">(%</span><span class="nv">rbx</span><span class="o">)</span>      <span class="o">#</span> <span class="err">正确：清零</span> <span class="mi">8</span> <span class="err">个字节</span>
</code></pre></div></div>

<h4 id="253-arm--risc-v-架构助记符内含宽度">2.5.3 ARM / RISC-V 架构：助记符内含宽度</h4>

<p>与 x86 不同，ARM 和 RISC-V 等 RISC 架构通常将数据宽度直接<strong>编码在指令助记符本身</strong>中，而不是依赖 GAS 的通用后缀。</p>

<p>在这些架构中，你通常不会看到 <code class="language-plaintext highlighter-rouge">movl</code> 这种写法，而是通过不同的指令变体来实现：</p>

<ul>
  <li><strong>RISC-V</strong>:
    <ul>
      <li><code class="language-plaintext highlighter-rouge">lb</code> (Load Byte): 加载 8 位</li>
      <li><code class="language-plaintext highlighter-rouge">lh</code> (Load Half-word): 加载 16 位</li>
      <li><code class="language-plaintext highlighter-rouge">lw</code> (Load Word): 加载 32 位 (<strong>注意：这里 Word 是 32 位</strong>)</li>
      <li><code class="language-plaintext highlighter-rouge">ld</code> (Load Double-word): 加载 64 位</li>
    </ul>
  </li>
  <li><strong>ARM</strong>:
    <ul>
      <li><code class="language-plaintext highlighter-rouge">LDRB</code> (Byte): 加载 8 位</li>
      <li><code class="language-plaintext highlighter-rouge">LDRH</code> (Half-word): 加载 16 位</li>
      <li><code class="language-plaintext highlighter-rouge">LDR</code> (Word): 加载 32 位</li>
    </ul>
  </li>
</ul>

<div class="notice--warning">
  <p><strong>“Word” 的陷阱</strong></p>

  <p>这是一个跨架构开发时最容易混淆的概念：</p>

  <ul>
    <li>在 <strong>x86</strong> 汇编中，<code class="language-plaintext highlighter-rouge">Word</code> = <strong>16 bits</strong>。</li>
    <li>在 <strong>ARM / RISC-V</strong> 汇编中，<code class="language-plaintext highlighter-rouge">Word</code> = <strong>32 bits</strong>。</li>
  </ul>

  <p>当你使用 GAS 的 <code class="language-plaintext highlighter-rouge">.word</code> 伪指令定义数据时，它在所有架构上通常都表示 32 位（或 16 位，取决于具体配置，但在现代环境下多为 32 位）。务必查阅具体目标架构的文档确认 <code class="language-plaintext highlighter-rouge">.word</code> 的确切长度，或者直接使用 <code class="language-plaintext highlighter-rouge">.2byte</code> / <code class="language-plaintext highlighter-rouge">.4byte</code> 这种无歧义的写法。</p>
</div>

<h2 id="3-伪指令系统-directives">3. 伪指令系统 (Directives)</h2>

<p>伪指令以 <code class="language-plaintext highlighter-rouge">.</code> 开头，它们不对应具体的 CPU 机器码，而是指挥汇编器如何组织二进制结构。</p>

<h3 id="31-数据定义如何填充字节">3.1 数据定义：如何填充字节</h3>

<p>这些指令直接在当前地址计数器位置“发射”数据，常用于定义查找表或初始化变量。</p>

<ul>
  <li>
    <p><code class="language-plaintext highlighter-rouge">.byte</code> / <code class="language-plaintext highlighter-rouge">.hword</code> / <code class="language-plaintext highlighter-rouge">.word</code> / <code class="language-plaintext highlighter-rouge">.long</code> / <code class="language-plaintext highlighter-rouge">.quad</code>：</p>

    <p>在当前地址写入特定宽度的整数。注意<strong>字节序（Endianness）</strong>取决于目标架构（x86 为小端，ARM/RISC-V 默认为小端，但可配置）。</p>

    <p>代码段</p>

    <div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">.</span><span class="nl">byte</span> <span class="err">0</span><span class="nb">x12</span><span class="err">,</span> <span class="mh">0x34</span>    <span class="o">#</span> <span class="err">写入两个字节</span>
<span class="err">.</span><span class="nl">word</span> <span class="err">0</span><span class="nf">xAABBCCDD</span>    <span class="o">#</span> <span class="err">写入</span> <span class="mi">4</span> <span class="err">字节。小端模式下内存为：</span><span class="nv">DD</span> <span class="nb">CC</span> <span class="nv">BB</span> <span class="nv">AA</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">.ascii</code> vs <code class="language-plaintext highlighter-rouge">.asciz</code> / <code class="language-plaintext highlighter-rouge">.string</code>：</p>

    <ul>
      <li><code class="language-plaintext highlighter-rouge">.ascii "Hello"</code>：仅填入字符 ASCII 码，<strong>无</strong>结尾 <code class="language-plaintext highlighter-rouge">\0</code>。</li>
      <li><code class="language-plaintext highlighter-rouge">.asciz "Hello"</code> 或 <code class="language-plaintext highlighter-rouge">.string</code>：自动在末尾补 <code class="language-plaintext highlighter-rouge">0x00</code>（C 语言标准字符串）。</li>
    </ul>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">.fill</code> / <code class="language-plaintext highlighter-rouge">.skip</code> / <code class="language-plaintext highlighter-rouge">.space</code> / <code class="language-plaintext highlighter-rouge">.zero</code>：</p>

    <p>用于预留空间或清零，常用于定义栈（Stack）或 BSS 段。</p>

    <p>代码段</p>

    <div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">stack_top</span><span class="err">:</span>
<span class="err">.</span><span class="nl">skip</span> <span class="err">1024,</span> <span class="mi">0</span>      <span class="o">#</span> <span class="err">预留</span> <span class="mi">1024</span> <span class="err">字节，全部填</span> <span class="mi">0</span>
<span class="nl">stack_bottom</span><span class="err">:</span>      <span class="k">#</span> <span class="err">栈底标号（假设栈向下生长，</span><span class="nv">SP</span> <span class="err">初始指向此处）</span>
</code></pre></div>    </div>
  </li>
</ul>

<h3 id="32-内存控制如何管理地址">3.2 内存控制：如何管理地址</h3>

<ul>
  <li>
    <p><code class="language-plaintext highlighter-rouge">.section</code>：</p>

    <p>告诉汇编器：“<strong>接下来的数据/指令，请分类归档到哪个‘抽屉’里</strong>”。</p>

    <ul>
      <li><code class="language-plaintext highlighter-rouge">.section .text</code>：代码段（只读，可执行）。</li>
      <li><code class="language-plaintext highlighter-rouge">.section .data</code>：已初始化的全局变量（可读写）。</li>
      <li><code class="language-plaintext highlighter-rouge">.section .bss</code>：未初始化变量（不占磁盘空间，程序启动时自动清零）。</li>
      <li><strong>自定义段</strong>：例如 <code class="language-plaintext highlighter-rouge">.section .isr_vector</code>，配合链接脚本（Linker Script），可以将中断向量表精确固定在 Flash 的 0 地址处。</li>
    </ul>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">.align</code> vs <code class="language-plaintext highlighter-rouge">.balign</code>：</p>

    <ul>
      <li><code class="language-plaintext highlighter-rouge">.align</code>：行为不统一！在 x86 下 <code class="language-plaintext highlighter-rouge">.align 4</code> 表示 4 字节对齐；但在 ARM 下可能表示 $2^4=16$ 字节对齐。</li>
      <li><code class="language-plaintext highlighter-rouge">.balign</code>（Byte Align）：<strong>推荐使用</strong>。<code class="language-plaintext highlighter-rouge">.balign 4, 0</code> 明确表示保证地址能被 4 整除，如果需要填充，则填 0。</li>
    </ul>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">.org</code>：</p>

    <p>强制修改当前的地址计数器。</p>

    <ul>
      <li><strong>示例</strong>：编写 MBR 引导扇区时，需要在第 510 字节写结束标志 <code class="language-plaintext highlighter-rouge">0x55AA</code>，会用到 <code class="language-plaintext highlighter-rouge">.org 510</code>。</li>
    </ul>
  </li>
</ul>

<h3 id="33-符号管理与-c-语言的交互接口">3.3 符号管理：与 C 语言的交互接口</h3>

<p>这是 C 和汇编混合编程的桥梁。</p>

<ul>
  <li>
    <p><code class="language-plaintext highlighter-rouge">.global</code>（或 <code class="language-plaintext highlighter-rouge">.globl</code>）：</p>

    <p>将符号标记为<strong>全局可见</strong>。如果不加此标记，符号仅在当前文件内部可见（类似 C 的 <code class="language-plaintext highlighter-rouge">static</code>），链接器无法找到它。</p>

    <ul>
      <li><strong>场景</strong>：Startup 文件（启动文件）必须声明 <code class="language-plaintext highlighter-rouge">.global Reset_Handler</code>，否则硬件复位后找不到入口。</li>
    </ul>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">.extern</code>：</p>

    <p>声明符号在外部定义。虽然 GAS 通常能自动推断，但显式声明能提高代码可读性。</p>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">.equ</code> / <code class="language-plaintext highlighter-rouge">.set</code>：</p>

    <p>常量定义，相当于 C 的 <code class="language-plaintext highlighter-rouge">#define</code>。它们只存在于汇编阶段，不占用运行时的内存空间。</p>

    <p>代码段</p>

    <div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">.</span><span class="nl">equ</span> <span class="nf">STACK_SIZE</span><span class="err">,</span> <span class="mh">0x400</span>
</code></pre></div>    </div>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">.type</code>：</p>

    <p>明确告诉链接器符号的类型（是函数还是数据）。</p>

    <ul>
      <li><strong>重要性</strong>：在 ARM Thumb-2 指令集中，如果链接器知道某符号是 <code class="language-plaintext highlighter-rouge">%function</code>，它会在计算跳转地址时自动将最低位置 1（Thumb 状态标志）。若省略此指令，可能导致切换状态失败而进入 HardFault。</li>
    </ul>
  </li>
  <li>
    <p><code class="language-plaintext highlighter-rouge">.weak</code>：</p>

    <p><strong>弱符号定义</strong>。</p>

    <ul>
      <li><strong>机制</strong>：如果在其他地方定义了同名的强符号（普通的 <code class="language-plaintext highlighter-rouge">.global</code>），链接器优先使用那个；如果没有，则使用这里的默认定义。</li>
      <li><strong>应用</strong>：STM32 启动文件中的中断处理函数通常用 <code class="language-plaintext highlighter-rouge">.weak</code> 定义为死循环。这允许用户在 C 代码中重写同名函数（如 <code class="language-plaintext highlighter-rouge">SysTick_Handler</code>）来覆盖默认行为，而无需修改启动代码。</li>
    </ul>
  </li>
</ul>

<h2 id="4-gas-的特色功能-syntactic-sugar">4. GAS 的特色功能 (Syntactic Sugar)</h2>

<h3 id="41-局部数值标签-local-numeric-labels">4.1 局部数值标签 (Local Numeric Labels)</h3>

<p>为了避免给简单的循环绞尽脑汁起名字（如 <code class="language-plaintext highlighter-rouge">loop1</code>, <code class="language-plaintext highlighter-rouge">loop_start</code>），GAS 允许使用数字作为临时标号。</p>

<ul>
  <li><strong>定义</strong>：<code class="language-plaintext highlighter-rouge">&lt;num&gt;:</code></li>
  <li><strong>引用</strong>：<code class="language-plaintext highlighter-rouge">&lt;num&gt;f</code> (forward, 向前/向下找)，<code class="language-plaintext highlighter-rouge">&lt;num&gt;b</code> (backward, 向后/向上找)。</li>
</ul>

<p><strong>示例</strong>：</p>

<p>代码段</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">1</span><span class="err">:</span>          <span class="k">#</span> <span class="err">定义标号</span> <span class="mi">1</span>
    <span class="nb">wfi</span>     <span class="o">#</span> <span class="nv">Wait</span> <span class="nv">for</span> <span class="nv">interrupt</span>
    <span class="nb">j</span> <span class="mi">1</span><span class="nv">b</span>    <span class="o">#</span> <span class="err">向后跳转到最近的标号</span> <span class="mi">1</span> <span class="o">(</span><span class="err">相当于</span> <span class="nv">while</span><span class="o">(</span><span class="mi">1</span><span class="o">)</span><span class="c">;)</span>
</code></pre></div></div>

<h3 id="42-宏-macros">4.2 宏 (Macros)</h3>

<p>用于封装重复代码片段：</p>

<p>代码段</p>

<div class="language-armasm highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">.</span><span class="nl">macro</span> <span class="err">宏名称</span> <span class="err">参数</span><span class="mi">1</span><span class="o">,</span> <span class="err">参数</span><span class="mi">2</span>
    <span class="err">//</span> <span class="err">宏体内容，可使用</span> <span class="err">\参数</span><span class="mi">1</span> <span class="err">引用</span>
<span class="err">.</span><span class="nl">endm</span>
</code></pre></div></div>

<h3 id="43--current-location">4.3 <code class="language-plaintext highlighter-rouge">.</code> (Current Location)</h3>

<p>符号 <code class="language-plaintext highlighter-rouge">.</code> 代表当前地址计数器的值。</p>

<ul>
  <li><strong>常见用法</strong>：<code class="language-plaintext highlighter-rouge">b .</code> 等同于 <code class="language-plaintext highlighter-rouge">while (1);</code>（跳转到当前地址，死循环）。</li>
</ul>]]></content><author><name>bfmhno3</name></author><category term="Note" /><category term="ARM" /><category term="RISC-V" /><category term="Assembly" /><category term="i.MX6ULL" /><category term="GNU" /><summary type="html"><![CDATA[由于机器指令及助记符高度依赖于芯片的具体架构，本文不讨论特定指令集的全部细节，而是聚焦于 GAS（GNU Assembler）通用的伪指令系统及其基本语法规则。 1. 汇编器的本质与工作流 GNU Assembler（简称 as）的核心工作流可以用以下公式概括： $$ \text{源代码 (.s)} \xrightarrow{\text{汇编器 (as)}} \text{可重定位目标文件 (.o)} $$ 生成的 .o 文件主要由 Section（段）和 Symbol Table（符号表）组成。我们常用的 GAS 伪指令（Directives/Pseudo-ops），其根本目的就是为了控制这两个核心要素的生成。 从本质上来说，GNU Assembler 的核心职能并非简单的 “代码翻译”，而是内存空间的规划与符号地址的解析。 汇编器内部维护着一个至关重要的变量：地址计数器（Location Counter，通常记为 .）。当汇编器读取源代码时，它实际上是在执行一个状态机循环： 解析伪指令：根据指令调整地址计数器的位置（例如跳转到新的段、预留空白空间）或修改符号的属性（如设置为全局可见）。 发射字节流：将翻译好的机器指令或定义的静态数据 “填充” 到地址计数器当前指向的内存单元，随后让计数器自增，指向下一个空闲位置。 构建符号表：记录所有标号（Label）对应的最终物理地址或相对于段的偏移量。 因此，当我们编写汇编代码时，实际上是在做两件事： 填充内存：明确告诉汇编器，在当前的内存地址上应当放置什么数据（注意：机器指令在内存中本质上也是数据）。 标记位置：给特定的内存地址起一个人类可读的名字（Label/Symbol），以便后续进行跳转或读写操作。 2. 指令语法 GAS 语法的核心难点在于不同架构下对操作数方向的处理，以及如何消除符号的歧义。 2.1 注释风格 不同的 CPU 架构在 GAS 中通常使用不同的单行注释符号。这里仅列举常见架构的规范，更详细的列表可查阅 Binutils Docs: Machine Dependent Features。 推荐直接查阅 “Machine Dependent Features” 文档，官方通用的 “Comments” 文档包含大量历史遗留信息，较为混乱。 多行注释在所有架构中是通用的，沿用了 C 语言风格： /* The only way to include a newline ('\n') in a comment is to use this sort of comment. */ /* This sort of comment does not nest. */ 单行注释则因架构而异： 2.1.1 x86-64 根据 Binutils Docs: i386_002dChars 的说明： The presence of a # appearing anywhere on a line indicates the start of a comment that extends to the end of that line. If a # appears as the first character of a line then the whole line is treated as a comment, but in this case the line can also be a logical line number directive… If the –divide command line option has not been specified then the `/’ character appearing anywhere on a line also introduces a line comment. The ; character can be used to separate statements on the same line. 核心解读： 首选注释符：使用 #。只要行内出现 #，其后内容均为注释。 特殊情况：如果 # 出现在行首，汇编器会优先检查它是否为预处理命令（如 #include）或逻辑行号。 避坑指南：虽然默认情况下 / 也可以作为注释开始，但这极易产生歧义。例如在指令 movq $(10/2), %rax 中，汇编器可能会把 /2 误判为注释。除非指定了 --divide 选项，否则严禁使用 / 写注释。 语句分隔：分号 ; 用于在同一行写多条指令，类似 C 语言。 2.1.2 ARM (32-bit) 根据 Binutils Docs: ARM_002dChars 的说明： The presence of a @ anywhere on a line indicates the start of a comment that extends to the end of that line. If a # appears as the first character of a line then the whole line is treated as a comment… The ; character can be used instead of a newline to separate statements. Either # or $ can be used to indicate immediate operands. 核心解读： 首选注释符：使用 @。这是最安全的方式。 分号的作用：在 GNU ARM 汇编中，; 是语句分隔符（注意：这与 ARM 官方汇编器如 Keil/armasm 不同，后者用 ; 做注释）。 # 的双重身份： 如果在行首，它是注释。 如果在指令中，它表示立即数（例如 MOV R0, #10）。 警告：切勿在指令行尾使用 # 做注释，汇编器会将其误认为是非法的立即数格式。 立即数：虽然支持 x86 风格的 $，但为了代码移植性，强烈建议使用 # 表示立即数。 2.1.3 AArch64 (ARM64) GNU 对 32 位 ARM 和 64 位 AArch64 做了严格区分，语法有所不同。 Binutils Docs: AArch64_002dChars 说明如下： The presence of a // on a line indicates the start of a comment that extends to the end of the current line. If a # appears as the first character of a line, the whole line is treated as a comment. The # can be optionally used to indicate immediate operands. 核心解读： 首选注释符：使用 //（C++ 风格）。 立即数：# 是可选的，但在阅读习惯上保留它更为清晰。 2.1.4 RISC-V RISC-V 的 GAS 实现较为现代，通常兼容性较好： 支持行首 # 注释。 通常支持 // 或 # 作为行尾注释（具体取决于工具链版本，建议测试确认）。 建议以 # 为主，不建议使用 // 作为通用写法。 2.2 最大的分歧：数据流向（Direction） 阅读 GAS 代码的第一道门槛是源操作数与目的操作数的顺序。GAS 历史上继承了 AT&amp;T 语法（源自 Unix），而芯片厂商文档通常使用 Intel/Native 语法。 2.2.1 x86 环境下的 GAS（AT&amp;T 风格） x86 GAS 遵循 “源在前，目的在后” 的原则。读代码时请默念“把左边的值给右边”。 operation source, destination 例如： movl %eax, %ebx # 含义：EBX = EAX (将 EAX 的值搬运到 EBX) 2.2.2 ARM/RISC-V 环境下的 GAS（官方风格） 为了减少混乱，GNU 针对 ARM 和 RISC-V 做了妥协，采用了更符合官方文档的 “目的在前，源在后” 顺序： operation destination, source 例如： mov r0, r1 # 含义：r0 = r1 (将 r1 的值搬运到 r0) 2.3 消除歧义：前缀与修饰符 汇编器必须通过“前缀”来区分一个数字到底是数值、地址还是寄存器。 2.3.1 寄存器 x86 (AT&amp;T)：必须加 %，例如 %eax, %esp。 ARM/RISC-V：通常不加前缀，直接写 r0, sp, pc。为了避免与 C 语言变量名冲突，偶尔可见 %r0 写法，但不常见。 2.3.2 立即数 汇编器如何知道 10 是指“内存地址 10”还是“数字 10”？ x86 (AT&amp;T)：数字前必须加 $。 ARM：官方推荐加 #。 RISC-V：极其精简，通常不加前缀，依靠指令名（如 li, addi）的上下文自动推断。 对比示例： # x86 (AT&amp;T) movl $10, %eax # EAX = 10 (立即数) movl 10, %eax # EAX = *(int*)(0x0A) (读取内存地址 10 的内容) # ARM MOV R0, #10 @ R0 = 10 # RISC-V li a0, 10 # Load Immediate: a0 = 10 2.4 内存寻址模式 在 64 位系统中，内存地址空间高达 $2^{64}$，需要 8 字节才能完整表达。然而，一条机器指令通常只有 4 字节（RISC）或变长（x86）。如何在有限的指令长度内精确指向遥远的内存地址？这是寻址模式设计的核心挑战。 2.4.1 x86 (AT&amp;T) x86 是 CISC（复杂指令集），其最大特点是允许运算指令直接操作内存。硬件内建了强大的 SIB（Scale-Index-Base）寻址单元，将地址计算视为一个多项式： 通用格式： offset(base, index, scale) 计算公式： [\text{Address} = \text{base} + (\text{index} \times \text{scale}) + \text{offset}] 应用场景：这非常适合访问数组。例如 array[i]，其中 base 是数组首地址，index 是变量 i，scale 是元素大小（如 int 为 4）。 movl 4(%ebp), %eax # 取地址 (ebp + 4) 处的数据 -&gt; eax movl array(,%edi,4), %eax # 取地址 (array + edi * 4) 处的数据 -&gt; eax 2.4.2 ARM ARM 是 RISC 且为 Load/Store 架构。这意味着不能直接对内存里的数进行 add 等运算，必须先 ldr（Load）到寄存器，运算后再 str（Store）回去。 ARM 的特色在于 Address Write-back（地址回写）。为了方便遍历数组，它允许在读写内存的同时，自动更新基址寄存器的值。 GAS 中使用中括号 [] 表示内存引用： 常规偏移：[Base, Offset] 寄存器偏移：[Base, Register]，甚至支持移位 [Base, Reg, lsl #3]。 前索引（Pre-indexed）：[r1, #8]! —— 访问前先加 8，且修改 x1 的值（x1 += 8）。 后索引（Post-indexed）：[r1], #8 —— 访问后再加 8，且修改 x1 的值。 2.4.3 RISC-V RISC-V 追求极致的硬件精简。它不支持 ARM 那种复杂的自动回写，也不支持 x86 的复杂比例因子。 RISC-V 的基础指令多为 32 bit（但启用压缩扩展后也有 16 bit 指令），除去操作码和寄存器位，留给立即数偏移的空间只有 12 位（即 $\pm 2\text{KB}$）。 通用格式： offset(base) 计算公式： [\text{Address} = \text{base} + \text{offset}] 如何访问大范围地址？ 由于 12 位无法容纳 32/64 位地址，必须将大地址拆分为“高位”和“低位”两条指令处理： # 小范围寻址 (Offset &lt; 2048) lw a0, 8(sp) # 从 sp + 8 处加载一个 word sw a0, 0(t1) # 存入 t1 + 0 处 # 大范围/全局变量寻址 (Hi/Lo 拆分) lui t0, %hi(VAL) # 加载 VAL 地址的高 20 位到 t0 lw a0, %lo(VAL)(t0) # 加载 VAL 地址的低 12 位，作为偏移加到 t0 上读取 2.5 指令后缀：操作数宽度 (Size Suffixes) 在机器层面，CPU 处理数据时必须明确一个核心参数：位宽 (Width)。硬件需要知道它是应该只驱动数据总线上的 8 根线，还是激活全部 64 根线？是将数据放入寄存器的低 8 位（如 AL），还是覆盖整个 64 位（如 RAX）？ GAS 通过在指令助记符后添加后缀来显式控制这一行为。 2.5.1 GAS 通用命名规则 GAS 使用一套单字母后缀来表示数据宽度。需要特别警惕的是，不同架构对 “Word” 的定义存在历史分歧。 后缀 全称 大小 (Bits) 字节数 说明 b Byte 8-bit 1 通用。 w Word 16-bit 2 注意！ 在 x86 语境下，Word 是 16 位（源于 8086）。但在 ARM/RISC-V 文档中，Word 通常指 32 位。GAS 的 w 后缀专指 16 位。 l Long 32-bit 4 x86 的标准 32 位后缀。 q Quad 64-bit 8 四字（4 x 16-bit），即 64 位。 2.5.2 x86 架构：显式后缀 (Explicit Suffixes) x86 是变长指令集，且寄存器存在嵌套关系（RAX 包含 EAX 包含 AX 包含 AL）。为了消除歧义，GAS 强烈推荐在所有操作码后加上后缀。 虽然现代汇编器有时能根据寄存器名字（如 %eax）推断出你是想操作 32 位，但在涉及内存操作数（无法看出大小）或立即数时，不写后缀极易报错。 movb $0xFF, %al # 移动 1 个字节 (8-bit) movw $0xFFFF, %ax # 移动 1 个字 (16-bit) movl $0xFFFFFFFF, %eax # 移动 1 个长字 (32-bit) movq $1, %rax # 移动 1 个四字 (64-bit) # 歧义示例： # mov $0, (%rbx) # 错误！汇编器不知道你想把 rb 指向的内存清零 1 个字节还是 8 个字节。 movq $0, (%rbx) # 正确：清零 8 个字节 2.5.3 ARM / RISC-V 架构：助记符内含宽度 与 x86 不同，ARM 和 RISC-V 等 RISC 架构通常将数据宽度直接编码在指令助记符本身中，而不是依赖 GAS 的通用后缀。 在这些架构中，你通常不会看到 movl 这种写法，而是通过不同的指令变体来实现： RISC-V: lb (Load Byte): 加载 8 位 lh (Load Half-word): 加载 16 位 lw (Load Word): 加载 32 位 (注意：这里 Word 是 32 位) ld (Load Double-word): 加载 64 位 ARM: LDRB (Byte): 加载 8 位 LDRH (Half-word): 加载 16 位 LDR (Word): 加载 32 位 “Word” 的陷阱 这是一个跨架构开发时最容易混淆的概念： 在 x86 汇编中，Word = 16 bits。 在 ARM / RISC-V 汇编中，Word = 32 bits。 当你使用 GAS 的 .word 伪指令定义数据时，它在所有架构上通常都表示 32 位（或 16 位，取决于具体配置，但在现代环境下多为 32 位）。务必查阅具体目标架构的文档确认 .word 的确切长度，或者直接使用 .2byte / .4byte 这种无歧义的写法。 3. 伪指令系统 (Directives) 伪指令以 . 开头，它们不对应具体的 CPU 机器码，而是指挥汇编器如何组织二进制结构。 3.1 数据定义：如何填充字节 这些指令直接在当前地址计数器位置“发射”数据，常用于定义查找表或初始化变量。 .byte / .hword / .word / .long / .quad： 在当前地址写入特定宽度的整数。注意字节序（Endianness）取决于目标架构（x86 为小端，ARM/RISC-V 默认为小端，但可配置）。 代码段 .byte 0x12, 0x34 # 写入两个字节 .word 0xAABBCCDD # 写入 4 字节。小端模式下内存为：DD CC BB AA .ascii vs .asciz / .string： .ascii "Hello"：仅填入字符 ASCII 码，无结尾 \0。 .asciz "Hello" 或 .string：自动在末尾补 0x00（C 语言标准字符串）。 .fill / .skip / .space / .zero： 用于预留空间或清零，常用于定义栈（Stack）或 BSS 段。 代码段 stack_top: .skip 1024, 0 # 预留 1024 字节，全部填 0 stack_bottom: # 栈底标号（假设栈向下生长，SP 初始指向此处） 3.2 内存控制：如何管理地址 .section： 告诉汇编器：“接下来的数据/指令，请分类归档到哪个‘抽屉’里”。 .section .text：代码段（只读，可执行）。 .section .data：已初始化的全局变量（可读写）。 .section .bss：未初始化变量（不占磁盘空间，程序启动时自动清零）。 自定义段：例如 .section .isr_vector，配合链接脚本（Linker Script），可以将中断向量表精确固定在 Flash 的 0 地址处。 .align vs .balign： .align：行为不统一！在 x86 下 .align 4 表示 4 字节对齐；但在 ARM 下可能表示 $2^4=16$ 字节对齐。 .balign（Byte Align）：推荐使用。.balign 4, 0 明确表示保证地址能被 4 整除，如果需要填充，则填 0。 .org： 强制修改当前的地址计数器。 示例：编写 MBR 引导扇区时，需要在第 510 字节写结束标志 0x55AA，会用到 .org 510。 3.3 符号管理：与 C 语言的交互接口 这是 C 和汇编混合编程的桥梁。 .global（或 .globl）： 将符号标记为全局可见。如果不加此标记，符号仅在当前文件内部可见（类似 C 的 static），链接器无法找到它。 场景：Startup 文件（启动文件）必须声明 .global Reset_Handler，否则硬件复位后找不到入口。 .extern： 声明符号在外部定义。虽然 GAS 通常能自动推断，但显式声明能提高代码可读性。 .equ / .set： 常量定义，相当于 C 的 #define。它们只存在于汇编阶段，不占用运行时的内存空间。 代码段 .equ STACK_SIZE, 0x400 .type： 明确告诉链接器符号的类型（是函数还是数据）。 重要性：在 ARM Thumb-2 指令集中，如果链接器知道某符号是 %function，它会在计算跳转地址时自动将最低位置 1（Thumb 状态标志）。若省略此指令，可能导致切换状态失败而进入 HardFault。 .weak： 弱符号定义。 机制：如果在其他地方定义了同名的强符号（普通的 .global），链接器优先使用那个；如果没有，则使用这里的默认定义。 应用：STM32 启动文件中的中断处理函数通常用 .weak 定义为死循环。这允许用户在 C 代码中重写同名函数（如 SysTick_Handler）来覆盖默认行为，而无需修改启动代码。 4. GAS 的特色功能 (Syntactic Sugar) 4.1 局部数值标签 (Local Numeric Labels) 为了避免给简单的循环绞尽脑汁起名字（如 loop1, loop_start），GAS 允许使用数字作为临时标号。 定义：&lt;num&gt;: 引用：&lt;num&gt;f (forward, 向前/向下找)，&lt;num&gt;b (backward, 向后/向上找)。 示例： 代码段 1: # 定义标号 1 wfi # Wait for interrupt j 1b # 向后跳转到最近的标号 1 (相当于 while(1);) 4.2 宏 (Macros) 用于封装重复代码片段： 代码段 .macro 宏名称 参数1, 参数2 // 宏体内容，可使用 \参数1 引用 .endm 4.3 . (Current Location) 符号 . 代表当前地址计数器的值。 常见用法：b . 等同于 while (1);（跳转到当前地址，死循环）。]]></summary></entry><entry><title type="html">ARMv7-A 指令体系全解：数据处理、访存、分支与同步</title><link href="https://bfmhno3.github.io/note/armv7-a-isa/" rel="alternate" type="text/html" title="ARMv7-A 指令体系全解：数据处理、访存、分支与同步" /><published>2026-04-05T08:20:00+00:00</published><updated>2026-04-05T08:20:00+00:00</updated><id>https://bfmhno3.github.io/note/armv7-a-isa</id><content type="html" xml:base="https://bfmhno3.github.io/note/armv7-a-isa/"><![CDATA[<p>I.MX6ULL 的核心是 ARM Cortex-A7，它基于 ARMv7-A 架构。</p>

<p>要真正理解指令集，就不能只是背诵汇编手册，而是要回答一个核心问题：如果不看代码，只看电路，CPU 在做什么？</p>

<p>本质上，每一条指令都是对 CPU 内部寄存器状态或外部<strong>总线</strong>（内存/外设）的一次<strong>原子操作</strong>。</p>

<div class="notice--info">
  <p><strong>总线</strong>（Bus）通俗理解就是主板上的 “高速公路”。它的硬件本质则是一组并排的导线，通过这组导线，连接 CPU 内部各部分或连接 CPU 和外设。</p>

  <p>总线分为<strong>数据总线</strong>（传数据）、<strong>地址总线</strong>（告诉内存我要读哪一个单元）和<strong>控制总线</strong>（读还是写）。</p>
</div>

<div class="notice--info">
  <p><strong>原子操作</strong>（Atomic Operation）通俗理解就是，要么做完，要么完全不做，中间绝不允许被打断的操作。</p>

  <p>在 ARM 中，单条读写指令通常是原子的，但 “读-改-写” 三步操作默认不是原子的，需要特殊指令（<code class="language-plaintext highlighter-rouge">ldrex</code> / <code class="language-plaintext highlighter-rouge">strex</code>） 保证。</p>
</div>

<h2 id="1-物理本质">1. 物理本质</h2>

<p>在 ARM 状态下（Cortex-A7 上电默认状态），每一条指令都是严格的 32 位（4 字节）二进制码。</p>

<p>与 x86（变长指令）不同，ARM 的定长指令让译码器设计更加简洁高效。一条典型的 ARM 指令在物理电路上被切割为以下结构：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Instruction <span class="o">=</span> <span class="o">[</span>Codition Code <span class="o">(</span>4 bits<span class="o">)]</span> + <span class="o">[</span>OpCode &amp; Flags] + <span class="o">[</span>Operands <span class="o">(</span>Registers/Immediates<span class="o">)]</span>
</code></pre></div></div>

<h3 id="11-核心差异条件执行conditional-execution">1.1 核心差异：条件执行（Conditional Execution）</h3>

<p>这是 ARM 架构最吸引人的设计之一。几乎每一条指令的高 4 位（Bits 31-28）都是条件码。这意味着：<strong>任何指令都可以是条件跳转指令的变体</strong>。</p>

<table>
  <thead>
    <tr>
      <th>条件后缀</th>
      <th>标志位要求（CPSR）</th>
      <th>解释</th>
      <th>汇编举例</th>
      <th>C 语言映射</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>eq</td>
      <td><code class="language-plaintext highlighter-rouge">Z == 1</code></td>
      <td>Equal（相等）</td>
      <td><code class="language-plaintext highlighter-rouge">addeq r0, r1, r2</code><br />（如果相等，则相加）</td>
      <td><code class="language-plaintext highlighter-rouge">if (a == b)</code></td>
    </tr>
    <tr>
      <td>ne</td>
      <td><code class="language-plaintext highlighter-rouge">Z == 0</code></td>
      <td>Not Equal（不想等）</td>
      <td><code class="language-plaintext highlighter-rouge">movne r0, #1</code><br />（如果不等，则赋值）</td>
      <td><code class="language-plaintext highlighter-rouge">if (a != b)</code></td>
    </tr>
    <tr>
      <td>al</td>
      <td>Ignored</td>
      <td>Always（默认）</td>
      <td><code class="language-plaintext highlighter-rouge">add r0, r1, r2</code><br />（指令不写后缀默认就是 AL）</td>
      <td>（无条件执行）</td>
    </tr>
    <tr>
      <td>gt/lt</td>
      <td> </td>
      <td>大于 / 小于</td>
      <td><code class="language-plaintext highlighter-rouge">subgt ...</code></td>
      <td><code class="language-plaintext highlighter-rouge">&gt;</code> 或 <code class="language-plaintext highlighter-rouge">&lt;</code></td>
    </tr>
  </tbody>
</table>

<h2 id="2-寄存器模型">2. 寄存器模型</h2>

<p>在执行指令前，必须知道指令操作的对象。I.MX6ULL 是 Load/Store 架构。</p>

<ul>
  <li>原则：ALU（算术逻辑单元）只能处理<strong>通用寄存器</strong>中的数据。</li>
  <li>即不能对内存里的变量做运算，必须：搬运到寄存器 $\rightarrow$ 计算 $\rightarrow$ 搬回内存。</li>
</ul>

<p>I.MX6ULL 有 16 个核心寄存器（R0-R15）：</p>

<ul>
  <li>R0-R12：通用寄存器。</li>
  <li>R13（SP）：栈指针（当前的内存栈顶）。</li>
  <li>R14（LR）：链接寄存器（保存函数调用的返回地址）。</li>
  <li>R15（PC）：程序计数器（指向当前正在取指的地址）。<strong>修改 PC 就是跳转</strong>。</li>
</ul>

<p>在 ARM 开发中，寄存器最重要的属性是它的 AAPCS 角色（ARM Architecture Procedure Call Standard）。这决定了编译器如何使用它们。</p>

<table>
  <thead>
    <tr>
      <th>寄存器</th>
      <th>别名</th>
      <th>硬件功能</th>
      <th>AAPCS</th>
      <th>谁负责保存？</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>R0</td>
      <td>a1</td>
      <td>通用</td>
      <td>参数 1 / 返回值</td>
      <td>调用者（Caller）</td>
    </tr>
    <tr>
      <td>R1</td>
      <td>a2</td>
      <td>通用</td>
      <td>参数 2 / 返回值（64 位时）</td>
      <td>调用者（Caller）</td>
    </tr>
    <tr>
      <td>R2</td>
      <td>a3</td>
      <td>通用</td>
      <td>参数 3</td>
      <td>调用者（Caller）</td>
    </tr>
    <tr>
      <td>R3</td>
      <td>a4</td>
      <td>通用</td>
      <td>参数 4</td>
      <td>调用者（Caller）</td>
    </tr>
    <tr>
      <td>R4</td>
      <td>v1</td>
      <td>通用</td>
      <td>局部变量</td>
      <td>被调用者（Callee）</td>
    </tr>
    <tr>
      <td>R5</td>
      <td>v2</td>
      <td>通用</td>
      <td>局部变量</td>
      <td>被调用者（Callee）</td>
    </tr>
    <tr>
      <td>R6</td>
      <td>v3</td>
      <td>通用</td>
      <td>局部变量</td>
      <td>被调用者（Callee）</td>
    </tr>
    <tr>
      <td>R7</td>
      <td>v4</td>
      <td>通用</td>
      <td>局部变量（在 Thumb 模式下常用做栈帧指针）</td>
      <td>被调用者（Callee）</td>
    </tr>
    <tr>
      <td>R8</td>
      <td>v5</td>
      <td>通用</td>
      <td>局部变量</td>
      <td>被调用者（Callee）</td>
    </tr>
    <tr>
      <td>R9</td>
      <td>v6/SB</td>
      <td>通用</td>
      <td>局部变量（静态基址，平台相关）</td>
      <td>被调用者（Callee）</td>
    </tr>
    <tr>
      <td>R10</td>
      <td>v7</td>
      <td>通用</td>
      <td>局部变量</td>
      <td>被调用者（Callee）</td>
    </tr>
    <tr>
      <td>R11</td>
      <td>v8/FP</td>
      <td>通用</td>
      <td>帧指针（Frame Pointer）</td>
      <td>被调用者（Callee）</td>
    </tr>
    <tr>
      <td>R12</td>
      <td>IP</td>
      <td>通用</td>
      <td>过程调用中间寄存器（Intra-Procedure-call scratch）</td>
      <td>调用者（Caller）</td>
    </tr>
    <tr>
      <td>R13</td>
      <td>SP</td>
      <td>栈指针</td>
      <td>指向当前栈顶（Stach Pointer）</td>
      <td>-</td>
    </tr>
    <tr>
      <td>R14</td>
      <td>LR</td>
      <td>链接寄存器</td>
      <td>保存函数返回地址（Link Register）</td>
      <td>-</td>
    </tr>
    <tr>
      <td>R15</td>
      <td>PC</td>
      <td>程序计数器</td>
      <td>指向取指地址（Program Counter）</td>
      <td>-</td>
    </tr>
  </tbody>
</table>

<p>除此之外还有两个非常重要的特殊状态寄存器 CPSR 和 SPSR。</p>

<table>
  <thead>
    <tr>
      <th>寄存器</th>
      <th>全称</th>
      <th>解释</th>
      <th>关键位域</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>CPSR</td>
      <td>Current Program Status Register</td>
      <td>当前程序状态寄存器</td>
      <td>N, Z, C, V（条件标志位）<br />I, F（中断屏蔽位）<br />Mode（运行模式位：User, SVC, IRQ 等）</td>
    </tr>
    <tr>
      <td>SPSR</td>
      <td>Saved Program Status Register</td>
      <td>备份程序状态寄存器</td>
      <td>仅在异常模式下存在<br />当中断发生时，硬件自动把 CPSR 里的值拷贝到 SPSR<br />当中断返回时，再从 SPSR 恢复到 CPSR</td>
    </tr>
  </tbody>
</table>

<p>ARMv7-A 实际上不止上述的 18 个寄存器，还有<strong>影子寄存器</strong>（Banked Registers）。</p>

<h3 id="21-为什么要区分调用者保存和被调用者保存">2.1 为什么要区分调用者保存和被调用者保存？</h3>

<p>简单来说，这是 ARM 官方制定的规范，叫做 AAPCS（Procedure Call Standard for the ARM Architecture，ARM 架构过程调用标准）。ARM 公司不仅设计 CPU 硬件架构，还定义了软件应如何与这些硬件交互的 ABI（Application Binary Interface，应用程序二进制接口）。</p>

<p>制定这一规范的核心目的在于<strong>统一标准</strong>。它确保了二进制层面的兼容性——无论代码是使用 GCC 还是 ARM Compiler 编译的，只要遵循 AAPCS，它们就能无缝协作。你无需担心不同编译器在参数传递或堆栈管理上的差异，从而可以放心地调用第三方提供的预编译库。</p>

<p>当 Caller 调用 Callee 时，参数通过 R0-R3 传递。调用结束后，返回值通常保存在 R0 或 R0-R1 中。这就带来了一个问题：Callee 执行时不可避免地会使用寄存器，Caller 如何确保自己的数据不被覆盖？</p>

<p>AAPCS 为此制定了明确的规范：</p>

<ul>
  <li><strong>易失性寄存器</strong>（Volatile Registers, R0-R3, R12）：AAPCS 规定 Callee 可以随意修改它们。因此，如果 Caller 在 R0-R3 中存放了重要数据且在调用后仍需要使用，Caller 必须自己负责在调用前将它们压栈保存（Caller-saved）。</li>
  <li><strong>非易失性寄存器</strong>（Non-volatile Registers, R4-R11）：AAPCS 规定 Callee 必须保证函数返回时，这些寄存器的值与进入函数时完全一致。如果 Callee 内部需要使用 R4-R11，Callee 必须负责先将其压栈保存，并在返回前恢复（Callee-saved）。这样 Caller 就可以放心地将关键数据放在这里，无需担心被篡改。</li>
</ul>

<p>上述规则解决了函数间的协作问题，但<strong>中断</strong>（Interrupt）打破了这种默契。中断是异步突发的，主程序（Caller）根本不知道何时会被打断，因此无法预先保存 R0-R3 等易失性寄存器。</p>

<p>为了解决这个问题，ARM Cortex-M 架构引入了<strong>硬件压栈</strong>（Hardware Stacking）机制。当 CPU 响应中断时，硬件会自动将 Caller 上下文（包括 R0-R3、R12、LR、PC 和 xPSR）。这一过程无需软件干预。</p>

<p>因此，进入中断服务函数（ISR）后，寄存器已经被硬件保护好了。ISR 此时只需像普通函数一样，遵守 AAPCS 规则处理 R4-R11（即如果用到则保存）即可。这种硬件机制极大降低了中断延迟，也简化了软件设计。</p>

<h3 id="22-r12ip是干什么的">2.2 R12（IP）是干什么的？</h3>

<p>它是一个被很多人容易忽略的寄存器，主要用于链接器（Linker）插入的代码。当你的程序非常大，<code class="language-plaintext highlighter-rouge">bl target</code> 跳转的目标太远（超过 $\pm 32\text{MB}$）时，<code class="language-plaintext highlighter-rouge">bl</code> 指令够不着。此时，链接器会自动插入一段小代码（Veneer/Trampoline），先跳转到 R12 指向的中转站，再由 R12 赋值给 PC 进行长跳转。</p>

<h3 id="23-r15pc为什么有-8-现象">2.3 R15（PC）为什么有 +8 现象？</h3>

<p>在 ARM 状态（32 位指令）下，读取 PC 的值，通常是当前指令地址 + 8。这则是因为 CPU 的流水线（Pipeline）设计，当 CPU 执行当前指令时，取指单元已经在取下下条指令了（当前指令地址 +8 地址处）。</p>

<h2 id="3-指令集讲解">3. 指令集讲解</h2>

<h3 id="31-数据处理指令data-processing">3.1 数据处理指令（Data Processing）</h3>

<p class="notice--info"><strong>核心逻辑</strong>：ALU 运算，结果写回寄存器，并可选更新 CPSR（如果指令后加 <code class="language-plaintext highlighter-rouge">s</code>，如 <code class="language-plaintext highlighter-rouge">adds</code>）</p>

<h4 id="311-算术类">3.1.1 算术类</h4>

<table>
  <thead>
    <tr>
      <th style="text-align: center">指令</th>
      <th>解释</th>
      <th>汇编举例</th>
      <th>C 语言映射</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">add</code><br />(Add)</td>
      <td>加法。</td>
      <td><code class="language-plaintext highlighter-rouge">add r0, r1, r2</code><br />(<code class="language-plaintext highlighter-rouge">R0 = R1 + R2</code>)</td>
      <td><code class="language-plaintext highlighter-rouge">a = b + c;</code></td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">sub</code><br />(Subtract)</td>
      <td>减法。</td>
      <td><code class="language-plaintext highlighter-rouge">sub r0, r1, #1</code><br />(<code class="language-plaintext highlighter-rouge">R0 = R1 - 1</code>)</td>
      <td><code class="language-plaintext highlighter-rouge">a = b - 1;</code></td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">rsb</code><br />(Reverse Substract)</td>
      <td>逆向减法。<br />用于 R0 = 常数 - Rx 的场景。</td>
      <td><code class="language-plaintext highlighter-rouge">rsb r0, r1, #0</code><br />(<code class="language-plaintext highlighter-rouge">R0 = 0 - R1</code>)</td>
      <td><code class="language-plaintext highlighter-rouge">a = -b;</code></td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">adc</code><br />(Add with Carry)</td>
      <td>带进位加法。<br />用于实现 64 位加法的高 32 位运算。</td>
      <td><code class="language-plaintext highlighter-rouge">adc r0, r1, r2</code><br />(<code class="language-plaintext highlighter-rouge">R0 = R1 + R2 + Carry</code>)</td>
      <td><code class="language-plaintext highlighter-rouge">(long long )a + (long long)b</code> 的高位部分</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">sbc</code><br />(Subtract with Carry)</td>
      <td>带借位减法。<br />用于 64 位减法。</td>
      <td><code class="language-plaintext highlighter-rouge">sbc r0, r1, r2</code><br />(<code class="language-plaintext highlighter-rouge">R0 = R1 - R2 - !Carry</code>)</td>
      <td>64 位减法的高位部分</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">mul</code><br />(Multiply)</td>
      <td>32 位乘法（结果取低 32 位）</td>
      <td><code class="language-plaintext highlighter-rouge">mul r0, r1, r2</code><br />(<code class="language-plaintext highlighter-rouge">R0 = R1 * R2</code>)</td>
      <td><code class="language-plaintext highlighter-rouge">int a = b * c;</code></td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">mla</code><br />(Multiply Accumulate)</td>
      <td>乘法运算。<br />DSP 算法核心指令。</td>
      <td><code class="language-plaintext highlighter-rouge">mul r0, r1, r2, r3</code><br />(<code class="language-plaintext highlighter-rouge">R0 = R1 * R2 +  R3</code>)</td>
      <td><code class="language-plaintext highlighter-rouge">a = (b * c) + d;</code></td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">udiv</code><br />(Unsigned Divide)</td>
      <td>无符号除法。</td>
      <td><code class="language-plaintext highlighter-rouge">udiv r0, r1, r2</code></td>
      <td><code class="language-plaintext highlighter-rouge">unsigned a = b / c;</code></td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">sdiv</code><br />(Signed Divided)</td>
      <td>有符号除法。</td>
      <td><code class="language-plaintext highlighter-rouge">sdiv r0, r1, r2</code></td>
      <td><code class="language-plaintext highlighter-rouge">int a = b / c;</code></td>
    </tr>
  </tbody>
</table>

<h4 id="312-逻辑类">3.1.2 逻辑类</h4>

<table>
  <thead>
    <tr>
      <th style="text-align: center">指令</th>
      <th>解释</th>
      <th>汇编举例</th>
      <th>C 语言映射</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">and</code><br />(Logical AND)</td>
      <td>按位与。<br />常用于**提取**某几位。</td>
      <td><code class="language-plaintext highlighter-rouge">and r0, r1, #0xff</code></td>
      <td><code class="language-plaintext highlighter-rouge">a = b &amp; 0xff;</code></td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">orr</code><br />(Logical OR)</td>
      <td>按位或。<br />常用于**置位**（设为 1）。</td>
      <td><code class="language-plaintext highlighter-rouge">orr r0, r0, #(1&lt;&lt;3)</code></td>
      <td><code class="language-plaintext highlighter-rouge">r0                | = (1 &lt;&lt; 3);</code></td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">eor</code><br />(Exclusive OR)</td>
      <td>按位异或。<br />常用于**翻转**某几位。</td>
      <td><code class="language-plaintext highlighter-rouge">eor r0, r0, #1</code></td>
      <td><code class="language-plaintext highlighter-rouge">a = b ^ 1;</code></td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">bic</code><br />(Bit Clear)</td>
      <td>**位清除**。<br /><code class="language-plaintext highlighter-rouge">and</code> 的反向操作：<code class="language-plaintext highlighter-rouge">dest = op1 and (not op2)</code></td>
      <td><code class="language-plaintext highlighter-rouge">bic r0, r0, #(1&lt;&lt;3)</code></td>
      <td><code class="language-plaintext highlighter-rouge">r0 &amp;= ~(1 &lt;&lt; 3);</code></td>
    </tr>
  </tbody>
</table>

<h4 id="313-数据搬运">3.1.3 数据搬运</h4>

<table>
  <thead>
    <tr>
      <th>指令</th>
      <th>解释</th>
      <th>汇编举例</th>
      <th>C 语言映射</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">mov</code><br />(Move)</td>
      <td>寄存器间传值，或立即数赋值。</td>
      <td><code class="language-plaintext highlighter-rouge">mov r0, r1</code></td>
      <td><code class="language-plaintext highlighter-rouge">a = b;</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">mvn</code><br />(Move Not)</td>
      <td>**按位取反**后传送。</td>
      <td><code class="language-plaintext highlighter-rouge">mvn r0, #0</code><br />(<code class="language-plaintext highlighter-rouge">r0 = 0xffffffff</code>)</td>
      <td><code class="language-plaintext highlighter-rouge">a = ~0;</code></td>
    </tr>
  </tbody>
</table>

<div class="notice--warning">
  <p>ARM 指令是定长 32 位的。除去操作码和条件码，留给立即数的空间只有 12 位。并且这 12 位还不是直接存数字，而是被拆分位 8 位常数 + 4 位循环右移。</p>

  <p>这就意味着在向寄存器写入某些立即数时，例如 <code class="language-plaintext highlighter-rouge">mov r0, #0x12345678</code> 是会失败的，因为 <code class="language-plaintext highlighter-rouge">0x12345678</code> 这个立即数太乱了，无法通过 8 位常数 + 4 位循环右移凑出来。</p>

  <p>解决办法是使用 <code class="language-plaintext highlighter-rouge">ldr r0, =0x12345678</code>。此时，汇编器会悄悄在代码段的末尾（Literal Pool）找个地方放 <code class="language-plaintext highlighter-rouge">0x12345678</code>，然后把这条指令翻译成去那个地址读数据。</p>
</div>

<h4 id="314-比较指令">3.1.4 比较指令</h4>

<p class="notice--info"><strong>不保存运算结果</strong>，只更新 CPSR 的标志位（N, Z, C, V），为后面的条件执行做铺垫。</p>

<table>
  <thead>
    <tr>
      <th>指令</th>
      <th>解释</th>
      <th>汇编举例</th>
      <th>C 语言映射</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">cmp</code><br />(Compare)</td>
      <td>本质是 <code class="language-plaintext highlighter-rouge">sub</code>。<br />比较大小。</td>
      <td><code class="language-plaintext highlighter-rouge">cmp r0, #5</code><br />(<code class="language-plaintext highlighter-rouge">r0 - 5</code>，更新 Z 标志位)</td>
      <td><code class="language-plaintext highlighter-rouge">if (a &gt;= 5)</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">tst</code><br />(Test)</td>
      <td>本质是 <code class="language-plaintext highlighter-rouge">and</code>。<br />测试某位是否为 1。</td>
      <td><code class="language-plaintext highlighter-rouge">tst r0, #(1&lt;&lt;3)</code><br />(<code class="language-plaintext highlighter-rouge">r0 &amp; 0x8</code>，更新 Z 标志位)</td>
      <td><code class="language-plaintext highlighter-rouge">if (a &amp; (1&lt;&lt;3))</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">teq</code><br />(Test Equivalence)</td>
      <td>本质是 <code class="language-plaintext highlighter-rouge">eor</code>。<br />测试两个数是否相等（不影响进位标志）。</td>
      <td><code class="language-plaintext highlighter-rouge">teq r0, r1</code></td>
      <td>常用于汇编手写检查</td>
    </tr>
  </tbody>
</table>

<h4 id="315-桶形移位器barrel-shifter">3.1.5 桶形移位器（Barrel Shifter）</h4>

<p>在 ARM 中，移位操作不是独立指令，而是数据进入 ALU 之前的<strong>预处理</strong>。它是免费的。指令 <code class="language-plaintext highlighter-rouge">add r0, r1, r2, lsl #2</code> 的物理含义是：</p>

<p>$$
\text{R0} = \text{R1} + (\text{R2} \ll 2)
$$</p>

<p>这在单周期内完成，这就是为什么 ARM 处理数组索引（地址 = 基址 + 索引 $\times$ 4）特别快。</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">模式</th>
      <th>物理动作</th>
      <th>C 标志位更新</th>
      <th style="text-align: center">数学含义</th>
      <th>C 语言映射</th>
      <th>典型应用场景</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">lsl</code><br />(Logical Shift Left)</td>
      <td>**左移**。<br />低位补 0，高位移出</td>
      <td>最后移出的位</td>
      <td style="text-align: center">$\times 2^n$</td>
      <td><code class="language-plaintext highlighter-rouge">x &lt;&lt; n</code></td>
      <td>乘法优化、位字段对齐、生成掩码</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">lsr</code><br />(Logical Shift Right)</td>
      <td>**右移**。<br />高位补 0，低位移出</td>
      <td>最后移出的位</td>
      <td style="text-align: center">$/2^n$<br />（无符号）</td>
      <td><code class="language-plaintext highlighter-rouge">(unsigned)x &gt;&gt; n</code></td>
      <td>无符号数除法、读取特定位段</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">asr</code><br />(Arithmetic Shift Right)</td>
      <td>**算术右移**。<br />高位补**符号位**，低位移出</td>
      <td>最后移出的位</td>
      <td style="text-align: center">$/2^n$<br />（有符号）</td>
      <td><code class="language-plaintext highlighter-rouge">(signed)x &gt;&gt; n</code></td>
      <td>有符号数除法</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">ror</code><br />(Rotate Right)</td>
      <td>**循环右移**。<br />低位移出后，**填补到高位**</td>
      <td>最后移出的位</td>
      <td style="text-align: center">-</td>
      <td><code class="language-plaintext highlighter-rouge">(x &gt;&gt; n)          | (x &lt;&lt; (32-n))</code></td>
      <td>加密算法、哈希函数、位图操作</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">rrx</code><br />(Rotate Right with Extend)</td>
      <td>**带扩展循环右移**。<br />33 位循环<br />（32 位寄存器 + 1 位 Carry）</td>
      <td>寄存器移出的 LSB</td>
      <td style="text-align: center">-</td>
      <td>无</td>
      <td>大数运算、多精度算术</td>
    </tr>
  </tbody>
</table>

<h3 id="32-内存访问指令memory-access">3.2 内存访问指令（Memory Access）</h3>

<p>这是 CPU 与外部世界（DDR 内存、GPIO 控制器等）交互的唯一桥梁。</p>

<h4 id="321-单寄存器">3.2.1 单寄存器</h4>

<table>
  <thead>
    <tr>
      <th style="text-align: center">指令</th>
      <th>解释</th>
      <th>汇编举例</th>
      <th>C 语言映射</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">ldr</code><br />(Load Register)</td>
      <td>从内存读 4 字节到寄存器。</td>
      <td><code class="language-plaintext highlighter-rouge">ldr r0, [r1]</code></td>
      <td><code class="language-plaintext highlighter-rouge">r0 = *r1;</code></td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">str</code><br />(Store Register)</td>
      <td>把寄存器的值写入内存 4 字节。</td>
      <td><code class="language-plaintext highlighter-rouge">str r0, [r1]</code></td>
      <td><code class="language-plaintext highlighter-rouge">*r1 = r0;</code></td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">ldrb</code> (8 bit)<br />(Load Byte)</td>
      <td>读 1 字节，高 24 位**补零**。</td>
      <td><code class="language-plaintext highlighter-rouge">ldrb r0, [r1]</code></td>
      <td><code class="language-plaintext highlighter-rouge">r0 = (unsigned char)*r1;</code></td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">ldrh</code> (16 bit)<br />(Load Halfword)</td>
      <td>读 2 字节，高 16 位**补零**。</td>
      <td><code class="language-plaintext highlighter-rouge">ldrh r0, [r1]</code></td>
      <td><code class="language-plaintext highlighter-rouge">r0 = (unsigned short)*r1;</code></td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">ldrsb</code><br />(Load Signed Byte)</td>
      <td>读 1 字节，高 24 位**补符号位**。</td>
      <td><code class="language-plaintext highlighter-rouge">ldrsb r0, [r1]</code></td>
      <td><code class="language-plaintext highlighter-rouge">r0 = (char)*r1;</code></td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">ldrsh</code><br />(Load Signed Halfword)</td>
      <td>读 2 字节，高 16 位**补符号位**。</td>
      <td><code class="language-plaintext highlighter-rouge">ldrsh r0, [r1]</code></td>
      <td><code class="language-plaintext highlighter-rouge">r0 = (short)*r1;</code></td>
    </tr>
  </tbody>
</table>

<h4 id="322-寻址模式">3.2.2 寻址模式</h4>

<table>
  <thead>
    <tr>
      <th style="text-align: center">寻址模式</th>
      <th>汇编举例</th>
      <th>C 语言映射</th>
      <th>状态</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">立即数偏移<br />（Offset）</td>
      <td><code class="language-plaintext highlighter-rouge">ldr r0, [r1, #4]</code></td>
      <td><code class="language-plaintext highlighter-rouge">r0 = *(r1 + 1);</code></td>
      <td><code class="language-plaintext highlighter-rouge">r1</code> 保持不变</td>
    </tr>
    <tr>
      <td style="text-align: center">前索引<br />（Pre-indexed）</td>
      <td><code class="language-plaintext highlighter-rouge">ldr r0, [r1, #4]!</code></td>
      <td><code class="language-plaintext highlighter-rouge">r0 = *(++ptr);</code></td>
      <td><code class="language-plaintext highlighter-rouge">r1</code> 在访问内存**前**更新为 <code class="language-plaintext highlighter-rouge">r1 + 4</code></td>
    </tr>
    <tr>
      <td style="text-align: center">后索引<br />（Post-indexed）</td>
      <td><code class="language-plaintext highlighter-rouge">ldr r0, [r1], #4</code></td>
      <td><code class="language-plaintext highlighter-rouge">r0 = *(ptr++);</code></td>
      <td>访问内存**后**，<code class="language-plaintext highlighter-rouge">r1</code> 更新为 <code class="language-plaintext highlighter-rouge">r1 + 4</code></td>
    </tr>
  </tbody>
</table>

<h4 id="323-多寄存器">3.2.3 多寄存器</h4>

<p class="notice--info">这是 ARM 实现<strong>栈</strong>（Stack）和<strong>上下文切换</strong>（Context Switch）的基石。</p>

<table>
  <thead>
    <tr>
      <th>指令</th>
      <th>解释</th>
      <th>汇编举例</th>
      <th>场景</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ldm</code><br />(Load Multiple)</td>
      <td>内存 $\rightarrow$ 多个寄存器。</td>
      <td><code class="language-plaintext highlighter-rouge">ldm r0, {r4-r6, pc}</code></td>
      <td>恢复现场并返回</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">stm</code><br />(Store Multiple)</td>
      <td>多个寄存器 $\rightarrow$ 内存。</td>
      <td><code class="language-plaintext highlighter-rouge">stm r0, {r4-r6, lr}</code></td>
      <td>保存现场</td>
    </tr>
  </tbody>
</table>

<p>说明：</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">ldm r0, {r4-r6, pc}</code>：从以 <code class="language-plaintext highlighter-rouge">r0</code> 为基址的内存地址开始，依次加载数据到寄存器 <code class="language-plaintext highlighter-rouge">r4</code>、<code class="language-plaintext highlighter-rouge">r5</code>、<code class="language-plaintext highlighter-rouge">r6</code> 和 <code class="language-plaintext highlighter-rouge">pc</code>。</li>
  <li><code class="language-plaintext highlighter-rouge">stm r0, {r4-r6, lr}</code>：从寄存器 <code class="language-plaintext highlighter-rouge">r4</code>、<code class="language-plaintext highlighter-rouge">r5</code>、<code class="language-plaintext highlighter-rouge">r6</code> 和 <code class="language-plaintext highlighter-rouge">lr</code> 中，依次将数据存储到以 <code class="language-plaintext highlighter-rouge">r0</code> 为基址的内存地址。</li>
</ul>

<h5 id="后缀模式">后缀模式</h5>

<p>虽然有 <code class="language-plaintext highlighter-rouge">ia</code>、<code class="language-plaintext highlighter-rouge">ib</code>、<code class="language-plaintext highlighter-rouge">da</code> 和 <code class="language-plaintext highlighter-rouge">db</code> 四种，但 ARM 官方推荐在操作栈时使用别名，方便记忆：</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">stmfd</code> (Full Descending) = <code class="language-plaintext highlighter-rouge">stmdb</code> (Decrement Before)：入栈（压栈）。</li>
  <li><code class="language-plaintext highlighter-rouge">ldmfd</code> (Full Descending) = <code class="language-plaintext highlighter-rouge">ldmia</code> (Increment After)：出栈。</li>
  <li><code class="language-plaintext highlighter-rouge">push {reglist}</code> 等价于 <code class="language-plaintext highlighter-rouge">stmdb sp!, {reglist}</code>。</li>
  <li><code class="language-plaintext highlighter-rouge">pop {reglist}</code> 等价于 <code class="language-plaintext highlighter-rouge">ldmia sp!, {reglist}</code>。</li>
</ul>

<h3 id="33-分支与控制流branch--control">3.3 分支与控制流（Branch &amp; Control）</h3>

<p>打破顺序执行，实现 <code class="language-plaintext highlighter-rouge">if</code>、<code class="language-plaintext highlighter-rouge">for</code>、<code class="language-plaintext highlighter-rouge">function call</code>。</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">指令</th>
      <th>解释</th>
      <th>汇编举例</th>
      <th>C 语言映射</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">b</code><br />(Branch)</td>
      <td>相对跳转（范围 $\pm 32\text{MB}$）</td>
      <td><code class="language-plaintext highlighter-rouge">b label</code></td>
      <td><code class="language-plaintext highlighter-rouge">goto label;</code></td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">bl</code><br />(Branch with Link)</td>
      <td>带连接跳转。<br />**保存下一条指令地址到 <code class="language-plaintext highlighter-rouge">lr</code>**。</td>
      <td><code class="language-plaintext highlighter-rouge">bl func</code></td>
      <td><code class="language-plaintext highlighter-rouge">func();</code></td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">bx</code><br />(Branch Exchange)</td>
      <td>跳转并根据目标地址最低位切换状态<br />（ARM $\rightarrow$ Thumb）。</td>
      <td><code class="language-plaintext highlighter-rouge">bx lr</code></td>
      <td><code class="language-plaintext highlighter-rouge">return;</code>（函数返回）</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">blx</code><br />(Branch Link Exchange)</td>
      <td><code class="language-plaintext highlighter-rouge">bl</code> + <code class="language-plaintext highlighter-rouge">bx</code> 的结合。<br />调用不同指令集的函数。</td>
      <td><code class="language-plaintext highlighter-rouge">blx r0</code></td>
      <td>函数指针调用</td>
    </tr>
  </tbody>
</table>

<p class="notice--info">还可以直接修改 PC 寄存器实现跳转，例如 <code class="language-plaintext highlighter-rouge">mov pc, lr</code>，这是函数返回的底层实现。</p>

<h3 id="34-系统与状态指令system--status">3.4 系统与状态指令（System &amp; Status）</h3>

<p>这部分在使用上层语言实现业务逻辑时几乎看不到，但是对于操作系统而言至关重要。</p>

<h4 id="341-状态寄存器访问">3.4.1 状态寄存器访问</h4>

<table>
  <thead>
    <tr>
      <th style="text-align: center">指令</th>
      <th>解释</th>
      <th>汇编举例</th>
      <th>场景</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">mrs</code><br />(Move Register from Status)</td>
      <td>读取 CPSR/SPSR 到通用寄存器。</td>
      <td><code class="language-plaintext highlighter-rouge">mrs r0, cpsr</code></td>
      <td>读取当前中断状态</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">msr</code><br />(Move Status from Register)</td>
      <td>将通用寄存器写会 CPSR/SPSR。</td>
      <td><code class="language-plaintext highlighter-rouge">msr cpsr_c, r0</code></td>
      <td>开/关中断</td>
    </tr>
  </tbody>
</table>

<h4 id="342-异常与中断生成">3.4.2 异常与中断生成</h4>

<table>
  <thead>
    <tr>
      <th style="text-align: center">指令</th>
      <th>解释</th>
      <th>场景</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">svc</code><br />(Supervisor Call)</td>
      <td>**软中断**。<br />CPU 进入 SVC 模式。<br />Linux 中用于从用户态（User）陷入内核态（Kernel）。</td>
      <td>系统调用（<code class="language-plaintext highlighter-rouge">open</code>，<code class="language-plaintext highlighter-rouge">read</code> 等）。</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">bkpt</code><br />(Breakpoint)</td>
      <td>断点指令。<br />执行到此时 CPU 暂停并通知调试器。</td>
      <td>JTAG/GDB 调试。</td>
    </tr>
  </tbody>
</table>

<h4 id="343-协处理器指令">3.4.3 协处理器指令</h4>

<p>I.MX6ULL 中，CP15 协处理器控制着 MMU、Cache 和中断向量表基地址。</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">指令</th>
      <th>解释</th>
      <th>助记法</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">mcr</code><br />(Move to Coprocessor from Register)</td>
      <td>CPU $\rightarrow$ CP15<br />（写配置）</td>
      <td>R（Register）在最后，表示源</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">mrc</code><br />(Move to Register from Coprocessor)</td>
      <td>CP15 $\rightarrow$ CPU<br />（读状态）</td>
      <td>C（Coprocessor）在最后，表示源</td>
    </tr>
  </tbody>
</table>

<p>例如：<code class="language-plaintext highlighter-rouge">mcr p15, 0, r0, c1, c0, 0</code>（开启 MMU）。</p>

<p class="notice--info"><strong>协处理器</strong>（Coprocessor）时依附于 CPU 主核的独立电路单元。ARM 主核（ALU）只管算逻辑和搬砖，像 MMU（内存映射管理）、Cache 配置这些脏活累活，都丢给 CP15 协处理器；浮点运算丢给 CP10/CP11。</p>

<h4 id="344-内存屏障memory-barriers">3.4.4 内存屏障（Memory Barriers）</h4>

<table>
  <thead>
    <tr>
      <th style="text-align: center">指令</th>
      <th>解释</th>
      <th>场景</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">dmb</code><br />(Data Memory Barrier)</td>
      <td>确保指令前的内存访问在指令后的内存访问之前完成。</td>
      <td>操作 DMA 描述符时</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">dsb</code><br />(Data Synchronization Barrier)</td>
      <td>比 DMB 更强。<br />确保前面的访存全部完成，CPU 才会执行下一条指令。</td>
      <td>修改页表后</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">isb</code><br />(Instruction Synchronization Barrier)</td>
      <td>冲刷流水线。确保重新取指。</td>
      <td>修改了代码段或中断向量表后</td>
    </tr>
  </tbody>
</table>

<h3 id="35-同步与原子操作synchronization">3.5 同步与原子操作（Synchronization）</h3>

<p>对于多任务操作系统，需要处理竞态条件。</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">指令</th>
      <th>解释</th>
      <th>C 语言映射</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">ldrex</code><br />(Load Register Exclusive)</td>
      <td>读取并设置独占监视器</td>
      <td><code class="language-plaintext highlighter-rouge">atomic_load</code> (C11)</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">strex</code><br />(Store Register Exclusive)</td>
      <td>如果监视状态未被破坏，则写入。<br />返回 0 成功，1 失败。</td>
      <td><code class="language-plaintext highlighter-rouge">atomic_compare_exchange</code> (C11)</td>
    </tr>
    <tr>
      <td style="text-align: center"><code class="language-plaintext highlighter-rouge">clrex</code><br />(Clear Exclusive)</td>
      <td>清除监视器状态。</td>
      <td>上下文切换时清理残余状态</td>
    </tr>
  </tbody>
</table>

<div class="notice--info">
  <p>ARM 没有 x86 的 <code class="language-plaintext highlighter-rouge">lock</code> 总线锁，它使用 “<strong>独占监视器</strong>” 硬件。</p>

  <ul>
    <li><code class="language-plaintext highlighter-rouge">ldrex</code> 读取数据的同时标记该地址为 “被我监视”。</li>
    <li><code class="language-plaintext highlighter-rouge">strex</code> 尝试写入。如果期间没有其他核心 / DMA 修改过该地址，写入成功；否则写入失败。</li>
  </ul>

  <p>这会实现 <code class="language-plaintext highlighter-rouge">mutex</code>（互斥锁）和 <code class="language-plaintext highlighter-rouge">semaphore</code>（信号量）的基石。</p>
</div>

<div class="notice--info">
  <p>互斥锁（Mutex）&amp; 信号量（Semaphore）：</p>

  <ul>
    <li>互斥锁（厕所钥匙）：只有一个资源（厕所）。你拿了钥匙进去锁门，别惹只能在门口排队（阻塞），直到你出来还钥匙。用户保护<strong>临界区</strong>。</li>
    <li>信号量（餐厅叫号）：有 $N$ 个资源（空桌子）。来一个人 $N-1$，走一个人 $N+1$。当 $N=0$ 时，新来的人必须等到。</li>
  </ul>

  <p>这两个都是软件概念，但底层必须由<strong>原子指令</strong>支持。</p>
</div>]]></content><author><name>bfmhno3</name></author><category term="Note" /><category term="ARM" /><category term="Assembly" /><category term="i.MX6ULL" /><summary type="html"><![CDATA[I.MX6ULL 的核心是 ARM Cortex-A7，它基于 ARMv7-A 架构。 要真正理解指令集，就不能只是背诵汇编手册，而是要回答一个核心问题：如果不看代码，只看电路，CPU 在做什么？ 本质上，每一条指令都是对 CPU 内部寄存器状态或外部总线（内存/外设）的一次原子操作。 总线（Bus）通俗理解就是主板上的 “高速公路”。它的硬件本质则是一组并排的导线，通过这组导线，连接 CPU 内部各部分或连接 CPU 和外设。 总线分为数据总线（传数据）、地址总线（告诉内存我要读哪一个单元）和控制总线（读还是写）。 原子操作（Atomic Operation）通俗理解就是，要么做完，要么完全不做，中间绝不允许被打断的操作。 在 ARM 中，单条读写指令通常是原子的，但 “读-改-写” 三步操作默认不是原子的，需要特殊指令（ldrex / strex） 保证。 1. 物理本质 在 ARM 状态下（Cortex-A7 上电默认状态），每一条指令都是严格的 32 位（4 字节）二进制码。 与 x86（变长指令）不同，ARM 的定长指令让译码器设计更加简洁高效。一条典型的 ARM 指令在物理电路上被切割为以下结构： Instruction = [Codition Code (4 bits)] + [OpCode &amp; Flags] + [Operands (Registers/Immediates)] 1.1 核心差异：条件执行（Conditional Execution） 这是 ARM 架构最吸引人的设计之一。几乎每一条指令的高 4 位（Bits 31-28）都是条件码。这意味着：任何指令都可以是条件跳转指令的变体。 条件后缀 标志位要求（CPSR） 解释 汇编举例 C 语言映射 eq Z == 1 Equal（相等） addeq r0, r1, r2（如果相等，则相加） if (a == b) ne Z == 0 Not Equal（不想等） movne r0, #1（如果不等，则赋值） if (a != b) al Ignored Always（默认） add r0, r1, r2（指令不写后缀默认就是 AL） （无条件执行） gt/lt   大于 / 小于 subgt ... &gt; 或 &lt; 2. 寄存器模型 在执行指令前，必须知道指令操作的对象。I.MX6ULL 是 Load/Store 架构。 原则：ALU（算术逻辑单元）只能处理通用寄存器中的数据。 即不能对内存里的变量做运算，必须：搬运到寄存器 $\rightarrow$ 计算 $\rightarrow$ 搬回内存。 I.MX6ULL 有 16 个核心寄存器（R0-R15）： R0-R12：通用寄存器。 R13（SP）：栈指针（当前的内存栈顶）。 R14（LR）：链接寄存器（保存函数调用的返回地址）。 R15（PC）：程序计数器（指向当前正在取指的地址）。修改 PC 就是跳转。 在 ARM 开发中，寄存器最重要的属性是它的 AAPCS 角色（ARM Architecture Procedure Call Standard）。这决定了编译器如何使用它们。 寄存器 别名 硬件功能 AAPCS 谁负责保存？ R0 a1 通用 参数 1 / 返回值 调用者（Caller） R1 a2 通用 参数 2 / 返回值（64 位时） 调用者（Caller） R2 a3 通用 参数 3 调用者（Caller） R3 a4 通用 参数 4 调用者（Caller） R4 v1 通用 局部变量 被调用者（Callee） R5 v2 通用 局部变量 被调用者（Callee） R6 v3 通用 局部变量 被调用者（Callee） R7 v4 通用 局部变量（在 Thumb 模式下常用做栈帧指针） 被调用者（Callee） R8 v5 通用 局部变量 被调用者（Callee） R9 v6/SB 通用 局部变量（静态基址，平台相关） 被调用者（Callee） R10 v7 通用 局部变量 被调用者（Callee） R11 v8/FP 通用 帧指针（Frame Pointer） 被调用者（Callee） R12 IP 通用 过程调用中间寄存器（Intra-Procedure-call scratch） 调用者（Caller） R13 SP 栈指针 指向当前栈顶（Stach Pointer） - R14 LR 链接寄存器 保存函数返回地址（Link Register） - R15 PC 程序计数器 指向取指地址（Program Counter） - 除此之外还有两个非常重要的特殊状态寄存器 CPSR 和 SPSR。 寄存器 全称 解释 关键位域 CPSR Current Program Status Register 当前程序状态寄存器 N, Z, C, V（条件标志位）I, F（中断屏蔽位）Mode（运行模式位：User, SVC, IRQ 等） SPSR Saved Program Status Register 备份程序状态寄存器 仅在异常模式下存在当中断发生时，硬件自动把 CPSR 里的值拷贝到 SPSR当中断返回时，再从 SPSR 恢复到 CPSR ARMv7-A 实际上不止上述的 18 个寄存器，还有影子寄存器（Banked Registers）。 2.1 为什么要区分调用者保存和被调用者保存？ 简单来说，这是 ARM 官方制定的规范，叫做 AAPCS（Procedure Call Standard for the ARM Architecture，ARM 架构过程调用标准）。ARM 公司不仅设计 CPU 硬件架构，还定义了软件应如何与这些硬件交互的 ABI（Application Binary Interface，应用程序二进制接口）。 制定这一规范的核心目的在于统一标准。它确保了二进制层面的兼容性——无论代码是使用 GCC 还是 ARM Compiler 编译的，只要遵循 AAPCS，它们就能无缝协作。你无需担心不同编译器在参数传递或堆栈管理上的差异，从而可以放心地调用第三方提供的预编译库。 当 Caller 调用 Callee 时，参数通过 R0-R3 传递。调用结束后，返回值通常保存在 R0 或 R0-R1 中。这就带来了一个问题：Callee 执行时不可避免地会使用寄存器，Caller 如何确保自己的数据不被覆盖？ AAPCS 为此制定了明确的规范： 易失性寄存器（Volatile Registers, R0-R3, R12）：AAPCS 规定 Callee 可以随意修改它们。因此，如果 Caller 在 R0-R3 中存放了重要数据且在调用后仍需要使用，Caller 必须自己负责在调用前将它们压栈保存（Caller-saved）。 非易失性寄存器（Non-volatile Registers, R4-R11）：AAPCS 规定 Callee 必须保证函数返回时，这些寄存器的值与进入函数时完全一致。如果 Callee 内部需要使用 R4-R11，Callee 必须负责先将其压栈保存，并在返回前恢复（Callee-saved）。这样 Caller 就可以放心地将关键数据放在这里，无需担心被篡改。 上述规则解决了函数间的协作问题，但中断（Interrupt）打破了这种默契。中断是异步突发的，主程序（Caller）根本不知道何时会被打断，因此无法预先保存 R0-R3 等易失性寄存器。 为了解决这个问题，ARM Cortex-M 架构引入了硬件压栈（Hardware Stacking）机制。当 CPU 响应中断时，硬件会自动将 Caller 上下文（包括 R0-R3、R12、LR、PC 和 xPSR）。这一过程无需软件干预。 因此，进入中断服务函数（ISR）后，寄存器已经被硬件保护好了。ISR 此时只需像普通函数一样，遵守 AAPCS 规则处理 R4-R11（即如果用到则保存）即可。这种硬件机制极大降低了中断延迟，也简化了软件设计。 2.2 R12（IP）是干什么的？ 它是一个被很多人容易忽略的寄存器，主要用于链接器（Linker）插入的代码。当你的程序非常大，bl target 跳转的目标太远（超过 $\pm 32\text{MB}$）时，bl 指令够不着。此时，链接器会自动插入一段小代码（Veneer/Trampoline），先跳转到 R12 指向的中转站，再由 R12 赋值给 PC 进行长跳转。 2.3 R15（PC）为什么有 +8 现象？ 在 ARM 状态（32 位指令）下，读取 PC 的值，通常是当前指令地址 + 8。这则是因为 CPU 的流水线（Pipeline）设计，当 CPU 执行当前指令时，取指单元已经在取下下条指令了（当前指令地址 +8 地址处）。 3. 指令集讲解 3.1 数据处理指令（Data Processing） 核心逻辑：ALU 运算，结果写回寄存器，并可选更新 CPSR（如果指令后加 s，如 adds） 3.1.1 算术类 指令 解释 汇编举例 C 语言映射 add(Add) 加法。 add r0, r1, r2(R0 = R1 + R2) a = b + c; sub(Subtract) 减法。 sub r0, r1, #1(R0 = R1 - 1) a = b - 1; rsb(Reverse Substract) 逆向减法。用于 R0 = 常数 - Rx 的场景。 rsb r0, r1, #0(R0 = 0 - R1) a = -b; adc(Add with Carry) 带进位加法。用于实现 64 位加法的高 32 位运算。 adc r0, r1, r2(R0 = R1 + R2 + Carry) (long long )a + (long long)b 的高位部分 sbc(Subtract with Carry) 带借位减法。用于 64 位减法。 sbc r0, r1, r2(R0 = R1 - R2 - !Carry) 64 位减法的高位部分 mul(Multiply) 32 位乘法（结果取低 32 位） mul r0, r1, r2(R0 = R1 * R2) int a = b * c; mla(Multiply Accumulate) 乘法运算。DSP 算法核心指令。 mul r0, r1, r2, r3(R0 = R1 * R2 + R3) a = (b * c) + d; udiv(Unsigned Divide) 无符号除法。 udiv r0, r1, r2 unsigned a = b / c; sdiv(Signed Divided) 有符号除法。 sdiv r0, r1, r2 int a = b / c; 3.1.2 逻辑类 指令 解释 汇编举例 C 语言映射 and(Logical AND) 按位与。常用于**提取**某几位。 and r0, r1, #0xff a = b &amp; 0xff; orr(Logical OR) 按位或。常用于**置位**（设为 1）。 orr r0, r0, #(1&lt;&lt;3) r0 | = (1 &lt;&lt; 3); eor(Exclusive OR) 按位异或。常用于**翻转**某几位。 eor r0, r0, #1 a = b ^ 1; bic(Bit Clear) **位清除**。and 的反向操作：dest = op1 and (not op2) bic r0, r0, #(1&lt;&lt;3) r0 &amp;= ~(1 &lt;&lt; 3); 3.1.3 数据搬运 指令 解释 汇编举例 C 语言映射 mov(Move) 寄存器间传值，或立即数赋值。 mov r0, r1 a = b; mvn(Move Not) **按位取反**后传送。 mvn r0, #0(r0 = 0xffffffff) a = ~0; ARM 指令是定长 32 位的。除去操作码和条件码，留给立即数的空间只有 12 位。并且这 12 位还不是直接存数字，而是被拆分位 8 位常数 + 4 位循环右移。 这就意味着在向寄存器写入某些立即数时，例如 mov r0, #0x12345678 是会失败的，因为 0x12345678 这个立即数太乱了，无法通过 8 位常数 + 4 位循环右移凑出来。 解决办法是使用 ldr r0, =0x12345678。此时，汇编器会悄悄在代码段的末尾（Literal Pool）找个地方放 0x12345678，然后把这条指令翻译成去那个地址读数据。 3.1.4 比较指令 不保存运算结果，只更新 CPSR 的标志位（N, Z, C, V），为后面的条件执行做铺垫。 指令 解释 汇编举例 C 语言映射 cmp(Compare) 本质是 sub。比较大小。 cmp r0, #5(r0 - 5，更新 Z 标志位) if (a &gt;= 5) tst(Test) 本质是 and。测试某位是否为 1。 tst r0, #(1&lt;&lt;3)(r0 &amp; 0x8，更新 Z 标志位) if (a &amp; (1&lt;&lt;3)) teq(Test Equivalence) 本质是 eor。测试两个数是否相等（不影响进位标志）。 teq r0, r1 常用于汇编手写检查 3.1.5 桶形移位器（Barrel Shifter） 在 ARM 中，移位操作不是独立指令，而是数据进入 ALU 之前的预处理。它是免费的。指令 add r0, r1, r2, lsl #2 的物理含义是： $$ \text{R0} = \text{R1} + (\text{R2} \ll 2) $$ 这在单周期内完成，这就是为什么 ARM 处理数组索引（地址 = 基址 + 索引 $\times$ 4）特别快。 模式 物理动作 C 标志位更新 数学含义 C 语言映射 典型应用场景 lsl(Logical Shift Left) **左移**。低位补 0，高位移出 最后移出的位 $\times 2^n$ x &lt;&lt; n 乘法优化、位字段对齐、生成掩码 lsr(Logical Shift Right) **右移**。高位补 0，低位移出 最后移出的位 $/2^n$（无符号） (unsigned)x &gt;&gt; n 无符号数除法、读取特定位段 asr(Arithmetic Shift Right) **算术右移**。高位补**符号位**，低位移出 最后移出的位 $/2^n$（有符号） (signed)x &gt;&gt; n 有符号数除法 ror(Rotate Right) **循环右移**。低位移出后，**填补到高位** 最后移出的位 - (x &gt;&gt; n) | (x &lt;&lt; (32-n)) 加密算法、哈希函数、位图操作 rrx(Rotate Right with Extend) **带扩展循环右移**。33 位循环（32 位寄存器 + 1 位 Carry） 寄存器移出的 LSB - 无 大数运算、多精度算术 3.2 内存访问指令（Memory Access） 这是 CPU 与外部世界（DDR 内存、GPIO 控制器等）交互的唯一桥梁。 3.2.1 单寄存器 指令 解释 汇编举例 C 语言映射 ldr(Load Register) 从内存读 4 字节到寄存器。 ldr r0, [r1] r0 = *r1; str(Store Register) 把寄存器的值写入内存 4 字节。 str r0, [r1] *r1 = r0; ldrb (8 bit)(Load Byte) 读 1 字节，高 24 位**补零**。 ldrb r0, [r1] r0 = (unsigned char)*r1; ldrh (16 bit)(Load Halfword) 读 2 字节，高 16 位**补零**。 ldrh r0, [r1] r0 = (unsigned short)*r1; ldrsb(Load Signed Byte) 读 1 字节，高 24 位**补符号位**。 ldrsb r0, [r1] r0 = (char)*r1; ldrsh(Load Signed Halfword) 读 2 字节，高 16 位**补符号位**。 ldrsh r0, [r1] r0 = (short)*r1; 3.2.2 寻址模式 寻址模式 汇编举例 C 语言映射 状态 立即数偏移（Offset） ldr r0, [r1, #4] r0 = *(r1 + 1); r1 保持不变 前索引（Pre-indexed） ldr r0, [r1, #4]! r0 = *(++ptr); r1 在访问内存**前**更新为 r1 + 4 后索引（Post-indexed） ldr r0, [r1], #4 r0 = *(ptr++); 访问内存**后**，r1 更新为 r1 + 4 3.2.3 多寄存器 这是 ARM 实现栈（Stack）和上下文切换（Context Switch）的基石。 指令 解释 汇编举例 场景 ldm(Load Multiple) 内存 $\rightarrow$ 多个寄存器。 ldm r0, {r4-r6, pc} 恢复现场并返回 stm(Store Multiple) 多个寄存器 $\rightarrow$ 内存。 stm r0, {r4-r6, lr} 保存现场 说明： ldm r0, {r4-r6, pc}：从以 r0 为基址的内存地址开始，依次加载数据到寄存器 r4、r5、r6 和 pc。 stm r0, {r4-r6, lr}：从寄存器 r4、r5、r6 和 lr 中，依次将数据存储到以 r0 为基址的内存地址。 后缀模式 虽然有 ia、ib、da 和 db 四种，但 ARM 官方推荐在操作栈时使用别名，方便记忆： stmfd (Full Descending) = stmdb (Decrement Before)：入栈（压栈）。 ldmfd (Full Descending) = ldmia (Increment After)：出栈。 push {reglist} 等价于 stmdb sp!, {reglist}。 pop {reglist} 等价于 ldmia sp!, {reglist}。 3.3 分支与控制流（Branch &amp; Control） 打破顺序执行，实现 if、for、function call。 指令 解释 汇编举例 C 语言映射 b(Branch) 相对跳转（范围 $\pm 32\text{MB}$） b label goto label; bl(Branch with Link) 带连接跳转。**保存下一条指令地址到 lr**。 bl func func(); bx(Branch Exchange) 跳转并根据目标地址最低位切换状态（ARM $\rightarrow$ Thumb）。 bx lr return;（函数返回） blx(Branch Link Exchange) bl + bx 的结合。调用不同指令集的函数。 blx r0 函数指针调用 还可以直接修改 PC 寄存器实现跳转，例如 mov pc, lr，这是函数返回的底层实现。 3.4 系统与状态指令（System &amp; Status） 这部分在使用上层语言实现业务逻辑时几乎看不到，但是对于操作系统而言至关重要。 3.4.1 状态寄存器访问 指令 解释 汇编举例 场景 mrs(Move Register from Status) 读取 CPSR/SPSR 到通用寄存器。 mrs r0, cpsr 读取当前中断状态 msr(Move Status from Register) 将通用寄存器写会 CPSR/SPSR。 msr cpsr_c, r0 开/关中断 3.4.2 异常与中断生成 指令 解释 场景 svc(Supervisor Call) **软中断**。CPU 进入 SVC 模式。Linux 中用于从用户态（User）陷入内核态（Kernel）。 系统调用（open，read 等）。 bkpt(Breakpoint) 断点指令。执行到此时 CPU 暂停并通知调试器。 JTAG/GDB 调试。 3.4.3 协处理器指令 I.MX6ULL 中，CP15 协处理器控制着 MMU、Cache 和中断向量表基地址。 指令 解释 助记法 mcr(Move to Coprocessor from Register) CPU $\rightarrow$ CP15（写配置） R（Register）在最后，表示源 mrc(Move to Register from Coprocessor) CP15 $\rightarrow$ CPU（读状态） C（Coprocessor）在最后，表示源 例如：mcr p15, 0, r0, c1, c0, 0（开启 MMU）。 协处理器（Coprocessor）时依附于 CPU 主核的独立电路单元。ARM 主核（ALU）只管算逻辑和搬砖，像 MMU（内存映射管理）、Cache 配置这些脏活累活，都丢给 CP15 协处理器；浮点运算丢给 CP10/CP11。 3.4.4 内存屏障（Memory Barriers） 指令 解释 场景 dmb(Data Memory Barrier) 确保指令前的内存访问在指令后的内存访问之前完成。 操作 DMA 描述符时 dsb(Data Synchronization Barrier) 比 DMB 更强。确保前面的访存全部完成，CPU 才会执行下一条指令。 修改页表后 isb(Instruction Synchronization Barrier) 冲刷流水线。确保重新取指。 修改了代码段或中断向量表后 3.5 同步与原子操作（Synchronization） 对于多任务操作系统，需要处理竞态条件。 指令 解释 C 语言映射 ldrex(Load Register Exclusive) 读取并设置独占监视器 atomic_load (C11) strex(Store Register Exclusive) 如果监视状态未被破坏，则写入。返回 0 成功，1 失败。 atomic_compare_exchange (C11) clrex(Clear Exclusive) 清除监视器状态。 上下文切换时清理残余状态 ARM 没有 x86 的 lock 总线锁，它使用 “独占监视器” 硬件。 ldrex 读取数据的同时标记该地址为 “被我监视”。 strex 尝试写入。如果期间没有其他核心 / DMA 修改过该地址，写入成功；否则写入失败。 这会实现 mutex（互斥锁）和 semaphore（信号量）的基石。 互斥锁（Mutex）&amp; 信号量（Semaphore）： 互斥锁（厕所钥匙）：只有一个资源（厕所）。你拿了钥匙进去锁门，别惹只能在门口排队（阻塞），直到你出来还钥匙。用户保护临界区。 信号量（餐厅叫号）：有 $N$ 个资源（空桌子）。来一个人 $N-1$，走一个人 $N+1$。当 $N=0$ 时，新来的人必须等到。 这两个都是软件概念，但底层必须由原子指令支持。]]></summary></entry><entry><title type="html">如何将 WSL2 镜像无损迁移至非系统盘</title><link href="https://bfmhno3.github.io/tutorial/wsl2-image-migration-guide/" rel="alternate" type="text/html" title="如何将 WSL2 镜像无损迁移至非系统盘" /><published>2026-03-14T05:16:00+00:00</published><updated>2026-03-14T05:16:00+00:00</updated><id>https://bfmhno3.github.io/tutorial/wsl2-image-migration-guide</id><content type="html" xml:base="https://bfmhno3.github.io/tutorial/wsl2-image-migration-guide/"><![CDATA[<h2 id="1-引言">1 引言</h2>

<p>随着开发环境的日益庞大，默认安装在 <code class="language-plaintext highlighter-rouge">C:\</code> 盘的 WSL2 (Windows Subsystem for Linux) 镜像体积也会不断膨胀。本文将分享如何通过命令行操作，将 WSL 迁移至 <code class="language-plaintext highlighter-rouge">E:\</code> 盘乃至其他盘符，彻底释放系统盘空间。</p>

<h3 id="11-笔者环境">1.1 笔者环境</h3>

<table>
  <thead>
    <tr>
      <th style="text-align: center">名称</th>
      <th style="text-align: left">版本</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">Windows 11 Pro</td>
      <td style="text-align: left">25H2 26200.8037</td>
    </tr>
    <tr>
      <td style="text-align: center">PWSH</td>
      <td style="text-align: left">7.5.5</td>
    </tr>
    <tr>
      <td style="text-align: center">WSL</td>
      <td style="text-align: left">2.6.3.0</td>
    </tr>
    <tr>
      <td style="text-align: center">Ubuntu</td>
      <td style="text-align: left">22.04 LTS</td>
    </tr>
    <tr>
      <td style="text-align: center">Ubuntu</td>
      <td style="text-align: left">24.04 LTS</td>
    </tr>
  </tbody>
</table>

<p class="notice--info"><strong>注意</strong>：这里的 PWSH 并不是指 Windows 系统内置的经典 PowerShell，而是跨平台的开源项目 <a href="https://github.com/PowerShell/PowerShell">PowerShell Core</a>。下文中使用 PowerShell 指代系统默认版本，PWSH 指代开源版本。</p>

<h2 id="2-准备工作">2 准备工作</h2>

<h3 id="21-确认当前安装状态">2.1 确认当前安装状态</h3>

<p>如果你还没有安装镜像，或想查看当前支持的发行版，可以使用以下命令：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 查看支持的在线发行版</span>
wsl <span class="nt">--list</span> <span class="nt">--online</span>

<span class="c"># 安装 Ubuntu 镜像</span>
wsl <span class="nt">--install</span> Ubuntu-22.04
wsl <span class="nt">--install</span> Ubuntu-24.04
</code></pre></div></div>

<p>在终端输入以下命令，查看当前已安装的发行版：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>wsl <span class="nt">-l</span> <span class="nt">-v</span>
</code></pre></div></div>

<h3 id="21-确定原镜像及目标路径">2.1 确定原镜像及目标路径</h3>

<p>WSL2 的虚拟磁盘文件通常为 <code class="language-plaintext highlighter-rouge">ext4.vhdx</code>。你可以使用强大的文件搜索工具 <a href="https://www.voidtools.com/zh-cn/">Everything</a> 来定位它们。不同版本的 Ubuntu 默认安装路径可能略有不同。</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">镜像</th>
      <th style="text-align: left">原始路径</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">Ubuntu 22.04 LTS</td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">C:\Users\&lt;your_windows_username&gt;\AppData\Local\Packages\CanonicalGroupLimited.Ubuntu22.04LTS_...\LocalState</code></td>
    </tr>
    <tr>
      <td style="text-align: center">Ubuntu 24.04 LTS</td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">C:\Users\&lt;your_windows_username&gt;\AppData\wsl\{UUID}</code></td>
    </tr>
  </tbody>
</table>

<p>可以看到这里两者的路径有着细微差别，Ubuntu 22.04 LTS 安装在 <code class="language-plaintext highlighter-rouge">Local\Packages</code> 下，而 Ubuntu 24.04 LTS 却安装在 <code class="language-plaintext highlighter-rouge">Local</code> 同级的 <code class="language-plaintext highlighter-rouge">wsl</code> 下。</p>

<p>具体原因犹未可知，笔者暂时没有时间去查阅官方文档，但是并不影响后续步骤以及日常使用。</p>

<h3 id="22-建立新的位置">2.2 建立新的位置</h3>

<p>在上一步中已经获取到了原始的位置，根据笔者的风格，迁移之后的镜像路径应该尽可能维持原来的目录结构，但是笔者还是做了一点细微的修改，迁移路径具体如下：</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">镜像</th>
      <th style="text-align: left">迁移路径</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">Ubuntu 22.04 LTS</td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">E:\Users\&lt;your_windows_username&gt;\AppData\wsl\ubuntu2204</code></td>
    </tr>
    <tr>
      <td style="text-align: center">Ubuntu 24.04 LTS</td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">E:\Users\&lt;your_windows_username&gt;\AppData\wsl\ubuntu2404</code></td>
    </tr>
  </tbody>
</table>

<p class="notice--warning">请在后续的命令行中将 <code class="language-plaintext highlighter-rouge">&lt;your_windows_username&gt;</code> 替换为你的实际 Windows 用户名。</p>

<p>在新版的 PWSH 中可以直接使用以下命令创建目录：</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">mkdir</span><span class="w"> </span><span class="nt">-p</span><span class="w"> </span><span class="nx">E:\Users\</span><span class="err">&lt;</span><span class="nx">your_windows_username</span><span class="err">&gt;</span><span class="nx">\AppData\wsl\ubuntu2204\</span><span class="w">
</span><span class="n">mkdir</span><span class="w"> </span><span class="nt">-p</span><span class="w"> </span><span class="nx">E:\Users\</span><span class="err">&lt;</span><span class="nx">your_windows_username</span><span class="err">&gt;</span><span class="nx">\AppData\wsl\ubuntu2404\</span><span class="w">
</span></code></pre></div></div>

<p>或者以下命令（支持在 PowerShell 中使用）：</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">New-Item</span><span class="w"> </span><span class="nt">-Path</span><span class="w"> </span><span class="nx">E:\Users\</span><span class="err">&lt;</span><span class="nx">your_windows_username</span><span class="err">&gt;</span><span class="nx">\AppData\wsl\ubuntu2204\</span><span class="w"> </span><span class="nt">-ItemType</span><span class="w"> </span><span class="nx">Directory</span><span class="w">
</span><span class="n">New-Item</span><span class="w"> </span><span class="nt">-Path</span><span class="w"> </span><span class="nx">E:\Users\</span><span class="err">&lt;</span><span class="nx">your_windows_username</span><span class="err">&gt;</span><span class="nx">\AppData\wsl\ubuntu2404\</span><span class="w"> </span><span class="nt">-ItemType</span><span class="w"> </span><span class="nx">Directory</span><span class="w">
</span></code></pre></div></div>

<h2 id="3-迁移镜像">3 迁移镜像</h2>

<h3 id="31-导出原-wsl-分发">3.1 导出原 WSL 分发</h3>

<p>在进行导出操作前，<strong>强烈建议先停止 WSL2 中的所有工作并彻底关闭所有正在运行的 WSL 实例</strong>以防止数据损坏：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>wsl <span class="nt">--shutdown</span>
</code></pre></div></div>

<p>接着，将现有的系统状态导出为 <code class="language-plaintext highlighter-rouge">.tar</code> 压缩包：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>wsl <span class="nt">--export</span> Ubuntu-22.04 E:<span class="se">\U</span>sers<span class="se">\&lt;</span>your_windows_username&gt;<span class="se">\A</span>ppData<span class="se">\w</span>sl<span class="se">\u</span>buntu2204.tar
wsl <span class="nt">--export</span> Ubuntu-24.04 E:<span class="se">\U</span>sers<span class="se">\&lt;</span>your_windows_username&gt;<span class="se">\A</span>ppData<span class="se">\w</span>sl<span class="se">\u</span>buntu2404.tar
</code></pre></div></div>

<h3 id="32-将压缩包导入新位置">3.2 将压缩包导入新位置</h3>

<p>使用 <code class="language-plaintext highlighter-rouge">wsl --import</code> 命令将刚才导出的 <code class="language-plaintext highlighter-rouge">.tar</code> 文件导入到我们在 E 盘创建的新目录中。为了方便区分，我们将导入后的新实例命名为小写的 <code class="language-plaintext highlighter-rouge">ubuntu2204</code> 和 <code class="language-plaintext highlighter-rouge">ubuntu240</code>：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>wsl <span class="nt">--import</span> ubuntu2204 E:<span class="se">\U</span>sers<span class="se">\&lt;</span>your_windows_username&gt;<span class="se">\A</span>ppData<span class="se">\w</span>sl<span class="se">\u</span>buntu2204 E:<span class="se">\U</span>sers<span class="se">\a</span>mara<span class="se">\A</span>ppData<span class="se">\w</span>sl<span class="se">\u</span>buntu2204.tar
wsl <span class="nt">--import</span> ubuntu2404 E:<span class="se">\U</span>sers<span class="se">\&lt;</span>your_windows_username&gt;<span class="se">\A</span>ppData<span class="se">\w</span>sl<span class="se">\u</span>buntu2404 E:<span class="se">\U</span>sers<span class="se">\a</span>mara<span class="se">\A</span>ppData<span class="se">\w</span>sl<span class="se">\u</span>buntu2404.tar
</code></pre></div></div>

<p class="notice--info">利用 <code class="language-plaintext highlighter-rouge">wsl --import</code>，你可以将同一个 <code class="language-plaintext highlighter-rouge">.tar</code> 包导入多次并赋予不同名称，从而实现多开同一个发行版。虽然在隔离环境方面类似于 Docker，但 WSL2 原生支持 WSLg，可以直接运行 Linux 图形化应用，这在某些桌面开发场景下比 Docker 更具优势。</p>

<h3 id="33-恢复默认用户">3.3 恢复默认用户</h3>

<p>导入完成并启动新的分发后，你可能会发现默认登录用户变成了 <code class="language-plaintext highlighter-rouge">root</code>。我们需要将其改回你原本的 Linux 用户名。如果你的分发并没有发现此类问题，可以跳过本节。</p>

<p>进入新的 WSL 实例：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>wsl ~ <span class="nt">-d</span> ubuntu2204
</code></pre></div></div>

<p>使用 <code class="language-plaintext highlighter-rouge">nano</code> 编辑器修改 WSL 配置文件（如果你更习惯图形化界面，也可以执行 <code class="language-plaintext highlighter-rouge">apt update &amp;&amp; apt install gedit -y</code> 后使用 <code class="language-plaintext highlighter-rouge">gedit</code>）：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>nano /etc/wsl.conf
</code></pre></div></div>

<p>在文件中添加或修改以下内容（将 <code class="language-plaintext highlighter-rouge">&lt;your_linux_username&gt;</code> 替换为你在 WSL 中的用户名）：</p>

<div class="language-ini highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[user]</span>
<span class="py">default</span><span class="p">=</span><span class="s">&lt;your_linux_username&gt;</span>
</code></pre></div></div>

<p>保存并退出：依次按下 <kbd>Ctrl</kbd> + <kbd>X</kbd>，输入 <kbd>Y</kbd> 确认保存，最后按 <kbd>Enter</kbd> 退出。</p>

<p>退出当前 WSL 会话，并在 Windows 终端中重启改实例使配置生效：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 终止实例</span>
wsl <span class="nt">--terminate</span> ubuntu2204
<span class="c"># 重新进入验证用户是否恢复正常</span>
wsl ~ <span class="nt">-d</span> ubuntu2204
</code></pre></div></div>

<h3 id="34-设置为默认分发并清理善后">3.4 设置为默认分发并清理善后</h3>

<p>如果一切验证无误，将新的实例设置为系统的默认 WSL 分发：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>wsl <span class="nt">--set-default</span> ubuntu2404
</code></pre></div></div>

<p>确认无误后，就可以卸载 C 盘中的旧分发了：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>wsl <span class="nt">--unregister</span> Ubuntu-22.04
wsl <span class="nt">--unregister</span> Ubuntu-24.04
</code></pre></div></div>

<p>最后，删除存放在 E 盘的过渡文件 <code class="language-plaintext highlighter-rouge">.tar</code> 压缩包以释放空间。此时再次执行 <code class="language-plaintext highlighter-rouge">wsl -l -v</code>，你应该能看到干净清爽的列表了。</p>

<h2 id="4-总结">4 总结</h2>

<p>迁移安装最大的好处就是能显著减小 C 盘的存储压力。不过，如果你的物理磁盘总容量本身较小（比如只有 512GB 单盘），笔者依然更推荐直接重装系统并只保留一个 C 盘分区，统一管理存储空间。</p>

<p>此外，根据<a href="https://bfmhno3.github.io/tutorial/wsl2-image-migration-guide/">微软官方文档的建议</a>，为了获得最佳的文件 I/O 性能，进行 Linux 相关的开发时，请务必将项目文件直接存储在 WSL 的 Linux 文件系统（如 <code class="language-plaintext highlighter-rouge">~</code> 目录下），而不是跨文件系统存储在 Windows 盘符（如 <code class="language-plaintext highlighter-rouge">/mnt/c</code>）中。</p>

<h2 id="参考资料">参考资料</h2>

<ol>
  <li><a href="https://superuser.com/questions/1701175/installing-ubuntu-on-mnt-d-with-wsl">Installing Ubuntu on /mnt/d with WSL</a></li>
  <li><a href="https://github.com/OsbertWang/install-latex-guide-zh-cn">install-latex-guide-zh-cn (附录B.5 迁移 WSL 的安装位置)</a></li>
</ol>]]></content><author><name>bfmhno3</name></author><category term="Tutorial" /><category term="Windows" /><category term="WSL2" /><category term="Ubuntu" /><summary type="html"><![CDATA[1 引言 随着开发环境的日益庞大，默认安装在 C:\ 盘的 WSL2 (Windows Subsystem for Linux) 镜像体积也会不断膨胀。本文将分享如何通过命令行操作，将 WSL 迁移至 E:\ 盘乃至其他盘符，彻底释放系统盘空间。 1.1 笔者环境 名称 版本 Windows 11 Pro 25H2 26200.8037 PWSH 7.5.5 WSL 2.6.3.0 Ubuntu 22.04 LTS Ubuntu 24.04 LTS 注意：这里的 PWSH 并不是指 Windows 系统内置的经典 PowerShell，而是跨平台的开源项目 PowerShell Core。下文中使用 PowerShell 指代系统默认版本，PWSH 指代开源版本。 2 准备工作 2.1 确认当前安装状态 如果你还没有安装镜像，或想查看当前支持的发行版，可以使用以下命令： # 查看支持的在线发行版 wsl --list --online # 安装 Ubuntu 镜像 wsl --install Ubuntu-22.04 wsl --install Ubuntu-24.04 在终端输入以下命令，查看当前已安装的发行版： wsl -l -v 2.1 确定原镜像及目标路径 WSL2 的虚拟磁盘文件通常为 ext4.vhdx。你可以使用强大的文件搜索工具 Everything 来定位它们。不同版本的 Ubuntu 默认安装路径可能略有不同。 镜像 原始路径 Ubuntu 22.04 LTS C:\Users\&lt;your_windows_username&gt;\AppData\Local\Packages\CanonicalGroupLimited.Ubuntu22.04LTS_...\LocalState Ubuntu 24.04 LTS C:\Users\&lt;your_windows_username&gt;\AppData\wsl\{UUID} 可以看到这里两者的路径有着细微差别，Ubuntu 22.04 LTS 安装在 Local\Packages 下，而 Ubuntu 24.04 LTS 却安装在 Local 同级的 wsl 下。 具体原因犹未可知，笔者暂时没有时间去查阅官方文档，但是并不影响后续步骤以及日常使用。 2.2 建立新的位置 在上一步中已经获取到了原始的位置，根据笔者的风格，迁移之后的镜像路径应该尽可能维持原来的目录结构，但是笔者还是做了一点细微的修改，迁移路径具体如下： 镜像 迁移路径 Ubuntu 22.04 LTS E:\Users\&lt;your_windows_username&gt;\AppData\wsl\ubuntu2204 Ubuntu 24.04 LTS E:\Users\&lt;your_windows_username&gt;\AppData\wsl\ubuntu2404 请在后续的命令行中将 &lt;your_windows_username&gt; 替换为你的实际 Windows 用户名。 在新版的 PWSH 中可以直接使用以下命令创建目录： mkdir -p E:\Users\&lt;your_windows_username&gt;\AppData\wsl\ubuntu2204\ mkdir -p E:\Users\&lt;your_windows_username&gt;\AppData\wsl\ubuntu2404\ 或者以下命令（支持在 PowerShell 中使用）： New-Item -Path E:\Users\&lt;your_windows_username&gt;\AppData\wsl\ubuntu2204\ -ItemType Directory New-Item -Path E:\Users\&lt;your_windows_username&gt;\AppData\wsl\ubuntu2404\ -ItemType Directory 3 迁移镜像 3.1 导出原 WSL 分发 在进行导出操作前，强烈建议先停止 WSL2 中的所有工作并彻底关闭所有正在运行的 WSL 实例以防止数据损坏： wsl --shutdown 接着，将现有的系统状态导出为 .tar 压缩包： wsl --export Ubuntu-22.04 E:\Users\&lt;your_windows_username&gt;\AppData\wsl\ubuntu2204.tar wsl --export Ubuntu-24.04 E:\Users\&lt;your_windows_username&gt;\AppData\wsl\ubuntu2404.tar 3.2 将压缩包导入新位置 使用 wsl --import 命令将刚才导出的 .tar 文件导入到我们在 E 盘创建的新目录中。为了方便区分，我们将导入后的新实例命名为小写的 ubuntu2204 和 ubuntu240： wsl --import ubuntu2204 E:\Users\&lt;your_windows_username&gt;\AppData\wsl\ubuntu2204 E:\Users\amara\AppData\wsl\ubuntu2204.tar wsl --import ubuntu2404 E:\Users\&lt;your_windows_username&gt;\AppData\wsl\ubuntu2404 E:\Users\amara\AppData\wsl\ubuntu2404.tar 利用 wsl --import，你可以将同一个 .tar 包导入多次并赋予不同名称，从而实现多开同一个发行版。虽然在隔离环境方面类似于 Docker，但 WSL2 原生支持 WSLg，可以直接运行 Linux 图形化应用，这在某些桌面开发场景下比 Docker 更具优势。 3.3 恢复默认用户 导入完成并启动新的分发后，你可能会发现默认登录用户变成了 root。我们需要将其改回你原本的 Linux 用户名。如果你的分发并没有发现此类问题，可以跳过本节。 进入新的 WSL 实例： wsl ~ -d ubuntu2204 使用 nano 编辑器修改 WSL 配置文件（如果你更习惯图形化界面，也可以执行 apt update &amp;&amp; apt install gedit -y 后使用 gedit）： sudo nano /etc/wsl.conf 在文件中添加或修改以下内容（将 &lt;your_linux_username&gt; 替换为你在 WSL 中的用户名）： [user] default=&lt;your_linux_username&gt; 保存并退出：依次按下 Ctrl + X，输入 Y 确认保存，最后按 Enter 退出。 退出当前 WSL 会话，并在 Windows 终端中重启改实例使配置生效： # 终止实例 wsl --terminate ubuntu2204 # 重新进入验证用户是否恢复正常 wsl ~ -d ubuntu2204 3.4 设置为默认分发并清理善后 如果一切验证无误，将新的实例设置为系统的默认 WSL 分发： wsl --set-default ubuntu2404 确认无误后，就可以卸载 C 盘中的旧分发了： wsl --unregister Ubuntu-22.04 wsl --unregister Ubuntu-24.04 最后，删除存放在 E 盘的过渡文件 .tar 压缩包以释放空间。此时再次执行 wsl -l -v，你应该能看到干净清爽的列表了。 4 总结 迁移安装最大的好处就是能显著减小 C 盘的存储压力。不过，如果你的物理磁盘总容量本身较小（比如只有 512GB 单盘），笔者依然更推荐直接重装系统并只保留一个 C 盘分区，统一管理存储空间。 此外，根据微软官方文档的建议，为了获得最佳的文件 I/O 性能，进行 Linux 相关的开发时，请务必将项目文件直接存储在 WSL 的 Linux 文件系统（如 ~ 目录下），而不是跨文件系统存储在 Windows 盘符（如 /mnt/c）中。 参考资料 Installing Ubuntu on /mnt/d with WSL install-latex-guide-zh-cn (附录B.5 迁移 WSL 的安装位置)]]></summary></entry><entry><title type="html">C++ 中的 stack</title><link href="https://bfmhno3.github.io/note/stack-in-cpp/" rel="alternate" type="text/html" title="C++ 中的 stack" /><published>2026-02-19T02:45:00+00:00</published><updated>2026-02-19T02:45:00+00:00</updated><id>https://bfmhno3.github.io/note/stack-in-cpp</id><content type="html" xml:base="https://bfmhno3.github.io/note/stack-in-cpp/"><![CDATA[<p>要真正理解<strong>栈</strong>这种结构，意味着我们要剥离事物表象，追究其最本质的问题，即<strong>我们到底面临什么样的数据处理需求，才导致了 “栈” 这种结构的诞生？</strong></p>

<p>我们不需要把栈仅仅看作是教科书上的定义，而是把它看作是<strong>为了解决特定时序问题而设计的一种 “受限” 模型</strong>。</p>

<h2 id="1-什么是栈">1. 什么是栈？</h2>

<p>从本质上来说，计算机处理任务通常有两种模式：</p>

<ol>
  <li><strong>并行/随机</strong>：所有任务都在桌面上铺开，想拿哪个拿哪个（类似数组或哈希表）。</li>
  <li><strong>嵌套/依赖</strong>：任务 B 必须依赖任务 A 的环境，任务 C 又依赖 B。要完成 A，必须先挂起 A 去做 B；要做 B，必须先挂起 B 去做 C。</li>
</ol>

<p><strong>栈</strong>（Stack）就是为了解决第二种模式而诞生的。</p>

<h3 id="11-核心定义lifo">1.1 核心定义：LIFO</h3>

<p>栈是一种<strong>线性表</strong>，但它是一种受限的线性表。它的核心逻辑是<strong>后进先出</strong>（Last In, First Out - LIFO）。</p>

<p>你可以把它想象成<strong>叠盘子</strong>：</p>

<ul>
  <li>压入（Push）：你只能把新盘子放在最上面。</li>
  <li>弹出（Pop）：你只能取走最上面的盘子。</li>
  <li>查看（Top/Peek）：你只能看到最上面盘子的花纹，无法直接看到底部的盘子。</li>
</ul>

<figure class=" centered-75-figure"><a href="/assets/images/a_stack_of_plates.jpg" class="image-popup" title="生活中的栈模型：想要拿到底部的盘子，必须先移走顶部的盘子
"><img src="/assets/images/a_stack_of_plates.jpg" alt="餐厅里的一摞盘子，展示了后进先出的物理结构" /></a><figcaption>
      生活中的栈模型：想要拿到底部的盘子，必须先移走顶部的盘子

    </figcaption></figure>

<h3 id="12-为什么它是-受限-的">1.2 为什么它是 “受限” 的？</h3>

<p>数组（Array）允许你访问 <code class="language-plaintext highlighter-rouge">index[0]</code> 也可以访问 <code class="language-plaintext highlighter-rouge">index[100]</code>。栈为什么要把这种能力 “阉割” 掉？</p>

<p>答案就是<strong>为了强制秩序</strong>。</p>

<p>通过限制 “只能操作顶部”，栈强制程序员必须按照严格的 “原路返回” 逻辑来处理数据，从而消除了随机访问带来的复杂性和潜在错误。</p>

<h2 id="2-为什么需要栈">2. 为什么需要栈？</h2>

<p>如果计算机世界没有栈，很多基础逻辑就会崩塌。我们需要栈，是因为它是<strong>保存上下文</strong>（Context）和<strong>实现回朔</strong>（Backtacking）最自然的模型。</p>

<h3 id="21-函数调用the-call-stack">2.1 函数调用（The Call Stack）</h3>

<p>这是栈存在最底层、也最不可或缺的原因。</p>

<p>从本质上来说，CPU 在执行代码时是个高度专注的执行机器，它完全依赖程序计数器（PC, Program Counter）来决定下一步跑哪条指令。当代码从 <code class="language-plaintext highlighter-rouge">main()</code> 跳转到 <code class="language-plaintext highlighter-rouge">func_a()</code> 时，本质上是强行修改了 PC 寄存器的值。随之而来的致命问题是：当 <code class="language-plaintext highlighter-rouge">func_a()</code> 跑完，CPU 怎么知道该跳回 <code class="language-plaintext highlighter-rouge">main()</code> 里的哪一行继续执行？</p>

<p>你可能会想，用一个专门的寄存器（比如链接寄存器 LR）把返回地址保存起来不就行了？</p>

<p>在只有单层调用的情况下，这确实可行。但现实是，代码中充满了<strong>嵌套调用</strong>：<code class="language-plaintext highlighter-rouge">main()</code> 调用了 <code class="language-plaintext highlighter-rouge">func_a()</code>，而 <code class="language-plaintext highlighter-rouge">func_a()</code> 中又调用了 <code class="language-plaintext highlighter-rouge">func_b()</code>。如果只依赖单一寄存器，当调起 <code class="language-plaintext highlighter-rouge">func_b()</code> 时，新的返回地址回无情地覆盖掉之前 <code class="language-plaintext highlighter-rouge">func_a()</code> 的返回地址。等到 <code class="language-plaintext highlighter-rouge">func_b()</code> 执行结束，程序就再也找不到回 <code class="language-plaintext highlighter-rouge">main()</code> 的路了。</p>

<p>此外，函数不仅需要记住 “从哪儿来”，还要保护好自己的 “现场”。每个函数都有自己的局部变量，执行时还会征用各种通用寄存器。在跳入下一个函数前，当前的这些状态（Context）必须被原封不动地保存下来。</p>

<p>由于函数调用的逻辑是绝对的 “嵌套与回溯”——最后被调用的 <code class="language-plaintext highlighter-rouge">func_b()</code> 必须最先执行完毕并交出控制权，<code class="language-plaintext highlighter-rouge">func_a()</code> 才能继续，做后才轮到最外层的 <code class="language-plaintext highlighter-rouge">main()</code>。这种 “<strong>后出发的先结束，先出发的最后兜底</strong>” 的生命周期，与 LIFO (Last-In, First-Out) 结构严丝合缝。</p>

<p>所有，系统在内存中专门开辟了<strong>调用栈</strong>（Call Stack）。每次进入新函数，就把它的返回地址、局部变量和寄存器状态打包成一个<strong>栈帧</strong>（Stack Frame）压入栈顶；函数一结束，就将栈顶<strong>弹出</strong>，完美恢复上一层函数的现场。没有栈，现代编程语言的函数嵌套和递归机制就根本无法实现。</p>

<figure class=" centered-75-figure"><a href="/assets/images/call_stack_layout.svg" class="image-popup" title="调用栈示例：如果子程序 DrawSquare 调用了 DrawLine，DrawLine 的栈帧就会被压入栈顶（如图中绿色部分所示）。栈指针（Stack Pointer）始终指向最新的栈顶位置
"><img src="/assets/images/call_stack_layout.svg" alt="调用栈内存布局示意图：展示了DrawLine子程序位于栈顶，DrawSquare位于下方" /></a><figcaption>
      调用栈示例：如果子程序 <code class="language-plaintext highlighter-rouge">DrawSquare</code> 调用了 <code class="language-plaintext highlighter-rouge">DrawLine</code>，<code class="language-plaintext highlighter-rouge">DrawLine</code> 的栈帧就会被压入栈顶（如图中绿色部分所示）。栈指针（Stack Pointer）始终指向最新的栈顶位置

    </figcaption></figure>

<h3 id="22-递归recursion">2.2 递归（Recursion）</h3>

<p>既然函数调用依赖栈来保存上下文，那么<strong>递归</strong>可以说是把栈的这种特性压榨到了机制。</p>

<figure class=" centered-75-figure"><a href="/assets/images/recursive_drawing_of_a_sierpinski_triangle_through_turtle_graphics.gif" class="image-popup" title="使用海龟绘图（Turtle Graphics）递归绘制谢尔宾斯基三角形（Sierpinski triangle）的过程
"><img src="/assets/images/recursive_drawing_of_a_sierpinski_triangle_through_turtle_graphics.gif" alt="使用海龟绘图递归绘制谢尔宾斯基三角形的动画" /></a><figcaption>
      使用海龟绘图（Turtle Graphics）递归绘制谢尔宾斯基三角形（Sierpinski triangle）的过程

    </figcaption></figure>

<p>从人类的思维来看，递归是<strong>自己调用自己</strong>，但在 CPU 和内存眼里，根本没有 “同一个函数” 的概念。对于计算机而言，不管是 <code class="language-plaintext highlighter-rouge">func_a()</code> 调用 <code class="language-plaintext highlighter-rouge">func)b()</code>，还是 <code class="language-plaintext highlighter-rouge">func_a()</code> 调用 <code class="language-plaintext highlighter-rouge">func_a()</code>，动作是完全一样的——仅仅是 PC（程序计数器）指针跳转，并在内存中开辟一个新的栈帧。</p>

<p>假设我们在写一个计算阶乘的递归函数（<code class="language-plaintext highlighter-rouge">fact(n)</code>）。当 <code class="language-plaintext highlighter-rouge">func(3)</code> 内部调用 <code class="language-plaintext highlighter-rouge">fact(2)</code> 时，此时的局部变量 <code class="language-plaintext highlighter-rouge">n=3</code> 必须被冻结并保护起来。如果没有栈 <code class="language-plaintext highlighter-rouge">fact(2)</code> 内部的变量就会直接覆盖掉原来 <code class="language-plaintext highlighter-rouge">n=3</code> 的内存空间。等到递归触底反弹时，程序根本想不起最初传入的参数是什么。</p>

<p>得益于栈的 LIFO 机制，每一次自我调用，系统都会把当前的参数、局部变量和返回地址像叠盘子一样<strong>压入</strong>（Push）栈中。不管向下深挖了多少层，每一层的数据都被独立且安全地封存在自己的栈帧里。当遇到终止条件（Base Case）开始回归时，栈再一层层<strong>弹出</strong>（Pop），精准地恢复每一层的现场。可以说，<strong>栈时递归能够成立的物理载体</strong>。</p>

<p>当然，如果嵌套层数过深导致栈空间耗尽，就会引发经典的 Stack Overflow 栈溢出。</p>

<h3 id="23-状态撤销undo与历史回溯">2.3 状态撤销（Undo）与历史回溯</h3>

<p>你在编辑器里敲击键盘，后者在设计软件中画图，想要按下 <kbd>Ctrl</kbd> + <kbd>Z</kbd> 撤销刚刚的失误。</p>

<p>人类的时间是线性向前的，但 “撤销” 操作要求我们能够<strong>逆转时间</strong>。这种逆转不是随机的，而是必须严格遵守 “最近发生的事情，最先被抹去” 的自然法则。你最后敲下的那一个字符，必须是第一个被删除的。</p>

<p>所以，这里的历史记录被抽象成了一个栈：</p>

<ul>
  <li><strong>入栈</strong>（Push）：每当你执行一个操作（比如输入字符、改变颜色、移动光标），系统就会把这个动作的 “状态快照” 或 “增量数据” 打包，压入历史记录栈的栈顶。</li>
  <li><strong>出栈</strong>（Pop）：当年你触发撤销（Undo）时，系统直接从栈顶取回最后一次保存的状态，并将其弹出，界面随之恢复到上一个时刻。</li>
</ul>

<p>没有栈这种 “后进先出” 的模型，系统就无法建立起具有时间顺序的后悔药机制。<strong>任何需要 “原路” 返回的场景</strong>（比如浏览器的 “后退” 按钮、迷宫算法的回溯路径），其背后的灵魂数据机构都是栈。</p>

<h3 id="24-符号匹配与语法解析">2.4 符号匹配与语法解析</h3>

<p>编译器在编译代码时，或者解析器在处理一段复杂公式时，需要检查代码中的 <code class="language-plaintext highlighter-rouge">{ [ ( ) ] }</code> 括号是否成对闭合、合法嵌套。</p>

<p>编译器阅读代码的顺序是从左到右的单向扫描。但在代码的世界里，<strong>最晚开启的作用于，必须最早被关闭</strong>。这种一种典型的 LIFO 结构需求。</p>

<p>面对一长串括号，系统处理的算法极其优雅，完全依赖栈来实现：</p>

<ol>
  <li><strong>扫描与入栈</strong>：逐个字符读取，只要遇到 “左括号”（如 <code class="language-plaintext highlighter-rouge">{</code>、<code class="language-plaintext highlighter-rouge">[</code>、<code class="language-plaintext highlighter-rouge">(</code>），不管三七二十一，直接压入栈中。这代表着进入了一个新的层级，正在等待它的结束。</li>
  <li><strong>匹配与出栈</strong>：一旦遇到 “右括号”（如 <code class="language-plaintext highlighter-rouge">)</code>、<code class="language-plaintext highlighter-rouge">]</code>、<code class="language-plaintext highlighter-rouge">}</code>），此时立刻去查看<strong>栈顶</strong>元素。如果栈顶正好是能与它匹配的左括号，说明当前最内层的逻辑闭环了，于是将栈顶弹出，继续往下走。</li>
</ol>

<figure class=" centered-75-figure"><a href="/assets/images/parentheses_matching.webp" class="image-popup" title="利用栈进行括号匹配的逻辑：左括号入栈，右括号消栈，最后栈空则合法
"><img src="/assets/images/parentheses_matching.webp" alt="栈用于括号匹配的流程图" /></a><figcaption>
      利用栈进行括号匹配的逻辑：左括号入栈，右括号消栈，最后栈空则合法

    </figcaption></figure>

<p>如果遇到右括号时栈是空的（说明没有多余的左括号等待闭合），或者栈顶的左括号类型不匹配（比如 <code class="language-plaintext highlighter-rouge">{</code> 遇到了 <code class="language-plaintext highlighter-rouge">]</code>），那么编译器就可以立刻抛出 <code class="language-plaintext highlighter-rouge">Syntax Error</code>（语法错误）。当整个文件扫描完，如果栈里面还有残留的左括号，说明有作用域没关上。</p>

<p>利用栈，编译器不仅能判断对错，还能精准定位到哪一层的括号出了问题。</p>

<h2 id="3-c-stl-中的-stdstack">3. C++ STL 中的 <code class="language-plaintext highlighter-rouge">std::stack</code></h2>

<p>在 C++ 标准模板库（STL）中，<code class="language-plaintext highlighter-rouge">std::stack</code> 被设计得非常精妙。它不是一个从零开始写的容器，而是一个<strong>容器适配器</strong>（Container Adapter）。</p>

<h3 id="31-什么是-容器适配器">3.1 什么是 “容器适配器”？</h3>

<p><code class="language-plaintext highlighter-rouge">std::stack</code> 本身不直接管理内存，它更像是一个外壳。它内部包裹着另一个真正的容器（比如 <code class="language-plaintext highlighter-rouge">std::vector</code>、<code class="language-plaintext highlighter-rouge">std::deque</code> 后 <code class="language-plaintext highlighter-rouge">std::list</code>）。</p>

<p>它屏蔽了底层容器的随机访问结构（如 <code class="language-plaintext highlighter-rouge">[]</code>），只暴露 <code class="language-plaintext highlighter-rouge">push</code>、<code class="language-plaintext highlighter-rouge">pop</code>、<code class="language-plaintext highlighter-rouge">top</code> 等接口，从而强制你遵守栈的规则。</p>

<h3 id="32-核心语法与操作">3.2 核心语法与操作</h3>

<p>在使用前需包含头文件：</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stack&gt;</span><span class="cp">
</span></code></pre></div></div>

<p>基本操作（时间复杂度均为 $O(1)$）。</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">操作</th>
      <th style="text-align: left">函数</th>
      <th style="text-align: left">描述</th>
      <th style="text-align: left">注意事项</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center">入栈</td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">push(val)</code></td>
      <td style="text-align: left">将元素拷贝到栈顶</td>
      <td style="text-align: left">设计拷贝/移动构造</td>
    </tr>
    <tr>
      <td style="text-align: center">原地构造</td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">emplace(args)</code></td>
      <td style="text-align: left">在栈顶直接构造元素</td>
      <td style="text-align: left">**推荐**，比 <code class="language-plaintext highlighter-rouge">push</code> 更高效，省去拷贝</td>
    </tr>
    <tr>
      <td style="text-align: center">出栈</td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">pop()</code></td>
      <td style="text-align: left">移除栈顶元素</td>
      <td style="text-align: left">**返回值为 <code class="language-plaintext highlighter-rouge">void</code>**，不返回被移除的元素</td>
    </tr>
    <tr>
      <td style="text-align: center">查看</td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">top()</code></td>
      <td style="text-align: left">返回栈顶元素的引用</td>
      <td style="text-align: left">如果栈为空调用此函数回导致未定义行为（通常崩溃）</td>
    </tr>
    <tr>
      <td style="text-align: center">判空</td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">empty()</code></td>
      <td style="text-align: left">栈为空返回 <code class="language-plaintext highlighter-rouge">true</code></td>
      <td style="text-align: left">操作前务必检查</td>
    </tr>
    <tr>
      <td style="text-align: center">大小</td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">size()</code></td>
      <td style="text-align: left">返回元素数量</td>
      <td style="text-align: left">-</td>
    </tr>
  </tbody>
</table>

<figure class=" centered-75-figure"><a href="/assets/images/a_stack_runtime_with_push_and_pop_operations.svg" class="image-popup" title="Push 操作将元素压入栈顶，Pop 操作移除栈顶元素，两者都不能触及栈底
"><img src="/assets/images/a_stack_runtime_with_push_and_pop_operations.svg" alt="栈的 Push 和 Pop 操作示意图" /></a><figcaption>
      Push 操作将元素压入栈顶，Pop 操作移除栈顶元素，两者都不能触及栈底

    </figcaption></figure>

<h3 id="33-为什么-c-的-pop-不返回元素">3.3 为什么 C++ 的 <code class="language-plaintext highlighter-rouge">pop()</code> 不返回元素？</h3>

<p>在很多其他语言（如 Java, Python）中，<code class="language-plaintext highlighter-rouge">pop()</code> 会移除并返回栈顶元素。但在 C++ STL 中，<code class="language-plaintext highlighter-rouge">pop()</code> 是 <code class="language-plaintext highlighter-rouge">void</code>，你需要先 <code class="language-plaintext highlighter-rouge">top()</code> 获取值，再 <code class="language-plaintext highlighter-rouge">pop()</code> 移除。</p>

<p>究其原因是<strong>异常安全</strong>（Exception Safety）。</p>

<p>如果 <code class="language-plaintext highlighter-rouge">pop()</code> 函数即移除元素又返回元素（按值返回），在返回过程中如果发生了<strong>拷贝构造异常</strong>（比如内存不足），那么这个元素即从栈里丢了，又没能成功交给调用者，导致数据永久丢失。</p>

<p>将 <code class="language-plaintext highlighter-rouge">top()</code> 和 <code class="language-plaintext highlighter-rouge">pop()</code> 分离是 C++ 追求安全的体现。</p>

<h2 id="4-算法题实战">4. 算法题实战</h2>

<p>点击题目即可跳转。</p>

<h3 id="41-洛谷b3614模板栈">4.1 <a href="https://www.luogu.com.cn/problem/B3614">洛谷：B3614【模板】栈</a></h3>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;cstdint&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;string&gt;</span><span class="cp">
</span>
<span class="k">using</span> <span class="k">namespace</span> <span class="n">std</span><span class="p">;</span>

<span class="k">const</span> <span class="kt">uint64_t</span> <span class="n">N</span> <span class="o">=</span> <span class="mf">10e6</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>
<span class="kt">uint64_t</span> <span class="n">stk</span><span class="p">[</span><span class="n">N</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span><span class="mi">0</span><span class="p">};</span>
<span class="kt">uint64_t</span> <span class="n">top_idx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="c1">// 栈顶指针，指向栈顶元素的下一个位置</span>

<span class="kt">int</span> <span class="n">main</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">uint64_t</span> <span class="n">t</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="n">cin</span> <span class="o">&gt;&gt;</span> <span class="n">t</span><span class="p">;</span> <span class="c1">// 数据组数</span>

    <span class="k">while</span> <span class="p">(</span><span class="n">t</span><span class="o">--</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">top_idx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="c1">// 每组数据都清空栈</span>
        <span class="kt">uint64_t</span> <span class="n">n</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="c1">// 操作的次数</span>
        <span class="n">cin</span> <span class="o">&gt;&gt;</span> <span class="n">n</span><span class="p">;</span>

        <span class="k">while</span> <span class="p">(</span><span class="n">n</span><span class="o">--</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">string</span> <span class="n">operation</span><span class="p">;</span>
            <span class="kt">uint64_t</span> <span class="n">x</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
            <span class="n">cin</span> <span class="o">&gt;&gt;</span> <span class="n">operation</span><span class="p">;</span>

            <span class="k">if</span> <span class="p">(</span><span class="s">"push"</span> <span class="o">==</span> <span class="n">operation</span><span class="p">)</span> <span class="p">{</span>
                <span class="n">cin</span> <span class="o">&gt;&gt;</span> <span class="n">x</span><span class="p">;</span>
                <span class="n">stk</span><span class="p">[</span><span class="n">top_idx</span><span class="o">++</span><span class="p">]</span> <span class="o">=</span> <span class="n">x</span><span class="p">;</span>
            <span class="p">}</span> <span class="k">else</span> <span class="k">if</span> <span class="p">(</span><span class="s">"pop"</span> <span class="o">==</span> <span class="n">operation</span><span class="p">)</span> <span class="p">{</span>
                <span class="k">if</span> <span class="p">(</span><span class="mi">0</span> <span class="o">==</span> <span class="n">top_idx</span><span class="p">)</span> <span class="p">{</span>
                    <span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Empty"</span> <span class="o">&lt;&lt;</span> <span class="n">endl</span><span class="p">;</span>
                <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
                    <span class="o">--</span><span class="n">top_idx</span><span class="p">;</span>
                <span class="p">}</span>
            <span class="p">}</span> <span class="k">else</span> <span class="k">if</span> <span class="p">(</span><span class="s">"query"</span> <span class="o">==</span> <span class="n">operation</span><span class="p">)</span> <span class="p">{</span>
                <span class="k">if</span> <span class="p">(</span><span class="mi">0</span> <span class="o">==</span> <span class="n">top_idx</span><span class="p">)</span> <span class="p">{</span>
                    <span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Anguei!"</span> <span class="o">&lt;&lt;</span> <span class="n">endl</span><span class="p">;</span>
                <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
                    <span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">stk</span><span class="p">[</span><span class="n">top_idx</span> <span class="o">-</span> <span class="mi">1</span><span class="p">]</span> <span class="o">&lt;&lt;</span> <span class="n">endl</span><span class="p">;</span>
                <span class="p">}</span>
            <span class="p">}</span> <span class="k">else</span> <span class="k">if</span> <span class="p">(</span><span class="s">"size"</span> <span class="o">==</span> <span class="n">operation</span><span class="p">)</span> <span class="p">{</span>
                <span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">top_idx</span> <span class="o">&lt;&lt;</span> <span class="n">endl</span><span class="p">;</span>
            <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
                <span class="k">continue</span><span class="p">;</span>
            <span class="p">}</span>
        <span class="p">}</span>
    <span class="p">}</span>

    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="42-力扣20-有效的括号">4.2 <a href="https://leetcode.cn/problems/valid-parentheses/description/">力扣：20. 有效的括号</a></h3>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Solution</span> <span class="p">{</span>
<span class="nl">public:</span>
    <span class="kt">bool</span> <span class="n">isValid</span><span class="p">(</span><span class="n">string</span> <span class="n">s</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">stack</span><span class="o">&lt;</span><span class="kt">char</span><span class="o">&gt;</span> <span class="n">stk</span><span class="p">;</span>
        
        <span class="k">for</span> <span class="p">(</span><span class="k">const</span> <span class="kt">char</span><span class="o">&amp;</span> <span class="n">c</span> <span class="o">:</span> <span class="n">s</span><span class="p">)</span> <span class="p">{</span>
            <span class="k">if</span> <span class="p">(</span><span class="sc">'('</span> <span class="o">==</span> <span class="n">c</span> <span class="o">||</span> <span class="sc">'['</span> <span class="o">==</span> <span class="n">c</span> <span class="o">||</span> <span class="sc">'{'</span> <span class="o">==</span> <span class="n">c</span><span class="p">)</span> <span class="p">{</span>
                <span class="n">stk</span><span class="p">.</span><span class="n">push</span><span class="p">(</span><span class="n">c</span><span class="p">);</span>
            <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
                <span class="k">if</span> <span class="p">(</span><span class="n">stk</span><span class="p">.</span><span class="n">empty</span><span class="p">())</span> <span class="p">{</span>
                    <span class="k">return</span> <span class="nb">false</span><span class="p">;</span>
                <span class="p">}</span>

                <span class="kt">char</span> <span class="n">top</span> <span class="o">=</span> <span class="n">stk</span><span class="p">.</span><span class="n">top</span><span class="p">();</span>

                <span class="k">if</span> <span class="p">(</span><span class="sc">')'</span> <span class="o">==</span> <span class="n">c</span> <span class="o">&amp;&amp;</span> <span class="sc">'('</span> <span class="o">!=</span> <span class="n">top</span><span class="p">)</span> <span class="p">{</span>
                    <span class="k">return</span> <span class="nb">false</span><span class="p">;</span>
                <span class="p">}</span>

                <span class="k">if</span> <span class="p">(</span><span class="sc">']'</span> <span class="o">==</span> <span class="n">c</span> <span class="o">&amp;&amp;</span> <span class="sc">'['</span> <span class="o">!=</span> <span class="n">top</span><span class="p">)</span> <span class="p">{</span>
                    <span class="k">return</span> <span class="nb">false</span><span class="p">;</span>
                <span class="p">}</span>

                <span class="k">if</span> <span class="p">(</span><span class="sc">'}'</span> <span class="o">==</span> <span class="n">c</span> <span class="o">&amp;&amp;</span> <span class="sc">'{'</span> <span class="o">!=</span> <span class="n">top</span><span class="p">)</span> <span class="p">{</span>
                    <span class="k">return</span> <span class="nb">false</span><span class="p">;</span>
                <span class="p">}</span>

                <span class="n">stk</span><span class="p">.</span><span class="n">pop</span><span class="p">();</span>
            <span class="p">}</span>
        <span class="p">}</span>

        <span class="k">return</span> <span class="n">stk</span><span class="p">.</span><span class="n">empty</span><span class="p">();</span>
    <span class="p">}</span>
<span class="p">};</span>
</code></pre></div></div>

<h3 id="43-洛谷深基15习9验证栈序列">4.3 <a href="https://www.luogu.com.cn/problem/P4387">洛谷：【深基15.习9】验证栈序列</a></h3>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;vector&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stack&gt;</span><span class="cp">
</span>
<span class="k">constexpr</span> <span class="kt">int</span> <span class="n">N</span> <span class="o">=</span> <span class="mi">1000000</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">pushed</span><span class="p">(</span><span class="n">N</span><span class="p">);</span>
    <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">popped</span><span class="p">(</span><span class="n">N</span><span class="p">);</span>

    <span class="kt">int</span> <span class="n">q</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cin</span> <span class="o">&gt;&gt;</span> <span class="n">q</span><span class="p">;</span>

    <span class="k">while</span> <span class="p">(</span><span class="n">q</span><span class="o">--</span><span class="p">)</span> <span class="p">{</span>
        <span class="kt">int</span> <span class="n">n</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cin</span> <span class="o">&gt;&gt;</span> <span class="n">n</span><span class="p">;</span>

        <span class="c1">// 读取入栈序列</span>
        <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">std</span><span class="o">::</span><span class="n">cin</span> <span class="o">&gt;&gt;</span> <span class="n">pushed</span><span class="p">[</span><span class="n">i</span><span class="p">];</span>
        <span class="p">}</span>

        <span class="c1">// 读取出栈序列</span>
        <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">std</span><span class="o">::</span><span class="n">cin</span> <span class="o">&gt;&gt;</span> <span class="n">popped</span><span class="p">[</span><span class="n">i</span><span class="p">];</span>
        <span class="p">}</span>

        <span class="c1">// 模拟验证</span>
        <span class="n">std</span><span class="o">::</span><span class="n">stack</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">stk</span><span class="p">;</span>
        <span class="kt">int</span> <span class="n">popped_idx</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>

        <span class="c1">// 依次入栈</span>
        <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">stk</span><span class="p">.</span><span class="n">push</span><span class="p">(</span><span class="n">pushed</span><span class="p">[</span><span class="n">i</span><span class="p">]);</span>

            <span class="k">while</span> <span class="p">(</span><span class="n">popped_idx</span> <span class="o">&lt;</span> <span class="n">n</span> <span class="o">&amp;&amp;</span> <span class="o">!</span><span class="n">stk</span><span class="p">.</span><span class="n">empty</span><span class="p">()</span> <span class="o">&amp;&amp;</span> <span class="n">stk</span><span class="p">.</span><span class="n">top</span><span class="p">()</span> <span class="o">==</span> <span class="n">popped</span><span class="p">[</span><span class="n">popped_idx</span><span class="p">])</span> <span class="p">{</span>
                <span class="n">stk</span><span class="p">.</span><span class="n">pop</span><span class="p">();</span>
                <span class="n">popped_idx</span><span class="o">++</span><span class="p">;</span>
            <span class="p">}</span>
        <span class="p">}</span>

        <span class="c1">// 判断</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">stk</span><span class="p">.</span><span class="n">empty</span><span class="p">())</span> <span class="p">{</span>
            <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Yes"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
        <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
            <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"No"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
        <span class="p">}</span>
    <span class="p">}</span>


    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="44-洛谷p1449-后缀表达式">4.4 <a href="https://www.luogu.com.cn/problem/P1449">洛谷：P1449 后缀表达式</a></h3>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stack&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;string&gt;</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">std</span><span class="o">::</span><span class="n">stack</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">stk</span><span class="p">;</span>
    <span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="n">expression</span><span class="p">;</span>
    <span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="n">num_str</span><span class="p">;</span>
    <span class="kt">int</span> <span class="n">num</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
    <span class="kt">int</span> <span class="n">result</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>

    <span class="n">std</span><span class="o">::</span><span class="n">cin</span> <span class="o">&gt;&gt;</span> <span class="n">expression</span><span class="p">;</span>

    <span class="k">for</span> <span class="p">(</span><span class="k">const</span> <span class="kt">char</span><span class="o">&amp;</span> <span class="n">ch</span> <span class="o">:</span> <span class="n">expression</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">ch</span> <span class="o">==</span> <span class="sc">'@'</span><span class="p">)</span> <span class="k">break</span><span class="p">;</span>
    
        <span class="k">if</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">isdigit</span><span class="p">(</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="kt">unsigned</span> <span class="kt">char</span><span class="o">&gt;</span><span class="p">(</span><span class="n">ch</span><span class="p">)))</span> <span class="p">{</span>
            <span class="n">num_str</span> <span class="o">+=</span> <span class="n">ch</span><span class="p">;</span>
        <span class="p">}</span> <span class="k">else</span> <span class="k">if</span> <span class="p">(</span><span class="n">ch</span> <span class="o">==</span> <span class="sc">'.'</span><span class="p">)</span> <span class="p">{</span>
            <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">num_str</span><span class="p">.</span><span class="n">empty</span><span class="p">())</span> <span class="p">{</span>
                <span class="n">stk</span><span class="p">.</span><span class="n">push</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">stoi</span><span class="p">(</span><span class="n">num_str</span><span class="p">));</span>
                <span class="n">num_str</span><span class="p">.</span><span class="n">clear</span><span class="p">();</span>
            <span class="p">}</span>
        <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
            <span class="kt">int</span> <span class="n">a</span> <span class="o">=</span> <span class="n">stk</span><span class="p">.</span><span class="n">top</span><span class="p">();</span> <span class="n">stk</span><span class="p">.</span><span class="n">pop</span><span class="p">();</span>
            <span class="kt">int</span> <span class="n">b</span> <span class="o">=</span> <span class="n">stk</span><span class="p">.</span><span class="n">top</span><span class="p">();</span> <span class="n">stk</span><span class="p">.</span><span class="n">pop</span><span class="p">();</span>
            <span class="kt">int</span> <span class="n">result</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
            <span class="k">switch</span> <span class="p">(</span><span class="n">ch</span><span class="p">)</span> <span class="p">{</span>
                <span class="k">case</span> <span class="sc">'+'</span><span class="p">:</span> <span class="n">result</span> <span class="o">=</span> <span class="n">b</span> <span class="o">+</span> <span class="n">a</span><span class="p">;</span> <span class="k">break</span><span class="p">;</span>
                <span class="k">case</span> <span class="sc">'-'</span><span class="p">:</span> <span class="n">result</span> <span class="o">=</span> <span class="n">b</span> <span class="o">-</span> <span class="n">a</span><span class="p">;</span> <span class="k">break</span><span class="p">;</span>
                <span class="k">case</span> <span class="sc">'*'</span><span class="p">:</span> <span class="n">result</span> <span class="o">=</span> <span class="n">b</span> <span class="o">*</span> <span class="n">a</span><span class="p">;</span> <span class="k">break</span><span class="p">;</span>
                <span class="k">case</span> <span class="sc">'/'</span><span class="p">:</span> <span class="n">result</span> <span class="o">=</span> <span class="n">b</span> <span class="o">/</span> <span class="n">a</span><span class="p">;</span> <span class="k">break</span><span class="p">;</span>
            <span class="p">}</span>
            <span class="n">stk</span><span class="p">.</span><span class="n">push</span><span class="p">(</span><span class="n">result</span><span class="p">);</span>
        <span class="p">}</span>
    <span class="p">}</span>

    <span class="n">result</span> <span class="o">=</span> <span class="n">stk</span><span class="p">.</span><span class="n">top</span><span class="p">();</span>

    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">result</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>

    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="45-洛谷p1241-括号序列">4.5 <a href="https://www.luogu.com.cn/problem/P1241">洛谷：P1241 括号序列</a></h3>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;string&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stack&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;vector&gt;</span><span class="cp">
</span>
<span class="kt">bool</span> <span class="nf">is_match</span><span class="p">(</span><span class="kt">char</span> <span class="n">left</span><span class="p">,</span> <span class="kt">char</span> <span class="n">right</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">return</span> <span class="p">(</span><span class="sc">'('</span> <span class="o">==</span> <span class="n">left</span> <span class="o">&amp;&amp;</span> <span class="sc">')'</span> <span class="o">==</span> <span class="n">right</span><span class="p">)</span> <span class="o">||</span> <span class="p">(</span><span class="sc">'['</span> <span class="o">==</span> <span class="n">left</span> <span class="o">&amp;&amp;</span> <span class="sc">']'</span> <span class="o">==</span> <span class="n">right</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="n">main</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="n">expression</span><span class="p">;</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cin</span> <span class="o">&gt;&gt;</span> <span class="n">expression</span><span class="p">;</span>

    <span class="n">std</span><span class="o">::</span><span class="n">stack</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span> <span class="n">stk</span><span class="p">;</span>
    <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">bool</span><span class="o">&gt;</span> <span class="n">matched</span><span class="p">(</span><span class="n">expression</span><span class="p">.</span><span class="n">size</span><span class="p">(),</span> <span class="nb">false</span><span class="p">);</span>

    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span><span class="p">(</span><span class="n">expression</span><span class="p">.</span><span class="n">size</span><span class="p">());</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
        <span class="kt">char</span> <span class="n">ch</span> <span class="o">=</span> <span class="n">expression</span><span class="p">[</span><span class="n">i</span><span class="p">];</span>
        <span class="k">if</span> <span class="p">(</span><span class="sc">'('</span> <span class="o">==</span> <span class="n">ch</span> <span class="o">||</span> <span class="sc">'['</span> <span class="o">==</span> <span class="n">ch</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">stk</span><span class="p">.</span><span class="n">push</span><span class="p">(</span><span class="n">i</span><span class="p">);</span>
            <span class="k">continue</span><span class="p">;</span>
        <span class="p">}</span>

        <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">stk</span><span class="p">.</span><span class="n">empty</span><span class="p">()</span> <span class="o">&amp;&amp;</span> <span class="n">is_match</span><span class="p">(</span><span class="n">expression</span><span class="p">[</span><span class="n">stk</span><span class="p">.</span><span class="n">top</span><span class="p">()],</span> <span class="n">ch</span><span class="p">))</span> <span class="p">{</span>
            <span class="n">matched</span><span class="p">[</span><span class="n">stk</span><span class="p">.</span><span class="n">top</span><span class="p">()]</span> <span class="o">=</span> <span class="nb">true</span><span class="p">;</span>
            <span class="n">matched</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="nb">true</span><span class="p">;</span>
            <span class="n">stk</span><span class="p">.</span><span class="n">pop</span><span class="p">();</span>
        <span class="p">}</span>
    <span class="p">}</span>

    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span><span class="p">(</span><span class="n">expression</span><span class="p">.</span><span class="n">size</span><span class="p">());</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">matched</span><span class="p">[</span><span class="n">i</span><span class="p">])</span> <span class="p">{</span>
            <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">expression</span><span class="p">[</span><span class="n">i</span><span class="p">];</span>
            <span class="k">continue</span><span class="p">;</span>
        <span class="p">}</span>

        <span class="k">if</span> <span class="p">(</span><span class="sc">'('</span> <span class="o">==</span> <span class="n">expression</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">||</span> <span class="sc">')'</span> <span class="o">==</span> <span class="n">expression</span><span class="p">[</span><span class="n">i</span><span class="p">])</span> <span class="p">{</span>
            <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"()"</span><span class="p">;</span>
        <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
            <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"[]"</span><span class="p">;</span>
        <span class="p">}</span>
    <span class="p">}</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
    
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="5-进阶知识底层容器的选择">5. 进阶知识（底层容器的选择）</h2>

<p><a href="https://en.cppreference.com/w/cpp/container/stack.html"><code class="language-plaintext highlighter-rouge">std::stack</code></a> 是一个模板类（定义在头文件 <code class="language-plaintext highlighter-rouge">&lt;stack&gt;</code>），定义如下：</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">template</span><span class="o">&lt;</span><span class="k">class</span> <span class="nc">T</span><span class="p">,</span> <span class="k">class</span> <span class="nc">Container</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">deque</span><span class="o">&lt;</span><span class="n">T</span><span class="p">&gt;</span><span class="o">&gt;</span>
<span class="k">class</span> <span class="nc">stack</span><span class="p">;</span>
</code></pre></div></div>

<p>注意第二个参数 <code class="language-plaintext highlighter-rouge">Container</code>。默认情况下，C++ 使用 <code class="language-plaintext highlighter-rouge">std::deque</code>（双端队列）作为底层容器，而不是 <code class="language-plaintext highlighter-rouge">std::vector</code>。</p>

<h3 id="51-为什么默认用-stddeque-而不是-stdvector">5.1 为什么默认用 <code class="language-plaintext highlighter-rouge">std::deque</code> 而不是 <code class="language-plaintext highlighter-rouge">std::vector</code>？</h3>

<ol>
  <li><strong>扩容代价</strong>：<code class="language-plaintext highlighter-rouge">std::vector</code> 在内存不足扩容时，需要把所有元素搬运到新内存块，代价较大。<code class="language-plaintext highlighter-rouge">std::deque</code> 是分段内存，扩容时只需分配新段，不需要搬运旧数据。</li>
  <li><strong>内存效率</strong>：对于大量进出的栈操作，<code class="language-plaintext highlighter-rouge">std::deque</code> 的内存回收和分配策略通常比 <code class="language-plaintext highlighter-rouge">std::vector</code> 更平滑。</li>
</ol>

<figure class=" centered-75-figure"><a href="/assets/images/double_ended_queue_in_stl.png" class="image-popup" title="std::deque 的内部结构：由中控器（Map）管理的一系列分段连续内存缓冲区，非常适合两端操作
"><img src="/assets/images/double_ended_queue_in_stl.png" alt="STL 中双端队列 deque 的内部结构示意图" /></a><figcaption>
      std::deque 的内部结构：由中控器（Map）管理的一系列分段连续内存缓冲区，非常适合两端操作

    </figcaption></figure>

<p>当然，如果你确定需要极致的连续内存性能，也可以手动指定：</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">std</span><span class="o">::</span><span class="n">stack</span><span class="o">&lt;</span><span class="kt">int</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;&gt;</span> <span class="n">stk</span><span class="p">;</span>
</code></pre></div></div>

<h2 id="参考资料">参考资料</h2>

<ol>
  <li><a href="https://en.wikipedia.org/wiki/Stack_(abstract_data_type)">Wikipedia: Stack (abstract data type)</a></li>
  <li><a href="https://en.wikipedia.org/wiki/Call_stack">Wikipedia: Call stack</a></li>
  <li><a href="https://en.wikipedia.org/wiki/Recursion_(computer_science)">Wikipedia: Recursion (computer science)</a></li>
  <li><a href="https://medium.com/@goyalarchana17/scala-interview-series-valid-parentheses-in-a-string-ac7002c79642">Medium: Valid Parentheses in a String</a></li>
  <li><a href="https://stackoverflow.com/questions/1436020/whats-the-difference-between-deque-and-list-stl-containers">StackOverflow: Difference between deque and list</a></li>
  <li><a href="https://cppreference.com/w/cpp/container/stack">cppreference.com: std::stack</a></li>
</ol>]]></content><author><name>bfmhno3</name></author><category term="Note" /><category term="C++" /><summary type="html"><![CDATA[要真正理解栈这种结构，意味着我们要剥离事物表象，追究其最本质的问题，即我们到底面临什么样的数据处理需求，才导致了 “栈” 这种结构的诞生？ 我们不需要把栈仅仅看作是教科书上的定义，而是把它看作是为了解决特定时序问题而设计的一种 “受限” 模型。 1. 什么是栈？ 从本质上来说，计算机处理任务通常有两种模式： 并行/随机：所有任务都在桌面上铺开，想拿哪个拿哪个（类似数组或哈希表）。 嵌套/依赖：任务 B 必须依赖任务 A 的环境，任务 C 又依赖 B。要完成 A，必须先挂起 A 去做 B；要做 B，必须先挂起 B 去做 C。 栈（Stack）就是为了解决第二种模式而诞生的。 1.1 核心定义：LIFO 栈是一种线性表，但它是一种受限的线性表。它的核心逻辑是后进先出（Last In, First Out - LIFO）。 你可以把它想象成叠盘子： 压入（Push）：你只能把新盘子放在最上面。 弹出（Pop）：你只能取走最上面的盘子。 查看（Top/Peek）：你只能看到最上面盘子的花纹，无法直接看到底部的盘子。 生活中的栈模型：想要拿到底部的盘子，必须先移走顶部的盘子 1.2 为什么它是 “受限” 的？ 数组（Array）允许你访问 index[0] 也可以访问 index[100]。栈为什么要把这种能力 “阉割” 掉？ 答案就是为了强制秩序。 通过限制 “只能操作顶部”，栈强制程序员必须按照严格的 “原路返回” 逻辑来处理数据，从而消除了随机访问带来的复杂性和潜在错误。 2. 为什么需要栈？ 如果计算机世界没有栈，很多基础逻辑就会崩塌。我们需要栈，是因为它是保存上下文（Context）和实现回朔（Backtacking）最自然的模型。 2.1 函数调用（The Call Stack） 这是栈存在最底层、也最不可或缺的原因。 从本质上来说，CPU 在执行代码时是个高度专注的执行机器，它完全依赖程序计数器（PC, Program Counter）来决定下一步跑哪条指令。当代码从 main() 跳转到 func_a() 时，本质上是强行修改了 PC 寄存器的值。随之而来的致命问题是：当 func_a() 跑完，CPU 怎么知道该跳回 main() 里的哪一行继续执行？ 你可能会想，用一个专门的寄存器（比如链接寄存器 LR）把返回地址保存起来不就行了？ 在只有单层调用的情况下，这确实可行。但现实是，代码中充满了嵌套调用：main() 调用了 func_a()，而 func_a() 中又调用了 func_b()。如果只依赖单一寄存器，当调起 func_b() 时，新的返回地址回无情地覆盖掉之前 func_a() 的返回地址。等到 func_b() 执行结束，程序就再也找不到回 main() 的路了。 此外，函数不仅需要记住 “从哪儿来”，还要保护好自己的 “现场”。每个函数都有自己的局部变量，执行时还会征用各种通用寄存器。在跳入下一个函数前，当前的这些状态（Context）必须被原封不动地保存下来。 由于函数调用的逻辑是绝对的 “嵌套与回溯”——最后被调用的 func_b() 必须最先执行完毕并交出控制权，func_a() 才能继续，做后才轮到最外层的 main()。这种 “后出发的先结束，先出发的最后兜底” 的生命周期，与 LIFO (Last-In, First-Out) 结构严丝合缝。 所有，系统在内存中专门开辟了调用栈（Call Stack）。每次进入新函数，就把它的返回地址、局部变量和寄存器状态打包成一个栈帧（Stack Frame）压入栈顶；函数一结束，就将栈顶弹出，完美恢复上一层函数的现场。没有栈，现代编程语言的函数嵌套和递归机制就根本无法实现。 调用栈示例：如果子程序 DrawSquare 调用了 DrawLine，DrawLine 的栈帧就会被压入栈顶（如图中绿色部分所示）。栈指针（Stack Pointer）始终指向最新的栈顶位置 2.2 递归（Recursion） 既然函数调用依赖栈来保存上下文，那么递归可以说是把栈的这种特性压榨到了机制。 使用海龟绘图（Turtle Graphics）递归绘制谢尔宾斯基三角形（Sierpinski triangle）的过程 从人类的思维来看，递归是自己调用自己，但在 CPU 和内存眼里，根本没有 “同一个函数” 的概念。对于计算机而言，不管是 func_a() 调用 func)b()，还是 func_a() 调用 func_a()，动作是完全一样的——仅仅是 PC（程序计数器）指针跳转，并在内存中开辟一个新的栈帧。 假设我们在写一个计算阶乘的递归函数（fact(n)）。当 func(3) 内部调用 fact(2) 时，此时的局部变量 n=3 必须被冻结并保护起来。如果没有栈 fact(2) 内部的变量就会直接覆盖掉原来 n=3 的内存空间。等到递归触底反弹时，程序根本想不起最初传入的参数是什么。 得益于栈的 LIFO 机制，每一次自我调用，系统都会把当前的参数、局部变量和返回地址像叠盘子一样压入（Push）栈中。不管向下深挖了多少层，每一层的数据都被独立且安全地封存在自己的栈帧里。当遇到终止条件（Base Case）开始回归时，栈再一层层弹出（Pop），精准地恢复每一层的现场。可以说，栈时递归能够成立的物理载体。 当然，如果嵌套层数过深导致栈空间耗尽，就会引发经典的 Stack Overflow 栈溢出。 2.3 状态撤销（Undo）与历史回溯 你在编辑器里敲击键盘，后者在设计软件中画图，想要按下 Ctrl + Z 撤销刚刚的失误。 人类的时间是线性向前的，但 “撤销” 操作要求我们能够逆转时间。这种逆转不是随机的，而是必须严格遵守 “最近发生的事情，最先被抹去” 的自然法则。你最后敲下的那一个字符，必须是第一个被删除的。 所以，这里的历史记录被抽象成了一个栈： 入栈（Push）：每当你执行一个操作（比如输入字符、改变颜色、移动光标），系统就会把这个动作的 “状态快照” 或 “增量数据” 打包，压入历史记录栈的栈顶。 出栈（Pop）：当年你触发撤销（Undo）时，系统直接从栈顶取回最后一次保存的状态，并将其弹出，界面随之恢复到上一个时刻。 没有栈这种 “后进先出” 的模型，系统就无法建立起具有时间顺序的后悔药机制。任何需要 “原路” 返回的场景（比如浏览器的 “后退” 按钮、迷宫算法的回溯路径），其背后的灵魂数据机构都是栈。 2.4 符号匹配与语法解析 编译器在编译代码时，或者解析器在处理一段复杂公式时，需要检查代码中的 { [ ( ) ] } 括号是否成对闭合、合法嵌套。 编译器阅读代码的顺序是从左到右的单向扫描。但在代码的世界里，最晚开启的作用于，必须最早被关闭。这种一种典型的 LIFO 结构需求。 面对一长串括号，系统处理的算法极其优雅，完全依赖栈来实现： 扫描与入栈：逐个字符读取，只要遇到 “左括号”（如 {、[、(），不管三七二十一，直接压入栈中。这代表着进入了一个新的层级，正在等待它的结束。 匹配与出栈：一旦遇到 “右括号”（如 )、]、}），此时立刻去查看栈顶元素。如果栈顶正好是能与它匹配的左括号，说明当前最内层的逻辑闭环了，于是将栈顶弹出，继续往下走。 利用栈进行括号匹配的逻辑：左括号入栈，右括号消栈，最后栈空则合法 如果遇到右括号时栈是空的（说明没有多余的左括号等待闭合），或者栈顶的左括号类型不匹配（比如 { 遇到了 ]），那么编译器就可以立刻抛出 Syntax Error（语法错误）。当整个文件扫描完，如果栈里面还有残留的左括号，说明有作用域没关上。 利用栈，编译器不仅能判断对错，还能精准定位到哪一层的括号出了问题。 3. C++ STL 中的 std::stack 在 C++ 标准模板库（STL）中，std::stack 被设计得非常精妙。它不是一个从零开始写的容器，而是一个容器适配器（Container Adapter）。 3.1 什么是 “容器适配器”？ std::stack 本身不直接管理内存，它更像是一个外壳。它内部包裹着另一个真正的容器（比如 std::vector、std::deque 后 std::list）。 它屏蔽了底层容器的随机访问结构（如 []），只暴露 push、pop、top 等接口，从而强制你遵守栈的规则。 3.2 核心语法与操作 在使用前需包含头文件： #include &lt;stack&gt; 基本操作（时间复杂度均为 $O(1)$）。 操作 函数 描述 注意事项 入栈 push(val) 将元素拷贝到栈顶 设计拷贝/移动构造 原地构造 emplace(args) 在栈顶直接构造元素 **推荐**，比 push 更高效，省去拷贝 出栈 pop() 移除栈顶元素 **返回值为 void**，不返回被移除的元素 查看 top() 返回栈顶元素的引用 如果栈为空调用此函数回导致未定义行为（通常崩溃） 判空 empty() 栈为空返回 true 操作前务必检查 大小 size() 返回元素数量 - Push 操作将元素压入栈顶，Pop 操作移除栈顶元素，两者都不能触及栈底 3.3 为什么 C++ 的 pop() 不返回元素？ 在很多其他语言（如 Java, Python）中，pop() 会移除并返回栈顶元素。但在 C++ STL 中，pop() 是 void，你需要先 top() 获取值，再 pop() 移除。 究其原因是异常安全（Exception Safety）。 如果 pop() 函数即移除元素又返回元素（按值返回），在返回过程中如果发生了拷贝构造异常（比如内存不足），那么这个元素即从栈里丢了，又没能成功交给调用者，导致数据永久丢失。 将 top() 和 pop() 分离是 C++ 追求安全的体现。 4. 算法题实战 点击题目即可跳转。 4.1 洛谷：B3614【模板】栈 #include &lt;iostream&gt; #include &lt;cstdint&gt; #include &lt;string&gt; using namespace std; const uint64_t N = 10e6 + 1; uint64_t stk[N] = {0}; uint64_t top_idx = 0; // 栈顶指针，指向栈顶元素的下一个位置 int main(void) { uint64_t t = 0; cin &gt;&gt; t; // 数据组数 while (t--) { top_idx = 0; // 每组数据都清空栈 uint64_t n = 0; // 操作的次数 cin &gt;&gt; n; while (n--) { string operation; uint64_t x = 0; cin &gt;&gt; operation; if ("push" == operation) { cin &gt;&gt; x; stk[top_idx++] = x; } else if ("pop" == operation) { if (0 == top_idx) { cout &lt;&lt; "Empty" &lt;&lt; endl; } else { --top_idx; } } else if ("query" == operation) { if (0 == top_idx) { cout &lt;&lt; "Anguei!" &lt;&lt; endl; } else { cout &lt;&lt; stk[top_idx - 1] &lt;&lt; endl; } } else if ("size" == operation) { cout &lt;&lt; top_idx &lt;&lt; endl; } else { continue; } } } return 0; } 4.2 力扣：20. 有效的括号 class Solution { public: bool isValid(string s) { std::stack&lt;char&gt; stk; for (const char&amp; c : s) { if ('(' == c || '[' == c || '{' == c) { stk.push(c); } else { if (stk.empty()) { return false; } char top = stk.top(); if (')' == c &amp;&amp; '(' != top) { return false; } if (']' == c &amp;&amp; '[' != top) { return false; } if ('}' == c &amp;&amp; '{' != top) { return false; } stk.pop(); } } return stk.empty(); } }; 4.3 洛谷：【深基15.习9】验证栈序列 #include &lt;iostream&gt; #include &lt;vector&gt; #include &lt;stack&gt; constexpr int N = 1000000 + 1; int main(void) { std::vector&lt;int&gt; pushed(N); std::vector&lt;int&gt; popped(N); int q = 0; std::cin &gt;&gt; q; while (q--) { int n = 0; std::cin &gt;&gt; n; // 读取入栈序列 for (int i = 0; i &lt; n; i++) { std::cin &gt;&gt; pushed[i]; } // 读取出栈序列 for (int i = 0; i &lt; n; i++) { std::cin &gt;&gt; popped[i]; } // 模拟验证 std::stack&lt;int&gt; stk; int popped_idx = 0; // 依次入栈 for (int i = 0; i &lt; n; i++) { stk.push(pushed[i]); while (popped_idx &lt; n &amp;&amp; !stk.empty() &amp;&amp; stk.top() == popped[popped_idx]) { stk.pop(); popped_idx++; } } // 判断 if (stk.empty()) { std::cout &lt;&lt; "Yes" &lt;&lt; std::endl; } else { std::cout &lt;&lt; "No" &lt;&lt; std::endl; } } return 0; } 4.4 洛谷：P1449 后缀表达式 #include &lt;iostream&gt; #include &lt;stack&gt; #include &lt;string&gt; int main(void) { std::stack&lt;int&gt; stk; std::string expression; std::string num_str; int num = 0; int result = 0; std::cin &gt;&gt; expression; for (const char&amp; ch : expression) { if (ch == '@') break; if (std::isdigit(static_cast&lt;unsigned char&gt;(ch))) { num_str += ch; } else if (ch == '.') { if (!num_str.empty()) { stk.push(std::stoi(num_str)); num_str.clear(); } } else { int a = stk.top(); stk.pop(); int b = stk.top(); stk.pop(); int result = 0; switch (ch) { case '+': result = b + a; break; case '-': result = b - a; break; case '*': result = b * a; break; case '/': result = b / a; break; } stk.push(result); } } result = stk.top(); std::cout &lt;&lt; result &lt;&lt; std::endl; return 0; } 4.5 洛谷：P1241 括号序列 #include &lt;iostream&gt; #include &lt;string&gt; #include &lt;stack&gt; #include &lt;vector&gt; bool is_match(char left, char right) { return ('(' == left &amp;&amp; ')' == right) || ('[' == left &amp;&amp; ']' == right); } int main(void) { std::string expression; std::cin &gt;&gt; expression; std::stack&lt;int&gt; stk; std::vector&lt;bool&gt; matched(expression.size(), false); for (int i = 0; i &lt; static_cast&lt;int&gt;(expression.size()); ++i) { char ch = expression[i]; if ('(' == ch || '[' == ch) { stk.push(i); continue; } if (!stk.empty() &amp;&amp; is_match(expression[stk.top()], ch)) { matched[stk.top()] = true; matched[i] = true; stk.pop(); } } for (int i = 0; i &lt; static_cast&lt;int&gt;(expression.size()); ++i) { if (matched[i]) { std::cout &lt;&lt; expression[i]; continue; } if ('(' == expression[i] || ')' == expression[i]) { std::cout &lt;&lt; "()"; } else { std::cout &lt;&lt; "[]"; } } std::cout &lt;&lt; std::endl; return 0; } 5. 进阶知识（底层容器的选择） std::stack 是一个模板类（定义在头文件 &lt;stack&gt;），定义如下： template&lt;class T, class Container = std::deque&lt;T&gt;&gt; class stack; 注意第二个参数 Container。默认情况下，C++ 使用 std::deque（双端队列）作为底层容器，而不是 std::vector。 5.1 为什么默认用 std::deque 而不是 std::vector？ 扩容代价：std::vector 在内存不足扩容时，需要把所有元素搬运到新内存块，代价较大。std::deque 是分段内存，扩容时只需分配新段，不需要搬运旧数据。 内存效率：对于大量进出的栈操作，std::deque 的内存回收和分配策略通常比 std::vector 更平滑。 std::deque 的内部结构：由中控器（Map）管理的一系列分段连续内存缓冲区，非常适合两端操作 当然，如果你确定需要极致的连续内存性能，也可以手动指定： std::stack&lt;int, std::vector&lt;int&gt;&gt; stk; 参考资料 Wikipedia: Stack (abstract data type) Wikipedia: Call stack Wikipedia: Recursion (computer science) Medium: Valid Parentheses in a String StackOverflow: Difference between deque and list cppreference.com: std::stack]]></summary></entry></feed>